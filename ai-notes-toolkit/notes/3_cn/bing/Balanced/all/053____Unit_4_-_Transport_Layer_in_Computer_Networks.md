## Unit 4 - Transport Layer in Computer Networks

- The transport layer is a conceptual division of methods in the layered architecture of protocols in the network stack in the Internet protocol suite and the OSI model.
- The protocols of this layer provide end-to-end communication services for applications.
- The transport layer takes data from the application layer and then breaks it into smaller size segments, numbers each byte, and hands over to the network layer for delivery.
- The transport layer also reassembles the segments at the destination and delivers the complete message to the application layer.
- The transport layer performs the following functions   :
  - **Addressing**: The transport layer provides the user address which is specified as a station or port. The port variable represents a specific process or service running on a host. The transport layer protocols need to know the port number of the source and destination processes to deliver the data correctly.
  - **Multiplexing and Demultiplexing**: The transport layer can multiplex multiple application processes on a single host by assigning them different port numbers and sending their data segments over the same network layer connection. The transport layer can also demultiplex the incoming data segments by using the port numbers to deliver them to the correct application processes.
  - **Reliability**: The transport layer can provide reliable or unreliable delivery of data depending on the protocol used. Reliable delivery means that the transport layer ensures that the data segments are delivered to the destination without errors, loss, duplication, or out-of-order arrival. Unreliable delivery means that the transport layer does not guarantee the delivery of data segments and leaves the error detection and correction to the application layer.
  - **Flow Control**: The transport layer can regulate the rate of data transmission between the source and destination hosts to avoid congestion and buffer overflow. The transport layer can use various mechanisms such as sliding window, stop-and-wait, or feedback-based to control the flow of data segments.
  - **Error Control**: The transport layer can detect and correct errors in the data segments by using techniques such as checksum, acknowledgment, retransmission, or timeout. The transport layer can also handle the corrupted, lost, or duplicated segments by discarding them or requesting for retransmission.
  - **Congestion Control**: The transport layer can prevent or reduce the congestion in the network by adjusting the rate of data transmission according to the network conditions. The transport layer can use various algorithms such as additive increase multiplicative decrease (AIMD), slow start, congestion avoidance, or congestion recovery to control the congestion.
  - **Quality of Service**: The transport layer can provide different levels of quality of service (QoS) to different applications based on their requirements. The transport layer can use parameters such as bandwidth, delay, jitter, or loss to measure and guarantee the QoS.
- The transport layer can use different protocols to provide different services to the applications. Some of the common transport layer protocols are  :
  - **Transmission Control Protocol (TCP)**: TCP is a connection-oriented, reliable, byte-stream protocol that provides error control, flow control, and congestion control. TCP is widely used for applications that require reliable and ordered delivery of data, such as web browsing, email, file transfer, or remote login  .
  - **User Datagram Protocol (UDP)**: UDP is a connectionless, unreliable, datagram protocol that provides minimal error control and no flow control or congestion control. UDP is widely used for applications that require fast and efficient delivery of data, such as voice over IP (VoIP), video streaming, online gaming, or domain name system (DNS)  .
  - **Stream Control Transmission Protocol (SCTP)**: SCTP is a connection-oriented, reliable, message-oriented protocol that provides error control, flow control, congestion control, and multiplexing. SCTP is widely used for applications that require multiple streams of data, such as telephony signaling, web conferencing, or multimedia transmission  .
  - **Datagram Congestion Control Protocol (DCCP)**: DCCP is a connection-oriented, unreliable, datagram protocol that provides congestion control and QoS. DCCP is widely used for applications that require controlled rate of data transmission,