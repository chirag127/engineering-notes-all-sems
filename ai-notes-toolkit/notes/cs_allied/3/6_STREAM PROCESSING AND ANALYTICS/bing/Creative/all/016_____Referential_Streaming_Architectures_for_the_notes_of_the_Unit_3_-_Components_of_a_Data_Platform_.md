# Referential Streaming Architectures

- Streaming architectures are designed to process data in real time as it is generated by various sources, such as sensors, devices, applications, or logs.
- Streaming architectures typically consist of the following components:
  - Data sources: These are the originators of the data streams, such as IoT devices, web applications, or log files. They can produce data at different rates and volumes, and in different formats and protocols.
  - Stream buffering: This is the layer that ingests, buffers, and distributes the data streams from the sources to the processing layer. It provides scalability, reliability, and fault tolerance for the streaming data. Examples of stream buffering services are Azure Event Hubs, Azure IoT Hub, and Kafka.
  - Stream processing: This is the layer that performs various operations on the data streams, such as filtering, aggregation, transformation, enrichment, or correlation. It can also apply complex event processing (CEP) logic to detect patterns, anomalies, or trends in the data. Examples of stream processing services are Azure Stream Analytics, Apache Spark Streaming, and Apache Flink.
  - Stream storage: This is the layer that stores the processed data streams for further analysis or consumption. It can also provide query capabilities to access the data in real time or near real time. Examples of stream storage services are Azure Data Lake Storage, Azure Cosmos DB, and Apache HBase.
  - Stream consumption: This is the layer that consumes the stored data streams for various purposes, such as reporting, dashboarding, alerting, or machine learning. Examples of stream consumption services are Azure Power BI, Azure Synapse Analytics, and Apache Kafka Connect.
- Referential streaming architectures are a type of streaming architectures that use reference data to enrich or correlate the data streams. Reference data is a set of static or slowly changing data that provides additional context or information to the streaming data. For example, reference data can be a lookup table, a configuration file, a master data set, or a historical data set.
- Referential streaming architectures can use different approaches to access and join the reference data with the streaming data, such as:
  - Broadcast join: This approach broadcasts the reference data to all the nodes in the stream processing cluster, and performs the join operation locally on each node. This approach is suitable for small or medium-sized reference data sets that can fit in memory.
  - Lookup join: This approach queries the reference data from an external data source, such as a database or a cache, and performs the join operation on the fly. This approach is suitable for large or frequently updated reference data sets that cannot fit in memory.
  - Temporal join: This approach uses a time window to join the streaming data with the reference data based on their timestamps. This approach is suitable for historical or time-series reference data sets that need to be aligned with the streaming data.