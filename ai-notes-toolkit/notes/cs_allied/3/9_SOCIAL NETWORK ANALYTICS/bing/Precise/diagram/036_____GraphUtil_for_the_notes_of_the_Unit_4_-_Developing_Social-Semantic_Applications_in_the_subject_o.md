### GraphUtil

GraphUtil is a utility class that provides a set of static methods for working with graphs. These methods can be used to perform common graph operations such as finding the shortest path between two nodes, computing the degree of a node, or finding the connected components of a graph.

Here are some key points to remember about GraphUtil:

1. GraphUtil is a utility class, which means that it provides a set of static methods that can be used without creating an instance of the class.
2. The methods provided by GraphUtil can be used to perform common graph operations such as finding the shortest path between two nodes, computing the degree of a node, or finding the connected components of a graph.
3. GraphUtil is commonly used in the development of social-semantic applications, where graphs are used to represent and analyze relationships between entities.
4. GraphUtil is part of Unit 4 - Developing Social-Semantic Applications in the subject of SOCIAL NETWORK ANALYTICS.
