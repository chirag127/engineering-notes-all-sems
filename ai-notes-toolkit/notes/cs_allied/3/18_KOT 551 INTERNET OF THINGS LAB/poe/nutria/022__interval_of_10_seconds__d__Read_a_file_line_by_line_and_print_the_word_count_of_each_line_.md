

#### Interval of 10 Seconds
1. A file can be read line by line with a loop that runs for 10 seconds with an interval of 10 seconds. 
2. The loop should increment a variable that stores the current line of the file. 
3. Each line should be read and the word count should be calculated. 
4. The word count should be stored in a variable and printed at the end of the loop.