#### Interval of 10 seconds
- An interval of 10 seconds is a measure of time that is equal to 10 units of one second each.
- A second is the base unit of time in the International System of Units (SI) and is defined as the duration of 9,192,631,770 periods of the radiation corresponding to the transition between the two hyperfine levels of the ground state of the cesium-133 atom.
- An interval of 10 seconds can be used to measure the duration of events, processes, or phenomena that occur or change within a short span of time, such as the frequency of a sound wave, the speed of a moving object, or the reaction time of a person.

#### Read a file line by line and print the word count of each line
- To read a file line by line and print the word count of each line, one possible method is to use a programming language that provides built-in functions or libraries for file handling and string manipulation, such as Python, Java, or C++.
- The general steps for this method are as follows:
  - Open the file in read mode and assign it to a variable, such as file.
  - Use a loop to iterate over the file object, which returns one line at a time.
  - For each line, use a function or a method to split the line into a list of words, such as split() in Python, split() in Java, or strtok() in C++.
  - Use a function or a method to get the length of the list, such as len() in Python, size() in Java, or sizeof() in C++.
  - Print the length of the list, which is the word count of the line, along with the line itself or any other desired output format.
  - Close the file after the loop is finished.