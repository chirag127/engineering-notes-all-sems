# Protocols Governing Web

Protocols are a set of rules and standards that enable communication between different devices and applications on a network. The web, or the World Wide Web, is a system of interlinked documents and resources that can be accessed through the internet using a web browser. The web relies on various protocols to function and provide services to users. Some of the most common protocols governing the web are:

- **TCP/IP (Transmission Control Protocol/Internet Protocol)**: This is the fundamental protocol suite that enables data transmission across the internet and other networks. TCP/IP consists of four layers: the application layer, the transport layer, the internet layer, and the network access layer. Each layer performs a specific function and communicates with the adjacent layers using interfaces. TCP/IP provides reliable, ordered, and error-checked delivery of data packets between devices .

- **HTTP (HyperText Transfer Protocol)**: This is the protocol that defines how web browsers and web servers communicate and exchange information. HTTP is based on the client-server model, where a client (such as a web browser) sends a request to a server (such as a web server) and the server responds with a response. HTTP uses methods such as GET, POST, PUT, and DELETE to specify the type of request, and status codes such as 200 OK, 404 Not Found, and 500 Internal Server Error to indicate the outcome of the request. HTTP also supports headers, cookies, and caching to enhance the functionality and performance of the web.

- **DNS (Domain Name System)**: This is the protocol that translates domain names (such as www.example.com) into IP addresses (such as 192.168.1.1) that are used by TCP/IP to locate and identify devices on a network. DNS is a hierarchical and distributed system that consists of root servers, top-level domain servers, and authoritative name servers. DNS uses queries and responses to resolve domain names into IP addresses and vice versa. DNS also supports various types of records, such as A, CNAME, MX, and NS, to provide different kinds of information about a domain.

- **SMTP (Simple Mail Transfer Protocol)**: This is the protocol that enables the sending and receiving of email messages over the internet. SMTP is also based on the client-server model, where a client (such as an email client) connects to a server (such as an email server) and sends commands and data to the server. SMTP uses commands such as HELO, MAIL, RCPT, DATA, and QUIT to initiate and terminate a session, and to specify the sender, recipient, and content of an email message. SMTP also supports extensions, such as ESMTP, to provide additional features and security.

- **FTP (File Transfer Protocol)**: This is the protocol that enables the transfer of files between devices on a network. FTP is also based on the client-server model, where a client (such as an FTP client) connects to a server (such as an FTP server) and sends commands and data to the server. FTP uses two channels: a control channel and a data channel. The control channel is used to send commands and responses, such as USER, PASS, LIST, and RETR, and the data channel is used to transfer the actual files. FTP also supports modes, such as active and passive, to establish the data channel connection.