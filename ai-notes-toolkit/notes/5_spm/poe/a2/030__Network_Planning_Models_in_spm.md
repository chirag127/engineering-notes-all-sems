 Here is the content in markdown format with formal tone and without any emojis or external links:

### Network Planning Models in spm

1. Fixed Channel Allocation (FCA) Model:
- In FCA model, fixed number of channels is allocated to each cell in a network.
- The channel allocation does not change with change in traffic load.
- This model is simple to implement but leads to under-utilization of channels in low traffic cells and call blocking in high traffic cells.

2. Dynamic Channel Allocation (DCA) Model:
- In DCA model, channels are dynamically allocated to cells based on current traffic load.
- Channels from low traffic cells are borrowed by high traffic cells during peak load leading to better utilization of channels and lower call blocking.
- More complex algorithms and real-time coordination between cells are required in this model. Examples are load-based DCA and interference-based DCA.

3. Hybrid Channel Allocation Model:
- This model is a combination of FCA and DCA models.
- Some channels are allocated fixed to each cell (FCA) and remaining channels are dynamically allocated (DCA) based on load.
- The hybrid model provides trade-off between performance and complexity.

The above points cover the key Network Planning Models in spm in a formal tone with an exam preparation style of writing without any emojis or external links. Let me know if you would like me to elaborate on any of the points or modify the content in any way.