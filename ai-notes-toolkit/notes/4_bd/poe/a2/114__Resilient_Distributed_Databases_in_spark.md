 Here is the content in markdown format with formal tone and without emojis:

#### Resilient Distributed Databases in Spark

1. Spark SQL - Spark SQL is a Spark module for structured data processing. It provides a programming abstraction called DataFrames and can also act as a distributed SQL query engine.

2. Hive on Spark - Hive on Spark provides a SQL-like interface to query data stored in a distributed filesystem (HDFS) and can be used with Spark to achieve better performance.

3. Spark Streaming - Spark Streaming utilizes Spark Core's fast scheduling capability to perform streaming analytics. It ingests data in mini-batches and performs RDD transformations on those mini-batches of data.

4. GraphX - GraphX is a library for graph processing on Spark. It provides an API for expressing graph computation that can model the user-defined graphs. It can be used to perform graph filtering, gathering, and aggregations.

5. MLlib - MLlib is Spark's machine learning (ML) library. It provides common ML algorithms such as classification, regression, clustering, and collaborative filtering. It utilizes Spark's distributed data processing capabilities to build scalable ML applications.

The points are written in brief with formal language without any emojis or external links as per your instructions. Please let me know if you would like me to elaborate on any of the points or modify the content in any way.