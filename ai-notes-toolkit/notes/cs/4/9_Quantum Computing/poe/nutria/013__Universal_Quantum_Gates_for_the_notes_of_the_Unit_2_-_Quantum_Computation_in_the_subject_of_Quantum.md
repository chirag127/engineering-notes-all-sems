
### Universal Quantum Gates

1. A **universal quantum gate** is a quantum gate that can be used to construct any unitary operator, given enough uses of the gate.
2. A universal gate set typically consists of two or more gates that together can approximate any unitary operator to a desired accuracy.
3. The most common universal gate set is the **Toffoli gate**, which is a three-qubit gate.
4. Other universal gate sets include the **Fredkin gate**, which is a three-qubit gate, and the **Deutsch-Jozsa** algorithm, which is a two-qubit gate.
5. The **Hadamard gate** is also a universal gate, as it can be used to construct any unitary operator given enough uses of the gate.
6. The **controlled-NOT gate** (CNOT) is a two-qubit gate that can be used to construct any unitary operator given enough uses of the gate.
7. The **phase gate** is a single-qubit gate that can be used to construct any unitary operator given enough uses of the gate.
8. The **controlled phase gate** is a two-qubit gate that can be used to construct any unitary operator given enough uses of the gate.
9. The **controlled-V gate** is a three-qubit gate that can be used to construct any unitary operator given enough uses of the gate.
10. The **controlled-U gate** is a three-qubit gate that can be used to construct any unitary operator given enough uses of the gate.