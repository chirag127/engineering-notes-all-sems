
### Planar Graphs

Planar graphs are graphs that can be drawn in the plane without any edges crossing. They have a number of interesting properties that make them useful in a variety of applications.

1. A planar graph can be colored using at most four colors such that no two adjacent vertices have the same color. This is known as the Four Color Theorem.

2. Every planar graph has a planar embedding, which means that it can be drawn in the plane without any edges crossing.

3. Every planar graph has a unique planar embedding, which means that the graph can be drawn in the plane without any edges crossing in a unique way.

4. Every planar graph can be represented as a graph of faces, where each face is a set of edges that form a closed loop.

5. Every planar graph can be represented as a graph of edges, where each edge is a set of vertices that are connected by an edge.

6. Every planar graph can be represented as a graph of vertices, where each vertex is a set of edges that are incident to the vertex.

7. Every planar graph can be represented as a graph of regions, where each region is a set of edges that form a closed loop.

8. Every planar graph can be represented as a graph of edges and vertices, where each edge is a set of vertices that are connected by an edge, and each vertex is a set of edges that are incident to the vertex.