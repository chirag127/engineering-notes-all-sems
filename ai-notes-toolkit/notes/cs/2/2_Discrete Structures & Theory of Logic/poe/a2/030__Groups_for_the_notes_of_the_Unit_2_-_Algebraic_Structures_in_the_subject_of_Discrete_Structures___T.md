 Here is the content in markdown format without any emojis or external links:

### Groups

1. A group is a set G of elements together with an operation * that combines any two elements of G to form another element of G such that the following properties are satisfied:

- Closure: For all a, b in G, a*b is in G
- Associativity: For all a, b, c in G, (a*b)*c = a*(b*c)
- Identity: There exists an element e in G such that for all a in G, e*a = a*e = a
- Invertibility: For all a in G, there exists an element a^-1 in G such that a*a^-1 = a^-1*a = e

2. Some examples of groups are:

- Integers under addition: (Z,+)
- Non-zero reals under multiplication: (R-{0},*)
- Permutations of a set under composition: (S_n, o) where S_n is the set of all permutations on n letters and o is the composition of functions

3. Properties of groups:

- If e is the identity element of a group G, then e is unique.
- If a has an inverse a^-1, then a^-1 is unique.
- For all a, b, c in G, (a*b)^-1 = b^-1*a^-1
- For all a in G, a*e = a and e*a = a
- For all a, b, c in G, a*(b*c) = (a*b)*c

4. Subgroups: A subset H of a group G is a subgroup if H is itself a group under the same operation as G. That is, H must be closed, associative, have an identity, and all elements of H must have inverses in H.