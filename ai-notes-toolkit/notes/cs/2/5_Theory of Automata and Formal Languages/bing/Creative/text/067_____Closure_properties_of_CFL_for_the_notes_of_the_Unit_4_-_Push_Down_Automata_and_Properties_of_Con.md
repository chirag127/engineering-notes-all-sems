### Closure properties of CFL

- A closure property of a class of languages is a property that says that if we apply a certain operation on the languages in the class, the resulting language will also be in the same class.
- For example, if L1 and L2 are regular languages, then L1 ∪ L2 (union), L1L2 (concatenation), and L1* (Kleene closure) are also regular languages. This means that regular languages are closed under union, concatenation, and Kleene closure.
- Similarly, we can study the closure properties of context-free languages (CFLs), which are languages that can be generated by context-free grammars (CFGs).
- The following are some of the closure properties of CFLs:

  - **Union**: If L1 and L2 are CFLs, then L1 ∪ L2 is also a CFL. This can be proved by constructing a CFG for L1 ∪ L2 using the CFGs for L1 and L2. For example, if G1 = (V1, T1, S1, P1) and G2 = (V2, T2, S2, P2) are CFGs for L1 and L2, then we can construct a CFG G = (V, T, S, P) for L1 ∪ L2 as follows:

    - V = V1 ∪ V2 ∪ {S}
    - T = T1 ∪ T2
    - S is a new start symbol
    - P = P1 ∪ P2 ∪ {S → S1 | S2}

  - **Concatenation**: If L1 and L2 are CFLs, then L1L2 is also a CFL. This can be proved by constructing a CFG for L1L2 using the CFGs for L1 and L2. For example, if G1 = (V1, T1, S1, P1) and G2 = (V2, T2, S2, P2) are CFGs for L1 and L2, then we can construct a CFG G = (V, T, S, P) for L1L2 as follows:

    - V = V1 ∪ V2 ∪ {S}
    - T = T1 ∪ T2
    - S is a new start symbol
    - P = P1 ∪ P2 ∪ {S → S1S2}

  - **Kleene closure**: If L is a CFL, then L* is also a CFL. This can be proved by constructing a CFG for L* using the CFG for L. For example, if G = (V, T, S, P) is a CFG for L, then we can construct a CFG G' = (V', T', S', P') for L* as follows:

    - V' = V ∪ {S'}
    - T' = T
    - S' is a new start symbol
    - P' = P ∪ {S' → ε | SS'}

  - **Reversal**: If L is a CFL, then LR (the language obtained by reversing the strings in L) is also a CFL. This can be proved by constructing a CFG for LR using the CFG for L. For example, if G = (V, T, S, P) is a CFG for L, then we can construct a CFG G' = (V, T, S, P') for LR as follows:

    - P' is obtained by reversing the right-hand sides of the rules in P. For example, if A → aBc is a rule in P, then A → caB is a rule in P'.

  - **Homomorphism**: If L is a CFL and h is a homomorphism (a function that maps each symbol in the alphabet to a string over the same or another alphabet), then h(L) (the language obtained by applying h to each string in L) is also a CFL. This can be proved by constructing a CFG for h(L) using the CFG for L. For example, if G = (V, T, S, P) is a CFG for L and h is a homomorphism from T to T', then we can construct a CFG G' = (V, T', S, P') for h(L) as follows:

    - P' is obtained by applying h to the right-hand sides of the rules in P. For example, if A → aBc