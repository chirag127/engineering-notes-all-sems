 Here is the content in markdown format without any emojis or external links and in a formal tone:

### Functions

- A function is a block of code that is executed only when it is called.
- Functions allow you to organize your code into reusable blocks, thereby making the code more efficient and easy to read.
- Functions can take inputs known as arguments and return an output.
- Functions help avoid repetition of code. The same code does not have to be written again and again. The function can be called whenever required.
- Function definition includes:
-- The function keyword
-- The name of the function
-- A list of parameters (inputs) enclosed in parentheses
-- The JavaScript statements that define the function, enclosed in curly brackets { }

For example:
function myFunction(x) {
  return x * x;
}

myFunction(5); // Returns 25

- Functions allow a program to be broken into many small, reusable and organized parts (modules) that can be managed efficiently.

I have written the content in points and in a formal tone without emojis or external links as requested. Please let me know if you would like me to modify or add anything.