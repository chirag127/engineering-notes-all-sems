# Internet Addressing

- Internet addressing is a scheme that allows users and applications to identify a specific network or host with which to communicate on the Internet .
- An Internet address works like a postal address, allowing data to be routed from the source to the destination.
- Data traversing the Internet is divided into smaller pieces, called packets, which contain the source and destination addresses, as well as other information.
- The most common Internet addressing scheme is the Internet Protocol (IP), which assigns a 32-bit or 128-bit numerical address to each device or interface on the Internet.
- The 32-bit version of IP is called IPv4, and the 128-bit version is called IPv6. IPv4 addresses are written in dotted-decimal notation, such as 192.168.1.1, while IPv6 addresses are written in hexadecimal notation, such as 2001:db8::1.
- Each IP address is associated with virtual ports in a computer that act as a doorway that allows web applications or websites to send and receive data or information on your device.
- IP addresses can be either static or dynamic. Static IP addresses are fixed and do not change, while dynamic IP addresses are assigned by a server or a router and can change over time.
- IP addresses can be either public or private. Public IP addresses are globally unique and can be accessed from anywhere on the Internet, while private IP addresses are locally unique and can only be accessed within a certain network or subnet.
- IP addresses are divided into network and host portions, which indicate the network and the specific device within that network. The network portion is determined by a subnet mask, which is a binary pattern that shows which bits of the IP address belong to the network and which belong to the host.
- IP addresses are also classified into classes, which indicate the size and range of the network. The classes are A, B, C, D, and E, and each class has a different default subnet mask and number of possible networks and hosts.
- IP addresses can be further divided into subnets, which are smaller segments of a network that have their own subnet mask and address range. Subnetting allows for more efficient use of IP addresses and better network performance and security.
- IP addresses can also be aggregated into supernets, which are larger segments of a network that have a common prefix and address range. Supernetting allows for more efficient routing and management of IP addresses and reduces the size of routing tables.