 Here is the content in markdown format without any emojis or external links and in a formal tone:

### Create a Cookie and add these four user id’s and passwords to this Cookie for the notes of the Unit 5 - Design server site applications using JDDC,ODBC and section tracking API in the subject of Web Technology Lab.

1. Create a Cookie:
- A Cookie is a small piece of data sent from a website and stored in the user's web browser while the user is browsing.
- Cookies were designed to be a reliable mechanism for websites to remember stateful information (such as items added in the shopping cart in an online store) or to record the user's browsing activity (including clicking particular buttons, logging in, or recording which pages were visited in the past).
- To create a Cookie, you use the setCookie() method in JavaScript. The syntax is: document.cookie = "cookieName=cookieValue";

2. Add user id's and passwords to the Cookie:
- To add user id's and passwords to the created Cookie, you specify the user id's and passwords as the values to the cookieValue in the setCookie() method.
- For example, to add four user id's (userId1, userId2, userId3, userId4) and passwords (password1, password2, password3, password4) to the Cookie, you will use:
document.cookie = "userId1=password1;userId2=password2;userId3=password3;userId4=password4";

3. Notes for Unit 5:
- JDBC (Java Database Connectivity) is an API that allows Java programs to interact with a relational database.
- ODBC (Open Database Connectivity) is a standard API for accessing database management systems.
- A section tracking API is used to track the sections or parts of a web page that a user has viewed. It helps to identify the user's browsing pattern and interests.

The above content summarizes how to create a Cookie and add user id's and passwords to it as well as provides notes on the given topics as requested, in a formal tone and without any emojis or external links. Please let me know if you would like me to modify or expand the answer.