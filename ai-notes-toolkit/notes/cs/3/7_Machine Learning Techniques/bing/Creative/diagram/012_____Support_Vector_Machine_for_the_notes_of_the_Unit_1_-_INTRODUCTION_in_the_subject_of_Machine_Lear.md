### Support Vector Machine

- A support vector machine (SVM) is a supervised machine learning algorithm that uses classification algorithms for two-group classification problems  .
- It can also be used for regression and outlier detection problems.
- It works by finding the optimal hyperplane that separates the data points of different classes with the maximum margin  .
- The data points that are closest to the hyperplane are called support vectors, and they determine the margin of the classifier  .
- SVMs can handle high-dimensional and nonlinear data by using kernel functions that map the data to a higher-dimensional feature space where a linear hyperplane can be found  .
- The advantages of SVMs are:
  - They are effective in high-dimensional spaces.
  - They are robust to noise and outliers .
  - They can capture complex patterns and relationships in the data .
- The disadvantages of SVMs are:
  - They can be computationally expensive and slow to train on large datasets  .
  - They can be sensitive to the choice of kernel parameters and regularization  .
  - They can suffer from overfitting if the data is not linearly separable .