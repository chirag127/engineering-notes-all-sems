## Unit 1 - DIGITAL IMAGE FUNDAMENTALS

- A digital image is a representation of a two-dimensional image as a finite set of digital values, called pixels or picture elements.
- Each pixel has a numeric value that corresponds to its brightness, color, or intensity.
- A digital image can be stored, processed, and displayed by a computer system.
- A digital image can be created from various sources, such as a scanner, a camera, a screen capture, or a synthetic image generator.

### Elements of Digital Image Processing Systems

- A digital image processing system consists of the following components:

  - Image acquisition: The process of capturing an image from a physical scene using a sensor, such as a camera or a scanner.
  - Image enhancement: The process of improving the appearance or quality of an image by modifying its pixel values, such as contrast, brightness, sharpness, or noise reduction.
  - Image restoration: The process of recovering an image that has been degraded by some factors, such as blurring, distortion, or noise, using a mathematical model of the degradation.
  - Image compression: The process of reducing the amount of data required to represent an image, while maintaining its quality or fidelity, using various techniques, such as lossy or lossless compression.
  - Image segmentation: The process of dividing an image into meaningful regions or objects, based on some criteria, such as color, texture, shape, or edge detection.
  - Image representation and description: The process of encoding the features or properties of an image or its regions, such as shape, size, orientation, or color histogram, using various methods, such as boundary, region, or chain codes.
  - Image recognition and interpretation: The process of identifying or classifying an image or its regions, based on their features or properties, using various techniques, such as pattern matching, template matching, or machine learning.

### Basic Concepts of Digital Image Processing

- Some basic concepts of digital image processing are:

  - Spatial domain: The domain where an image is represented as a matrix of pixel values, and the operations are performed directly on the pixels, such as arithmetic, logical, or filtering operations.
  - Frequency domain: The domain where an image is represented as a function of frequency, and the operations are performed on the frequency components, such as Fourier transform, filtering, or enhancement operations.
  - Sampling and quantization: The processes of converting a continuous image into a discrete image, by dividing it into a finite number of pixels (sampling) and assigning each pixel a finite number of values (quantization).
  - Resolution: The measure of the detail or clarity of an image, which depends on the number of pixels (spatial resolution) and the number of values per pixel (gray-level or color resolution).
  - Histogram: The graphical representation of the distribution of pixel values in an image, which can be used for analyzing the contrast, brightness, or dynamic range of an image.
  - Image transformation: The process of changing the spatial or frequency domain representation of an image, using various functions, such as linear, nonlinear, geometric, or point operations.