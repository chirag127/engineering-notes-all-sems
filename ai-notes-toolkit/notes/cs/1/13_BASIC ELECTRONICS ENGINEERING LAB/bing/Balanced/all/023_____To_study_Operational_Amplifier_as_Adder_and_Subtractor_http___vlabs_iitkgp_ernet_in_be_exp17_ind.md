# To study Operational Amplifier as Adder and Subtractor

- An operational amplifier (op-amp) is a high-gain electronic voltage amplifier with a differential input and a single-ended output.
- Op-amps can be used as part of a positive or negative feedback amplifier or as an adder or subtractor type circuit using just pure resistances in both the input and the feedback loop.
- An adder circuit is a type of op-amp circuit that can add two or more input voltages and produce a single output voltage that is proportional to the sum of the inputs.
- A subtractor circuit is a type of op-amp circuit that can subtract one input voltage from another and produce a single output voltage that is proportional to the difference of the inputs.
- A differential amplifier is a type of op-amp circuit that can combine the functions of an adder and a subtractor by using both inverting and noninverting inputs .
- The circuit diagrams and formulas for the op-amp adder, subtractor and differential amplifier are shown below:

![Op-amp adder circuit](https://www.gopracticals.com/wp-content/uploads/2017/08/Op-amp-adder-circuit.png)

Vout = (Rf/R1) * (V1 + V2 + V3 + ...)

![Op-amp subtractor circuit](https://www.eeeguide.com/wp-content/uploads/2017/07/Subtractor-using-Op-Amp-or-Difference-Amplifier-Circuit.png)

Vout = (Rf/R1) * (V2 - V1)

![Op-amp differential amplifier circuit](https://eng.libretexts.org/@api/deki/files/1100/4.2.25.png)

Vout = (Rf/R1) * (V2 - V1) + (Rf/R3) * V3