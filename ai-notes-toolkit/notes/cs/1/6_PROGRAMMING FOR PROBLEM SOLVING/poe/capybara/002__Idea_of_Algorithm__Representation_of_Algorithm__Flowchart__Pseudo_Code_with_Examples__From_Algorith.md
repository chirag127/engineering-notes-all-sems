### Idea of Algorithm: Representation of Algorithm, Flowchart, Pseudo Code with Examples, From Algorithms to Programs, Source Code.

Algorithms are step-by-step procedures that are designed to solve a particular problem. They are used in various fields such as computer science, mathematics, engineering, and more. The idea of algorithms is to provide a systematic approach to problem-solving. In order to understand algorithms, it is important to understand their representations, flowcharts, pseudo code, examples, and how they are translated into programs. 

#### Representation of Algorithm

An algorithm can be represented in several ways. The most common methods of representation are:

- Natural language: Algorithms can be described in plain English or any natural language. This is the easiest way to represent an algorithm, but it can be ambiguous and unclear.
- Flowcharts: Flowcharts are graphical representations of algorithms. They use symbols and arrows to represent the steps in the algorithm. Flowcharts are easy to understand and provide a clear visual representation of the algorithm.
- Pseudo code: Pseudo code is a mix of natural language and programming language. It provides a clear and concise description of the algorithm without being tied to a specific programming language.

#### Flowchart

A flowchart is a graphical representation of an algorithm. It is composed of symbols and arrows that represent the steps in the algorithm. The symbols used in flowcharts are standardized and well-known. The most common symbols used in flowcharts are:

- Start/End symbol: Represents the start or end of the algorithm.
- Process symbol: Represents a process or operation.
- Decision symbol: Represents a decision or branching point in the algorithm.
- Input/Output symbol: Represents input or output.

#### Pseudo Code

Pseudo code is a mix of natural language and programming language. It is used to represent an algorithm in a clear and concise way without being tied to a specific programming language. Pseudo code is easy to read and understand, and it can be easily translated into a programming language. 

#### Examples

Let's take a simple example of finding the sum of two numbers. The algorithm for finding the sum of two numbers can be represented in several ways:

- Natural language: "Add two numbers together to find their sum."
- Flowchart: 
![Flowchart for finding the sum of two numbers](https://i.imgur.com/W0J2Q7V.png)
- Pseudo code: 
```
Start
Declare variables a, b, and sum
Input values for a and b
Add a and b and assign the result to sum
Print the value of sum
End
```

#### From Algorithm to Programs

An algorithm is the first step in developing a program. Once the algorithm is developed, it needs to be translated into a programming language in order to create a program. The translation process involves converting the algorithm into code that the computer can execute. 

#### Source Code

Source code is the code that is written in a programming language. It is the set of instructions that the computer will execute to perform a specific task. Source code can be written in several programming languages such as Java, Python, C++, and more. Once the source code is written, it is compiled or interpreted to create an executable program. 

In conclusion, algorithms are important in problem-solving and are used in various fields. Understanding the different representations of an algorithm, such as flowcharts and pseudo code, can help in developing programs. The translation of algorithms into source code is the final step in creating a program.