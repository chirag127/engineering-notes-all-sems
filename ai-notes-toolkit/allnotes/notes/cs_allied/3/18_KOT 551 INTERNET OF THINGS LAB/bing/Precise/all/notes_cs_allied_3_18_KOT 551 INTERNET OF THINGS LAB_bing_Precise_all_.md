

# KOT 551 INTERNET OF THINGS LAB

The Internet of Things (IoT) is a network of interconnected devices that can communicate with each other and share data. The IoT Lab course (KOT 551) is designed to provide students with hands-on experience in developing IoT applications.

In this course, students will learn about:

1. The basics of IoT, including its architecture, protocols, and standards.
2. The use of sensors, actuators, and microcontrollers in IoT systems.
3. The design and implementation of IoT applications using cloud platforms and services.
4. The security and privacy issues associated with IoT systems.

The course will involve a series of lab exercises and projects, where students will have the opportunity to apply their knowledge and skills in building IoT systems. The course will also cover the use of various tools and technologies, such as Arduino, Raspberry Pi, and MQTT, in the development of IoT applications.

Overall, the KOT 551 IoT Lab course aims to provide students with a solid foundation in the field of IoT and prepare them for further studies or careers in this rapidly growing field.



# KCS

KCS stands for Knowledge-Centered Service. It is a best practice methodology that provides a detailed description of how service organizations can work more effectively with knowledge in order to improve service delivery, become more productive in the service organization, decrease costs, and increase service levels to customers .

- KCS is also known as knowledge-centered support.
- Support teams not only provide real-time customer, system, or employee support, but also create and maintain documentation as part of the same process .
- KCS is about getting the in-depth knowledge of IT teams out of their heads and onto the page, creating detailed documentation that employees, system users, and new or less experienced engineers can use without constantly bombarding the service desk with the same requests .



## Course Outcome (CO) Bloom’s Knowledge Level (KL)

Course outcomes (COs) are statements that describe the knowledge, skills, and abilities that students should possess upon completion of a course. These outcomes are typically aligned with the overall program outcomes and are used to assess the effectiveness of the course in achieving its intended goals.

Bloom’s taxonomy is a framework for categorizing educational goals and objectives into different levels of complexity and specificity. The taxonomy consists of six levels of cognitive learning, ranging from lower-order thinking skills (LOTS) to higher-order thinking skills (HOTS). These levels are:

1. **Remembering**: The ability to recall or retrieve previously learned information.
2. **Understanding**: The ability to comprehend the meaning of material.
3. **Applying**: The ability to use learned material in new and concrete situations.
4. **Analyzing**: The ability to break down material into its component parts so that its organizational structure may be understood.
5. **Evaluating**: The ability to make judgments about the value of ideas or materials.
6. **Creating**: The ability to put parts together to form a new whole.

Bloom’s Knowledge Level (KL) refers to the level of cognitive learning that a course outcome is intended to achieve. By aligning course outcomes with the appropriate level of Bloom’s taxonomy, instructors can ensure that their course is designed to effectively promote the development of higher-order thinking skills in their students. This alignment also helps to ensure that assessments are appropriately designed to measure the intended learning outcomes.



# At the end of the course, the student will be able to:

1. Demonstrate a comprehensive understanding of the course material.
2. Apply the concepts and theories learned in the course to real-world situations.
3. Analyze and evaluate information critically and effectively.
4. Communicate ideas and arguments clearly and effectively in both written and oral forms.
5. Work collaboratively with others to achieve common goals.
6. Demonstrate ethical and professional behavior in academic and professional settings.
7. Use technology effectively to enhance learning and communication.
8. Engage in lifelong learning and professional development.




#### CO 1 Understand the concept of Internet of Things K3

The Internet of Things (IoT) refers to the interconnection of physical devices, vehicles, buildings, and other objects embedded with electronics, software, sensors, and network connectivity, which enables these objects to collect and exchange data.

1. IoT allows objects to be sensed and controlled remotely across existing network infrastructure, creating opportunities for more direct integration of the physical world into computer-based systems, and resulting in improved efficiency, accuracy, and economic benefit.
2. IoT is expected to offer advanced connectivity of devices, systems, and services that goes beyond machine-to-machine (M2M) communications and covers a variety of protocols, domains, and applications.
3. IoT is a complex ecosystem that involves various technologies, including wireless communication, sensors, micro-electromechanical systems (MEMS), and embedded systems.
4. IoT has the potential to impact various industries, including healthcare, transportation, agriculture, and manufacturing, by enabling new business models, improving operational efficiency, and enhancing customer experiences.
5. Security and privacy are major concerns in the IoT ecosystem, as the large amount of data collected and exchanged by IoT devices can potentially be vulnerable to cyber attacks and unauthorized access.



#### CO 2 Implement interfacing of various sensors with Arduino/Raspberry Pi K4, K5

Interfacing sensors with Arduino or Raspberry Pi involves connecting the sensor to the microcontroller and writing code to read data from the sensor. Here are the steps to interface various sensors with Arduino/Raspberry Pi:

1. **Identify the sensor and its communication protocol**: Different sensors use different communication protocols such as I2C, SPI, or UART. It is important to identify the communication protocol used by the sensor to properly interface it with the microcontroller.

2. **Connect the sensor to the microcontroller**: The sensor can be connected to the microcontroller using jumper wires or a breadboard. The connections will vary depending on the communication protocol used by the sensor.

3. **Install necessary libraries**: Some sensors require specific libraries to be installed in the Arduino or Raspberry Pi development environment. These libraries provide functions to easily read data from the sensor.

4. **Write code to read data from the sensor**: Once the sensor is connected and the necessary libraries are installed, code can be written to read data from the sensor. The code will vary depending on the communication protocol used by the sensor and the specific functions provided by the sensor library.

5. **Test the sensor**: After the code is written, it is important to test the sensor to ensure that it is properly interfaced with the microcontroller and that the data is being read correctly.

Interfacing various sensors with Arduino or Raspberry Pi allows for the creation of a wide range of projects and applications. By following these steps, one can easily interface sensors with these microcontrollers and start collecting data from the physical world.



#### CO 3 Demonstrate the ability to transmit data wirelessly between different devices. K4

1. **Wireless data transmission** refers to the transfer of information between two or more devices without the use of wires or cables.
2. There are several **wireless technologies** that can be used for data transmission, including Wi-Fi, Bluetooth, cellular networks, and satellite communication.
3. **Wi-Fi** is a popular wireless technology that uses radio waves to transmit data between devices. It is commonly used for internet access and local area networking.
4. **Bluetooth** is another wireless technology that uses radio waves to transmit data between devices. It is commonly used for short-range communication, such as between a smartphone and a wireless headset.
5. **Cellular networks** use radio waves to transmit data between mobile devices and cell towers. These networks are commonly used for mobile internet access and voice communication.
6. **Satellite communication** uses satellites in orbit around the Earth to transmit data between devices. This technology is commonly used for global positioning systems (GPS) and satellite television.
7. To demonstrate the ability to transmit data wirelessly between different devices, one could set up a wireless network using Wi-Fi or Bluetooth and transfer files or stream media between devices.
8. Another way to demonstrate this ability is to use a cellular network to access the internet on a mobile device or to make a voice call.
9. It is important to understand the **strengths and limitations** of each wireless technology in order to choose the most appropriate one for a given task. For example, Wi-Fi is well-suited for high-speed data transfer over short distances, while satellite communication is better for long-distance communication in remote areas.



#### CO 4 Show an ability to upload/download sensor data on cloud and server K2

1. **Uploading sensor data to the cloud and server:** To upload sensor data to the cloud or server, one must first establish a connection between the sensor and the cloud or server. This can be done using various communication protocols such as Wi-Fi, Bluetooth, or cellular networks. Once the connection is established, the sensor data can be transmitted to the cloud or server in real-time or at regular intervals.

2. **Downloading sensor data from the cloud and server:** To download sensor data from the cloud or server, one must first establish a connection between the device and the cloud or server. This can be done using various communication protocols such as Wi-Fi, Bluetooth, or cellular networks. Once the connection is established, the sensor data can be retrieved from the cloud or server and displayed or processed on the device.

3. **Benefits of uploading and downloading sensor data on the cloud and server:** Uploading and downloading sensor data on the cloud and server allows for remote access to the data, making it easier to monitor and analyze. It also allows for data backup and recovery, ensuring that the data is not lost in case of device failure. Additionally, it enables data sharing and collaboration, allowing multiple users to access and work with the data.

4. **Security considerations:** When uploading and downloading sensor data on the cloud and server, it is important to consider the security of the data. This includes ensuring that the data is encrypted during transmission and storage, and that access to the data is restricted to authorized users only. It is also important to regularly update the security measures to protect against new threats.



#### CO 5 Examine various SQL queries from MySQL database K4, K5

SQL (Structured Query Language) is a standard language used to manage and manipulate data stored in relational databases. MySQL is one of the most popular open-source relational database management systems that use SQL.

Here are some common SQL queries that can be used in a MySQL database:

1. **SELECT**: This query is used to retrieve data from one or more tables in a database. The basic syntax is `SELECT column1, column2, ... FROM table_name;`. You can also use the `WHERE` clause to filter the results based on a condition.

2. **INSERT**: This query is used to insert new records into a table. The basic syntax is `INSERT INTO table_name (column1, column2, ...) VALUES (value1, value2, ...);`.

3. **UPDATE**: This query is used to update existing records in a table. The basic syntax is `UPDATE table_name SET column1 = value1, column2 = value2, ... WHERE condition;`.

4. **DELETE**: This query is used to delete records from a table. The basic syntax is `DELETE FROM table_name WHERE condition;`.

5. **CREATE**: This query is used to create a new table in a database. The basic syntax is `CREATE TABLE table_name (column1 datatype, column2 datatype, ...);`.

6. **ALTER**: This query is used to add, modify, or delete columns in an existing table. The basic syntax is `ALTER TABLE table_name ADD column_name datatype;` to add a new column, `ALTER TABLE table_name MODIFY COLUMN column_name datatype;` to modify an existing column, and `ALTER TABLE table_name DROP COLUMN column_name;` to delete a column.

7. **DROP**: This query is used to delete a table from a database. The basic syntax is `DROP TABLE table_name;`.

These are just some of the basic SQL queries that can be used in a MySQL database. There are many more advanced queries and functions that can be used to manipulate and manage data in a MySQL database. It is important to have a good understanding of these queries and their syntax to effectively work with a MySQL database.



## DETAILED SYLLABUS

A detailed syllabus is a comprehensive outline of the topics, assignments, and assessments that will be covered in a course. It is typically provided by the instructor at the beginning of the course and serves as a guide for students to understand the expectations and requirements of the course.

Some key components of a detailed syllabus may include:

1. Course description: A brief overview of the course, including its objectives and goals.
2. Course schedule: A list of topics and assignments, along with their due dates, that will be covered throughout the course.
3. Grading policy: An explanation of how grades will be calculated, including the weight of each assignment and assessment.
4. Course materials: A list of required and recommended textbooks, articles, and other resources for the course.
5. Attendance and participation policy: Information on the expectations for attendance and participation in the course, including any penalties for absences or lack of participation.
6. Academic integrity policy: A statement on the importance of academic integrity and the consequences of academic misconduct.
7. Contact information: The instructor's contact information, including office hours and preferred methods of communication.

A detailed syllabus is an important tool for students to succeed in a course, as it provides a clear roadmap of what is expected of them and how they will be evaluated. It is important for students to review the syllabus carefully and refer to it throughout the course to ensure they are meeting the course requirements.



# The Importance of Hands-On Experience with Sensors

It is important for students to have hands-on experience with various sensors such as temperature, humidity, smoke, light, etc. for several reasons:

1. **Practical Application:** Hands-on experience allows students to apply theoretical knowledge in a practical setting. This helps them to better understand how sensors work and how they can be used in real-world applications.

2. **Skill Development:** Working with sensors helps students to develop important skills such as problem-solving, critical thinking, and data analysis. These skills are valuable not only in the field of sensor technology but also in other areas of study and work.

3. **Increased Engagement:** Hands-on activities are often more engaging and interesting for students than purely theoretical lessons. This can help to increase their motivation and interest in the subject.

4. **Preparation for Future Studies and Careers:** Having hands-on experience with sensors can prepare students for further studies or careers in fields such as engineering, science, and technology. It can also give them a competitive edge when applying for jobs or internships.

Overall, hands-on experience with sensors is an important part of a well-rounded education in science and technology. It allows students to apply their knowledge, develop important skills, and prepare for future opportunities.



# Controlling Web Camera, Network, and Relays Connected to the Raspberry Pi

The Raspberry Pi is a versatile single-board computer that can be used for a variety of purposes. One of its capabilities is to control web cameras, networks, and relays connected to it. Here are some points to consider when using the Raspberry Pi for these purposes:

1. **Web Camera:** The Raspberry Pi can be used to control a web camera connected to it via USB. This can be done using various programming languages such as Python or C++. There are also several libraries available that can simplify the process of capturing and processing images from the camera.

2. **Network:** The Raspberry Pi has built-in networking capabilities, including Ethernet and Wi-Fi. This allows it to be connected to a local network or the internet. The Raspberry Pi can be used as a network server or client, and can be programmed to perform various network-related tasks.

3. **Relays:** Relays are electromechanical switches that can be used to control the flow of electricity. The Raspberry Pi can be used to control relays connected to it via its GPIO (General Purpose Input/Output) pins. This can be done using various programming languages and libraries, and allows the Raspberry Pi to control devices such as lights, motors, and other electrical appliances.

In summary, the Raspberry Pi is a powerful and versatile device that can be used to control web cameras, networks, and relays. With the right programming skills and knowledge, it is possible to create complex and sophisticated systems using the Raspberry Pi and its various capabilities.



#### 1. Start Raspberry Pi and try various Linux commands in command terminal window: ls, cd, touch, mv, rm, man

- **ls**: This command is used to list the contents of a directory. By default, it lists the contents of the current directory.
- **cd**: This command is used to change the current working directory. For example, `cd Documents` would change the current working directory to the Documents directory.
- **touch**: This command is used to create a new, empty file. For example, `touch newfile.txt` would create a new file named `newfile.txt` in the current working directory.
- **mv**: This command is used to move or rename files or directories. For example, `mv file1.txt file2.txt` would rename `file1.txt` to `file2.txt`, while `mv file.txt Documents/` would move `file.txt` to the Documents directory.
- **rm**: This command is used to remove files or directories. For example, `rm file.txt` would remove the file named `file.txt` from the current working directory.
- **man**: This command is used to display the manual page for a command. For example, `man ls` would display the manual page for the `ls` command, providing detailed information on its usage and options.

These are some basic Linux commands that can be used in the command terminal window of a Raspberry Pi. It is recommended to try them out and experiment with their usage to become more familiar with the Linux command line.



#### mkdir, rmdir, tar, gzip, cat, more, less, ps, sudo, cron, chown, chgrp, ping etc.

- `mkdir`: This command is used to create a new directory.
- `rmdir`: This command is used to remove an empty directory.
- `tar`: This command is used to create, extract, or manipulate tar archives.
- `gzip`: This command is used to compress or decompress files using the gzip format.
- `cat`: This command is used to concatenate files and print their contents to the standard output.
- `more`: This command is used to display the contents of a file one screen at a time.
- `less`: This command is similar to `more`, but it allows backward movement in the file as well as forward movement.
- `ps`: This command is used to report information about the currently running processes.
- `sudo`: This command is used to execute a command as another user, typically the superuser.
- `cron`: This command is used to schedule commands to be executed periodically.
- `chown`: This command is used to change the owner of a file or directory.
- `chgrp`: This command is used to change the group of a file or directory.
- `ping`: This command is used to test the reachability of a host on an IP network.

These are some of the basic commands used in Linux and Unix-like operating systems. They can be used to perform various tasks such as file management, process management, and system administration. It is important to have a good understanding of these commands to be able to use the operating system effectively.



#### 2. Run some python programs on Pi like:

1. **Blinking LED:** This is a simple program that can be run on a Raspberry Pi to blink an LED connected to one of the GPIO pins. The program uses the RPi.GPIO library to control the GPIO pins and turn the LED on and off at regular intervals.

2. **Temperature Sensor:** Another program that can be run on a Raspberry Pi is one that reads data from a temperature sensor connected to the GPIO pins. The program can use the Adafruit_DHT library to read data from the sensor and display the current temperature on the screen.

3. **Web Server:** A Raspberry Pi can also be used to run a simple web server using Python. The Flask library can be used to create a web application that can be accessed from any device on the same network as the Raspberry Pi. The web application can display data from sensors connected to the GPIO pins or control devices connected to the Raspberry Pi.

4. **Robot Control:** A Raspberry Pi can also be used to control a robot using Python. The RPi.GPIO library can be used to control the motors of the robot and the OpenCV library can be used to process images from a camera to enable the robot to navigate its environment.

These are just a few examples of the many Python programs that can be run on a Raspberry Pi. The possibilities are endless and the only limit is your imagination.



#### a) Read your name and print Hello message with name

1. To read your name, you can use the `input()` function in Python. This function prompts the user to enter some data and returns the data as a string.
2. To print the Hello message with your name, you can use the `print()` function. This function outputs the specified message to the screen.
3. Here is an example of how you can read your name and print a Hello message with your name in Python:

```python
name = input("Enter your name: ")
print("Hello, " + name + "!")
```

In this example, the `input()` function prompts the user to enter their name. The entered name is then stored in the `name` variable. The `print()` function is then used to output the Hello message with the user's name.



#### b) Read two numbers and print their sum, difference, product and division.

1. To read two numbers, we can use the `input()` function in Python. This function reads a line from the input and returns it as a string. We can then convert this string to an integer using the `int()` function.

```python
num1 = int(input("Enter the first number: "))
num2 = int(input("Enter the second number: "))
```

2. To calculate the sum of the two numbers, we can use the `+` operator.

```python
sum = num1 + num2
print("The sum of the two numbers is:", sum)
```

3. To calculate the difference of the two numbers, we can use the `-` operator.

```python
difference = num1 - num2
print("The difference of the two numbers is:", difference)
```

4. To calculate the product of the two numbers, we can use the `*` operator.

```python
product = num1 * num2
print("The product of the two numbers is:", product)
```

5. To calculate the division of the two numbers, we can use the `/` operator. Note that this operator returns a float, so we may want to format the result to a certain number of decimal places.

```python
division = num1 / num2
print("The division of the two numbers is: {:.2f}".format(division))
```

In summary, we can read two numbers from the user, and then use the `+`, `-`, `*`, and `/` operators to calculate and print their sum, difference, product, and division.



#### c) Word and character count of a given string.

- To count the number of words in a given string, one can split the string into a list of words using the `split()` method and then find the length of the list using the `len()` function.
- To count the number of characters in a given string, one can use the `len()` function on the string itself.
- It is important to note that the `len()` function counts the number of characters in the string, including spaces and punctuation.
- For example, consider the string `"Hello, World!"`. The word count of this string is `2`, as it contains two words separated by a space. The character count of this string is `13`, as it contains `13` characters, including spaces and punctuation.
- Here is an example code snippet in Python that demonstrates how to count the number of words and characters in a given string:

```python
string = "Hello, World!"
words = string.split()
word_count = len(words)
char_count = len(string)
print(f"Word count: {word_count}")
print(f"Character count: {char_count}")
```

This code outputs the following:

```
Word count: 2
Character count: 13
```



#### d) Area of a given shape (rectangle, triangle and circle) reading shape and appropriate values from standard

The area of a shape is the measure of the amount of space inside the shape. The formula for calculating the area of a shape depends on the shape in question. Here are the formulas for calculating the area of a rectangle, triangle, and circle:

1. **Rectangle**: The area of a rectangle is calculated by multiplying its length by its width. The formula is `A = l * w`, where `A` is the area, `l` is the length, and `w` is the width.

2. **Triangle**: The area of a triangle is calculated by multiplying its base by its height and dividing the result by two. The formula is `A = (b * h) / 2`, where `A` is the area, `b` is the base, and `h` is the height.

3. **Circle**: The area of a circle is calculated by multiplying the square of its radius by the mathematical constant pi (π). The formula is `A = π * r^2`, where `A` is the area and `r` is the radius.

To calculate the area of a given shape, one must first identify the shape and then use the appropriate formula with the given values. For example, to calculate the area of a rectangle with a length of 5 units and a width of 3 units, one would use the formula for the area of a rectangle and substitute the given values: `A = l * w = 5 * 3 = 15`. The area of the rectangle is 15 square units.



#### Input

- Input refers to the process of entering data or information into a computer or other electronic device.
- This can be done through various input devices such as a keyboard, mouse, touch screen, microphone, or scanner.
- The data or information entered can be in the form of text, images, audio, or video.
- Input is a crucial step in the process of computing as it allows the user to interact with the computer and provide it with the necessary data to perform a specific task.
- The type of input device used can vary depending on the task being performed and the user's preference.
- For example, a keyboard is commonly used for entering text, while a mouse is used for navigating and selecting items on the screen.
- Touch screens have become increasingly popular in recent years, allowing users to interact with their devices through touch gestures.
- Microphones can be used for voice input, allowing users to control their devices through voice commands or dictation.
- Scanners can be used to input physical documents or images into the computer for digital storage or processing.
- It is important to ensure that the input data is accurate and in the correct format, as errors in the input can lead to incorrect or undesired results.



#### 3. Run some python programs on Pi like: a) Print a name 'n' times, where name and n are read from standard input.

To run a Python program on a Raspberry Pi, you will need to have Python installed on the device. Python is pre-installed on the Raspberry Pi OS, so you should be able to run Python programs without any additional setup.

Here is an example of a Python program that will print a name 'n' times, where the name and the value of n are read from standard input:

```python
name = input("Enter a name: ")
n = int(input("Enter the number of times to print the name: "))

for i in range(n):
    print(name)
```

To run this program on a Raspberry Pi, you can follow these steps:

1. Open a terminal window on the Raspberry Pi.
2. Use a text editor to create a new file with the above Python code. For example, you can use the `nano` text editor by typing `nano program.py` in the terminal and then pasting the code into the editor.
3. Save the file and exit the text editor.
4. In the terminal, navigate to the directory where you saved the file and type `python3 program.py` to run the program.
5. Follow the prompts to enter the name and the value of n, and the program will print the name the specified number of times.

This is a simple example of how to run a Python program on a Raspberry Pi. You can use this as a starting point to create more complex programs and run them on the device.



#### Using for and while loops
- `for` and `while` loops are used to iterate over a block of code multiple times.
- `for` loops are used when the number of iterations is known beforehand.
- `while` loops are used when the number of iterations is not known beforehand and the loop continues until a certain condition is met.

#### Handle Divided by Zero Exception
- Dividing by zero is an illegal operation in mathematics and programming languages.
- In Python, attempting to divide by zero raises a `ZeroDivisionError` exception.
- This exception can be handled using a `try` and `except` block.
- Example:
```python
try:
    x = 5 / 0
except ZeroDivisionError:
    print("Error: Cannot divide by zero")
```

#### Print current time for 10 times with an interval of 1 second
- The `time` module in Python provides functions to work with time.
- The `time.time()` function returns the current time in seconds since the epoch.
- The `time.sleep()` function can be used to pause the execution of the program for a specified number of seconds.
- Example:
```python
import time

for i in range(10):
    print(time.ctime())
    time.sleep(1)
```



#### Interval of 10 Seconds
- An interval of 10 seconds refers to a time period or duration of 10 seconds.
- This interval can be used in various contexts, such as setting a timer, scheduling a task, or measuring the time between two events.

#### Read a File Line by Line and Print the Word Count of Each Line
- To read a file line by line, you can use a loop to iterate over each line in the file.
- As you read each line, you can use a function to count the number of words in the line.
- After counting the words, you can print the word count for that line.
- This process can be repeated for each line in the file until all lines have been read and their word counts have been printed.



#### 4. a) Light an LED through Python program

To light an LED through a Python program, you will need the following components:
1. A Raspberry Pi or similar single-board computer
2. An LED
3. A resistor
4. Jumper wires
5. A breadboard

Here are the steps to light an LED through a Python program:

1. Connect the LED to the Raspberry Pi using the breadboard and jumper wires. The longer leg of the LED should be connected to a GPIO pin on the Raspberry Pi through a resistor, while the shorter leg should be connected to a ground pin.

2. Install the necessary libraries on the Raspberry Pi. This can be done by running the command `sudo apt-get install python-rpi.gpio python3-rpi.gpio` in the terminal.

3. Write a Python program to control the LED. Here is an example program that will turn the LED on for 5 seconds and then turn it off:

```python
import RPi.GPIO as GPIO
import time

# Set the GPIO mode
GPIO.setmode(GPIO.BCM)

# Set the pin number for the LED
LED_PIN = 18

# Set the LED pin as an output
GPIO.setup(LED_PIN, GPIO.OUT)

# Turn the LED on
GPIO.output(LED_PIN, GPIO.HIGH)

# Wait for 5 seconds
time.sleep(5)

# Turn the LED off
GPIO.output(LED_PIN, GPIO.LOW)

# Clean up the GPIO pins
GPIO.cleanup()
```

4. Run the Python program on the Raspberry Pi. The LED should turn on for 5 seconds and then turn off.

This is a basic example of how to light an LED through a Python program. You can modify the program to control the LED in different ways, such as blinking or changing its brightness.



#### b) Get input from two switches and switch on corresponding LEDs

To get input from two switches and switch on corresponding LEDs, the following steps can be followed:

1. Connect the two switches to the input pins of the microcontroller.
2. Connect the two LEDs to the output pins of the microcontroller.
3. Write a program to read the input from the switches and turn on the corresponding LEDs.
4. Upload the program to the microcontroller.
5. Test the circuit by pressing the switches and observing the LEDs.

In this setup, when one of the switches is pressed, the corresponding LED will turn on. When the switch is released, the LED will turn off. The same applies to the other switch and LED.



#### c) Flash an LED at a given on time and off time cycle, where the two times are taken from a file.

1. To flash an LED at a given on time and off time cycle, the first step is to read the on and off times from a file. This can be done using a file input/output function in the programming language being used.
2. Once the on and off times have been read from the file, the next step is to set up the LED and the necessary circuitry. This will typically involve connecting the LED to a digital output pin on a microcontroller or other control device.
3. The control device can then be programmed to turn the LED on and off according to the on and off times read from the file. This can be done using a loop or other control structure in the programming language being used.
4. The LED will then flash on and off according to the specified on and off times, creating the desired flashing effect.



#### 5. a) Flash an LED based on cron output (acts as an alarm)

1. **Cron** is a time-based job scheduler in Unix-like operating systems. It is used to schedule commands or scripts to run automatically at a specified time and date.
2. To flash an LED based on cron output, you will need to connect an LED to your computer or device and write a script that controls the LED.
3. The script should be able to turn the LED on and off at specified intervals to create a flashing effect.
4. Once the script is written, you can use cron to schedule the script to run at a specific time and date.
5. To do this, you will need to edit the crontab file and add a new entry for your script. The entry should specify the time and date when the script should run, as well as the path to the script.
6. When the specified time and date arrives, cron will automatically run the script, causing the LED to flash and act as an alarm.



#### b) Switch on a relay at a given time using cron, where the relay's contact terminals are connected to a load.

1. A relay is an electrically operated switch that can be used to control a circuit by a low-power signal.
2. Cron is a time-based job scheduler in Unix-like operating systems, which can be used to schedule tasks to run periodically at fixed times, dates, or intervals.
3. To switch on a relay at a given time using cron, the following steps can be followed:
    - Identify the GPIO pin on the device that will be used to control the relay.
    - Write a script that sets the GPIO pin to high, which will switch on the relay.
    - Use the `crontab` command to open the crontab file for editing.
    - Add a new line to the crontab file with the desired time and date for the relay to be switched on, followed by the command to run the script.
    - Save and close the crontab file.
4. When the specified time and date is reached, cron will automatically run the script, which will set the GPIO pin to high and switch on the relay.
5. The relay's contact terminals can be connected to a load, such as a lamp or motor, which will be switched on when the relay is activated.



#### c) Get the status of a bulb at a remote place (on the LAN) through web.

To get the status of a bulb at a remote place on the LAN through the web, the following steps can be followed:

1. **Connect to the LAN:** Ensure that the device being used to check the status of the bulb is connected to the same LAN as the bulb.

2. **Access the bulb's web interface:** The bulb should have a web interface that can be accessed through a web browser. Enter the IP address of the bulb in the web browser to access its web interface.

3. **Check the bulb's status:** The bulb's web interface should display its current status, whether it is on or off. If the bulb's status is not displayed, there may be an option to request the status.

4. **Troubleshooting:** If the bulb's status cannot be determined, ensure that the bulb is properly connected to the LAN and that its web interface is functioning correctly. If the issue persists, contact the manufacturer for support.



#### Note: The Instructor may add/delete/modify/tune experiments

- The instructor has the authority to make changes to the experiments.
- These changes may include adding new experiments, deleting existing experiments, modifying the details of the experiments, or tuning the experiments to better fit the needs of the course.
- It is important for students to be aware of these potential changes and to stay up to date with any announcements or updates from the instructor.
- This flexibility allows the instructor to tailor the course to the needs of the students and to ensure that the experiments are relevant and effective.
- Students should be prepared to adapt to any changes made by the instructor and to approach the experiments with an open and flexible mindset.



# KOT 552 INTERNET OF THINGS LAB KCS

- KOT 552 is a course on the Internet of Things (IoT) Lab offered by the KCS department.
- The course focuses on the practical implementation of IoT concepts and technologies.
- Students will learn how to design, develop, and deploy IoT systems.
- The course covers topics such as sensors, actuators, microcontrollers, and communication protocols.
- Students will work on hands-on projects to apply their knowledge and skills.
- The course aims to provide students with a strong foundation in IoT and prepare them for careers in this field.




# Course Outcome (CO) and Bloom’s Knowledge Level (KL)

Course Outcome (CO) refers to the expected knowledge, skills, and abilities that a student should possess upon completion of a specific course. These outcomes are used to assess the effectiveness of the course and to ensure that the course content aligns with the overall program goals.

Bloom’s Knowledge Level (KL) is a framework for categorizing educational goals according to the level of cognitive complexity. The framework consists of six levels, ranging from simple recall of information to higher-order thinking skills such as analysis and evaluation. These levels are:

1. **Remembering:** The ability to recall previously learned information.
2. **Understanding:** The ability to comprehend the meaning of the material.
3. **Applying:** The ability to use learned material in new and concrete situations.
4. **Analyzing:** The ability to break down material into its component parts and understand its organizational structure.
5. **Evaluating:** The ability to make judgments about the value of material for a given purpose.
6. **Creating:** The ability to put parts together to form a new whole.

Course outcomes can be mapped to Bloom’s Knowledge Levels to ensure that the course content is appropriately challenging and promotes higher-order thinking skills. For example, a course outcome that states “Students will be able to analyze and evaluate arguments” would be mapped to the Analyzing and Evaluating levels of Bloom’s taxonomy.



# At the end of the course, the student will be able to:

- Demonstrate a comprehensive understanding of the course material.
- Apply the concepts and theories learned in the course to real-world situations.
- Analyze and evaluate information critically and effectively.
- Communicate ideas and arguments clearly and effectively in written and oral form.
- Work collaboratively with others to achieve common goals.
- Demonstrate the ability to think creatively and independently.
- Develop and demonstrate problem-solving skills.
- Demonstrate ethical and responsible behavior in academic and professional settings.
- Demonstrate the ability to learn independently and take responsibility for one's own learning.
- Demonstrate the ability to adapt to new situations and challenges.




# CO 1 Understand the concept of Internet of Things K3

- The Internet of Things (IoT) refers to the interconnection of physical devices, vehicles, buildings, and other items embedded with electronics, software, sensors, and network connectivity, which enables these objects to collect and exchange data.
- IoT allows objects to be sensed and controlled remotely across existing network infrastructure, creating opportunities for more direct integration of the physical world into computer-based systems, and resulting in improved efficiency, accuracy, and economic benefit.
- IoT is a rapidly growing field, with an estimated 50 billion connected devices by 2020.
- IoT has the potential to impact many industries, including healthcare, transportation, and manufacturing.
- IoT devices can range from simple sensors to complex systems, and can include consumer products such as smart home appliances and wearable technology.
- IoT relies on a variety of communication technologies, including Wi-Fi, Bluetooth, and cellular networks.
- Security and privacy are major concerns in the development and deployment of IoT devices and systems.
- The development of IoT requires collaboration between multiple disciplines, including computer science, engineering, and business.



# CO2 Implement interfacing of various sensors with Arduino/Raspberry Pi K4, K5

- Interfacing sensors with Arduino or Raspberry Pi involves connecting the sensor to the microcontroller and writing code to read data from the sensor.
- Both Arduino and Raspberry Pi have a range of compatible sensors, including temperature, humidity, light, and motion sensors.
- To interface a sensor with an Arduino or Raspberry Pi, the first step is to identify the type of sensor and its communication protocol. Common communication protocols include I2C, SPI, and UART.
- Once the communication protocol is identified, the sensor can be connected to the microcontroller using the appropriate pins. For example, an I2C sensor would be connected to the SDA and SCL pins on an Arduino or Raspberry Pi.
- After the sensor is connected, code can be written to read data from the sensor. This typically involves initializing the sensor, reading data from the sensor's registers, and converting the data into a usable format.
- Many sensors have libraries available that simplify the process of interfacing with the sensor. These libraries provide functions for initializing the sensor, reading data, and performing other common tasks.
- In summary, interfacing a sensor with an Arduino or Raspberry Pi involves identifying the sensor's communication protocol, connecting the sensor to the microcontroller, and writing code to read data from the sensor. Libraries can simplify this process by providing pre-written functions for common tasks.



# CO 3 Demonstrate the ability to transmit data wirelessly between different devices. K4

1. **Wireless data transmission** refers to the transfer of information between two or more devices without the use of wires or cables.
2. **Bluetooth** is a common wireless technology used for short-range data transmission between devices such as smartphones, laptops, and speakers.
3. **Wi-Fi** is another widely used wireless technology that allows devices to connect to a local area network (LAN) and the internet.
4. **Infrared** technology can also be used for wireless data transmission, although it is less common and has a shorter range than Bluetooth or Wi-Fi.
5. **Near Field Communication (NFC)** is another short-range wireless technology that allows devices to exchange data by simply touching them together or bringing them into close proximity.
6. To transmit data wirelessly between different devices, the devices must be equipped with the appropriate wireless technology and be within range of each other.
7. The process of transmitting data wirelessly between devices may vary depending on the technology used, but generally involves establishing a connection between the devices and selecting the data to be transferred.
8. Wireless data transmission has many advantages, including convenience, flexibility, and the ability to transfer data between devices without the need for physical connections.



# CO 4: Show an ability to upload/download sensor data on cloud and server K2

1. **Understand the basics of cloud computing and server K2**: Cloud computing is the delivery of computing services over the internet, including storage, servers, databases, and more. Server K2 is a specific type of server that can be used for storing and managing data.

2. **Set up a cloud account and server K2**: To upload and download sensor data, you will need to set up an account with a cloud service provider and configure a server K2.

3. **Connect the sensor to the cloud and server K2**: Once your cloud account and server K2 are set up, you will need to connect your sensor to them. This can typically be done through a web interface or API.

4. **Upload sensor data to the cloud and server K2**: After the sensor is connected, you can begin uploading data to the cloud and server K2. This can be done manually or automatically, depending on your needs.

5. **Download sensor data from the cloud and server K2**: You can also download sensor data from the cloud and server K2 for analysis and use. This can be done through the web interface or API.

6. **Manage and analyze sensor data**: Once the data is uploaded and downloaded, you can use various tools and techniques to manage and analyze the data. This can help you gain insights and make informed decisions.



# CO 5 Examine various SQL queries from MySQL database K4, K5

MySQL is an open-source relational database management system that uses Structured Query Language (SQL) to manage and manipulate data. SQL is a standard language for managing and querying relational databases.

Here are some common SQL queries that can be used in a MySQL database:

1. SELECT: The SELECT statement is used to query a database and retrieve data from one or more tables. The basic syntax of the SELECT statement is as follows:
```
SELECT column1, column2, ...
FROM table_name;
```

2. INSERT: The INSERT statement is used to add new records to a table. The basic syntax of the INSERT statement is as follows:
```
INSERT INTO table_name (column1, column2, ...)
VALUES (value1, value2, ...);
```

3. UPDATE: The UPDATE statement is used to modify existing records in a table. The basic syntax of the UPDATE statement is as follows:
```
UPDATE table_name
SET column1 = value1, column2 = value2, ...
WHERE condition;
```

4. DELETE: The DELETE statement is used to delete existing records from a table. The basic syntax of the DELETE statement is as follows:
```
DELETE FROM table_name
WHERE condition;
```

5. CREATE: The CREATE statement is used to create a new table in a database. The basic syntax of the CREATE statement is as follows:
```
CREATE TABLE table_name (
    column1 datatype,
    column2 datatype,
    ...
);
```

6. ALTER: The ALTER statement is used to add, modify, or delete columns in an existing table. The basic syntax of the ALTER statement is as follows:
```
ALTER TABLE table_name
ADD column_name datatype;
```

7. DROP: The DROP statement is used to delete a table from a database. The basic syntax of the DROP statement is as follows:
```
DROP TABLE table_name;
```

These are some of the basic SQL queries that can be used in a MySQL database. It is important to note that the syntax and usage of these queries may vary depending on the specific version of MySQL being used. It is always a good idea to consult the official MySQL documentation for the most up-to-date information.



# DETAILED SYLLABUS

A detailed syllabus is a comprehensive outline of the topics, assignments, and assessments that will be covered in a course. It provides students with a clear understanding of the course content, expectations, and requirements. A detailed syllabus typically includes the following information:

1. **Course Information:** This section includes the course title, course number, credit hours, meeting times, and location.

2. **Instructor Information:** This section includes the instructor's name, office location, office hours, contact information, and preferred method of communication.

3. **Course Description:** This section provides an overview of the course, including its objectives, goals, and main topics.

4. **Course Materials:** This section lists the required and recommended textbooks, readings, and other materials needed for the course.

5. **Course Schedule:** This section provides a detailed schedule of the topics, assignments, and assessments that will be covered in the course. It includes the dates and deadlines for all major assignments and exams.

6. **Course Policies:** This section outlines the policies and expectations for attendance, participation, late work, and academic integrity.

7. **Grading:** This section explains how grades will be calculated and assigned, including the weighting of assignments and exams.

8. **Accommodations:** This section provides information on accommodations for students with disabilities.

A detailed syllabus is an important tool for students to succeed in a course. It helps them to plan their time, understand the course expectations, and prepare for assessments. Instructors should provide a detailed syllabus at the beginning of the course and update it as needed throughout the semester.



# Hands-on Experience with Various Sensors

It is important for students to have hands-on experience with various sensors, including but not limited to:

1. **Temperature sensors**: These sensors measure the temperature of the environment or an object. They can be used in a variety of applications, such as monitoring the temperature of a room or a refrigerator, or measuring the temperature of a patient's body.

2. **Humidity sensors**: These sensors measure the amount of moisture in the air. They can be used in applications such as monitoring the humidity level in a room or greenhouse, or measuring the humidity level of soil.

3. **Smoke sensors**: These sensors detect the presence of smoke in the air. They are commonly used in fire alarms and can help prevent fires from spreading.

4. **Light sensors**: These sensors measure the amount of light in the environment. They can be used in applications such as automatically turning on lights when it gets dark, or adjusting the brightness of a display based on the ambient light.

Having hands-on experience with these sensors can help students understand how they work and how they can be used in real-world applications. It can also help students develop practical skills in using and integrating sensors into projects and systems.



# Using Control Web Camera, Network, and Relays Connected to the Pi

1. **Web Camera**: A web camera can be connected to the Raspberry Pi via USB. Once connected, the camera can be accessed and controlled using various software packages, such as `fswebcam` or `motion`.
2. **Network**: The Raspberry Pi can be connected to a network via Ethernet or Wi-Fi. This allows the Pi to access the internet and communicate with other devices on the network. The network settings can be configured using the `raspi-config` tool or by editing the `/etc/network/interfaces` file.
3. **Relays**: Relays can be connected to the Raspberry Pi's GPIO pins to control external devices, such as lights or motors. The `RPi.GPIO` library can be used to control the relays using Python code.

By using these components, it is possible to create a system where the Raspberry Pi can control a web camera, access the internet, and control external devices using relays. This can be useful for a wide range of applications, such as home automation or remote monitoring.



# Starting Raspberry Pi and trying various Linux commands in the command terminal window

1. **Starting Raspberry Pi**: To start Raspberry Pi, connect it to a power source and a display. Once it boots up, you should see the desktop or command line interface, depending on the configuration.

2. **Linux commands**: Linux commands are used to perform various tasks on the operating system. Here are some commonly used commands:

    - `ls`: This command is used to list the contents of a directory. By default, it lists the contents of the current directory.
    - `cd`: This command is used to change the current directory. For example, `cd Documents` will change the current directory to the `Documents` directory.
    - `touch`: This command is used to create an empty file. For example, `touch newfile.txt` will create an empty file named `newfile.txt`.
    - `mv`: This command is used to move or rename files or directories. For example, `mv oldfile.txt newfile.txt` will rename `oldfile.txt` to `newfile.txt`.
    - `rm`: This command is used to remove files or directories. For example, `rm file.txt` will remove the file named `file.txt`.
    - `man`: This command is used to display the manual page for a command. For example, `man ls` will display the manual page for the `ls` command.

These are just a few of the many Linux commands available. You can learn more about them by using the `man` command or by searching online.



# Linux Commands

Here is a brief overview of the Linux commands you mentioned:

- `mkdir`: This command is used to create a new directory.
- `rmdir`: This command is used to remove an empty directory.
- `tar`: This command is used to create or extract tar archives.
- `gzip`: This command is used to compress or decompress files using the gzip format.
- `cat`: This command is used to concatenate files and print their contents to the standard output.
- `more`: This command is used to display the contents of a file one screen at a time.
- `less`: This command is similar to `more`, but it allows backward movement in the file as well as forward movement.
- `ps`: This command is used to display information about the currently running processes.
- `sudo`: This command is used to execute a command with superuser privileges.
- `cron`: This command is used to schedule commands to be executed at a later time.
- `chown`: This command is used to change the owner of a file or directory.
- `chgrp`: This command is used to change the group of a file or directory.
- `ping`: This command is used to test the connectivity between two network hosts.




# Running Python Programs on Raspberry Pi

Raspberry Pi is a small, affordable, single-board computer that can run various operating systems, including Linux-based ones. One of the most popular programming languages used with Raspberry Pi is Python. Here are some steps to run Python programs on Raspberry Pi:

1. **Install Python**: Make sure Python is installed on your Raspberry Pi. You can check if it is already installed by opening a terminal and typing `python3 --version`. If it is not installed, you can install it by running `sudo apt-get update` and `sudo apt-get install python3`.

2. **Write a Python Program**: You can write a Python program using any text editor on Raspberry Pi. Save the file with a `.py` extension, for example, `myprogram.py`.

3. **Run the Python Program**: To run the Python program, open a terminal, navigate to the directory where the program is saved, and type `python3 myprogram.py`. This will execute the program.

4. **Using Python Libraries**: You can use various Python libraries to enhance the functionality of your program. To install a library, use the `pip3` command, for example, `pip3 install numpy`. You can then import the library in your Python program and use its functions.

These are some basic steps to run Python programs on Raspberry Pi. You can explore more advanced features and functionalities as you become more familiar with the platform.



# Read your name and print Hello message with name

1. To read your name, you can use the `input()` function in Python. This function prompts the user to enter a value, which is then stored as a string.
2. To print the Hello message with the name, you can use the `print()` function. This function outputs the specified message to the screen.
3. You can combine the two functions to read the user's name and then print the Hello message with the name.

Here is an example of how this can be done in Python:

```python
name = input("Enter your name: ")
print("Hello, " + name + "!")
```

In this example, the user is prompted to enter their name using the `input()` function. The entered value is then stored in the `name` variable. The `print()` function is then used to output the Hello message with the name. The `+` operator is used to concatenate the strings.



# b) Read two numbers and print their sum, difference, product and division.

1. To read two numbers, you can use the `input()` function in Python. This function reads a line from input and returns it as a string. You can then convert the string to an integer using the `int()` function.

```python
num1 = int(input("Enter the first number: "))
num2 = int(input("Enter the second number: "))
```

2. To calculate the sum of the two numbers, you can use the `+` operator.

```python
sum = num1 + num2
print("The sum of the two numbers is:", sum)
```

3. To calculate the difference of the two numbers, you can use the `-` operator.

```python
difference = num1 - num2
print("The difference of the two numbers is:", difference)
```

4. To calculate the product of the two numbers, you can use the `*` operator.

```python
product = num1 * num2
print("The product of the two numbers is:", product)
```

5. To calculate the division of the two numbers, you can use the `/` operator. Note that this operator returns a float, so you may want to convert the result to an integer using the `int()` function if you want an integer result.

```python
division = num1 / num2
print("The division of the two numbers is:", division)
```



# Word and Character Count of a Given String

1. Word count refers to the number of words in a given string or text.
2. Character count refers to the number of characters, including spaces and punctuation, in a given string or text.
3. To calculate the word count of a given string, one can split the string into a list of words using a delimiter such as a space, and then count the number of elements in the list.
4. To calculate the character count of a given string, one can simply use the `len()` function in Python, which returns the number of characters in a string.
5. Here is an example of how to calculate the word and character count of a given string in Python:

```python
my_string = "This is an example string."
word_count = len(my_string.split())
character_count = len(my_string)
print(f"Word count: {word_count}")
print(f"Character count: {character_count}")
```

This code will output the following:

```
Word count: 5
Character count: 25
```

6. It is important to note that the word count may vary depending on the delimiter used to split the string into words. For example, if a hyphen is used as a delimiter, the word count may be different than if a space is used as a delimiter.
7. Similarly, the character count may vary depending on whether or not spaces and punctuation are included in the count. Some applications may only count letters and numbers, while others may include all characters in the count.



# Area of a given shape (rectangle, triangle and circle) reading shape and appropriate values from standard

The area of a given shape can be calculated by using the appropriate formula for that shape. The formulas for calculating the area of a rectangle, triangle, and circle are as follows:

1. **Rectangle**: The area of a rectangle can be calculated by multiplying its length by its width. The formula for the area of a rectangle is `A = l * w`, where `A` is the area, `l` is the length, and `w` is the width.

2. **Triangle**: The area of a triangle can be calculated using the formula `A = (b * h) / 2`, where `A` is the area, `b` is the length of the base, and `h` is the height of the triangle.

3. **Circle**: The area of a circle can be calculated using the formula `A = π * r^2`, where `A` is the area, `π` is approximately equal to 3.14, and `r` is the radius of the circle.

To calculate the area of a given shape, one must first identify the shape and then use the appropriate formula to calculate its area. The values for the variables in the formula must be read from the standard, which specifies the units of measurement and the precision of the values.



# Input

- Input refers to the data or information that is entered into a computer system for processing.
- Input devices are the hardware components that allow users to enter data into a computer. Examples of input devices include keyboards, mice, touchscreens, and microphones.
- Input can also come from other sources, such as files, network connections, or sensors.
- The input data is processed by the computer's central processing unit (CPU) and then stored in memory or on a storage device.
- Input is an essential part of any computer system, as it allows users to interact with the system and provide the data needed for the system to perform its functions.
- The type of input required by a computer system depends on the tasks it is designed to perform. For example, a word processing program requires text input, while a graphics program requires input in the form of images or drawings.
- Input can be entered into a computer system in a variety of ways, including typing on a keyboard, speaking into a microphone, or using a touchscreen to draw or select options.
- The quality of the input data can affect the accuracy and reliability of the computer system's output. It is important to ensure that the input data is accurate and free from errors.



# Running Python Programs on Raspberry Pi

One of the programs that can be run on a Raspberry Pi using Python is printing a name 'n' times, where the name and the value of 'n' are read from standard input. Here is an example of how this can be done:

```python
name = input("Enter a name: ")
n = int(input("Enter the number of times to print the name: "))

for i in range(n):
    print(name)
```

In this program, the `input()` function is used to read the name and the value of 'n' from the user. The `int()` function is used to convert the value of 'n' from a string to an integer. The `for` loop is then used to print the name 'n' times.

This is just one example of a simple Python program that can be run on a Raspberry Pi. There are many other programs that can be created and run on this platform.



# Using for and while loops
- `for` and `while` loops are used to iterate over a block of code multiple times.
- The `for` loop is used when the number of iterations is known beforehand.
- The `while` loop is used when the number of iterations is not known beforehand and depends on a condition.

# Handling Divided by Zero Exception
- Dividing by zero is an illegal operation in mathematics and programming.
- In most programming languages, attempting to divide by zero results in a runtime error.
- To handle this error, you can use exception handling techniques such as `try` and `except` blocks in Python.

# Printing current time for 10 times with an interval
- To print the current time, you can use the `datetime` module in Python.
- To print the current time for 10 times with an interval, you can use a `for` loop and the `sleep` function from the `time` module.
- Here is an example code snippet that prints the current time for 10 times with an interval of 1 second:

```python
import datetime
import time

for i in range(10):
    print(datetime.datetime.now())
    time.sleep(1)
```



# Interval of 10 seconds

An interval of 10 seconds refers to a time period or duration of 10 seconds. It can be used in various contexts, such as measuring time, setting timers, or scheduling events.

# Reading a file line by line and printing the word count of each line

To read a file line by line and print the word count of each line, the following steps can be followed:

1. Open the file in read mode using a file object.
2. Use a loop to iterate over the lines in the file.
3. For each line, use the `split()` method to split the line into a list of words.
4. Use the `len()` function to find the number of words in the list.
5. Print the word count for the current line.
6. Close the file object once all lines have been processed.

Here is an example of how this can be implemented in Python:

```python
with open('file.txt', 'r') as f:
    for line in f:
        words = line.split()
        word_count = len(words)
        print(word_count)
```

This code opens the file `file.txt` in read mode, iterates over each line in the file, splits the line into a list of words, finds the word count, and prints it. Once all lines have been processed, the file object is closed.



# 4. a) Light an LED through Python program

To light an LED through a Python program, the following steps can be followed:

1. **Connect the LED**: Connect the LED to the appropriate GPIO (General Purpose Input/Output) pin on the Raspberry Pi or other microcontroller being used. Make sure to connect the longer leg (anode) of the LED to the GPIO pin and the shorter leg (cathode) to the ground pin through a resistor.

2. **Install necessary libraries**: Install the necessary libraries for controlling the GPIO pins, such as the RPi.GPIO library for the Raspberry Pi.

3. **Import the libraries**: In the Python program, import the necessary libraries, such as `import RPi.GPIO as GPIO`.

4. **Set up the GPIO pin**: Set up the GPIO pin being used to control the LED as an output pin using the `GPIO.setup()` function.

5. **Turn on the LED**: To turn on the LED, use the `GPIO.output()` function to set the GPIO pin to a high state.

6. **Turn off the LED**: To turn off the LED, use the `GPIO.output()` function to set the GPIO pin to a low state.

Here is an example Python program that turns on an LED connected to GPIO pin 18 on a Raspberry Pi:

```python
import RPi.GPIO as GPIO
import time

GPIO.setmode(GPIO.BCM)
GPIO.setup(18, GPIO.OUT)

try:
    while True:
        GPIO.output(18, GPIO.HIGH)
        time.sleep(1)
        GPIO.output(18, GPIO.LOW)
        time.sleep(1)
except KeyboardInterrupt:
    GPIO.cleanup()
```

This program sets up GPIO pin 18 as an output pin, then enters an infinite loop that turns the LED on and off with a delay of 1 second between each state change. The program can be stopped by pressing `Ctrl-C`, which triggers a `KeyboardInterrupt` and causes the `GPIO.cleanup()` function to be called, resetting the GPIO pins to their default state.



# Get input from two switches and switch on corresponding LEDs

To get input from two switches and switch on corresponding LEDs, you can follow these steps:

1. Connect the two switches to the input pins of a microcontroller or a digital logic circuit.
2. Connect the two LEDs to the output pins of the microcontroller or digital logic circuit.
3. Write a program or design a logic circuit that reads the state of the switches and turns on the corresponding LED when the switch is closed (or pressed).
4. Upload the program to the microcontroller or build the digital logic circuit.
5. Test the circuit by pressing the switches and observing the behavior of the LEDs.

This is a simple example of how to use switches to control the state of LEDs. You can expand on this concept to create more complex circuits and systems.



# Flashing an LED at a given on time and off time cycle

To flash an LED at a given on time and off time cycle, where the two times are taken from a file, the following steps can be followed:

1. **Read the on and off times from the file**: The first step is to read the on and off times from the file. This can be done using a file handling function in the programming language being used. The on and off times should be stored in variables for use in the next steps.

2. **Set up the LED**: The next step is to set up the LED. This involves connecting the LED to the appropriate pins on the microcontroller or development board being used. The LED should be connected in series with a current limiting resistor to prevent damage to the LED.

3. **Create a loop to flash the LED**: A loop should be created to flash the LED. Within the loop, the LED should be turned on for the duration of the on time, and then turned off for the duration of the off time. This can be done using a delay function to pause the program for the specified amount of time.

4. **Run the program**: The final step is to run the program. The LED should flash on and off at the specified on and off times, as read from the file.



# Flash an LED based on cron output (acts as an alarm)

1. **Cron** is a time-based job scheduler in Unix-like operating systems. It is used to schedule commands or scripts to run automatically at a specific time or date.
2. **LED** stands for Light Emitting Diode. It is a semiconductor device that emits light when an electric current is passed through it.
3. To flash an LED based on cron output, you will need to write a script that turns the LED on and off at specific intervals.
4. This script can then be scheduled to run using cron, by adding an entry to the crontab file.
5. The crontab file is a list of commands that are to be executed by cron at specified times. Each line of the file represents a separate job.
6. To add a new job to the crontab file, you can use the `crontab -e` command. This will open the crontab file in the default text editor.
7. In the crontab file, you can specify the time and date when the script should be executed, using the cron syntax.
8. Once the script is scheduled, cron will automatically execute it at the specified time, causing the LED to flash.
9. This can act as an alarm, alerting you to a specific event or time.



# Switch on a relay at a given time using cron

1. **Cron** is a time-based job scheduler in Unix-like operating systems. It is used to schedule commands or scripts to run automatically at a specified time and date.
2. A **relay** is an electrically operated switch. It is used to control a circuit by a separate low-power signal, or where several circuits must be controlled by one signal.
3. To switch on a relay at a given time using cron, the relay's contact terminals must be connected to a load.
4. The first step is to connect the relay to the appropriate GPIO pins on the device, such as a Raspberry Pi.
5. Next, a script must be written to control the relay. This script will use the appropriate commands to switch the relay on and off.
6. Once the script is written and tested, it can be scheduled to run at a specific time using cron. This is done by editing the crontab file and adding a new entry for the script.
7. The entry in the crontab file will specify the time and date when the script should run, as well as the command to run the script.
8. Once the entry is added to the crontab file, cron will automatically run the script at the specified time, switching on the relay and activating the connected load.




# Get the status of a bulb at a remote place (on the LAN) through web

To get the status of a bulb at a remote place (on the LAN) through the web, the following steps can be followed:

1. **Connect to the LAN:** Ensure that the device used to check the status of the bulb is connected to the same Local Area Network (LAN) as the bulb.

2. **Access the bulb's web interface:** The bulb should have a web interface that can be accessed through a web browser. Enter the IP address of the bulb in the web browser to access its web interface.

3. **Check the bulb's status:** Once the web interface is accessed, the status of the bulb can be checked. The status may be displayed on the web interface, or it may be necessary to navigate through the interface to find the status.

4. **Interpret the status:** The status of the bulb may be displayed in different ways, such as text, color, or an icon. Interpret the status according to the bulb's documentation.



# Note: The Instructor may add/delete/modify/tune experiments

- The instructor has the authority to make changes to the experiments.
- The instructor may add new experiments to the course.
- The instructor may delete experiments that are no longer relevant or necessary.
- The instructor may modify experiments to better suit the needs of the course or the students.
- The instructor may tune experiments to make them more effective or efficient.
- These changes may be made at any time and are at the discretion of the instructor.
- It is important for students to be aware of these potential changes and to be prepared to adapt to them.



# KOT 552 INTERNET OF THINGS LAB KCS

The Internet of Things (IoT) is a system of interconnected devices, machines, and objects that are able to collect and exchange data using embedded sensors and network connectivity. The IoT Lab course (KOT 552) focuses on the practical implementation of IoT concepts and technologies.

Some of the key topics covered in this course include:

1. Introduction to IoT and its applications
2. IoT architecture and protocols
3. IoT device programming and interfacing
4. Data acquisition, processing, and visualization
5. IoT security and privacy
6. IoT project development and management

This course provides hands-on experience with IoT technologies and tools, enabling students to design and implement IoT solutions for various applications. Students will work on projects that involve the use of sensors, microcontrollers, and cloud platforms to collect, process, and visualize data.

Overall, the KOT 552 IoT Lab course provides a comprehensive introduction to the field of IoT and prepares students for further study and career opportunities in this rapidly growing field.



## Course Outcome (CO) Bloom’s Knowledge Level (KL)

- Course Outcome (CO) refers to the expected knowledge, skills, and attitudes that students should possess upon completion of a specific course.
- Bloom’s Knowledge Level (KL) is a framework for categorizing educational goals according to the level of cognitive complexity.
- The framework consists of six levels: Remembering, Understanding, Applying, Analyzing, Evaluating, and Creating.
- COs are often mapped to Bloom’s KL to ensure that the course covers a range of cognitive complexity and to facilitate the assessment of student learning.
- Remembering involves recalling previously learned information.
- Understanding involves comprehending the meaning of the information.
- Applying involves using the information in a new situation.
- Analyzing involves breaking down the information into parts and understanding their relationships.
- Evaluating involves making judgments about the value of the information.
- Creating involves combining information to form a new whole.
- By mapping COs to Bloom’s KL, instructors can design course activities and assessments that target the appropriate level of cognitive complexity and ensure that students achieve the desired learning outcomes.



### At the end of the course, the student will be able to:

1. Demonstrate a thorough understanding of the course material.
2. Apply the concepts and theories learned in the course to real-world situations.
3. Analyze and evaluate information critically and effectively.
4. Communicate their ideas and arguments clearly and effectively in written and oral forms.
5. Work collaboratively with others to achieve common goals.
6. Demonstrate ethical and professional behavior in academic and professional settings.
7. Engage in lifelong learning and professional development.
8. Demonstrate the ability to think creatively and solve problems.
9. Use technology effectively to enhance learning and productivity.
10. Demonstrate cultural competence and an appreciation for diversity.




#### CO 1 Understand the concept of Internet of Things K3

- The Internet of Things (IoT) refers to the interconnection of physical devices, vehicles, buildings, and other objects embedded with sensors, software, and network connectivity, which enables these objects to collect and exchange data.
- IoT allows objects to be sensed and controlled remotely across existing network infrastructure, creating opportunities for more direct integration of the physical world into computer-based systems, and resulting in improved efficiency, accuracy, and economic benefit.
- IoT is expected to offer advanced connectivity of devices, systems, and services that goes beyond machine-to-machine (M2M) communications and covers a variety of protocols, domains, and applications.
- IoT is an emerging technology that is rapidly gaining popularity and is expected to have a significant impact on various industries, including healthcare, transportation, and manufacturing.
- IoT devices can range from simple sensors and actuators to complex systems such as autonomous vehicles and smart homes.
- IoT systems typically consist of several components, including sensors, actuators, communication modules, and a central control unit.
- IoT systems can be used for a wide range of applications, including environmental monitoring, smart homes, smart cities, and industrial automation.
- IoT technology is still evolving, and there are many challenges that need to be addressed, including security, privacy, and interoperability.



#### CO 2 Implement interfacing of various sensors with Arduino/Raspberry Pi K4, K5

Interfacing sensors with microcontrollers such as Arduino or Raspberry Pi is a fundamental skill in building electronic projects. Here are the steps to interface various sensors with these microcontrollers:

1. **Identify the sensor and its communication protocol:** Different sensors use different communication protocols such as I2C, SPI, or UART. It is important to identify the communication protocol used by the sensor and ensure that the microcontroller supports it.

2. **Connect the sensor to the microcontroller:** Once the communication protocol is identified, the sensor can be connected to the microcontroller using the appropriate pins. For example, if the sensor uses I2C communication, it should be connected to the SDA and SCL pins on the microcontroller.

3. **Install the necessary libraries:** Most sensors come with libraries that make it easy to interface with the microcontroller. These libraries can be installed using the Arduino Library Manager or by downloading them from the manufacturer's website.

4. **Write the code to interface with the sensor:** Once the sensor is connected and the libraries are installed, the code can be written to interface with the sensor. This typically involves initializing the sensor, reading data from it, and processing the data.

5. **Test the sensor:** After the code is written, it is important to test the sensor to ensure that it is working as expected. This can be done by uploading the code to the microcontroller and observing the output.

By following these steps, various sensors can be interfaced with microcontrollers such as Arduino or Raspberry Pi to build interesting and useful electronic projects.



#### CO 3 Demonstrate the ability to transmit data wirelessly between different devices. K4

1. Wireless data transmission is the transfer of information between two or more devices without the use of wires or cables.
2. There are several technologies that enable wireless data transmission, including Bluetooth, Wi-Fi, and cellular networks.
3. Bluetooth is a short-range wireless technology that is commonly used to connect devices such as smartphones, tablets, and laptops to peripherals such as headphones, speakers, and keyboards.
4. Wi-Fi is a wireless networking technology that allows devices to connect to the internet or to other devices on a local area network (LAN) without the use of cables.
5. Cellular networks, such as 3G, 4G, and 5G, allow mobile devices to connect to the internet and to make phone calls and send text messages without the need for a wired connection.
6. To transmit data wirelessly between different devices, the devices must be equipped with the appropriate wireless technology and must be within range of each other or of a wireless access point.
7. The process of transmitting data wirelessly involves encoding the data into a signal that can be transmitted over the airwaves, transmitting the signal, and then decoding the signal back into data at the receiving device.
8. Wireless data transmission has many advantages, including increased mobility, flexibility, and convenience. However, there are also some challenges, such as security and interference, that must be addressed to ensure reliable and secure wireless data transmission.



#### CO 4 Show an ability to upload/download sensor data on cloud and server K2

1. **Uploading sensor data to the cloud and server:** To upload sensor data to the cloud and server, one must first establish a connection between the sensor and the cloud or server. This can be done using various communication protocols such as Wi-Fi, Bluetooth, or cellular networks.

2. **Storing sensor data:** Once the connection is established, the sensor data can be transmitted to the cloud or server for storage. The data can be stored in various formats such as CSV, JSON, or XML.

3. **Retrieving sensor data:** To retrieve the sensor data from the cloud or server, one can use various methods such as API calls or database queries. The data can then be downloaded and processed for further analysis.

4. **Data security:** It is important to ensure the security of the sensor data during transmission and storage. This can be achieved through encryption and secure authentication methods.

5. **Data analysis:** Once the sensor data is retrieved, it can be analyzed using various tools and techniques to extract meaningful insights. This can help in making informed decisions and improving the performance of the system.

6. **Data visualization:** The sensor data can also be visualized using various tools to provide a graphical representation of the data. This can help in identifying trends and patterns in the data.

In summary, the ability to upload and download sensor data to and from the cloud and server is crucial for effective data management and analysis. It involves establishing a secure connection, transmitting and storing the data, retrieving and analyzing the data, and visualizing the results.



#### CO 5 Examine various SQL queries from MySQL database K4, K5

SQL (Structured Query Language) is a standard language for managing and querying relational databases. MySQL is one of the most popular open-source relational database management systems that use SQL.

Here are some common SQL queries that can be used in a MySQL database:

1. **SELECT**: This query is used to retrieve data from one or more tables in a database. The basic syntax is `SELECT column1, column2, ... FROM table_name;`. You can also use the `WHERE` clause to filter the results based on certain conditions.

2. **INSERT**: This query is used to insert new records into a table. The basic syntax is `INSERT INTO table_name (column1, column2, ...) VALUES (value1, value2, ...);`.

3. **UPDATE**: This query is used to update existing records in a table. The basic syntax is `UPDATE table_name SET column1 = value1, column2 = value2, ... WHERE condition;`.

4. **DELETE**: This query is used to delete existing records from a table. The basic syntax is `DELETE FROM table_name WHERE condition;`.

5. **CREATE**: This query is used to create a new table in a database. The basic syntax is `CREATE TABLE table_name (column1 datatype, column2 datatype, ...);`.

6. **ALTER**: This query is used to add, modify, or delete columns in an existing table. The basic syntax is `ALTER TABLE table_name ADD column_name datatype;` to add a new column, `ALTER TABLE table_name MODIFY COLUMN column_name datatype;` to modify an existing column, and `ALTER TABLE table_name DROP COLUMN column_name;` to delete a column.

7. **DROP**: This query is used to delete an entire table from a database. The basic syntax is `DROP TABLE table_name;`.

These are just some of the basic SQL queries that can be used in a MySQL database. There are many more advanced queries and functions that can be used to perform complex operations on the data. It is important to have a good understanding of SQL and its syntax to effectively work with a MySQL database.



## DETAILED SYLLABUS

A detailed syllabus is a comprehensive outline of the topics, assignments, and assessments that will be covered in a course. It is an essential tool for students to understand the expectations and requirements of the course, and to plan their studies accordingly. A detailed syllabus typically includes the following information:

1. **Course Overview:** A brief description of the course, its objectives, and its relevance to the student's program of study.
2. **Course Schedule:** A week-by-week breakdown of the topics that will be covered in the course, along with the corresponding readings, assignments, and assessments.
3. **Assessment:** A detailed description of the methods that will be used to evaluate the student's performance in the course, including exams, quizzes, papers, presentations, and participation.
4. **Grading Policy:** An explanation of how the student's final grade will be calculated, including the weighting of each assessment component.
5. **Course Materials:** A list of the required and recommended textbooks, articles, and other resources that the student will need to complete the course.
6. **Course Policies:** A set of rules and expectations for behavior in the course, including attendance, participation, late work, and academic integrity.
7. **Contact Information:** The instructor's contact information, including office hours and preferred methods of communication.

A detailed syllabus is an important resource for students, as it provides a clear roadmap for the course and helps them to manage their time and workload effectively. It is also a useful tool for instructors, as it helps to ensure that the course is well-organized and that the expectations and requirements are clearly communicated to the students.



# The Importance of Hands-On Experience with Sensors

It is important for students to have hands-on experience with various sensors such as temperature, humidity, smoke, light, etc. Here are some reasons why:

1. **Practical application of theoretical knowledge:** By working with sensors, students can apply the theoretical knowledge they have learned in the classroom to real-world situations. This helps to reinforce their understanding of the subject matter and makes the learning experience more engaging and meaningful.

2. **Development of problem-solving skills:** Working with sensors often involves troubleshooting and problem-solving. By gaining hands-on experience with sensors, students can develop their critical thinking and problem-solving skills, which are valuable in many areas of life.

3. **Preparation for future careers:** Many industries and fields rely on the use of sensors, including engineering, environmental science, and healthcare. By gaining hands-on experience with sensors, students can better prepare themselves for careers in these fields.

4. **Familiarity with technology:** Sensors are an important part of modern technology. By gaining hands-on experience with sensors, students can become more familiar with the technology and better understand how it works.

In summary, hands-on experience with sensors is valuable for students as it helps to reinforce theoretical knowledge, develop problem-solving skills, prepare for future careers, and become more familiar with technology.



# Control Web Camera, Network, and Relays Connected to the Pi

- The Raspberry Pi is a versatile single-board computer that can be used for a variety of purposes.
- One of the uses of the Raspberry Pi is to control a web camera, network, and relays connected to it.
- To control a web camera connected to the Pi, you can use the `fswebcam` command to capture images from the camera.
- To control the network on the Pi, you can use the `ifconfig` and `iwconfig` commands to configure the network interfaces and wireless settings.
- To control relays connected to the Pi, you can use the `gpio` command to control the GPIO pins on the Pi, which can be used to control the relays.
- By using these commands, you can control the web camera, network, and relays connected to the Pi, allowing you to create a variety of projects and applications.



#### 1. Start Raspberry Pi and try various Linux commands in command terminal window: ls, cd, touch, mv, rm, man

- **ls**: This command is used to list the contents of a directory. By default, it lists the contents of the current directory.
- **cd**: This command is used to change the current directory. It is used in conjunction with a directory path to navigate to a specific directory.
- **touch**: This command is used to create a new, empty file. It can also be used to update the timestamp of an existing file.
- **mv**: This command is used to move or rename files and directories. It takes two arguments: the source file or directory, and the destination.
- **rm**: This command is used to remove files and directories. It can be used with the `-r` flag to remove directories and their contents recursively.
- **man**: This command is used to display the manual page for a given command. It provides detailed information on how to use the command and its various options.

These are some basic Linux commands that can be used in the command terminal window of a Raspberry Pi. It is recommended to try them out and experiment with their various options to become familiar with their usage.



#### mkdir, rmdir, tar, gzip, cat, more, less, ps, sudo, cron, chown, chgrp, ping etc.

- `mkdir`: This command is used to create a new directory.
- `rmdir`: This command is used to remove an empty directory.
- `tar`: This command is used to create or extract tar archives.
- `gzip`: This command is used to compress or decompress files using the gzip format.
- `cat`: This command is used to concatenate files and print them to the standard output.
- `more`: This command is used to display the contents of a file one screen at a time.
- `less`: This command is similar to `more`, but it allows backward movement in the file as well as forward movement.
- `ps`: This command is used to display information about the currently running processes.
- `sudo`: This command is used to execute a command with superuser privileges.
- `cron`: This command is used to schedule commands to be executed at a later time.
- `chown`: This command is used to change the owner of a file or directory.
- `chgrp`: This command is used to change the group of a file or directory.
- `ping`: This command is used to test the reachability of a host on an IP network.

These are some of the commonly used commands in Linux and Unix-like operating systems. They can be very useful for managing files, directories, processes, and system resources. It is important to learn and understand how to use these commands effectively.



#### 2. Run some python programs on Pi like:

1. **Blinking an LED:** This is a simple program that can be run on a Raspberry Pi to blink an LED. The program uses the GPIO pins on the Raspberry Pi to control the LED.

2. **Displaying text on an LCD:** This program can be used to display text on an LCD screen connected to a Raspberry Pi. The program uses the GPIO pins on the Raspberry Pi to send data to the LCD.

3. **Reading sensor data:** This program can be used to read data from sensors connected to a Raspberry Pi. The program uses the GPIO pins on the Raspberry Pi to communicate with the sensors and retrieve data.

4. **Controlling a servo motor:** This program can be used to control the position of a servo motor connected to a Raspberry Pi. The program uses the GPIO pins on the Raspberry Pi to send signals to the servo motor.

5. **Playing sounds:** This program can be used to play sounds on a Raspberry Pi. The program uses the built-in audio output on the Raspberry Pi to play sounds.

These are just a few examples of the many python programs that can be run on a Raspberry Pi. The possibilities are endless and only limited by your imagination.



#### a) Read your name and print Hello message with name

1. To read your name, you can use the `input()` function in Python. This function prompts the user to enter a value and returns the value entered by the user as a string.
2. To print a message with your name, you can use the `print()` function in Python. This function outputs the specified message to the screen.
3. You can combine the `input()` and `print()` functions to read your name and print a hello message with your name.

Here is an example of how you can do this in Python:

```python
name = input("Enter your name: ")
print("Hello, " + name + "!")
```

In this example, the `input()` function prompts the user to enter their name. The value entered by the user is then stored in the `name` variable. The `print()` function is then used to output a hello message with the user's name. The `+` operator is used to concatenate the strings.



#### b) Read two numbers and print their sum, difference, product and division.

To solve this problem, we need to follow these steps:

1. Read two numbers from the user. This can be done using the `input()` function in Python or `scanf()` function in C.
2. Calculate the sum of the two numbers by adding them using the `+` operator.
3. Calculate the difference of the two numbers by subtracting the second number from the first using the `-` operator.
4. Calculate the product of the two numbers by multiplying them using the `*` operator.
5. Calculate the division of the two numbers by dividing the first number by the second using the `/` operator.
6. Print the results of the sum, difference, product, and division.

Here is an example of how this can be implemented in Python:

```python
# Read two numbers from the user
num1 = float(input("Enter the first number: "))
num2 = float(input("Enter the second number: "))

# Calculate the sum, difference, product, and division
sum = num1 + num2
difference = num1 - num2
product = num1 * num2
division = num1 / num2

# Print the results
print("Sum:", sum)
print("Difference:", difference)
print("Product:", product)
print("Division:", division)
```

This code will read two numbers from the user, calculate their sum, difference, product, and division, and then print the results. It is important to note that the division operator `/` will return a floating-point number, even if the two numbers being divided are integers. If you want to perform integer division, you can use the `//` operator instead. Also, make sure to handle the case where the second number is zero, as division by zero is undefined and will result in an error. You can do this by adding an `if` statement to check if the second number is zero before performing the division. If it is, you can print an error message and skip the division calculation.



#### c) Word and character count of a given string

- To count the number of words in a string, we can split the string into a list of words using the `split()` method and then find the length of the list using the `len()` function.
- To count the number of characters in a string, we can use the `len()` function directly on the string.
- For example, consider the string `"Hello, World!"`. The word count can be found using the following code:
```python
string = "Hello, World!"
words = string.split()
word_count = len(words)
print(word_count)
```
This will output `2`, as there are two words in the string.
- The character count can be found using the following code:
```python
string = "Hello, World!"
char_count = len(string)
print(char_count)
```
This will output `13`, as there are 13 characters in the string, including spaces and punctuation.
- It is important to note that the `split()` method splits the string at whitespace characters by default. If the string contains multiple consecutive spaces or other whitespace characters, they will be treated as a single delimiter. If a different delimiter is desired, it can be passed as an argument to the `split()` method.



#### d) Area of a given shape (rectangle, triangle and circle) reading shape and appropriate values from standard

The area of a given shape can be calculated using the appropriate formula for that shape. Here are the formulas for calculating the area of a rectangle, triangle, and circle:

1. **Rectangle**: The area of a rectangle can be calculated by multiplying its length by its width. The formula for the area of a rectangle is `A = l * w`, where `A` is the area, `l` is the length, and `w` is the width.

2. **Triangle**: The area of a triangle can be calculated using the formula `A = (b * h) / 2`, where `A` is the area, `b` is the length of the base, and `h` is the height of the triangle.

3. **Circle**: The area of a circle can be calculated using the formula `A = π * r^2`, where `A` is the area, `π` is approximately equal to 3.14, and `r` is the radius of the circle.

To calculate the area of a given shape, one must first identify the shape and then use the appropriate formula to calculate its area. The values for the variables in the formula can be read from standard input. For example, if the shape is a rectangle, the user can input the length and width of the rectangle, and the program can use these values to calculate the area of the rectangle using the formula `A = l * w`. Similarly, for a triangle, the user can input the base and height, and for a circle, the user can input the radius. The program can then use these values to calculate the area of the given shape using the appropriate formula.



#### Input

- Input refers to the process of entering data or information into a computer or other electronic device.
- This can be done through various input devices such as a keyboard, mouse, touch screen, microphone, or scanner.
- The data entered can be in the form of text, images, audio, or video.
- Input is an essential part of any computer system as it allows the user to interact with the system and provide it with the necessary information to perform tasks.
- The type of input device used can vary depending on the task being performed and the user's preferences.
- For example, a keyboard is commonly used for entering text, while a mouse is used for navigating and selecting items on a screen.
- Touch screens have become increasingly popular in recent years, allowing users to interact with their devices through touch gestures.
- Microphones can be used for voice input, allowing users to control their devices through voice commands or dictation.
- Scanners can be used to input physical documents or images into a computer system for digital storage or processing.



#### 3. Run some python programs on Pi like: a) Print a name 'n' times, where name and n are read from standard input,

To run a Python program on a Raspberry Pi, you will need to have Python installed on the device. Most Raspberry Pi operating systems come with Python pre-installed, but if not, you can install it using the following command:

```
sudo apt-get install python3
```

Once Python is installed, you can create a new Python file using a text editor such as nano. For example, to create a new file called `print_name.py`, you can use the following command:

```
nano print_name.py
```

In the text editor, you can write the following Python code to print a name 'n' times, where name and n are read from standard input:

```python
name = input("Enter a name: ")
n = int(input("Enter a number: "))

for i in range(n):
    print(name)
```

After writing the code, you can save and exit the text editor by pressing `Ctrl + X`, then `Y` to confirm, and `Enter` to save the changes.

To run the Python program, you can use the following command:

```
python3 print_name.py
```

This will prompt you to enter a name and a number, and then it will print the name the specified number of times.



#### Using for and while loops
- `for` and `while` loops are used to iterate over a block of code multiple times.
- `for` loops are used when the number of iterations is known beforehand.
- `while` loops are used when the number of iterations is not known beforehand and depends on a condition.

#### Handle Divided by Zero Exception
- Dividing by zero is an illegal operation in mathematics and programming.
- In most programming languages, attempting to divide by zero will result in an error or exception.
- To handle this exception, you can use a `try-except` block to catch the exception and handle it gracefully.

#### Print current time for 10 times with an interval of 1 second
- To print the current time, you can use the `datetime` module in Python.
- To print the current time for 10 times with an interval of 1 second, you can use a `for` loop and the `sleep` function from the `time` module.
- Here is an example code snippet:
```python
import datetime
import time

for i in range(10):
    print(datetime.datetime.now())
    time.sleep(1)
```



#### Interval of 10 seconds
- An interval of 10 seconds refers to a time period or duration of 10 seconds.
- This interval can be used in various contexts, such as setting a timer, scheduling a task, or measuring the time between two events.

#### Read a file line by line and print the word count of each line
- To read a file line by line, you can use a loop to iterate over each line in the file.
- For each line, you can use the `split()` method to split the line into a list of words.
- The length of this list will give you the word count of the line.
- You can then print the word count of each line as you iterate through the file.

Here is an example of how this can be done in Python:

```python
with open('file.txt', 'r') as f:
    for line in f:
        words = line.split()
        word_count = len(words)
        print(word_count)
```

This code opens the file `file.txt` in read mode, and uses a `for` loop to iterate over each line in the file. For each line, the `split()` method is used to split the line into a list of words, and the length of this list is calculated to give the word count of the line. The word count is then printed for each line.



#### 4. a) Light an LED through Python program

1. To light an LED through a Python program, you will need a Raspberry Pi, an LED, a breadboard, and some jumper wires.
2. Connect the LED to the Raspberry Pi using the breadboard and jumper wires. The positive leg of the LED should be connected to a GPIO pin on the Raspberry Pi, and the negative leg should be connected to a ground pin.
3. In your Python program, import the `RPi.GPIO` module, which allows you to control the GPIO pins on the Raspberry Pi.
4. Set the GPIO pin connected to the LED as an output pin using the `GPIO.setup()` function.
5. Use the `GPIO.output()` function to turn the LED on and off by setting the output pin to `GPIO.HIGH` or `GPIO.LOW`.
6. You can also control the brightness of the LED by using pulse-width modulation (PWM) to vary the duty cycle of the output pin.

Here is an example Python program that lights an LED connected to GPIO pin 17 on the Raspberry Pi:

```python
import RPi.GPIO as GPIO
import time

LED_PIN = 17

GPIO.setmode(GPIO.BCM)
GPIO.setup(LED_PIN, GPIO.OUT)

while True:
    GPIO.output(LED_PIN, GPIO.HIGH)
    time.sleep(1)
    GPIO.output(LED_PIN, GPIO.LOW)
    time.sleep(1)
```

This program turns the LED on for one second, then off for one second, in an infinite loop. You can modify the program to control the LED in different ways.



#### b) Get input from two switches and switch on corresponding LEDs

To get input from two switches and switch on corresponding LEDs, follow these steps:

1. Connect the two switches to the input pins of the microcontroller.
2. Connect the two LEDs to the output pins of the microcontroller.
3. Write a program to read the input from the switches.
4. If the first switch is pressed, turn on the first LED.
5. If the second switch is pressed, turn on the second LED.
6. If both switches are pressed, turn on both LEDs.
7. If neither switch is pressed, turn off both LEDs.

This is a simple way to get input from two switches and switch on corresponding LEDs. You can modify the program to add more functionality or to change the behavior of the LEDs. For example, you could make the LEDs blink or change color depending on the input from the switches. You can also use this setup to control other devices or to perform other actions based on the input from the switches.



#### c) Flash an LED at a given on time and off time cycle, where the two times are taken from a file.

1. To flash an LED at a given on time and off time cycle, the first step is to read the on and off times from a file. This can be done using a file input/output function in the programming language of choice.
2. Once the on and off times have been read from the file, the next step is to configure the LED to turn on and off according to the specified times. This can be done using a timer or delay function in the programming language of choice.
3. The LED can then be turned on for the specified on time and turned off for the specified off time, creating a flashing effect.
4. This process can be repeated in a loop to continuously flash the LED according to the specified on and off times.



#### 5. a) Flash an LED based on cron output (acts as an alarm)

Cron is a time-based job scheduler in Unix-like operating systems. It is used to schedule commands or scripts to run automatically at a specified time and date. Here are the steps to flash an LED based on cron output:

1. Connect an LED to the appropriate GPIO pin on your device.
2. Write a script to control the LED. This script should turn the LED on and off at the desired intervals to create a flashing effect.
3. Test the script to ensure that it is working as expected.
4. Use the `crontab` command to open the crontab file for editing.
5. Add a new line to the crontab file specifying the desired schedule for the LED flashing script. The format for a crontab entry is as follows: `m h dom mon dow command`, where `m` is the minute, `h` is the hour, `dom` is the day of the month, `mon` is the month, `dow` is the day of the week, and `command` is the command to be executed.
6. Save and close the crontab file. The cron daemon will automatically read the updated crontab file and schedule the LED flashing script to run at the specified time.

By following these steps, you can use cron to schedule an LED to flash at a specific time, acting as an alarm.



#### b) Switch on a relay at a given time using cron, where the relay's contact terminals are connected to a load.

1. A relay is an electrically operated switch that can be used to control a circuit by a low-power signal.
2. Cron is a time-based job scheduler in Unix-like operating systems that can be used to schedule tasks to run periodically at fixed times, dates, or intervals.
3. To switch on a relay at a given time using cron, the following steps can be followed:
    1. Connect the relay's contact terminals to the load that needs to be controlled.
    2. Write a script that sends a signal to the relay to switch it on.
    3. Open the crontab file by typing `crontab -e` in the terminal.
    4. Add a new line to the crontab file in the format `m h dom mon dow command`, where `m` is the minute, `h` is the hour, `dom` is the day of the month, `mon` is the month, `dow` is the day of the week, and `command` is the command to be executed. The command should be the path to the script that switches on the relay.
    5. Save and close the crontab file.
4. At the specified time, cron will execute the command, which will run the script and switch on the relay, thereby controlling the load connected to the relay's contact terminals.



#### c) Get the status of a bulb at a remote place (on the LAN) through web.

To get the status of a bulb at a remote place on the LAN through the web, the following steps can be taken:

1. **Connect the bulb to the network:** The bulb must be connected to the local area network (LAN) and have an IP address assigned to it.

2. **Set up a web server:** A web server must be set up on the network, which can communicate with the bulb and retrieve its status.

3. **Create a web page:** A web page must be created, which can send a request to the web server to retrieve the status of the bulb.

4. **Send a request to the web server:** The web page sends a request to the web server to retrieve the status of the bulb.

5. **Retrieve the status of the bulb:** The web server communicates with the bulb and retrieves its status.

6. **Display the status of the bulb:** The web server sends the status of the bulb back to the web page, which displays it to the user.

In summary, to get the status of a bulb at a remote place on the LAN through the web, the bulb must be connected to the network, a web server must be set up to communicate with the bulb, a web page must be created to send a request to the web server, and the web server must retrieve and send the status of the bulb back to the web page for display to the user.



#### Note: The Instructor may add/delete/modify/tune experiments

- The instructor has the authority to make changes to the experiments.
- These changes may include adding new experiments, deleting existing ones, modifying the details of the experiments, or tuning the experiments to better suit the needs of the course.
- It is important for students to be aware of this, as the experiments may change throughout the course.
- Students should regularly check for updates and changes to the experiments to ensure they are prepared for the course.



# KOT 553 INTERNET OF THINGS LAB KCS

- KOT 553 is a course on the Internet of Things (IoT) Lab offered by the KCS department.
- The course focuses on the practical implementation of IoT concepts and technologies.
- Students will learn how to design, develop, and deploy IoT systems.
- The course covers topics such as sensors, actuators, connectivity, cloud computing, and data analytics.
- Students will work on hands-on projects to apply their knowledge and skills.
- The course is designed to provide students with a strong foundation in IoT and prepare them for careers in this rapidly growing field.



# Course Outcome (CO) and Bloom’s Knowledge Level (KL)

Course Outcome (CO) refers to the expected knowledge, skills, and abilities that a student should possess upon completion of a specific course. COs are used to evaluate the effectiveness of the course and to ensure that the course content aligns with the overall program goals.

Bloom’s Knowledge Level (KL) is a framework for categorizing educational learning objectives into levels of complexity and specificity. The levels are: Remembering, Understanding, Applying, Analyzing, Evaluating, and Creating. These levels can be used to assess the level of understanding and mastery of a subject by a student.

In the context of COs, Bloom’s KL can be used to define the level of understanding and mastery expected from a student for each CO. For example, a CO may require a student to demonstrate understanding of a concept (Bloom’s KL: Understanding), while another CO may require a student to apply a concept to solve a problem (Bloom’s KL: Applying).

By aligning COs with Bloom’s KL, educators can ensure that the course content is appropriately challenging and that students are able to demonstrate mastery of the subject at the desired level of complexity and specificity. This can also help in the design of assessments and evaluation of student learning.

In summary, COs and Bloom’s KL are tools that can be used to ensure that course content is aligned with program goals and that students are able to demonstrate mastery of the subject at the desired level of complexity and specificity. They can also be used to design assessments and evaluate student learning.



# At the end of the course, the student will be able to:

1. Demonstrate a comprehensive understanding of the course material.
2. Apply the knowledge and skills acquired during the course to real-life situations.
3. Analyze and evaluate information critically and effectively.
4. Communicate ideas and arguments clearly and effectively.
5. Work collaboratively with others to achieve common goals.
6. Demonstrate ethical and responsible behavior.
7. Engage in lifelong learning and continuous professional development.




# CO 1 Understand the concept of Internet of Things K3

The Internet of Things (IoT) is a concept that describes the interconnectivity of everyday objects and devices through the internet or other communication networks. These devices, often referred to as "smart" devices, are equipped with sensors, processing ability, and software that allow them to collect, store, and share data with other devices and systems.

1. **Description:** IoT includes many "smart," computer-like devices that can connect with the Internet or interact via wireless networks. These "things" include phones, appliances, thermostats, lighting systems, irrigation systems, security cameras, vehicles, and even animals and cities .
2. **Data Collection and Sharing:** IoT devices can collect data, store data, or share data with each other over their connections. This data can only be accessed by other smart-things .
3. **Physical Objects:** IoT describes physical objects (or groups of such objects) with sensors, processing ability, software, and other technologies that connect and exchange data with other devices and systems .
4. **Applications:** IoT technology can be used in various applications, such as detecting popular areas in a showroom, identifying fast-selling components, and automatically aligning sales data with supply . It is also enabling smart cities and, in the future, driverless cars .
5. **Fourth Industrial Revolution:** Along with other emerging technologies such as AI, the IoT is part of the Fourth Industrial Revolution .




# CO2 Implement interfacing of various sensors with Arduino/Raspberry Pi K4, K5

1. **Introduction:** Interfacing sensors with microcontrollers such as Arduino or Raspberry Pi is a common practice in the field of electronics and robotics. This allows for the collection of data from the environment and the ability to control and interact with the physical world.

2. **Types of Sensors:** There are many types of sensors that can be interfaced with Arduino or Raspberry Pi, including temperature, humidity, light, sound, motion, and gas sensors. Each sensor has its own unique characteristics and methods of interfacing.

3. **Interfacing with Arduino:** To interface a sensor with an Arduino, the sensor's output must be connected to one of the Arduino's input pins. The Arduino can then read the sensor's output using its analog or digital input functions. The specific method of interfacing will vary depending on the type of sensor being used.

4. **Interfacing with Raspberry Pi:** Interfacing a sensor with a Raspberry Pi is similar to interfacing with an Arduino. The sensor's output must be connected to one of the Raspberry Pi's input pins, and the Raspberry Pi can then read the sensor's output using its input functions. The specific method of interfacing will vary depending on the type of sensor being used.

5. **Programming:** Once the sensor is interfaced with the microcontroller, it is necessary to write code to read the sensor's output and perform any desired actions based on the data. This can be done using the programming language and development environment of the microcontroller, such as the Arduino IDE or Python for the Raspberry Pi.

6. **Applications:** Interfacing sensors with microcontrollers has many practical applications, including environmental monitoring, home automation, robotics, and more. The specific application will depend on the type of sensor being used and the desired outcome.



# CO 3 Demonstrate the ability to transmit data wirelessly between different devices. K4

- Wireless data transmission is the transfer of information between two or more devices without the use of physical connections.
- There are several technologies that enable wireless data transmission, including Bluetooth, Wi-Fi, and cellular networks.
- Bluetooth is a short-range wireless technology that is commonly used to connect devices such as smartphones, tablets, and laptops to peripherals such as headphones, speakers, and keyboards.
- Wi-Fi is a wireless networking technology that uses radio waves to provide high-speed internet and network connections to devices within a local area network (LAN).
- Cellular networks use radio waves to transmit data between mobile devices and cell towers, enabling mobile internet access and communication over long distances.
- To transmit data wirelessly between different devices, the devices must be equipped with the appropriate wireless technology and be within range of each other or a wireless access point.
- The process of transmitting data wirelessly involves encoding the data into a signal that can be transmitted over the airwaves, transmitting the signal, and then decoding the signal back into data at the receiving device.
- Wireless data transmission has many advantages, including increased mobility, flexibility, and convenience. However, it also has some challenges, such as security and interference.



# CO 4: Show an ability to upload/download sensor data on cloud and server K2

1. **Uploading sensor data to the cloud and server K2**: To upload sensor data to the cloud and server K2, you need to have an account with a cloud service provider that supports data storage and retrieval. Once you have an account, you can use the provider's API or web interface to upload your data.

2. **Downloading sensor data from the cloud and server K2**: To download sensor data from the cloud and server K2, you need to have an account with a cloud service provider that supports data storage and retrieval. Once you have an account, you can use the provider's API or web interface to download your data.

3. **Benefits of uploading and downloading sensor data to/from the cloud and server K2**: Uploading and downloading sensor data to/from the cloud and server K2 allows you to store and access your data from anywhere with an internet connection. This can be useful for remote monitoring and analysis of sensor data.

4. **Security considerations when uploading and downloading sensor data to/from the cloud and server K2**: When uploading and downloading sensor data to/from the cloud and server K2, it is important to ensure that your data is secure. This can be achieved by using encryption and secure authentication methods when transferring data.

5. **Examples of cloud service providers that support uploading and downloading of sensor data**: Some examples of cloud service providers that support uploading and downloading of sensor data include Amazon Web Services, Microsoft Azure, and Google Cloud Platform. These providers offer a range of services and tools for storing and managing sensor data in the cloud.



# CO 5 Examine various SQL queries from MySQL database K4, K5

SQL (Structured Query Language) is a standard language used to manage and manipulate data stored in relational databases. MySQL is one of the most popular open-source relational database management systems that use SQL.

Here are some common SQL queries used in MySQL:

1. **SELECT**: Used to retrieve data from one or more tables in a database. For example, to retrieve all data from a table named `customers`, the query would be `SELECT * FROM customers;`.

2. **INSERT**: Used to add new records to a table. For example, to insert a new customer record into the `customers` table, the query would be `INSERT INTO customers (first_name, last_name, email) VALUES ('John', 'Doe', 'john.doe@email.com');`.

3. **UPDATE**: Used to modify existing records in a table. For example, to update the email address of a customer in the `customers` table, the query would be `UPDATE customers SET email = 'new.email@email.com' WHERE first_name = 'John' AND last_name = 'Doe';`.

4. **DELETE**: Used to delete records from a table. For example, to delete a customer record from the `customers` table, the query would be `DELETE FROM customers WHERE first_name = 'John' AND last_name = 'Doe';`.

5. **CREATE**: Used to create a new table in a database. For example, to create a new table named `orders` with columns `order_id`, `customer_id`, and `order_date`, the query would be `CREATE TABLE orders (order_id INT, customer_id INT, order_date DATE);`.

6. **ALTER**: Used to modify the structure of an existing table. For example, to add a new column named `order_total` to the `orders` table, the query would be `ALTER TABLE orders ADD COLUMN order_total DECIMAL(10,2);`.

7. **DROP**: Used to delete a table from a database. For example, to delete the `orders` table from the database, the query would be `DROP TABLE orders;`.

These are just a few examples of the many SQL queries that can be used in a MySQL database. It is important to have a good understanding of these queries and their syntax in order to effectively manage and manipulate data in a MySQL database.



# DETAILED SYLLABUS

A detailed syllabus is an outline or summary of the topics that will be covered in a course. It is typically provided by the instructor at the beginning of the course and serves as a guide for students to understand the course objectives, expectations, and requirements.

A detailed syllabus may include the following information:

1. Course title, number, and description
2. Instructor's name and contact information
3. Course objectives and learning outcomes
4. Course schedule, including dates and topics for lectures, assignments, and exams
5. Required and recommended reading materials
6. Grading policy and criteria for evaluation
7. Attendance and participation expectations
8. Policies on late work, missed exams, and academic integrity
9. Information on how to access course materials and resources
10. Any additional information or instructions specific to the course.

A detailed syllabus is an important tool for students to plan their studies and manage their time effectively. It is also a useful reference for instructors to ensure that the course content is delivered in a structured and organized manner. Students are encouraged to review the syllabus regularly and to communicate with the instructor if they have any questions or concerns.



# Hands-on Experience with Various Sensors

It is important for students to have hands-on experience with various sensors, including but not limited to:

1. **Temperature sensors**: These sensors measure the temperature of the environment or an object. They can be used in a variety of applications, such as monitoring the temperature of a room or a refrigerator, or measuring the temperature of a patient's body.

2. **Humidity sensors**: These sensors measure the amount of moisture in the air. They can be used in applications such as monitoring the humidity level in a room or greenhouse, or measuring the moisture content of soil.

3. **Smoke sensors**: These sensors detect the presence of smoke in the air. They are commonly used in fire alarms and can be used to detect fires in buildings or forests.

4. **Light sensors**: These sensors measure the amount of light in the environment. They can be used in applications such as automatically turning on lights when it gets dark, or adjusting the brightness of a display based on the ambient light.

Having hands-on experience with these sensors allows students to understand how they work and how they can be used in real-world applications. This knowledge can be valuable in a variety of fields, including engineering, science, and technology.



# Using Control Web Camera, Network, and Relays Connected to the Pi

1. **Web Camera**: A web camera can be connected to the Raspberry Pi via USB. Once connected, the camera can be controlled using various software packages, such as `fswebcam` or `motion`. These packages allow the user to capture images, record video, and stream live video from the camera.

2. **Network**: The Raspberry Pi can be connected to a network via Ethernet or Wi-Fi. Once connected, the user can access the internet, transfer files, and communicate with other devices on the network. The `ifconfig` and `iwconfig` commands can be used to configure the network settings on the Pi.

3. **Relays**: Relays can be connected to the Raspberry Pi's GPIO pins to control external devices, such as lights or motors. The `gpio` command can be used to control the state of the GPIO pins, and thus the state of the connected relay. The user can write scripts to automate the control of the relay, or use software packages such as `wiringPi` to control the relay in real-time.

In summary, the Raspberry Pi can be used to control a web camera, access a network, and control relays connected to its GPIO pins. This allows the user to create a wide range of projects, from home automation to remote monitoring and control.



# Starting Raspberry Pi and trying various Linux commands in the command terminal window

1. **Starting Raspberry Pi**: To start the Raspberry Pi, connect the power supply to the micro USB port on the board. The device should boot up and display the desktop or command line interface.

2. **Using the command terminal window**: To access the command terminal window, click on the terminal icon on the desktop or use the keyboard shortcut `Ctrl + Alt + T`.

3. **Linux commands**:
    - `ls`: This command is used to list the contents of a directory. By default, it lists the contents of the current directory.
    - `cd`: This command is used to change the current directory. For example, `cd Documents` would change the current directory to the `Documents` directory.
    - `touch`: This command is used to create a new, empty file. For example, `touch newfile.txt` would create a new file named `newfile.txt` in the current directory.
    - `mv`: This command is used to move or rename files or directories. For example, `mv oldfile.txt newfile.txt` would rename the file `oldfile.txt` to `newfile.txt`.
    - `rm`: This command is used to remove files or directories. For example, `rm file.txt` would remove the file `file.txt` from the current directory.
    - `man`: This command is used to display the manual page for a command. For example, `man ls` would display the manual page for the `ls` command.

These are some basic Linux commands that can be used in the command terminal window on a Raspberry Pi. It is recommended to refer to the manual pages for more detailed information on each command.



# Linux Commands

## mkdir
- `mkdir` is a command used to create a new directory.
- The syntax is `mkdir [options] directory_name`.
- Some common options include `-p` to create parent directories as needed, and `-m` to set the file mode.

## rmdir
- `rmdir` is a command used to remove an empty directory.
- The syntax is `rmdir [options] directory_name`.
- A common option is `-p` to remove the directory and its parent directories if they are empty.

## tar
- `tar` is a command used to create or extract tar archives.
- The syntax is `tar [options] [archive_name] [file1 file2 ...]`.
- Some common options include `-c` to create a new archive, `-x` to extract an archive, and `-f` to specify the archive file name.

## gzip
- `gzip` is a command used to compress or decompress files.
- The syntax is `gzip [options] [file1 file2 ...]`.
- Some common options include `-d` to decompress, `-k` to keep the original file, and `-l` to list the compressed file information.

## cat
- `cat` is a command used to concatenate files and print them to the standard output.
- The syntax is `cat [options] [file1 file2 ...]`.
- A common option is `-n` to number the output lines.

## more
- `more` is a command used to display the contents of a file one screen at a time.
- The syntax is `more [options] [file1 file2 ...]`.
- Some common options include `-d` to display help, and `-l` to suppress pause after form feed.

## less
- `less` is a command used to display the contents of a file with more advanced features than `more`.
- The syntax is `less [options] [file1 file2 ...]`.
- Some common options include `-N` to display line numbers, and `-S` to chop long lines.

## ps
- `ps` is a command used to report the current processes.
- The syntax is `ps [options]`.
- Some common options include `-e` to display all processes, and `-f` to display full format listing.

## sudo
- `sudo` is a command used to execute a command as another user, usually the superuser.
- The syntax is `sudo [options] [command]`.
- Some common options include `-u` to specify the user to run the command as, and `-l` to list the allowed commands.

## cron
- `cron` is a daemon used to run scheduled commands.
- The syntax for the crontab file is `m h dom mon dow command`.
- Each field represents a time unit, with `m` for minutes, `h` for hours, `dom` for day of the month, `mon` for month, and `dow` for day of the week.

## chown
- `chown` is a command used to change the owner of a file or directory.
- The syntax is `chown [options] owner[:group] file1 [file2 ...]`.
- Some common options include `-R` to operate on files and directories recursively, and `-h` to affect symbolic links.

## chgrp
- `chgrp` is a command used to change the group of a file or directory.
- The syntax is `chgrp [options] group file1 [file2 ...]`.
- Some common options include `-R` to operate on files and directories recursively, and `-h` to affect symbolic links.

## ping
- `ping` is a command used to test the reachability of a host on an IP network.
- The syntax is `ping [options] destination`.
- Some common options include `-c` to specify the number of packets to send, and `-W` to specify the timeout for each packet.



# Running Python Programs on Raspberry Pi

Raspberry Pi is a small, affordable, and versatile computer that can be used to run various programs, including those written in Python. Here are some steps to run Python programs on Raspberry Pi:

1. **Install Python**: Raspberry Pi comes with Python pre-installed, but it is always a good idea to make sure that you have the latest version. You can do this by running the following command in the terminal: `sudo apt-get update && sudo apt-get upgrade`.

2. **Write a Python Program**: You can write a Python program using any text editor on Raspberry Pi. Save the file with a `.py` extension.

3. **Run the Python Program**: To run the Python program, open the terminal, navigate to the directory where the program is saved, and type `python3 filename.py`, where `filename.py` is the name of the Python file.

4. **Use Python Libraries**: Raspberry Pi has a large collection of Python libraries that can be used to perform various tasks. You can install these libraries using the `pip3` command. For example, to install the `gpiozero` library, you can run the command `pip3 install gpiozero`.

These are some basic steps to run Python programs on Raspberry Pi. You can explore more advanced features and libraries to enhance the capabilities of your programs.



# Read your name and print Hello message with name

1. To read your name, you can use the `input()` function in Python. This function allows the user to enter a value, which is then stored as a string.
2. To print a message with the name, you can use the `print()` function. This function outputs the specified message to the screen.
3. You can use string concatenation to combine the "Hello" message with the name entered by the user.

Here is an example of how this can be done:

```python
name = input("Enter your name: ")
print("Hello, " + name + "!")
```

In this example, the user is prompted to enter their name using the `input()` function. The entered name is then stored in the `name` variable. The `print()` function is then used to output a greeting message that includes the user's name. The greeting message is created by concatenating the strings "Hello, ", the value of the `name` variable, and "!".



# b) Read two numbers and print their sum, difference, product and division.

1. To read two numbers, we can use the `input()` function in Python. This function reads a line from input, converts it to a string, and returns it. We can then convert this string to an integer using the `int()` function.

```python
num1 = int(input("Enter the first number: "))
num2 = int(input("Enter the second number: "))
```

2. To calculate the sum of the two numbers, we can use the `+` operator.

```python
sum = num1 + num2
print("The sum of the two numbers is:", sum)
```

3. To calculate the difference of the two numbers, we can use the `-` operator.

```python
difference = num1 - num2
print("The difference of the two numbers is:", difference)
```

4. To calculate the product of the two numbers, we can use the `*` operator.

```python
product = num1 * num2
print("The product of the two numbers is:", product)
```

5. To calculate the division of the two numbers, we can use the `/` operator.

```python
division = num1 / num2
print("The division of the two numbers is:", division)
```

6. The complete code to read two numbers and print their sum, difference, product and division is as follows:

```python
num1 = int(input("Enter the first number: "))
num2 = int(input("Enter the second number: "))

sum = num1 + num2
print("The sum of the two numbers is:", sum)

difference = num1 - num2
print("The difference of the two numbers is:", difference)

product = num1 * num2
print("The product of the two numbers is:", product)

division = num1 / num2
print("The division of the two numbers is:", division)
```



# Word and character count of a given string

1. Word count refers to the number of words in a given string or text.
2. Character count refers to the number of characters, including spaces and punctuation, in a given string or text.
3. To count the number of words in a string, one can split the string into a list of words using a delimiter such as a space, and then find the length of the list.
4. To count the number of characters in a string, one can simply find the length of the string.
5. In some cases, it may be necessary to exclude certain characters, such as spaces or punctuation, from the character count. This can be done by iterating over the characters in the string and only counting characters that meet certain criteria.
6. Both word and character counts can be useful for various purposes, such as determining the length of a document or measuring the complexity of a piece of text.




# Area of a given shape (rectangle, triangle and circle) reading shape and appropriate values from standard

The area of a shape is the measure of the region enclosed by the shape. The formulas for calculating the area of common shapes such as rectangles, triangles, and circles are as follows:

1. **Rectangle**: The area of a rectangle is calculated by multiplying its length by its width. The formula is `Area = length × width`.

2. **Triangle**: The area of a triangle can be calculated using the formula `Area = (base × height) ÷ 2`, where the base is the length of one side of the triangle and the height is the perpendicular distance from the base to the opposite vertex.

3. **Circle**: The area of a circle is calculated using the formula `Area = π × radius²`, where π is approximately equal to 3.14 and the radius is the distance from the center of the circle to any point on its circumference.

To calculate the area of a given shape, one must first identify the shape and then use the appropriate formula with the given values. For example, to calculate the area of a rectangle with a length of 5 units and a width of 3 units, one would use the formula for the area of a rectangle and substitute the given values to get `Area = 5 × 3 = 15 square units`.



# Input

- Input refers to the data or information that is entered into a computer system for processing.
- Input can come from various sources, such as a keyboard, mouse, microphone, scanner, or other input devices.
- Input devices are hardware components that allow users to interact with a computer by entering data or commands.
- The data entered through input devices is then processed by the computer's central processing unit (CPU) and stored in memory for later use.
- Input is an essential part of any computer system, as it allows users to provide the necessary data for the computer to perform tasks and operations.
- Common input devices include keyboards, mice, touchscreens, and voice recognition systems.
- Input can also come from external sources, such as a network connection or a storage device, and can be in various forms, such as text, images, audio, or video.
- The type of input device used can depend on the task being performed and the user's preferences.
- Input is typically processed by the computer's operating system and software applications, which interpret the data and perform the appropriate actions.



# Running Python Programs on Raspberry Pi

One of the many things you can do with a Raspberry Pi is to run Python programs. Here is an example of a simple Python program that can be run on a Raspberry Pi:

## Example: Printing a Name 'n' Times

This program will print a name 'n' times, where the name and the value of 'n' are read from standard input.

```python
name = input("Enter a name: ")
n = int(input("Enter the number of times to print the name: "))

for i in range(n):
    print(name)
```

To run this program on a Raspberry Pi, follow these steps:

1. Open a terminal window on the Raspberry Pi.
2. Use a text editor to create a new file with the above Python code. Save the file with a `.py` extension, for example `print_name.py`.
3. In the terminal, navigate to the directory where you saved the file.
4. Type the command `python3 print_name.py` and press enter to run the program.
5. Follow the prompts to enter the name and the number of times to print it.

After entering the required information, the program will print the name the specified number of times.



# Using for and while loops, Handling Divided by Zero Exception, and Printing current time for 10 times with an interval of 1 second

## For and While Loops
- `for` and `while` loops are used to iterate over a block of code multiple times.
- The `for` loop is used when the number of iterations is known beforehand.
- The `while` loop is used when the number of iterations is not known beforehand and depends on a condition.

## Handling Divided by Zero Exception
- Dividing a number by zero is not allowed in mathematics and results in an error in most programming languages.
- To handle this error, a try-except block can be used.
- The code that might cause the error is placed inside the `try` block.
- If an error occurs, the code inside the `except` block is executed.

## Printing current time for 10 times with an interval of 1 second
- The `time` module can be used to get the current time and to add a delay between iterations.
- The `time.time()` function returns the current time in seconds since the epoch.
- The `time.sleep()` function can be used to add a delay between iterations.
- A `for` loop can be used to iterate 10 times and print the current time with an interval of 1 second between each iteration.

Here is an example code that demonstrates the above concepts:

```python
import time

# Using for and while loops
for i in range(5):
    print(i)
    
i = 0
while i < 5:
    print(i)
    i += 1

# Handling Divided by Zero Exception
try:
    x = 5 / 0
except ZeroDivisionError:
    print("Error: Cannot divide by zero")

# Printing current time for 10 times with an interval of 1 second
for i in range(10):
    print(time.ctime())
    time.sleep(1)
```



# Interval of 10 seconds

An interval of 10 seconds refers to a time period or duration of 10 seconds. It can be used in various contexts, such as setting a timer, scheduling tasks, or measuring time between events.

# Reading a file line by line and printing the word count of each line

To read a file line by line and print the word count of each line, the following steps can be followed:

1. Open the file in read mode using a file object.
2. Use a loop to iterate over the file object, which will return one line at a time.
3. For each line, use the `split()` method to split the line into a list of words.
4. Use the `len()` function to find the number of words in the list.
5. Print the word count for the current line.
6. Continue iterating over the file object until all lines have been processed.
7. Close the file object.

Here is an example code snippet in Python that demonstrates this process:

```python
with open('file.txt', 'r') as f:
    for line in f:
        words = line.split()
        word_count = len(words)
        print(word_count)
```

This code opens the file `file.txt` in read mode, iterates over each line in the file, splits the line into a list of words, finds the word count, and prints it. The `with` statement is used to automatically close the file object when the block of code is finished.



# 4. a) Light an LED through Python program

To light an LED through a Python program, you will need the following components:
1. A single-board computer such as a Raspberry Pi or a microcontroller such as an Arduino.
2. An LED.
3. A resistor.
4. Jumper wires.
5. A breadboard.

Here are the steps to light an LED through a Python program:
1. Connect the LED to the single-board computer or microcontroller using the jumper wires and breadboard. Make sure to connect the longer leg of the LED (the anode) to a GPIO pin and the shorter leg (the cathode) to a ground pin through a resistor.
2. Install the necessary libraries for controlling the GPIO pins. For example, on a Raspberry Pi, you can use the RPi.GPIO library.
3. Write a Python program to control the GPIO pin connected to the LED. Here is an example program that turns the LED on for 5 seconds and then turns it off:

```python
import RPi.GPIO as GPIO
import time

# Set the pin numbering mode
GPIO.setmode(GPIO.BCM)

# Set the pin connected to the LED as an output pin
LED_PIN = 18
GPIO.setup(LED_PIN, GPIO.OUT)

# Turn the LED on
GPIO.output(LED_PIN, GPIO.HIGH)
time.sleep(5)

# Turn the LED off
GPIO.output(LED_PIN, GPIO.LOW)

# Clean up the GPIO pins
GPIO.cleanup()
```

4. Run the Python program to light the LED.

These are the basic steps to light an LED through a Python program. You can modify the program to control the LED in different ways, such as blinking or changing its brightness.



# Get input from two switches and switch on corresponding LEDs

To get input from two switches and switch on corresponding LEDs, the following steps can be followed:

1. Connect the two switches to the input pins of the microcontroller. The input pins should be configured as digital inputs.
2. Connect the two LEDs to the output pins of the microcontroller. The output pins should be configured as digital outputs.
3. In the program, read the state of the switches using the digitalRead() function.
4. If the state of the first switch is HIGH, turn on the first LED by setting the corresponding output pin to HIGH using the digitalWrite() function.
5. If the state of the second switch is HIGH, turn on the second LED by setting the corresponding output pin to HIGH using the digitalWrite() function.
6. If the state of the first switch is LOW, turn off the first LED by setting the corresponding output pin to LOW using the digitalWrite() function.
7. If the state of the second switch is LOW, turn off the second LED by setting the corresponding output pin to LOW using the digitalWrite() function.
8. Repeat the above steps in a loop to continuously check the state of the switches and control the LEDs accordingly.

This is a basic example of how to get input from two switches and switch on corresponding LEDs using a microcontroller. The exact implementation may vary depending on the specific microcontroller and programming language used.



# Flashing an LED at a Given On Time and Off Time Cycle

To flash an LED at a given on time and off time cycle, where the two times are taken from a file, the following steps can be followed:

1. **Read the on and off times from the file:** The first step is to read the on and off times from the file. This can be done using a file handling function in the programming language being used. The on and off times should be stored in variables for use in the next steps.

2. **Set up the LED:** The next step is to set up the LED. This involves connecting the LED to the appropriate pins on the microcontroller or development board being used. The LED should be connected in series with a current limiting resistor to prevent damage to the LED.

3. **Create a loop to flash the LED:** The next step is to create a loop that will flash the LED. The loop should turn the LED on for the on time specified in the file, then turn the LED off for the off time specified in the file. This can be done using a delay function to pause the program for the specified on and off times.

4. **Run the program:** The final step is to run the program. The LED should flash on and off at the specified on and off times, as read from the file.



# Flash an LED based on cron output (acts as an alarm)

1. **Cron** is a time-based job scheduler in Unix-like operating systems. It is used to schedule commands or scripts to run automatically at a specific time or date.

2. To flash an LED based on cron output, you will need to have a **Raspberry Pi** or similar device with **GPIO** (General Purpose Input/Output) pins, an **LED**, and a **resistor**.

3. First, connect the LED to one of the GPIO pins on the Raspberry Pi using the resistor to limit the current and prevent damage to the LED.

4. Next, create a **script** that will control the LED. This script should include commands to turn the LED on and off.

5. Once the script is created, you can use **crontab** to schedule the script to run at a specific time or date. This can be done by editing the crontab file using the command `crontab -e` and adding a new line with the desired schedule and the path to the script.

6. When the cron job runs at the scheduled time, it will execute the script and flash the LED, acting as an alarm.

7. You can also use the **output** of a cron job to control the LED. For example, if the cron job produces a certain output when it runs, you can use this output to trigger the LED to flash.

8. This can be done by modifying the script to read the output of the cron job and using conditional statements to control the LED based on the output.

9. In summary, flashing an LED based on cron output can be achieved by connecting an LED to a Raspberry Pi, creating a script to control the LED, and using crontab to schedule the script to run at a specific time or based on the output of a cron job. This can act as an alarm to notify you when a scheduled task has completed.



# Switch on a relay at a given time using cron

1. **Cron** is a time-based job scheduler in Unix-like operating systems. It is used to schedule commands or scripts to run automatically at a specified time and date.

2. **Relay** is an electrically operated switch. It is used to control a circuit by a separate low-power signal, or where several circuits must be controlled by one signal.

3. To switch on a relay at a given time using cron, the relay's contact terminals must be connected to a load.

4. The first step is to create a script that will switch on the relay. This script can be written in a programming language such as Python or Bash.

5. The script should include commands to control the relay, such as sending a signal to the relay's control pin to switch it on.

6. Once the script is created, it can be scheduled to run at a specific time using cron.

7. To schedule the script, open the crontab file by typing `crontab -e` in the terminal.

8. In the crontab file, add a new line with the time and date when the script should run, followed by the path to the script. For example, to run the script every day at 8:00 AM, the line would be `0 8 * * * /path/to/script`.

9. Save and close the crontab file. The script will now run automatically at the specified time, switching on the relay and controlling the connected load.

10. It is important to ensure that the script is executable and that the user running the cron job has the necessary permissions to execute the script and control the relay.



# Get the status of a bulb at a remote place (on the LAN) through web

1. To get the status of a bulb at a remote place on the LAN through the web, the bulb must be a smart bulb that can connect to the internet.
2. The smart bulb must be connected to the same LAN as the device being used to check its status.
3. The smart bulb must have an IP address assigned to it on the LAN.
4. The user must know the IP address of the smart bulb.
5. The user can then use a web browser to navigate to the IP address of the smart bulb.
6. The web page of the smart bulb should display its current status, whether it is on or off.
7. The user can also use a mobile application provided by the manufacturer of the smart bulb to check its status.
8. The mobile application must be connected to the same LAN as the smart bulb.
9. The mobile application will display the current status of the smart bulb.
10. The user can also use a home automation system to check the status of the smart bulb.
11. The home automation system must be connected to the same LAN as the smart bulb.
12. The home automation system will display the current status of the smart bulb.



# Note: The Instructor may add/delete/modify/tune experiments

- The instructor has the authority to make changes to the experiments.
- The instructor may add new experiments to the course.
- The instructor may delete experiments that are no longer relevant or necessary.
- The instructor may modify experiments to better suit the needs of the course or the students.
- The instructor may tune experiments to make them more effective or efficient.
- These changes may be made at any time and are at the discretion of the instructor.
- It is important for students to be aware of these potential changes and to be prepared to adapt to them.



# KOT 553 INTERNET OF THINGS LAB KCS

- Internet of Things (IoT) is a network of physical objects or "things" embedded with electronics, software, sensors, and connectivity to enable these objects to collect and exchange data.
- IoT allows objects to be sensed and controlled remotely across existing network infrastructure, creating opportunities for more direct integration of the physical world into computer-based systems, and resulting in improved efficiency, accuracy, and economic benefit.
- The IoT Lab course (KOT 553) provides students with hands-on experience in designing, building, and deploying IoT systems.
- The course covers the fundamental concepts and technologies of IoT, including sensors, actuators, communication protocols, and cloud computing.
- Students will learn how to use various hardware and software tools to develop IoT applications and will work on projects to design and implement IoT systems.
- The course is designed for students with a background in computer science or a related field, and assumes prior knowledge of programming and basic networking concepts.
- The course is part of the KCS (Korea Computer Science) program, which offers a range of courses in computer science and related fields.




## Course Outcome (CO) Bloom’s Knowledge Level (KL)

- Course Outcome (CO) refers to the expected knowledge, skills, and attitudes that students should possess upon completion of a specific course.
- Bloom’s Taxonomy is a framework for categorizing educational goals and objectives into different levels of complexity and specificity.
- Bloom’s Knowledge Level (KL) is the first and most basic level of Bloom’s Taxonomy, which focuses on the recall and recognition of information.
- At the Knowledge Level, students are expected to demonstrate their ability to remember and retrieve previously learned material.
- This can include recalling facts, terms, basic concepts, and answers to questions.
- Assessments at the Knowledge Level may include tasks such as defining, listing, identifying, and labeling.
- The Knowledge Level is an important foundation for higher levels of learning, as it provides the necessary information and understanding for more complex cognitive processes.
- In the context of Course Outcomes, the Knowledge Level can be used to assess whether students have achieved a basic understanding of the course material.
- By aligning Course Outcomes with Bloom’s Taxonomy, instructors can ensure that their assessments accurately measure student learning and progress.



### At the end of the course, the student will be able to:

1. Demonstrate a comprehensive understanding of the course material.
2. Apply the concepts and theories learned in the course to real-world situations.
3. Analyze and evaluate information critically and effectively.
4. Communicate ideas and arguments clearly and effectively in written and oral forms.
5. Work collaboratively with others to achieve common goals.
6. Demonstrate ethical and professional behavior in academic and professional settings.
7. Engage in lifelong learning and professional development.




#### CO 1 Understand the concept of Internet of Things K3

- The Internet of Things (IoT) refers to the interconnection of physical devices, vehicles, buildings, and other objects embedded with sensors, software, and network connectivity, which enables these objects to collect and exchange data.
- IoT allows objects to be sensed and controlled remotely across existing network infrastructure, creating opportunities for more direct integration of the physical world into computer-based systems, and resulting in improved efficiency, accuracy, and economic benefit.
- IoT is expected to offer advanced connectivity of devices, systems, and services that goes beyond machine-to-machine (M2M) communications and covers a variety of protocols, domains, and applications.
- IoT is a rapidly growing field, with an estimated 50 billion connected devices by 2020.
- IoT has the potential to impact various industries, including healthcare, transportation, agriculture, and manufacturing, among others.
- IoT devices can range from simple sensors to complex systems, and can include consumer devices such as smart home appliances, as well as industrial systems such as connected manufacturing equipment.
- IoT systems typically use a combination of technologies, including sensors, wireless communication, cloud computing, and data analytics, to collect, transmit, and analyze data from connected devices.
- Security and privacy are major concerns in the development and deployment of IoT systems, as the large amount of data collected and transmitted by these systems can potentially be vulnerable to hacking and misuse.
- The development of IoT requires collaboration and standardization across industries and technologies to ensure interoperability and maximize the potential benefits of this technology.



#### CO 2 Implement interfacing of various sensors with Arduino/Raspberry Pi K4, K5

Interfacing sensors with microcontrollers such as Arduino or Raspberry Pi is a crucial skill in building interactive electronic projects. Here are some key points to consider when implementing this:

1. **Identify the sensor and its communication protocol**: Different sensors use different communication protocols, such as I2C, SPI, or UART. It is important to identify the protocol used by the sensor and ensure that the microcontroller supports it.

2. **Connect the sensor to the microcontroller**: Once the communication protocol is identified, the sensor can be connected to the microcontroller using the appropriate pins. For example, if the sensor uses I2C, it should be connected to the SDA and SCL pins on the microcontroller.

3. **Install necessary libraries**: Some sensors require specific libraries to be installed in order to interface with the microcontroller. These libraries can usually be found on the manufacturer's website or through a simple web search.

4. **Write code to read data from the sensor**: Once the sensor is connected and the necessary libraries are installed, code can be written to read data from the sensor. This will vary depending on the sensor and communication protocol used.

5. **Test and calibrate the sensor**: After the code is written, it is important to test and calibrate the sensor to ensure that it is providing accurate readings. This may involve adjusting the code or the physical setup of the sensor.

Overall, interfacing sensors with microcontrollers such as Arduino or Raspberry Pi involves identifying the communication protocol, connecting the sensor, installing necessary libraries, writing code to read data, and testing and calibrating the sensor. By following these steps, one can successfully implement the interfacing of various sensors with microcontrollers.



#### CO 3 Demonstrate the ability to transmit data wirelessly between different devices. K4

1. **Wireless data transmission** refers to the transfer of information between two or more devices without the use of physical connections such as cables or wires.
2. There are several **wireless technologies** that can be used for data transmission, including Wi-Fi, Bluetooth, cellular networks, and satellite communication.
3. **Wi-Fi** is a popular wireless technology that allows devices to connect to a local area network (LAN) and access the internet. It operates in the 2.4 GHz and 5 GHz frequency bands and can achieve high data transfer rates.
4. **Bluetooth** is another widely used wireless technology that enables short-range communication between devices. It is commonly used for connecting peripherals such as keyboards, mice, and headphones to computers and mobile devices.
5. **Cellular networks** provide wireless communication over long distances and are used for mobile voice and data services. They operate using a system of interconnected base stations, or cell sites, that transmit and receive signals over a range of frequencies.
6. **Satellite communication** is a form of wireless communication that uses satellites to transmit data over long distances. It is commonly used for global positioning systems (GPS), satellite television, and satellite internet services.
7. To transmit data wirelessly between different devices, the devices must be equipped with the appropriate wireless technology and be within range of each other or a wireless access point. The data is then transmitted using radio waves or infrared signals.
8. Wireless data transmission has several advantages over wired transmission, including increased mobility, flexibility, and ease of installation. However, it can also be subject to interference, security risks, and range limitations.



#### CO 4 Show an ability to upload/download sensor data on cloud and server K2

1. **Uploading sensor data to the cloud:** Sensor data can be uploaded to the cloud using various methods such as using an API provided by the cloud service provider or by using a cloud storage client library. The data can be uploaded in various formats such as CSV, JSON, or XML.

2. **Downloading sensor data from the cloud:** Sensor data can be downloaded from the cloud using methods such as using an API provided by the cloud service provider or by using a cloud storage client library. The data can be downloaded in various formats such as CSV, JSON, or XML.

3. **Uploading sensor data to a server:** Sensor data can be uploaded to a server using various methods such as using an FTP client or by using a server-side script to handle the upload. The data can be uploaded in various formats such as CSV, JSON, or XML.

4. **Downloading sensor data from a server:** Sensor data can be downloaded from a server using methods such as using an FTP client or by using a server-side script to handle the download. The data can be downloaded in various formats such as CSV, JSON, or XML.

5. **Data security:** When uploading or downloading sensor data, it is important to ensure the security of the data. This can be achieved by using encryption, secure protocols such as HTTPS or SFTP, and by using strong authentication methods.

6. **Data integrity:** It is important to ensure the integrity of the sensor data when uploading or downloading it. This can be achieved by using checksums or by using data validation methods to ensure that the data has not been corrupted during transmission.

7. **Data management:** Proper data management is important when uploading or downloading sensor data. This includes organizing the data in a logical manner, ensuring that the data is properly backed up, and ensuring that the data is easily accessible to authorized users.



#### CO 5 Examine various SQL queries from MySQL database K4, K5

SQL (Structured Query Language) is a standard language used to manage and manipulate data stored in relational databases. MySQL is one of the most popular open-source relational database management systems that use SQL.

Here are some common SQL queries that can be used in a MySQL database:

1. **SELECT**: This query is used to retrieve data from one or more tables in a database. The basic syntax is `SELECT column1, column2, ... FROM table_name;`. You can also use the `WHERE` clause to filter the results based on certain conditions.

2. **INSERT**: This query is used to insert new records into a table. The basic syntax is `INSERT INTO table_name (column1, column2, ...) VALUES (value1, value2, ...);`.

3. **UPDATE**: This query is used to update existing records in a table. The basic syntax is `UPDATE table_name SET column1 = value1, column2 = value2, ... WHERE condition;`.

4. **DELETE**: This query is used to delete records from a table. The basic syntax is `DELETE FROM table_name WHERE condition;`.

5. **CREATE**: This query is used to create a new table in a database. The basic syntax is `CREATE TABLE table_name (column1 datatype, column2 datatype, ...);`.

6. **ALTER**: This query is used to add, modify, or delete columns in an existing table. The basic syntax is `ALTER TABLE table_name ADD column_name datatype;` to add a new column, `ALTER TABLE table_name MODIFY COLUMN column_name datatype;` to modify an existing column, and `ALTER TABLE table_name DROP COLUMN column_name;` to delete a column.

7. **DROP**: This query is used to delete a table, view, index, or other objects in a database. The basic syntax is `DROP object_type object_name;`.

These are just a few examples of the many SQL queries that can be used in a MySQL database. It is important to have a good understanding of these queries and their syntax in order to effectively manage and manipulate data in a MySQL database.



## DETAILED SYLLABUS

A detailed syllabus is a comprehensive outline of the topics, assignments, and assessments that will be covered in a course. It is typically provided by the instructor at the beginning of the course and serves as a guide for students to understand the expectations and requirements of the course.

Some key components of a detailed syllabus may include:

1. Course description: A brief overview of the course, including its objectives and goals.
2. Course schedule: A list of topics and assignments, along with their due dates, that will be covered throughout the course.
3. Assessment methods: A description of how students will be evaluated, including exams, quizzes, papers, and projects.
4. Grading policy: An explanation of how grades will be calculated and assigned.
5. Course materials: A list of required and recommended textbooks, articles, and other resources.
6. Instructor information: Contact information for the instructor, including office hours and preferred methods of communication.
7. Course policies: A list of rules and expectations for behavior in the course, including attendance, participation, and academic integrity.

A detailed syllabus is an important tool for students to succeed in a course. It provides a clear roadmap of what is expected and allows students to plan and manage their time effectively. It is important for students to review the syllabus carefully and ask any questions they may have to ensure they understand the course requirements.



# The Importance of Hands-On Experience with Sensors

It is important for students to have hands-on experience with various sensors such as temperature, humidity, smoke, light, etc. for several reasons:

1. **Practical Application:** Hands-on experience allows students to apply theoretical knowledge in a practical setting. This helps to reinforce learning and improve understanding of the subject matter.

2. **Skill Development:** Working with sensors helps students to develop important skills such as problem-solving, critical thinking, and data analysis. These skills are valuable in many fields and can be applied in a variety of contexts.

3. **Real-World Relevance:** Sensors are widely used in many industries, including agriculture, healthcare, and manufacturing. By gaining hands-on experience with sensors, students can better understand their real-world applications and potential uses.

4. **Innovation and Creativity:** Experimenting with sensors can inspire students to think creatively and come up with new ideas and solutions. This can foster a spirit of innovation and help students to develop an entrepreneurial mindset.

Overall, hands-on experience with sensors is an important part of a well-rounded education. It helps students to develop practical skills, deepen their understanding of the subject matter, and prepare for future careers.



# Control Web Camera, Network, and Relays Connected to the Pi

- The Raspberry Pi is a versatile single-board computer that can be used for a variety of purposes, including controlling a web camera, network, and relays.
- To control a web camera connected to the Pi, you can use the `fswebcam` command to capture images or the `motion` package to stream video.
- To control the network on the Pi, you can use the `ifconfig` command to configure the network interface or the `iwconfig` command to configure wireless networking.
- To control relays connected to the Pi, you can use the `gpio` command to control the GPIO pins on the Pi, which can be used to turn the relays on and off.
- By combining these commands and packages, you can create a powerful system for controlling a web camera, network, and relays connected to the Pi.



#### 1. Start Raspberry Pi and try various Linux commands in command terminal window: ls, cd, touch, mv, rm, man

To start using your Raspberry Pi, you will need to connect it to a power source and a display. Once it is powered on, you can access the command terminal window to try various Linux commands.

Here are some basic Linux commands that you can try in the command terminal window:

- `ls`: This command is used to list the contents of a directory. By default, it lists the contents of the current directory.
- `cd`: This command is used to change the current directory. You can use it to navigate through the file system.
- `touch`: This command is used to create a new, empty file. You can specify the name of the file you want to create as an argument.
- `mv`: This command is used to move or rename files or directories. You can specify the source and destination as arguments.
- `rm`: This command is used to remove files or directories. You can specify the file or directory you want to remove as an argument.
- `man`: This command is used to display the manual page for a command. You can specify the command you want to learn more about as an argument.

These are just a few of the many Linux commands that you can try on your Raspberry Pi. You can use the `man` command to learn more about each command and its usage.



#### mkdir, rmdir, tar, gzip, cat, more, less, ps, sudo, cron, chown, chgrp, ping etc.

- `mkdir`: This command is used to create a new directory.
- `rmdir`: This command is used to remove an empty directory.
- `tar`: This command is used to create and extract compressed archive files.
- `gzip`: This command is used to compress and decompress files.
- `cat`: This command is used to display the contents of a file.
- `more`: This command is used to display the contents of a file one screen at a time.
- `less`: This command is similar to `more`, but it allows backward movement in the file as well as forward movement.
- `ps`: This command is used to display information about the currently running processes.
- `sudo`: This command is used to execute a command with superuser privileges.
- `cron`: This command is used to schedule tasks to run automatically at specified times.
- `chown`: This command is used to change the owner of a file or directory.
- `chgrp`: This command is used to change the group ownership of a file or directory.
- `ping`: This command is used to test the connectivity between two network devices.

These are some of the commonly used commands in Linux and Unix-like operating systems. They can be very useful for managing files, directories, processes, and system resources. It is important to learn and understand how to use these commands effectively.



#### 2. Run some python programs on Pi like:

1. **Blinking LED:** This is a simple program that can be run on a Raspberry Pi to blink an LED. The program uses the GPIO pins on the Raspberry Pi to control the LED.

2. **Temperature Sensor:** This program reads data from a temperature sensor connected to the Raspberry Pi and displays the temperature on the screen.

3. **Web Server:** This program sets up a simple web server on the Raspberry Pi. The server can be accessed from a web browser to display information or control the Raspberry Pi.

4. **Camera Module:** This program uses the Raspberry Pi camera module to take pictures or record videos. The program can be set up to take pictures at regular intervals or to record a video when motion is detected.

5. **Robotics:** The Raspberry Pi can be used to control a robot. This program uses the GPIO pins on the Raspberry Pi to control the motors of the robot. The program can be set up to make the robot move in different directions or perform specific tasks.

These are some examples of python programs that can be run on a Raspberry Pi. There are many more possibilities and the programs can be customized to suit the needs of the user.



#### a) Read your name and print Hello message with name

1. To read your name, you can use the `input()` function in Python. This function prompts the user to enter some data and returns the data as a string.
2. To print the Hello message with the name, you can use the `print()` function. This function outputs the specified message to the screen.
3. Here is an example of how you can read your name and print a Hello message with the name:

```python
name = input("Enter your name: ")
print("Hello, " + name + "!")
```

In this example, the `input()` function prompts the user to enter their name. The entered name is then stored in the `name` variable. The `print()` function is then used to output the Hello message with the name. The `+` operator is used to concatenate the strings.



#### b) Read two numbers and print their sum, difference, product and division.

To solve this problem, we need to follow these steps:

1. Read two numbers from the user. This can be done using the `input()` function in Python, for example.
2. Calculate the sum of the two numbers by adding them together using the `+` operator.
3. Calculate the difference of the two numbers by subtracting one from the other using the `-` operator.
4. Calculate the product of the two numbers by multiplying them together using the `*` operator.
5. Calculate the division of the two numbers by dividing one by the other using the `/` operator.
6. Print the results of the calculations to the screen using the `print()` function.

Here is an example of how this can be implemented in Python:

```python
# Read two numbers from the user
num1 = float(input("Enter the first number: "))
num2 = float(input("Enter the second number: "))

# Calculate the sum, difference, product, and division
sum = num1 + num2
difference = num1 - num2
product = num1 * num2
division = num1 / num2

# Print the results
print("Sum:", sum)
print("Difference:", difference)
print("Product:", product)
print("Division:", division)
```

This code reads two numbers from the user, performs the necessary calculations, and prints the results to the screen. The user can then see the sum, difference, product, and division of the two numbers they entered.



#### c) Word and character count of a given string.

- Word count refers to the number of words in a given string.
- Character count refers to the number of characters in a given string.
- To count the number of words in a string, one can split the string into a list of words using a delimiter such as a space, and then find the length of the list.
- To count the number of characters in a string, one can simply find the length of the string.
- For example, consider the string "Hello, world!". The word count of this string is 2, as it contains two words separated by a space. The character count of this string is 13, as it contains 13 characters including punctuation and spaces.
- It is important to note that the definition of a "word" may vary depending on the context. Some may consider only alphabetic characters as words, while others may include numbers and punctuation as well.
- Similarly, the definition of a "character" may also vary. Some may consider only visible characters, while others may include whitespace and control characters as well.



#### d) Area of a given shape (rectangle, triangle and circle) reading shape and appropriate values from standard

The area of a given shape can be calculated using the appropriate formula for that shape. The following are the formulas for calculating the area of a rectangle, triangle, and circle:

1. **Rectangle**: The area of a rectangle can be calculated by multiplying its length by its width. The formula for the area of a rectangle is `A = l * w`, where `A` is the area, `l` is the length, and `w` is the width.

2. **Triangle**: The area of a triangle can be calculated using the formula `A = (b * h) / 2`, where `A` is the area, `b` is the length of the base, and `h` is the height of the triangle.

3. **Circle**: The area of a circle can be calculated using the formula `A = π * r^2`, where `A` is the area, `π` is approximately equal to 3.14, and `r` is the radius of the circle.

To calculate the area of a given shape, the appropriate values must be read from the standard. For example, to calculate the area of a rectangle, the length and width must be read from the standard. Similarly, to calculate the area of a triangle, the base and height must be read from the standard, and to calculate the area of a circle, the radius must be read from the standard. Once the appropriate values have been read, the formula for the given shape can be used to calculate its area.



#### Input

- Input refers to the process of entering data or information into a computer or other electronic device.
- Input can be done through various methods, such as using a keyboard, mouse, touch screen, voice recognition, or other input devices.
- The data or information entered through input can be in various forms, such as text, numbers, images, or audio.
- Input is an essential part of any computer system, as it allows the user to interact with the system and provide it with the necessary data to perform tasks.
- Input devices are designed to make the process of inputting data as easy and efficient as possible. Some common input devices include keyboards, mice, touch screens, and microphones.
- The type of input device used can depend on the task being performed and the preferences of the user. For example, a graphic designer may prefer to use a graphics tablet for input, while a gamer may prefer to use a gaming mouse.
- Input is processed by the computer's central processing unit (CPU) and stored in memory for use by the system. The data can then be manipulated, analyzed, or used to perform tasks as needed.
- Input is a crucial part of any computer system, and the efficiency and accuracy of input can greatly affect the performance of the system. It is important to choose the right input devices and methods to ensure that data is entered accurately and efficiently.



#### 3. Run some python programs on Pi like: a) Print a name 'n' times, where name and n are read from standard input

To run a Python program on a Raspberry Pi, you will need to have Python installed on the device. Python is pre-installed on the Raspberry Pi OS, so you can start writing and running Python programs right away.

Here is an example of a Python program that will print a name 'n' times, where the name and the value of n are read from standard input:

```python
name = input("Enter a name: ")
n = int(input("Enter the number of times to print the name: "))

for i in range(n):
    print(name)
```

To run this program on a Raspberry Pi, you can follow these steps:

1. Open a terminal window on the Raspberry Pi.
2. Use a text editor to create a new file with the above Python code. For example, you can use the `nano` text editor by typing `nano program.py` in the terminal and pasting the code into the editor.
3. Save the file and exit the text editor.
4. In the terminal, navigate to the directory where you saved the file and type `python3 program.py` to run the program.
5. Follow the prompts to enter a name and the number of times to print it.

This program will read the name and the value of n from standard input, and then use a for loop to print the name n times. You can modify the code to change the behavior of the program or to add additional functionality. For example, you could add error handling to check that the value of n is a valid integer, or you could add additional input prompts to collect more information from the user.



#### Using for and while loops
- `for` and `while` loops are used to iterate over a block of code multiple times.
- `for` loops are used when the number of iterations is known beforehand.
- `while` loops are used when the number of iterations is not known beforehand and the loop continues until a certain condition is met.

#### Handle Divided by Zero Exception
- Dividing by zero is an illegal operation in mathematics and programming languages.
- In Python, attempting to divide by zero raises a `ZeroDivisionError` exception.
- This exception can be handled using a `try`-`except` block to prevent the program from crashing.

#### Print current time for 10 times with an interval of 1 second
- The `time` module in Python provides functions to work with time-related tasks.
- The `time.time()` function returns the current time in seconds since the epoch.
- The `time.sleep()` function can be used to pause the execution of the program for a specified number of seconds.
- To print the current time for 10 times with an interval of 1 second, a `for` loop can be used in combination with the `time` module functions.

Here is an example code snippet that demonstrates this:

```python
import time

for i in range(10):
    print(time.ctime())
    time.sleep(1)
```



#### Interval of 10 seconds
- An interval of 10 seconds refers to a time period or duration of 10 seconds.
- This interval can be used in various contexts, such as setting a timer, scheduling a task, or measuring the time between two events.

#### Read a file line by line and print the word count of each line
- To read a file line by line, one can use a loop to iterate over each line in the file.
- The word count of each line can be calculated by splitting the line into words using a delimiter such as a space or a comma, and counting the number of resulting words.
- The word count can then be printed for each line as it is read from the file.
- This can be useful for analyzing the contents of a text file, such as counting the frequency of words or identifying patterns in the text.



#### 4. a) Light an LED through Python program

To light an LED through a Python program, the following steps can be followed:

1. **Gather the necessary components**: This includes a Raspberry Pi, an LED, a breadboard, a resistor, and jumper wires.
2. **Set up the circuit**: Connect the LED to the Raspberry Pi using the breadboard, resistor, and jumper wires. The resistor is used to limit the current flowing through the LED to prevent it from burning out.
3. **Write the Python code**: Use the GPIO library in Python to control the GPIO pins on the Raspberry Pi. Set the pin connected to the LED as an output pin and use the `output()` function to turn the LED on and off.
4. **Run the Python program**: Run the Python program on the Raspberry Pi to light the LED.

Here is an example of a Python program that can be used to light an LED:

```python
import RPi.GPIO as GPIO
import time

LED_PIN = 17

GPIO.setmode(GPIO.BCM)
GPIO.setup(LED_PIN, GPIO.OUT)

while True:
    GPIO.output(LED_PIN, GPIO.HIGH)
    time.sleep(1)
    GPIO.output(LED_PIN, GPIO.LOW)
    time.sleep(1)
```

This program sets the GPIO pin connected to the LED as an output pin and turns the LED on and off with a delay of 1 second between each state change. The `while True` loop ensures that the LED continues to blink until the program is stopped.



#### b) Get input from two switches and switch on corresponding LEDs

To get input from two switches and switch on corresponding LEDs, the following steps can be followed:

1. Connect the two switches to the input pins of the microcontroller.
2. Connect the two LEDs to the output pins of the microcontroller.
3. Write a program to read the input from the switches and turn on the corresponding LEDs.
4. Upload the program to the microcontroller.
5. Test the circuit by pressing the switches and observing the LEDs.

In this way, the input from the two switches can be used to control the corresponding LEDs. The program can be modified to implement different behaviors, such as turning on both LEDs when both switches are pressed, or turning off the LEDs when the switches are released. The circuit can also be expanded to include more switches and LEDs, or other components such as buzzers or motors.



#### c) Flash an LED at a given on time and off time cycle, where the two times are taken from a file.

1. To flash an LED at a given on time and off time cycle, you will need to use a microcontroller or a similar device that can control the LED.
2. The on time and off time values can be stored in a file, which can be read by the microcontroller.
3. The file can be in any format, such as a text file or a CSV file, as long as the microcontroller can read and parse the data.
4. Once the on time and off time values are read from the file, the microcontroller can use these values to control the LED.
5. The microcontroller can turn the LED on for the specified on time, then turn it off for the specified off time, and repeat this cycle indefinitely.
6. The on time and off time values can be changed by updating the file, and the microcontroller can read the new values and update the LED flashing accordingly.



#### 5. a) Flash an LED based on cron output (acts as an alarm)

Cron is a time-based job scheduler in Unix-like operating systems. It is used to schedule commands or scripts to run automatically at a specified time and date. Here are the steps to flash an LED based on cron output:

1. Connect the LED to the appropriate GPIO pin on your device.
2. Write a script that turns the LED on and off at the desired interval.
3. Use the `crontab` command to open the crontab file for editing.
4. Add a new line to the crontab file specifying the desired time and date for the script to run, followed by the path to the script.
5. Save and close the crontab file.

Once the specified time and date is reached, the cron daemon will automatically execute the script, causing the LED to flash. This can act as an alarm to notify the user of a scheduled event.



#### b) Switch on a relay at a given time using cron, where the relay's contact terminals are connected to a load.

1. A relay is an electrically operated switch that can be used to control the flow of electricity to a load.
2. Cron is a time-based job scheduler in Unix-like operating systems, which can be used to schedule tasks to run at specific times.
3. To switch on a relay at a given time using cron, the following steps can be followed:
    1. Write a script that sends a signal to the relay to switch it on.
    2. Open the crontab file by typing `crontab -e` in the terminal.
    3. Add a new line to the crontab file in the following format: `m h dom mon dow command`, where `m` is the minute, `h` is the hour, `dom` is the day of the month, `mon` is the month, `dow` is the day of the week, and `command` is the command to run the script.
    4. Save and close the crontab file.
4. At the specified time, cron will run the script, which will send a signal to the relay to switch it on, allowing electricity to flow to the load connected to the relay's contact terminals.



#### c) Get the status of a bulb at a remote place (on the LAN) through web.

1. To get the status of a bulb at a remote place on the LAN through the web, you need to have a device that can connect to the internet and access the LAN.
2. The bulb must be connected to a smart home system or a device that can communicate its status over the LAN.
3. You can use a web interface or an app to access the smart home system or device and check the status of the bulb.
4. The status of the bulb can be displayed as on or off, and in some cases, the brightness level or color of the bulb can also be shown.
5. You can also use a web API to retrieve the status of the bulb programmatically if the smart home system or device supports it.
6. It is important to ensure that the connection to the LAN and the smart home system or device is secure to prevent unauthorized access to the bulb's status.



#### Note: The Instructor may add/delete/modify/tune experiments

- The instructor has the authority to make changes to the experiments.
- These changes may include adding new experiments, deleting existing ones, modifying the details of the experiments, or fine-tuning them to better suit the needs of the course.
- It is important for students to be aware of this, as the experiments are an integral part of the course and their understanding of the subject matter.
- Students should regularly check for updates and changes to the experiments, and adjust their study plans accordingly.
- This flexibility allows the instructor to tailor the course to the needs of the students and ensure that the material is up-to-date and relevant.




# KOT 554 INTERNET OF THINGS LAB KCS

- KOT 554 is a course code for the Internet of Things Lab offered by the KCS department.
- The Internet of Things (IoT) is a network of interconnected devices that can communicate with each other and exchange data.
- In this lab, students will learn how to design, implement, and test IoT systems.
- Topics covered in this lab may include:
    - Introduction to IoT and its applications
    - IoT architecture and protocols
    - IoT device programming and interfacing
    - IoT data management and analytics
    - IoT security and privacy
- This lab provides hands-on experience with IoT technologies and prepares students for careers in the field of IoT.



# Course Outcome (CO) and Bloom’s Knowledge Level (KL)

Course Outcome (CO) refers to the expected knowledge, skills, and attitudes that students should possess upon completion of a specific course. These outcomes are used to assess the effectiveness of the course and to ensure that the course content aligns with the overall program goals.

Bloom’s Knowledge Level (KL) is a framework for categorizing educational learning objectives into levels of complexity and specificity. The levels are: Remembering, Understanding, Applying, Analyzing, Evaluating, and Creating. These levels can be used to assess the level of understanding that students have achieved in a course.

- COs are used to assess the effectiveness of a course and to ensure alignment with program goals.
- Bloom’s KL provides a framework for categorizing educational learning objectives.
- The levels of Bloom’s KL are: Remembering, Understanding, Applying, Analyzing, Evaluating, and Creating.
- These levels can be used to assess student understanding in a course.




# At the end of the course, the student will be able to:

1. Demonstrate a comprehensive understanding of the course material.
2. Apply the concepts and theories learned in the course to real-world situations.
3. Analyze and evaluate information critically and effectively.
4. Communicate ideas and arguments clearly and effectively in written and oral forms.
5. Work collaboratively with others to achieve common goals.
6. Demonstrate ethical and professional behavior in academic and professional settings.
7. Engage in lifelong learning and professional development.




# CO 1 Understand the concept of Internet of Things K3

The Internet of Things (IoT) is a network of interconnected devices that are embedded with sensors, software, and other technologies to exchange data with other devices and systems over the internet.

- IoT devices can range from simple sensors to complex machines, and can include consumer devices such as smart home appliances, wearable technology, and connected vehicles.
- IoT technology enables devices to communicate and interact with each other, allowing for the automation of various tasks and the collection of data for analysis and decision-making.
- IoT has the potential to revolutionize many industries, including healthcare, transportation, and manufacturing, by improving efficiency, reducing costs, and enhancing the user experience.
- The growth of IoT has been driven by advances in technology, such as the development of low-cost sensors and the increasing availability of high-speed internet connectivity.
- Security and privacy are major concerns in the development and deployment of IoT systems, as the large amount of data collected by these devices can be sensitive and potentially vulnerable to cyber attacks.



# CO2 Implement interfacing of various sensors with Arduino/Raspberry Pi K4, K5

1. Interfacing sensors with Arduino or Raspberry Pi involves connecting the sensor to the microcontroller and writing code to read data from the sensor.
2. Both Arduino and Raspberry Pi have a range of compatible sensors that can be easily connected using standard interfaces such as I2C, SPI, or UART.
3. Some common sensors that can be interfaced with Arduino or Raspberry Pi include temperature sensors, humidity sensors, light sensors, and motion sensors.
4. To interface a sensor with Arduino or Raspberry Pi, the first step is to identify the appropriate interface and connect the sensor to the microcontroller using the appropriate pins.
5. Once the sensor is connected, the next step is to write code to read data from the sensor. This typically involves initializing the sensor, configuring any necessary settings, and then reading data from the sensor at regular intervals.
6. Both Arduino and Raspberry Pi have extensive libraries and resources available to help with interfacing sensors, including example code and tutorials.
7. Interfacing sensors with Arduino or Raspberry Pi can be a powerful way to add sensing capabilities to a project, allowing for real-time data collection and analysis.



# CO 3: Demonstrate the ability to transmit data wirelessly between different devices. K4

1. **Wireless data transmission** refers to the transfer of information between two or more devices without the use of wires or cables.
2. **Bluetooth** is a common wireless technology used for short-range data transmission between devices such as smartphones, laptops, and speakers.
3. **Wi-Fi** is another wireless technology that allows devices to connect to a local area network (LAN) and the internet without the use of cables.
4. **Infrared** is a wireless technology that uses infrared light to transmit data between devices, such as between a remote control and a television.
5. **Near Field Communication (NFC)** is a short-range wireless technology that allows devices to exchange data when they are brought close together, typically within a few centimeters.
6. To transmit data wirelessly between different devices, the devices must be equipped with the appropriate wireless technology and be within range of each other.
7. The process of transmitting data wirelessly between devices typically involves establishing a connection, selecting the data to be transmitted, and initiating the transfer.
8. Wireless data transmission can be faster and more convenient than using cables, but it can also be less secure and more prone to interference.



# CO 4: Show an ability to upload/download sensor data on cloud and server K2

1. **Understand the basics of cloud computing and server K2**: Cloud computing is the delivery of computing services over the internet, including storage, servers, databases, and more. Server K2 is a specific type of server that can be used to store and manage data.

2. **Set up a cloud account and server K2**: To upload and download sensor data on the cloud and server K2, you will need to set up an account with a cloud service provider and configure a server K2.

3. **Connect the sensor to the cloud and server K2**: Once the cloud account and server K2 are set up, you will need to connect the sensor to the cloud and server K2. This can be done using various methods, such as Wi-Fi, Bluetooth, or a wired connection.

4. **Upload sensor data to the cloud and server K2**: After the sensor is connected to the cloud and server K2, you can upload the sensor data to the cloud and server K2. This can be done using various methods, such as using an API or a web interface.

5. **Download sensor data from the cloud and server K2**: Once the sensor data is uploaded to the cloud and server K2, you can download the data from the cloud and server K2. This can be done using various methods, such as using an API or a web interface.

6. **Manage and analyze sensor data on the cloud and server K2**: After the sensor data is uploaded to the cloud and server K2, you can use various tools and techniques to manage and analyze the data on the cloud and server K2.



# CO 5 Examine various SQL queries from MySQL database K4, K5

SQL (Structured Query Language) is a standard language used to manage and manipulate data stored in relational databases. MySQL is one of the most popular open-source relational database management systems that use SQL.

Here are some common SQL queries that can be used in a MySQL database:

1. **SELECT**: Used to retrieve data from a database table. For example, `SELECT * FROM table_name;` retrieves all data from the specified table.

2. **INSERT**: Used to add new data to a database table. For example, `INSERT INTO table_name (column1, column2, column3) VALUES (value1, value2, value3);` adds a new row of data to the specified table.

3. **UPDATE**: Used to modify existing data in a database table. For example, `UPDATE table_name SET column1 = value1, column2 = value2 WHERE condition;` updates data in the specified table that meets the specified condition.

4. **DELETE**: Used to delete data from a database table. For example, `DELETE FROM table_name WHERE condition;` deletes data from the specified table that meets the specified condition.

5. **CREATE**: Used to create a new database table. For example, `CREATE TABLE table_name (column1 data_type, column2 data_type, column3 data_type);` creates a new table with the specified columns and data types.

6. **ALTER**: Used to modify the structure of an existing database table. For example, `ALTER TABLE table_name ADD column_name data_type;` adds a new column to the specified table.

7. **DROP**: Used to delete a database table. For example, `DROP TABLE table_name;` deletes the specified table from the database.

These are just a few examples of the many SQL queries that can be used in a MySQL database. It is important to have a good understanding of these queries and how to use them effectively to manage and manipulate data in a MySQL database.



# DETAILED SYLLABUS

A detailed syllabus is a comprehensive outline of the topics, concepts, and skills that will be covered in a course. It typically includes the following information:

1. Course description: A brief overview of the course, its objectives, and its intended audience.
2. Course goals: A list of the specific learning outcomes that students are expected to achieve by the end of the course.
3. Course topics: A detailed list of the topics that will be covered in the course, often organized by week or unit.
4. Assessment methods: A description of how student learning will be evaluated, including the types of assignments, exams, and projects that will be used.
5. Course policies: Information about attendance, participation, late work, and other course-specific policies.
6. Course materials: A list of the required and recommended textbooks, articles, and other resources that students will need to complete the course.
7. Course schedule: A detailed schedule of the course, including the dates of exams, assignments, and other important events.

A detailed syllabus is an important tool for both instructors and students. It helps instructors to plan and organize their course, and it provides students with a clear understanding of what is expected of them and what they can expect to learn. It is important for students to review the syllabus carefully at the beginning of the course and to refer to it regularly throughout the semester.



# The Importance of Hands-On Experience with Sensors

It is important for students to have hands-on experience in using various sensors such as temperature, humidity, smoke, light, etc. for several reasons:

1. **Practical Application:** Hands-on experience allows students to apply theoretical knowledge to real-world situations. This helps them to better understand the concepts and principles behind the functioning of sensors.

2. **Skill Development:** Working with sensors helps students to develop practical skills such as problem-solving, critical thinking, and data analysis. These skills are valuable not only in the field of sensor technology but also in other areas of study and work.

3. **Increased Engagement:** Hands-on activities are often more engaging and interesting for students than purely theoretical lessons. This can help to increase their motivation and interest in the subject matter.

4. **Preparation for Future Careers:** Many careers in science, technology, engineering, and mathematics (STEM) fields require knowledge and experience with sensors and other technologies. Hands-on experience with sensors can help prepare students for these careers.

In summary, hands-on experience with sensors is an important part of a well-rounded education in science and technology. It helps students to apply theoretical knowledge, develop practical skills, increase engagement, and prepare for future careers.



# Using Control Web Camera, Network, and Relays Connected to the Pi

1. **Web Camera**: A web camera can be connected to the Raspberry Pi via USB. Once connected, the camera can be controlled using various software packages, such as `fswebcam` or `motion`. These packages allow the user to capture images, record video, and stream live video from the camera.

2. **Network**: The Raspberry Pi can be connected to a network via Ethernet or Wi-Fi. This allows the user to access the internet, share files, and communicate with other devices on the network. The `ifconfig` and `iwconfig` commands can be used to configure the network settings on the Pi.

3. **Relays**: Relays can be connected to the Raspberry Pi's GPIO pins to control external devices, such as lights or motors. The `gpio` command can be used to control the state of the GPIO pins, allowing the user to turn the relay on or off. Additionally, various programming languages, such as Python, can be used to write scripts to control the relays.

Overall, the Raspberry Pi provides a versatile platform for controlling a web camera, accessing a network, and controlling relays. With the appropriate hardware and software, the user can easily set up and control these devices using the Pi.



# Starting Raspberry Pi and trying various Linux commands in command terminal window

1. **ls**: This command is used to list the contents of a directory. By default, it lists the contents of the current directory.
2. **cd**: This command is used to change the current working directory. For example, `cd Documents` will change the current working directory to the `Documents` directory.
3. **touch**: This command is used to create a new, empty file. For example, `touch newfile.txt` will create a new file named `newfile.txt` in the current working directory.
4. **mv**: This command is used to move or rename files or directories. For example, `mv oldname.txt newname.txt` will rename the file `oldname.txt` to `newname.txt`.
5. **rm**: This command is used to remove files or directories. For example, `rm file.txt` will remove the file `file.txt` from the current working directory.
6. **man**: This command is used to display the manual page for a command. For example, `man ls` will display the manual page for the `ls` command, which provides detailed information about the command and its usage.

These are some basic Linux commands that can be used in the command terminal window of a Raspberry Pi. It is recommended to try these commands and experiment with their usage to become more familiar with the Linux operating system.



# mkdir, rmdir, tar, gzip

`mkdir` and `rmdir` are commands used to create and remove directories in a Linux or Unix-like operating system. `mkdir` stands for "make directory" and `rmdir` stands for "remove directory".

`tar` is a command used to create and extract archive files. It stands for "tape archive" and is commonly used to compress and decompress files.

`gzip` is a command used to compress and decompress files using the gzip compression algorithm. It is commonly used in conjunction with the `tar` command to create compressed archive files.

Here are some examples of how to use these commands:

- To create a new directory called "mydir", use the command `mkdir mydir`.
- To remove an empty directory called "mydir", use the command `rmdir mydir`.
- To create a compressed archive file called "myfiles.tar.gz" containing the files "file1" and "file2", use the command `tar -czf myfiles.tar.gz file1 file2`.
- To extract the contents of a compressed archive file called "myfiles.tar.gz", use the command `tar -xzf myfiles.tar.gz`.
- To compress a file called "myfile" using the gzip algorithm, use the command `gzip myfile`. This will create a compressed file called "myfile.gz".
- To decompress a file called "myfile.gz" using the gzip algorithm, use the command `gunzip myfile.gz`. This will restore the original file "myfile".

These are some basic uses of the `mkdir`, `rmdir`, `tar`, and `gzip` commands. There are many more options and features available for these commands, which can be found in their respective man pages. To view the man page for a command, use the `man` command followed by the name of the command, for example `man mkdir`.

