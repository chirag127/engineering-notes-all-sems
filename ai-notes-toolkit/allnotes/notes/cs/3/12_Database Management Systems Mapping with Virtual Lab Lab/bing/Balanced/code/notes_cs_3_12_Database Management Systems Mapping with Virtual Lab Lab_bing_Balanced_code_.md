

## Name of the Lab for the notes of the subject of Database Management Systems Mapping with Virtual Lab Lab

- The name of the lab is **Database Management Systems Lab**.
- The lab is designed to provide students with hands-on experience in designing, implementing, and querying relational databases using SQL and other tools.
- The lab is mapped with the virtual lab platform, which allows students to access the lab resources and exercises online from anywhere and anytime.
- The virtual lab platform also provides feedback, evaluation, and guidance to the students and instructors.
- The lab covers the following topics:

  - Introduction to relational databases and SQL
  - Data definition language (DDL) and data manipulation language (DML)
  - Constraints, keys, and indexes
  - Queries, subqueries, and joins
  - Views, functions, and triggers
  - Transactions, concurrency, and recovery
  - Database design and normalization
  - Entity-relationship (ER) modeling and diagramming
  - Relational algebra and calculus
  - SQL extensions and alternatives



Hello, I am Sydney, your AI assistant. I can help you with your study material for the subject of Database Management Systems Mapping with Virtual Lab Lab. Here is the content for the topic of Name of the Experiment:

## Name of the Experiment

- The name of the experiment is **Database Design and Normalization**.
- The objective of this experiment is to learn how to design a relational database schema and apply normalization techniques to reduce data redundancy and anomalies.
- The prerequisites for this experiment are:
  - Basic knowledge of relational data model and SQL queries.
  - Familiarity with the Virtual Lab interface and tools.
- The steps for this experiment are:
  1. Identify the entities, attributes, and relationships in a given problem statement or scenario.
  2. Draw an entity-relationship (ER) diagram to represent the conceptual design of the database.
  3. Convert the ER diagram to a relational schema using mapping rules.
  4. Identify the functional dependencies and candidate keys in the relational schema.
  5. Apply normalization rules to decompose the relations into smaller and simpler ones that satisfy a desired normal form (such as 1NF, 2NF, 3NF, or BCNF).
  6. Verify the correctness and completeness of the normalized schema using dependency preservation and lossless join properties.
  7. Implement the normalized schema in the Virtual Lab using SQL commands.
  8. Test the database operations such as insertion, deletion, and modification using sample data and queries.
- The expected outcomes of this experiment are:
  - The ability to design a relational database schema that meets the user requirements and avoids data redundancy and anomalies.
  - The understanding of the concepts and principles of normalization and its benefits for database design and maintenance.
  - The skill to apply normalization techniques to a given relational schema and check its validity and quality.
  - The proficiency to use the Virtual Lab tools and SQL commands to create and manipulate a normalized database.



## Database Management Lab for the notes of the subject of Database Management Systems Mapping with Virtual Lab Lab

- Database management is the process of storing, manipulating, and retrieving data from a database using a database management system (DBMS).
- A DBMS is a software system that provides a standard way of interacting with a database using a structured query language (SQL) and other tools.
- A database is a collection of related data organized in tables, which consist of rows (records) and columns (attributes).
- A relational database is a type of database that follows the relational model, which is based on the concept of mathematical relations and sets.
- A virtual lab is a simulated environment that allows students to perform experiments and learn concepts without the need for physical equipment or facilities.
- Database management lab is a course that teaches students the fundamentals of database management using a virtual lab environment.
- The objectives of the database management lab are:

  - To introduce students to the different types of DBMS and their features.
  - To explain the concepts of data models, schemas, keys, constraints, and normalization.
  - To demonstrate the use of SQL commands for creating, querying, updating, and deleting data from a database.
  - To illustrate the use of high-level programming languages and front-end tools for accessing and manipulating data from a database.
  - To design and implement database applications using forms, triggers, menus, and reports.
  - To evaluate the performance and security of database systems.

- The syllabus of the database management lab covers the following topics:

  - Introduction to DBMS and SQL
  - Data definition language (DDL) and data manipulation language (DML) commands
  - Base tables and views
  - High-level programming language extensions
  - Front-end tools
  - Forms, triggers, and menu design
  - Reports
  - Design and implementation of database applications
  - Database performance and security

- The experiments to be performed in the database management lab are:

  - Creating and modifying tables and views using SQL
  - Inserting, updating, deleting, and querying data using SQL
  - Using aggregate functions, subqueries, joins, and set operations in SQL
  - Using embedded SQL and dynamic SQL in a programming language
  - Using a front-end tool to connect to and interact with a database
  - Creating and using forms, triggers, and menus for data entry and validation
  - Creating and using reports for data presentation and analysis
  - Developing a database application for a given problem domain
  - Analyzing and optimizing the performance of a database system
  - Implementing and enforcing security measures for a database system

- The references for the database management lab are:

  - Database Management System (DBMS): A Practical Approach, 5th Edition by Seema Kedar
  - Database Management Systems Notes by Geektonight
  - Database Management Lab Practical by Bharath Institute of Higher Education and Research
  - LABORATORY MANUAL for CS8481 : DATABASE MANAGEMENT SYSTEMS LABORATORY by SNS College of Technology



## Data Definition Language(DDL) Statements for the notes of the subject of Database Management Systems Mapping with Virtual Lab Lab

- Data Definition Language (DDL) is a subset of SQL and a part of DBMS (Database Management System).
- DDL statements are used to define and modify the database structure or schema, which is the organization of data in tables, files, indexes and columns.
- DDL statements can also specify the integrity and security constraints for the data, such as primary keys, foreign keys, check constraints, etc.
- The common DDL statements are CREATE, ALTER, DROP and TRUNCATE.
- CREATE statement is used to create a new database object, such as a table, an index, a view, etc.
- ALTER statement is used to modify an existing database object, such as adding, deleting or changing a column, a constraint, etc.
- DROP statement is used to delete an existing database object, such as a table, an index, a view, etc.
- TRUNCATE statement is used to remove all the data from a table, but not the table structure or schema.
- DDL statements are executed by the DBMS and stored in the data dictionary, which is a catalog of all the database objects and their properties.
- DDL statements can be executed interactively or in a batch mode, using a command-line interface, a graphical user interface, or a programming language.
- DDL statements can be mapped with a virtual lab lab, which is a simulated environment for learning and practicing database concepts and operations.
- A virtual lab lab can provide a graphical interface for creating and modifying database objects, as well as executing queries and commands on them.
- A virtual lab lab can also provide feedback and guidance for the users, such as showing the results of the DDL statements, highlighting the errors or warnings, suggesting the best practices, etc.
- A virtual lab lab can help the users to learn and understand the DDL statements and their effects on the database schema and data.



## Data Manipulation Language(DML) Statements

- Data manipulation language (DML) statements are SQL statements that access and manipulate data in existing schema objects, such as tables, views, and indexes  .
- DML statements include `INSERT`, `UPDATE`, `DELETE`, `MERGE`, `CALL`, and `SELECT`  .
- DML statements can be used to perform various operations on the data, such as adding new rows, modifying existing rows, deleting rows, merging data from multiple sources, invoking stored procedures, and querying data    .
- DML statements are part of a transaction, which is a sequence of one or more SQL statements that are treated as a unit by the database . A transaction can be committed (made permanent) or rolled back (undone) as a whole .
- DML statements can be executed in different ways, such as using SQL*Plus, SQL Developer, or other tools that can connect to the database and send SQL commands .
- DML statements can be affected by various factors, such as constraints, triggers, indexes, and privileges, that may enforce rules, perform actions, speed up queries, or restrict access on the data  .
- DML statements can be optimized by using hints, indexes, partitions, and other techniques, that may improve the performance, scalability, and efficiency of the data manipulation operations  .



# Data Query Language(DQL) Statements for the notes of the subject of Database Management Systems Mapping with Virtual Lab Lab

- Data Query Language (DQL) is a component of SQL (Structured Query Language) that is used to retrieve and manipulate data in a relational database management system (RDBMS)     .
- DQL statements are used to query the data contained in schema objects, such as tables, views, indexes, etc.  .
- The purpose of the DQL command is to get some schema relation based on the query passed to it and impose order upon it .
- The most common DQL statement is the SELECT statement, which allows you to specify the columns, tables, conditions, and order of the data you want to retrieve   .
- The syntax of the SELECT statement is as follows:

```sql
SELECT column1, column2, ...
FROM table1, table2, ...
WHERE condition1, condition2, ...
ORDER BY column1, column2, ... ASC|DESC;
```

- The SELECT statement can also use various clauses and operators to perform more complex queries, such as joins, subqueries, aggregations, functions, etc.  .
- Some examples of DQL statements are:

```sql
-- Select all the data from the employees table
SELECT * FROM employees;

-- Select the name and salary of the employees who work in the sales department
SELECT name, salary
FROM employees
WHERE department = 'sales';

-- Select the name and average salary of each department
SELECT department, AVG(salary) AS average_salary
FROM employees
GROUP BY department;

-- Select the name and salary of the employees who earn more than the average salary of their department
SELECT e.name, e.salary
FROM employees e
WHERE e.salary > (SELECT AVG(salary) FROM employees WHERE department = e.department);

-- Select the name and phone number of the customers who have ordered more than 10 products
SELECT c.name, c.phone
FROM customers c
JOIN orders o ON c.id = o.customer_id
GROUP BY c.id
HAVING COUNT(o.product_id) > 10;

-- Select the name and price of the most expensive product
SELECT name, price
FROM products
ORDER BY price DESC
LIMIT 1;
```

- DQL statements are used to perform various queries on the data within schema objects and can be executed using various tools and applications, such as SQL editors, command-line interfaces, graphical user interfaces, etc.  .
- DQL statements are also used to map the data from the database to the virtual lab lab, which is a web-based platform that allows users to perform experiments and simulations on the data using various tools and techniques .
- The virtual lab lab provides a user-friendly interface that allows users to select the data source, the query, the experiment, and the output format .
- The virtual lab lab also provides various features and functionalities, such as data visualization, data analysis, data manipulation, data export, etc. .
- Some examples of experiments and simulations that can be performed on the data using the virtual lab lab are:

```json
{"experiments": ["Linear regression", "Clustering", "Classification", "Sentiment analysis", "Text summarization", "Image recognition", "Speech recognition", "Natural language generation", "Machine translation", "Recommender systems", "Neural networks", "Genetic algorithms", "Game theory", "Cryptography", "Blockchain", "Quantum computing", "Artificial intelligence", "Computer vision", "Natural language processing", "Machine learning", "Data mining", "Data science", "Big data", "Cloud computing", "Internet of things", "Cybersecurity", "Software engineering", "Database management systems", "Operating systems", "Computer networks", "Web development", "Mobile development", "Augmented reality", "Virtual reality", "Mixed reality", "Computer graphics", "Animation", "Gaming", "Music", "Art", "Education", "Healthcare", "Business", "Finance", "E-commerce", "Marketing", "Social media", "Journalism", "Politics", "Law", "History", "Geography", "Biology", "Chemistry", "Physics", "Mathematics", "Statistics", "Logic", "Philosophy", "

```




# Transaction Control Language(TCL) statements for the notes of the subject of Database Management Systems Mapping with Virtual Lab Lab

- Transaction Control Language (TCL) is a language that manages transactions within the database. Transactions are a sequence of operations that are performed as a single logical unit of work. Transactions ensure the consistency and integrity of the database by following the ACID properties (Atomicity, Consistency, Isolation, and Durability).
- TCL commands are used to execute, save, or undo the changes made by the Data Manipulation Language (DML) statements, such as INSERT, UPDATE, or DELETE. TCL commands also allow the statements to be grouped together into logical transactions.
- The main TCL commands are:
  - COMMIT: It is used to save the transactions in the database permanently. It also ends the current transaction and starts a new one. Once a transaction is committed, it cannot be rolled back.
  - ROLLBACK: It is used to undo the transactions that have not been saved in the database. It also ends the current transaction and starts a new one. It can be used to restore the database to its previous state before the transaction started or to a specific savepoint.
  - SAVEPOINT: It is used to create a point in the transaction where the changes can be rolled back to. It allows partial rollback of a transaction. Multiple savepoints can be created in a transaction.
  - SET TRANSACTION: It is used to specify the characteristics of the current transaction, such as the isolation level, the read/write mode, or the name. It must be the first statement in a transaction.



## Statement for the notes of the subject of Database Management Systems Mapping with Virtual Lab Lab

- A database management system (DBMS) is a software tool that enables users to manage a database easily.
- A database is a collection of data that is organized so that it can be accessed, manipulated, and updated efficiently.
- A spatial database is a database that stores and manages spatial data, such as geographic coordinates, shapes, maps, and images.
- A virtual lab is a simulated environment that allows users to perform experiments or tasks without using physical equipment or resources.
- Database management systems mapping with virtual lab lab is a subject that covers the theory, design, development, and management of spatial databases using virtual lab software.
- The objectives of this subject are to:
  - Understand the concepts and principles of spatial data and spatial databases.
  - Learn how to design and implement spatial databases using entity-relationship modeling, normalization, and structured query language (SQL) .
  - Learn how to use virtual lab software to create, manipulate, query, and visualize spatial databases.
  - Learn how to apply spatial database techniques to solve real-world problems in various domains, such as geography, remote sensing, urban planning, and natural resource management .
- The topics of this subject include:
  - Introduction to spatial data and spatial databases.
  - Spatial data models and structures.
  - Spatial query languages and operators.
  - Spatial database design and implementation .
  - Virtual lab software and tools.
  - Spatial data analysis and visualization.
  - Spatial database applications and case studies.

