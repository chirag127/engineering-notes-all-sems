


## Unit 1 - Develop static web pages using HTML

1. HTML stands for HyperText Markup Language, and is the language used to create webpages. 
2. HTML is a markup language, meaning it describes the content of a webpage using tags. 
3. HTML tags are used to structure and format the content of a webpage, such as headings, paragraphs, lists, and images. 
4. HTML documents must be written in a specific format, using a combination of HTML tags and plain text. 
5. HTML documents must also include a DOCTYPE declaration, which specifies the version of HTML used in the document. 
6. HTML documents must be valid, meaning they must follow the rules of HTML syntax. 
7. HTML documents must be well-structured, meaning they must use the correct HTML tags for the content being displayed. 
8. HTML documents must be viewable in all browsers, meaning they must be compatible with different browsers and different versions of HTML.




### Writing HTML/JavaScript to Display Your CV

1. To display your CV in a browser, you need to write HTML/JavaScript code.
2. HTML is a markup language used to create webpages. It consists of tags and attributes that define the structure of a page.
3. JavaScript is a scripting language used to create dynamic websites. It can be used to add interactivity to webpages.
4. To create a CV, you need to write HTML code to define the structure of your CV. This includes the headings, paragraphs, and other elements that make up the page.
5. You can also add styling to your CV using CSS. CSS is a language used to define the look and feel of a webpage.
6. You can also add interactivity to your CV using JavaScript. This includes creating buttons, forms, and other interactive elements.
7. Once you have written the HTML/JavaScript code, you can upload it to your Institute website, Department website, and Tutorial website for Unit 1 - Develop static webpages using HTML in the subject of Web Technology Lab.




### Writing an HTML Program to Design an Entry Form of Student Details and Send it to Store at Database Server

1. Begin by creating a form in HTML. This can be done by using the `<form>` tag.
2. Add the necessary form elements that will be used to collect student information. This can include text boxes, radio buttons, check boxes, and so on.
3. Add the `action` attribute to the `<form>` tag. This will specify the URL of the page that will process the data.
4. Add the `method` attribute to the `<form>` tag. This will specify the type of request that will be sent to the server (e.g. `GET` or `POST`).
5. Add the `name` attribute to each form element. This will specify the name of the data that will be sent to the server.
6. Add the `value` attribute to each form element. This will specify the value of the data that will be sent to the server.
7. Add a submit button to the form. This can be done by using the `<input>` tag with the `type` attribute set to `submit`.
8. Once the form is complete, save the HTML file.
9. Connect to the database server. This can be done using a programming language such as PHP, Python, or JavaScript.
10. Create a SQL query that will insert the data collected from the form into the database.
11. Execute the query.
12. Close the connection to the database.




## Unit 2 - Develop Java programs for window/web-based applications

1. Java is an object-oriented programming language that can be used to create applications for Windows and the web. 
2. Java programs are platform independent, meaning they can run on any system with a Java Virtual Machine (JVM). 
3. Java programs are composed of classes, which contain methods and variables. 
4. Java has a wide range of built-in data types and objects. 
5. Java programs can be compiled into bytecode, which can then be executed by the JVM. 
6. Java programs can be written in an integrated development environment (IDE) such as Eclipse or NetBeans. 
7. Java programs can be debugged using breakpoints, watches, and the Java debugger. 
8. Java programs can be tested using unit tests and integration tests. 
9. Java programs can be deployed to web servers using servlets and web services. 
10. Java programs can be deployed to mobile devices using Android Studio.




### Write programs using JavaScript for Web Page to display browsers information

1. JavaScript is a scripting language used to create and control dynamic web content.
2. JavaScript can be used to create interactive web pages and applications.
3. JavaScript can be used to detect the user's browser and display information about it.
4. The `navigator` object in JavaScript can be used to access information about the user's browser, including its name, version, and platform.
5. The `navigator.userAgent` property contains a string that describes the user's browser, which can be used to determine the browser type.
6. The `navigator.appName` property can be used to determine the name of the browser, such as Chrome, Firefox, or Safari.
7. The `navigator.appVersion` property can be used to determine the browser version.
8. The `navigator.platform` property can be used to determine the platform, such as Windows or Mac OS.
9. JavaScript can be used to display browser information, such as the name, version, and platform, to the user.




### Write a Java Applet to Display the Application Program Screen

1. Create a new Java project in your IDE.
2. Create a new Applet class and name it `MyApplet`.
3. Add the `<applet>` tag to your HTML page.
4. Use the `init()` method to initialize the applet and set the size of the window.
5. Use the `paint()` method to draw the application program window.
6. Use the `getParameter()` method to get the parameters passed to the applet.
7. Use the `Graphics` class to draw the components of the application program window such as a calculator, text fields, buttons, etc.
8. Use the `actionPerformed()` method to handle user input events.
9. Use the `repaint()` method to update the application program window.
10. Use the `destroy()` method to clean up resources when the applet is closed.




## Unit 3 - Design dynamic web pages using Javascript and XML

* Dynamic web pages are web pages that are generated as a result of a user's interaction with the page. 
* JavaScript is a scripting language used to create interactive web pages. It is used to create dynamic web pages that respond to user input and can also be used to create games and other interactive applications.
* XML (Extensible Markup Language) is a markup language used to store and transport data. It is used to store data in a structured way, making it easier to exchange information between different systems.
* JavaScript and XML can be used together to create dynamic web pages. JavaScript is used to provide the user interface and interactivity, while XML is used to store and transport data.
* JavaScript can be used to make requests to a server and receive data in XML format. This data can then be used to update the page dynamically.
* JavaScript can also be used to manipulate the XML data and display it in a user-friendly way.
* In addition, XML can be used to store the data that is generated by the user's interaction with the page, so that it can be accessed later.




### Writing program in XML for creation of DTD

- DTD stands for Document Type Definition, and is an XML-based language used to define the structure of an XML document.
- DTDs are used to define the elements, attributes, and entities that are allowed in an XML document.
- DTDs can also be used to define the rules for how elements, attributes, and entities can be used in an XML document.
- DTDs can be used to create a set of rules for the notes of the Unit 3 - Design dynamic web pages using Javascript and XML in the subject of Web Technology Lab.
- To create a DTD, a program must be written in XML. This program will define the elements, attributes, and entities that are allowed in an XML document.
- The program will also define the rules for how elements, attributes, and entities can be used in an XML document.
- Once the program is written, it can be used to create a DTD that specifies the set of rules for the notes of the Unit 3 - Design dynamic web pages using Javascript and XML in the subject of Web Technology Lab.




### Create a Style Sheet in CSS/XSL & Display the Document in Internet Explorer
1. CSS (Cascading Style Sheets) is a style sheet language used to describe the presentation of a document written in HTML or XML (including XHTML). CSS describes how elements should be rendered on screen, on paper, in speech, or on other media.
2. XSL (Extensible Stylesheet Language) is a language used to transform and render XML documents. XSL can be used to transform XML documents into HTML, text, or other XML documents.
3. To create a style sheet in CSS/XSL, you need to write a set of rules that define how the elements of the document should be rendered. These rules are written in the form of CSS/XSL selectors and declarations.
4. Once the style sheet is created, it can be linked to the HTML document in which the elements should be rendered. This can be done by adding a <link> element in the <head> section of the document, or by adding a <style> element in the <head> section.
5. To display the document in Internet Explorer, you need to include a reference to the style sheet in the HTML document. This can be done by adding a <link> element in the <head> section of the document, or by adding a <style> element in the <head> section. 
6. Once the style sheet is linked to the HTML document, the elements of the document will be rendered according to the rules specified in the style sheet.




## Unit 4 - Design dynamic web page using server site programming Ex. ASP/JSP/PHP

1. Dynamic web pages are web pages that are generated in real-time based on user input and/or server-side processes.
2. Server-side programming technologies, such as ASP, JSP and PHP, are commonly used to create dynamic web pages.
3. ASP (Active Server Pages) is a server-side scripting language developed by Microsoft. It is used to create dynamic web pages and is integrated with HTML.
4. JSP (Java Server Pages) is a server-side scripting language developed by Sun Microsystems. It is based on the Java programming language and is used to create dynamic web pages.
5. PHP (Hypertext Preprocessor) is a server-side scripting language developed by Zend Technologies. It is used to create dynamic web pages and is integrated with HTML.
6. In order to create a dynamic web page using server-side programming technologies, the web page must be written in the appropriate language and then uploaded to the web server.
7. Once the web page is uploaded to the web server, it can be accessed by users through their web browsers.
8. The web page can then be used to interact with users, process data and generate dynamic content in real-time.




### Program to illustrate JDBC connectivity

1. JDBC stands for Java Database Connectivity and is a Java API used to connect Java applications to databases. 
2. It provides a set of classes and interfaces that enable a Java application to access a database. 
3. JDBC provides methods to query and update data in a database, and is oriented towards relational databases.
4. To use JDBC, an application must have a JDBC driver, which is a set of classes that implements the JDBC interfaces. 
5. The driver is responsible for translating the calls from the Java application into a format that the database understands.
6. JDBC drivers are available for most popular databases, such as Oracle, MySQL, and PostgreSQL.
7. To use JDBC, the application must first create a JDBC connection. 
8. This is done by specifying the URL of the database, along with the username and password. 
9. Once the connection is established, the application can use the JDBC methods to query and update the database.
10. In the context of web programming, JDBC can be used to create dynamic web pages by connecting to a database and retrieving data to be displayed on the page.
11. For example, an ASP/JSP/PHP application can use JDBC to connect to a database and retrieve data to be displayed in an HTML table. 
12. This allows the application to create dynamic web pages that can be updated with data from the database.




### Program for Maintaining Database by Sending Queries for the Notes of the Unit 4 - Design Dynamic Web Page Using Server Site Programming Ex. ASP/JSP/PHP in the Subject of Web Technology Lab

1. A database is a collection of data, stored and organized in a manner that allows for easy retrieval and manipulation. 
2. In order to maintain a database, it is necessary to send queries to the database to retrieve data, or to update and delete data. 
3. Queries are written using a language called Structured Query Language (SQL). 
4. SQL is used to create, update, delete, and retrieve data from a database. 
5. Unit 4 of the Web Technology Lab focuses on the design of dynamic web pages using server-side programming languages such as ASP, JSP, and PHP. 
6. These languages allow the user to create web pages that can interact with a database, by sending SQL queries to the database. 
7. The user can use these languages to create dynamic web pages that can display data from the database, update the database with new data, or delete data from the database. 
8. In order to create a dynamic web page, the user must be familiar with the syntax of the language they are using, as well as the syntax of SQL.




### Design and implement a simple servlet book query with the help of JDBC & SQL

1. Understand the fundamentals of JDBC and SQL.
2. Install the necessary drivers for JDBC.
3. Create a database and table to store the book information.
4. Create a servlet for the book query.
5. Use JDBC to connect to the database.
6. Use SQL to query the database.
7. Return the book information from the database to the servlet.
8. Display the book information in the servlet.

### Design dynamic web page using server site programming Ex. ASP/JSP/PHP

1. Understand the fundamentals of server-side programming.
2. Install the necessary software for the server-side programming language.
3. Create a web page using HTML and CSS.
4. Use the server-side programming language to create dynamic web pages.
5. Use the programming language to connect to a database and query the data.
6. Return the data from the database to the web page.
7. Display the data on the web page.




### Create MS Access Database

* Create a new database in MS Access and name it according to the project requirements.
* Set up the tables, fields, and relationships needed to store the data.
* Create queries to retrieve data from the database.

### Create an ODBC Link

* Install the appropriate ODBC driver for the database in use.
* Create a System DSN in the ODBC Data Source Administrator.
* Configure the DSN with the appropriate connection parameters.

### Compile & Execute Java JDBC Socket

* Write Java code to create a JDBC Socket.
* Compile the code to generate the necessary class files.
* Execute the code to establish a connection with the database.

### Design Dynamic Web Page Using Server Side Programming

* Identify the language to be used for the project (e.g. ASP, JSP, PHP).
* Design the web page layout and structure.
* Write code to connect to the database and retrieve data.
* Use appropriate web technologies to display the data in the web page.




## Unit 5 - Design server site applications using JDDC, ODBC and section tracking API

1. JDDC (Java Database Connectivity) is an application programming interface (API) that enables Java programs to access a database. It is used to connect Java applications to a relational database.

2. ODBC (Open Database Connectivity) is an application programming interface (API) that enables programs written in C and other languages to access a database. It is used to connect programs written in C to a relational database.

3. Section Tracking API is an application programming interface (API) that enables programs to access a database. It is used to track sections of a web application, such as web pages, user forms, and other elements.

4. JDDC, ODBC and Section Tracking API can be used together to design server-side applications. JDDC and ODBC can be used to access the database, while the Section Tracking API can be used to track sections of the application.

5. When designing a server-side application using JDDC, ODBC and Section Tracking API, it is important to consider the security of the application. It is important to ensure that only authorized users can access the data in the database, and that the data is kept secure.





### Installing TOMCAT and APACHE

1. Download and install the latest version of [Apache Tomcat](https://tomcat.apache.org/) and [Apache HTTP Server](https://httpd.apache.org/).
2. Configure the environment variables to ensure that the Tomcat and Apache server are accessible from the command line.
3. Configure the Tomcat server to use the JDBC, ODBC and section tracking API.
4. Create a web application using Tomcat and Apache.
5. Test the application to ensure that it is functioning correctly.
6. Deploy the application on the web server.





### Access the Above Developed Static Web Pages for Books Web Site

* Using these servers, web pages can be developed for the notes of the Unit 5 - Design server site applications using JDDC, ODBC and section tracking API in the subject of Web Technology Lab. 
* JDDC stands for Java Database Connectivity and is an application programming interface (API) for the programming language Java. It is used to access and manipulate data stored in databases.
* ODBC stands for Open Database Connectivity and is an application programming interface (API) for the programming language C. It is used to access and manipulate data stored in databases.
* Section tracking API enables developers to track user activity on a website in real-time. It can be used to monitor user behavior, such as page visits and clicks, and send notifications when certain events occur.
* The web pages developed for the notes of the Unit 5 - Design server site applications using JDDC, ODBC and section tracking API in the subject of Web Technology Lab can be accessed using these servers.




### Assume four users user1, user2, user3 and user4 having the passwords pwd1, pwd2, pwd3 and pwd4 respectively for the notes of the Unit 5 - Design server site applications using JDDC,ODBC and section tracking API in the subject of Web Technology Lab

1. In order to design server-side applications using JDDC, ODBC and section tracking API, four users are required, namely user1, user2, user3 and user4.
2. Each user must have a unique password, which should be stored securely.
3. For user1, the password should be pwd1, for user2, the password should be pwd2, for user3, the password should be pwd3, and for user4, the password should be pwd4.
4. The passwords should be changed regularly in order to ensure the security of the server-side applications.
5. It is also important to ensure that the passwords are not shared with anyone else, as this could compromise the security of the server-side applications.




### Writing a Servlet for Unit 5 - Design Server Site Applications

1. Understand the concept of JDBC, ODBC, and Section Tracking API
2. Learn how to use JDBC, ODBC, and Section Tracking API to design server site applications
3. Understand how to write servlets to handle requests and responses
4. Learn how to implement servlets in Java
5. Understand the concepts of database connectivity and query processing
6. Learn how to establish a connection between a servlet and a database
7. Understand the concepts of session tracking
8. Learn how to use servlets to track client sessions
9. Understand how to use servlets to handle errors
10. Learn how to use servlets to implement security measures




### Create a Cookie and add these four user id’s and passwords to this Cookie

1. To create a Cookie, use the `Cookie` class in the JDBC library.
2. Use the `setProperty` method to add the four user id’s and passwords to the Cookie.
3. Use the `getProperty` method to retrieve the user id’s and passwords from the Cookie.
4. Use the `removeProperty` method to remove the user id’s and passwords from the Cookie.
5. Use the `store` method to store the Cookie in the server site application.
6. Use the `load` method to load the Cookie from the server site application.
7. Use the `ODBC` library to access the section tracking API.
8. Use the `executeQuery` method to execute queries on the section tracking API.
9. Use the `executeUpdate` method to update the section tracking API.




### Read the user id and passwords entered in the Login form and authenticate with the values available in the cookies

1. The user id and password must be entered into the login form.
2. The authentication process is done by comparing the values entered with the values stored in the cookies.
3. The cookies contain information about the user, such as their username and password.
4. The cookies must be checked to ensure that the user is authorized to access the notes for Unit 5 - Design server site applications using JDDC, ODBC and section tracking API in the subject of Web Technology Lab.
5. Once the authentication process is successful, the user can access the notes.





### Installing a Database

1. First, decide which type of database to use. Popular choices include MySQL and Oracle.
2. Install the database software on the server.
3. Create a database user and password.
4. Connect to the database using JDBC, ODBC, or a section tracking API.
5. Create the necessary tables and fields.
6. Populate the database with the necessary data.
7. Test the database to ensure it is functioning properly.
8. Finally, store the notes for the Unit 5 - Design server site applications in the database.




### Create a Table for Unit 5 - Design Server Site Applications Using JDDC, ODBC and Section Tracking API
When designing server site applications using JDDC, ODBC and section tracking API, it is important to create a table that contains the following fields:

* Name
* Password
* Email-ID
* Phone number

These fields are necessary for tracking the progress of the server site applications. The name field is used to identify the user, the password field is used to authenticate the user, the email-ID field is used to contact the user and the phone number field is used to verify the user's identity.




### Java Program/Servlet/JSP to Connect to a Database and Extract Data

* Java Database Connectivity (JDBC) is an API that allows applications written in Java to access and interact with databases. 
* A servlet is a Java technology component that runs on a server and generates dynamic content. It is used to create web applications.
* A JavaServer Page (JSP) is a technology used to create dynamic web content. It is based on the Java programming language.
* To connect to a database, a JDBC driver must be used. The driver is responsible for translating the SQL statements into the appropriate database commands.
* Once a connection is established, data can be extracted from the database using SQL statements.
* The data can then be used to generate dynamic web content, such as HTML or XML documents.
* Section tracking API is a technology used to track user actions on a website. It can be used to create reports and analyze user behavior.
* To use section tracking API, a servlet or JSP must be used to capture the user actions and store them in the database.
* Once the data is stored in the database, it can be used to generate reports or analyze user behavior.




### Insert the details of the users who register with the web site
When a new user clicks the submit button in the registration page for the notes of the Unit 5 - Design server site applications using JDDC, ODBC and section tracking API in the subject of Web Technology Lab, the following details should be inserted: 

1. User name 
2. Password 
3. Email address 
4. Phone number 
5. Physical address 
6. Date of birth 
7. Gender 
8. Any other personal information requested by the site.




### Write a JSP which insert the details of the 3 or 4 users who register with the web site by using registration form for the notes of the Unit 5 - Design server site applications using JDDC,ODBC and section tracking API in the subject of Web Technology Lab

1. JSP (Java Server Pages) is a technology used to create dynamic web pages. It allows developers to write HTML code with embedded Java code which is executed on the server side.

2. A registration form is used to capture user information such as name, email address, age, etc. This information is used to create a user account in the system.

3. JDBC (Java Database Connectivity) is a technology used to connect Java applications to databases. It provides a set of APIs for executing SQL statements, retrieving results and managing transactions.

4. ODBC (Open Database Connectivity) is a technology used to connect applications to databases. It provides a set of APIs for executing SQL statements, retrieving results and managing transactions.

5. Section Tracking API is a technology used to track sections of a web page. It allows developers to track the usage of a particular section of a web page and get analytics data.




### Authenticate the user when he submits the login form 

1. To authenticate the user when he submits the login form, the user name and password from the database must be retrieved. 
2. JDDC, ODBC and section tracking API can be used to access the database. 
3. The user name and password must be verified against the database to authenticate the user. 
4. Once the user is authenticated, the notes of Unit 5 - Design server site applications can be accessed.




### Design and Implement a Simple Shopping Cart Example with Session Tracking API

1. Understand the concept of session tracking and how it can be used to store data related to a user's shopping cart.
2. Learn about the different types of session tracking APIs, such as JDBC, ODBC, and section tracking API.
3. Design a simple shopping cart example using session tracking API.
4. Implement the design in a web application.
5. Test the application to ensure that it is functioning correctly.
6. Document the design and implementation of the shopping cart example.

