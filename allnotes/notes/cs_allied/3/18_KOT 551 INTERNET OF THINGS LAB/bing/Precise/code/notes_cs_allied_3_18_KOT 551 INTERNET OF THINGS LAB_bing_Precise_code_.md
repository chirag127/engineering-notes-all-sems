

# KOT 551 INTERNET OF THINGS LAB

The Internet of Things (IoT) is a network of interconnected devices that can communicate with each other and share data. In the KOT 551 Internet of Things Lab, students will learn about the following topics:

1. Introduction to IoT: Students will learn about the basic concepts of IoT, including its architecture, protocols, and applications.

2. Sensors and Actuators: Students will learn about different types of sensors and actuators, and how they can be used in IoT applications.

3. Data Acquisition and Processing: Students will learn about data acquisition techniques, and how to process and analyze data collected from IoT devices.

4. IoT Communication Protocols: Students will learn about different communication protocols used in IoT, including MQTT, CoAP, and HTTP.

5. IoT Security: Students will learn about the security challenges in IoT, and how to secure IoT devices and data.

6. IoT Applications: Students will learn about different IoT applications, including smart homes, smart cities, and industrial IoT.

7. IoT Project: Students will work on an IoT project, where they will design and implement an IoT system.

The KOT 551 Internet of Things Lab provides students with hands-on experience in designing and implementing IoT systems, and prepares them for a career in the field of IoT.



# KCS

KCS stands for Knowledge-Centered Service. It is a best practice methodology that provides a detailed description of how service organizations can work more effectively with knowledge in order to improve service delivery, become more productive in the service organization, decrease costs, and increase service levels to customers.

- KCS is also known as knowledge-centered support.
- KCS is about getting the in-depth knowledge of IT teams out of their heads and onto the page, creating detailed documentation that employees, system users, and new or less experienced engineers can use without constantly bombarding the service desk with the same requests.
- Support teams not only provide real-time customer, system, or employee support, but also create and maintain documentation as part of the same process.



## Course Outcome (CO) Bloom’s Knowledge Level (KL)

- Course Outcome (CO) refers to the expected knowledge, skills, and attitudes that students should possess upon completion of a specific course.
- Bloom’s Knowledge Level (KL) is a framework for categorizing educational goals and objectives based on the level of cognitive complexity.
- The framework consists of six levels: Remembering, Understanding, Applying, Analyzing, Evaluating, and Creating.
- COs are often mapped to Bloom’s KL to ensure that the course objectives are aligned with the desired level of cognitive complexity.
- This mapping helps instructors design assessments and instructional strategies that are appropriate for the desired level of learning.
- By aligning COs with Bloom’s KL, instructors can ensure that students are challenged to think critically and apply their knowledge in meaningful ways.



### At the end of the course, the student will be able to:

1. Demonstrate a comprehensive understanding of the course material.
2. Apply the knowledge and skills acquired during the course to real-life situations.
3. Analyze and evaluate information critically and effectively.
4. Communicate effectively, both orally and in writing.
5. Work collaboratively with others to achieve common goals.
6. Demonstrate ethical and professional behavior.
7. Engage in continuous learning and professional development.
8. Utilize technology effectively to enhance learning and productivity.



#### CO 1 Understand the concept of Internet of Things K3

- The Internet of Things (IoT) refers to the interconnection of physical devices, vehicles, buildings, and other objects, embedded with electronics, software, sensors, and network connectivity, which enables these objects to collect and exchange data.
- IoT allows objects to be sensed and controlled remotely across existing network infrastructure, creating opportunities for more direct integration of the physical world into computer-based systems, and resulting in improved efficiency, accuracy, and economic benefit.
- IoT involves extending Internet connectivity beyond standard devices, such as desktops, laptops, smartphones, and tablets, to any range of traditionally non-internet-enabled physical devices and everyday objects.
- IoT devices can communicate with each other and with humans, and can be controlled remotely, allowing for the creation of smart homes, smart cities, and other smart environments.
- IoT has the potential to significantly impact various industries, including healthcare, transportation, agriculture, and manufacturing, by improving efficiency, reducing costs, and enabling new business models.
- IoT also raises concerns about security, privacy, and the potential for misuse of personal data. It is important for individuals and organizations to be aware of these concerns and to take steps to protect their data and devices.



#### CO 2 Implement interfacing of various sensors with Arduino/Raspberry Pi K4, K5

Interfacing sensors with Arduino or Raspberry Pi involves connecting the sensor to the microcontroller and writing code to read data from the sensor. Here are the steps to interface various sensors with Arduino/Raspberry Pi:

1. **Identify the sensor and its communication protocol**: Different sensors use different communication protocols such as I2C, SPI, or UART. Identify the communication protocol used by the sensor.

2. **Connect the sensor to the microcontroller**: Connect the sensor to the microcontroller using the appropriate pins. For example, if the sensor uses I2C communication, connect the SDA and SCL pins of the sensor to the SDA and SCL pins of the microcontroller.

3. **Install necessary libraries**: Some sensors require specific libraries to be installed in order to interface with the microcontroller. Install the necessary libraries for the sensor.

4. **Write code to read data from the sensor**: Write code to read data from the sensor using the appropriate communication protocol. The code will vary depending on the sensor and the microcontroller used.

5. **Test the sensor**: Test the sensor to ensure that it is working correctly and providing accurate data.

These are the basic steps to interface various sensors with Arduino or Raspberry Pi. The specific details will vary depending on the sensor and the microcontroller used. It is important to consult the documentation for the sensor and the microcontroller to ensure proper interfacing.



#### CO 3 Demonstrate the ability to transmit data wirelessly between different devices. K4

1. Wireless data transmission is the transfer of information between two or more devices without the use of wires or cables.
2. There are several technologies that enable wireless data transmission, including Bluetooth, Wi-Fi, and cellular networks.
3. Bluetooth is a short-range wireless technology that is commonly used to connect devices such as smartphones, tablets, and laptops to peripherals such as headphones, speakers, and keyboards.
4. Wi-Fi is a wireless networking technology that uses radio waves to provide high-speed internet and network connections to devices within a local area network (LAN).
5. Cellular networks use radio waves to transmit data over long distances, allowing mobile devices to connect to the internet and make phone calls from almost anywhere.
6. To transmit data wirelessly between different devices, the devices must be equipped with the appropriate wireless technology and be within range of each other or a wireless access point.
7. The process of transmitting data wirelessly between different devices typically involves establishing a connection, sending the data, and confirming that the data has been received.
8. Wireless data transmission has many advantages, including increased mobility, flexibility, and convenience. However, it is important to ensure that wireless connections are secure to prevent unauthorized access to sensitive information.



#### CO 4 Show an ability to upload/download sensor data on cloud and server K2

To show an ability to upload and download sensor data on cloud and server K2, one must have knowledge of the following:

1. **Cloud Storage**: Cloud storage is a service model in which data is maintained, managed, backed up remotely, and made available to users over a network (typically the Internet). It allows users to store and access data on remote servers, rather than on local storage devices.

2. **Server**: A server is a computer program or a device that provides functionality for other programs or devices, called "clients". In the context of uploading and downloading sensor data, the server would be responsible for receiving, storing, and serving the data to clients.

3. **Sensor Data**: Sensor data refers to the output of a device that detects and responds to some type of input from the physical environment. This data can be in various formats, such as numerical values, text, or images.

4. **Data Transfer Protocols**: To upload and download sensor data to and from the cloud and server, one must use a data transfer protocol. Common protocols include FTP (File Transfer Protocol), HTTP (Hypertext Transfer Protocol), and SFTP (Secure File Transfer Protocol).

5. **Data Security**: When transferring sensor data to and from the cloud and server, it is important to ensure the security of the data. This can be achieved through the use of encryption, secure data transfer protocols, and access control measures.

By having knowledge of these concepts and implementing them correctly, one can show an ability to upload and download sensor data on cloud and server K2.



#### CO 5 Examine various SQL queries from MySQL database K4, K5

Structured Query Language (SQL) is a standard language used to manage and manipulate data stored in relational databases. MySQL is one of the most popular open-source relational database management systems that use SQL.

Here are some common SQL queries used in MySQL:

1. **SELECT**: Used to retrieve data from a database table. For example, `SELECT * FROM table_name;` retrieves all data from the table named `table_name`.
2. **INSERT**: Used to add new records to a database table. For example, `INSERT INTO table_name (column1, column2) VALUES (value1, value2);` adds a new record with values `value1` and `value2` for `column1` and `column2` respectively.
3. **UPDATE**: Used to modify existing records in a database table. For example, `UPDATE table_name SET column1 = value1 WHERE condition;` updates the value of `column1` to `value1` for all records that meet the specified condition.
4. **DELETE**: Used to delete records from a database table. For example, `DELETE FROM table_name WHERE condition;` deletes all records from the table named `table_name` that meet the specified condition.
5. **CREATE**: Used to create a new database table. For example, `CREATE TABLE table_name (column1 datatype, column2 datatype);` creates a new table named `table_name` with columns `column1` and `column2` of the specified datatypes.
6. **ALTER**: Used to modify the structure of an existing database table. For example, `ALTER TABLE table_name ADD column_name datatype;` adds a new column named `column_name` of the specified datatype to the table named `table_name`.
7. **DROP**: Used to delete a database table. For example, `DROP TABLE table_name;` deletes the table named `table_name`.

These are just a few examples of the various SQL queries that can be used in a MySQL database. It is important to have a good understanding of these queries and their syntax to effectively manage and manipulate data in a MySQL database.



## DETAILED SYLLABUS

A detailed syllabus is a comprehensive outline of the topics, assignments, and assessments that will be covered in a course. It is typically provided by the instructor at the beginning of the course and serves as a guide for students to understand the expectations and requirements of the course.

A detailed syllabus may include the following information:

1. Course description: A brief overview of the course, including its objectives and goals.
2. Course schedule: A list of topics that will be covered in the course, along with the dates and deadlines for assignments and assessments.
3. Course materials: A list of required and recommended textbooks, articles, and other resources for the course.
4. Grading policy: An explanation of how grades will be calculated, including the weight of assignments and assessments.
5. Attendance policy: The instructor's expectations for attendance and participation in the course.
6. Course policies: Any additional policies or rules specific to the course, such as academic integrity or technology use.

A detailed syllabus is an important tool for students to plan their time and manage their workload throughout the course. It also helps to ensure that all students have a clear understanding of the expectations and requirements of the course.



### The student should have hands on experience in using various sensors like temperature, humidity, smoke, light, etc.

- **Temperature Sensor:** A temperature sensor is a device that measures the temperature of its environment and converts the input data into electronic data to record, monitor, or signal temperature changes.
- **Humidity Sensor:** A humidity sensor is a device that measures the relative humidity of the air around it. It works by detecting changes in electrical resistance or capacitance caused by moisture in the air.
- **Smoke Sensor:** A smoke sensor is a device that detects the presence of smoke in the air. It is commonly used in fire alarms and smoke detectors to alert people to the presence of a fire.
- **Light Sensor:** A light sensor is a device that measures the amount of light in its environment. It is commonly used in automatic lighting systems to turn lights on or off based on the amount of natural light available.

Having hands-on experience with these sensors can help students understand how they work and how they can be used in various applications. It can also help them develop practical skills in using and integrating these sensors into different systems.



### Should be able to use control web camera, network, and relays connected to the Pi

- A Raspberry Pi is a small, low-cost computer that can be used for a variety of projects, including controlling a web camera, network, and relays.
- To control a web camera, the Raspberry Pi must be connected to the camera via USB or the camera module interface. The camera can then be controlled using software such as `raspistill` or `raspivid`.
- To control a network, the Raspberry Pi must be connected to the network via Ethernet or Wi-Fi. The network can then be controlled using software such as `ifconfig` or `iwconfig`.
- To control relays, the Raspberry Pi must be connected to the relays via GPIO pins. The relays can then be controlled using software such as `gpio` or `wiringPi`.
- It is important to ensure that the Raspberry Pi is properly configured and that the appropriate software is installed in order to control the web camera, network, and relays.



#### 1. Start Raspberry Pi and try various Linux commands in command terminal window: ls, cd, touch, mv, rm, man

- **ls**: This command is used to list the contents of a directory. By default, it lists the contents of the current directory.
- **cd**: This command is used to change the current directory. It stands for "change directory".
- **touch**: This command is used to create a new, empty file. It can also be used to update the timestamp of an existing file.
- **mv**: This command is used to move or rename files or directories. It stands for "move".
- **rm**: This command is used to remove files or directories. It stands for "remove".
- **man**: This command is used to display the manual page for a command. It stands for "manual".

These are some basic Linux commands that can be used in the command terminal window of a Raspberry Pi. It is recommended to try these commands and experiment with them to become familiar with the Linux command line interface.



#### mkdir, rmdir, tar, gzip, cat, more, less, ps, sudo, cron, chown, chgrp, ping etc.

- `mkdir`: This command is used to create a new directory.
- `rmdir`: This command is used to remove an empty directory.
- `tar`: This command is used to create and extract compressed archive files.
- `gzip`: This command is used to compress and decompress files.
- `cat`: This command is used to display the contents of a file.
- `more`: This command is used to display the contents of a file one page at a time.
- `less`: This command is similar to `more`, but it allows backward movement in the file as well as forward movement.
- `ps`: This command is used to display information about the currently running processes.
- `sudo`: This command is used to execute a command with superuser privileges.
- `cron`: This command is used to schedule tasks to run automatically at specified times.
- `chown`: This command is used to change the owner of a file or directory.
- `chgrp`: This command is used to change the group ownership of a file or directory.
- `ping`: This command is used to test the connectivity between two network devices.

These are some of the basic commands that are commonly used in a Linux/Unix environment. They can be very useful for managing files, directories, processes, and system administration tasks. It is important to have a good understanding of these commands and their usage to be able to work efficiently in a Linux/Unix environment.



#### 2. Run some python programs on Pi like:

- **Blinking an LED**: This is a simple program that can be run on a Raspberry Pi to blink an LED. The program uses the GPIO library to control the output pins of the Raspberry Pi and turn the LED on and off.

- **Displaying text on an LCD**: Another program that can be run on a Raspberry Pi is one that displays text on an LCD screen. This program also uses the GPIO library to control the pins of the Raspberry Pi and send data to the LCD.

- **Reading sensor data**: A more advanced program that can be run on a Raspberry Pi is one that reads data from a sensor, such as a temperature or humidity sensor. This program uses the appropriate library for the sensor to read the data and then processes it to display it on the screen or save it to a file.

- **Controlling a motor**: Another program that can be run on a Raspberry Pi is one that controls a motor. This program uses the GPIO library to control the output pins of the Raspberry Pi and send signals to the motor driver to control the speed and direction of the motor.

These are just a few examples of the many programs that can be run on a Raspberry Pi using Python. The possibilities are endless and the only limit is your imagination.



#### a) Read your name and print Hello message with name

To read your name and print a Hello message with your name, you can follow these steps:

1. Declare a variable to store your name. For example, in Python, you can use the `input()` function to read your name from the standard input and store it in a variable called `name`:
```python
name = input("Enter your name: ")
```
2. Use the `print()` function to print the Hello message with your name. You can use string concatenation or string formatting to include your name in the message. For example:
```python
print("Hello, " + name + "!")
```
or
```python
print(f"Hello, {name}!")
```

This will read your name from the standard input and print a Hello message with your name. For example, if you enter `John` as your name, the output will be `Hello, John!`.



#### b) Read two numbers and print their sum, difference, product and division.

1. To read two numbers, we can use the `input()` function in Python. This function reads a line from the input and returns it as a string. We can then convert the string to an integer using the `int()` function.
```python
num1 = int(input("Enter the first number: "))
num2 = int(input("Enter the second number: "))
```
2. To calculate the sum of the two numbers, we can use the `+` operator.
```python
sum = num1 + num2
print("The sum of the two numbers is:", sum)
```
3. To calculate the difference of the two numbers, we can use the `-` operator.
```python
difference = num1 - num2
print("The difference of the two numbers is:", difference)
```
4. To calculate the product of the two numbers, we can use the `*` operator.
```python
product = num1 * num2
print("The product of the two numbers is:", product)
```
5. To calculate the division of the two numbers, we can use the `/` operator. Note that this operator returns a float value.
```python
division = num1 / num2
print("The division of the two numbers is:", division)
```



#### c) Word and character count of a given string.

- To count the number of words in a given string, one can split the string into a list of words using the `split()` method and then find the length of the list using the `len()` function.
- To count the number of characters in a given string, one can use the `len()` function on the string itself.
- For example, consider the string `"Hello, world!"`. The word count can be found by splitting the string into a list of words using the `split()` method: `["Hello,", "world!"]`. The length of this list is `2`, so the word count is `2`.
- The character count can be found by using the `len()` function on the string itself: `len("Hello, world!")` which returns `13`, so the character count is `13`.
- It is important to note that the `split()` method splits the string on whitespace by default, so punctuation marks and other non-alphabetic characters are included in the word count. If a different definition of a word is desired, a custom function can be used to split the string into words.



#### d) Area of a given shape (rectangle, triangle and circle) reading shape and appropriate values from standard

The area of a given shape can be calculated using the appropriate formula for that shape. The formulas for calculating the area of a rectangle, triangle, and circle are as follows:

1. **Rectangle**: The area of a rectangle can be calculated by multiplying its length by its width. The formula for the area of a rectangle is `A = l × w`, where `A` is the area, `l` is the length, and `w` is the width.

2. **Triangle**: The area of a triangle can be calculated using the formula `A = ½ × b × h`, where `A` is the area, `b` is the length of the base, and `h` is the height of the triangle.

3. **Circle**: The area of a circle can be calculated using the formula `A = π × r²`, where `A` is the area, `π` is approximately equal to 3.14, and `r` is the radius of the circle.

To calculate the area of a given shape, the shape and the appropriate values must be read from the standard. For example, to calculate the area of a rectangle, the length and width of the rectangle must be read from the standard. Similarly, to calculate the area of a triangle, the base and height of the triangle must be read from the standard. And to calculate the area of a circle, the radius of the circle must be read from the standard. Once the appropriate values have been read, the formula for the given shape can be used to calculate its area.



#### Input

- Input refers to the process of providing data or information to a computer or other device.
- This can be done through various input devices such as a keyboard, mouse, microphone, or touch screen.
- Input is essential for a computer to perform tasks and process information.
- The type of input device used can vary depending on the task being performed and the user's preferences.
- Some common input devices include keyboards, mice, touchpads, touch screens, microphones, and scanners.
- Input devices can be either wired or wireless, and can connect to a computer through various means such as USB, Bluetooth, or Wi-Fi.
- The data or information provided through input is then processed by the computer and can be used to perform tasks, display information, or generate output.



#### 3. Run some python programs on Pi like: a) Print a name 'n' times, where name and n are read from standard input,

To run a Python program on a Raspberry Pi, you will need to have Python installed on the Pi. Most Raspberry Pi operating systems come with Python pre-installed, but if it is not installed, you can install it using the following command:

```
sudo apt-get install python3
```

Once Python is installed, you can write a Python program to print a name 'n' times, where name and n are read from standard input. Here is an example of such a program:

```python
name = input("Enter a name: ")
n = int(input("Enter the number of times to print the name: "))

for i in range(n):
    print(name)
```

This program prompts the user to enter a name and the number of times to print the name. It then uses a for loop to print the name the specified number of times.

To run this program on the Raspberry Pi, save it to a file with a `.py` extension, such as `print_name.py`. Then, open a terminal on the Pi and navigate to the directory where the file is saved. Finally, run the program using the following command:

```
python3 print_name.py
```

This will execute the program and prompt the user to enter the required inputs. Once the inputs are provided, the program will print the name the specified number of times.



#### Using For and While Loops
- For loops are used to iterate over a sequence (such as a list, tuple, or string) or other iterable object.
- The syntax for a for loop is: `for variable in sequence: statements`
- While loops are used to repeatedly execute a block of code as long as a condition is true.
- The syntax for a while loop is: `while condition: statements`

#### Handle Divided by Zero Exception
- Dividing by zero is an illegal operation in mathematics and will result in an error in most programming languages.
- In Python, attempting to divide by zero will raise a `ZeroDivisionError`.
- This error can be handled using a try-except block.
- The syntax for handling a ZeroDivisionError is:
```
try:
    # code that may raise a ZeroDivisionError
except ZeroDivisionError:
    # code to handle the error
```

#### Print Current Time for 10 Times with an Interval
- The `time` module in Python provides various time-related functions, including the `time()` function which returns the current time in seconds since the epoch.
- The `ctime()` function in the `time` module can be used to convert the time in seconds to a human-readable string.
- The `sleep()` function in the `time` module can be used to pause the execution of the program for a specified number of seconds.
- To print the current time for 10 times with an interval, the following code can be used:
```
import time

for i in range(10):
    print(time.ctime())
    time.sleep(1) # pause for 1 second
```



#### Interval of 10 seconds
An interval of 10 seconds refers to a time duration or a time period of 10 seconds. This interval can be used in various contexts, such as setting a timer, scheduling a task, or measuring the time between two events.

#### Read a file line by line and print the word count of each line
To read a file line by line and print the word count of each line, the following steps can be followed:

1. Open the file in read mode.
2. Use a loop to iterate over each line in the file.
3. For each line, use the `split()` function to split the line into a list of words.
4. Use the `len()` function to count the number of words in the list.
5. Print the word count for the current line.
6. Close the file after all lines have been processed.

Here is an example code snippet in Python that demonstrates this process:

```python
with open('file.txt', 'r') as f:
    for line in f:
        words = line.split()
        word_count = len(words)
        print(word_count)
```



#### 4. a) Light an LED through Python program

1. To light an LED through a Python program, you will need a few components: a Raspberry Pi, an LED, a resistor, and jumper wires.
2. Connect the LED to the Raspberry Pi using the jumper wires and the resistor. The resistor is used to limit the current flowing through the LED to prevent it from burning out.
3. Once the LED is connected, you can use the GPIO library in Python to control the LED. This library allows you to control the GPIO pins on the Raspberry Pi.
4. To turn the LED on, you need to set the GPIO pin connected to the LED to output mode and then set its value to high. Here is an example code snippet that shows how to do this:

```python
import RPi.GPIO as GPIO
GPIO.setmode(GPIO.BCM)
LED_PIN = 18
GPIO.setup(LED_PIN, GPIO.OUT)
GPIO.output(LED_PIN, GPIO.HIGH)
```

5. To turn the LED off, you can set the value of the GPIO pin to low:

```python
GPIO.output(LED_PIN, GPIO.LOW)
```

6. Remember to clean up the GPIO pins when you are done by calling the `GPIO.cleanup()` function. This will reset the GPIO pins to their default state.

```python
GPIO.cleanup()
```



#### b) Get input from two switches and switch on corresponding LEDs

To get input from two switches and switch on corresponding LEDs, the following steps can be followed:

1. Connect the two switches to the input pins of the microcontroller.
2. Connect the two LEDs to the output pins of the microcontroller.
3. Write a program to read the input from the switches.
4. In the program, use conditional statements to check the state of the switches.
5. If a switch is in the ON state, turn on the corresponding LED by sending a HIGH signal to the output pin connected to the LED.
6. If a switch is in the OFF state, turn off the corresponding LED by sending a LOW signal to the output pin connected to the LED.
7. Upload the program to the microcontroller and test the circuit.

This is a basic approach to getting input from two switches and switching on corresponding LEDs. The specific details and implementation may vary depending on the microcontroller and other components used. It is important to consult the datasheets and documentation for the specific components to ensure proper operation.



#### c) Flash an LED at a given on time and off time cycle, where the two times are taken from a file.

To flash an LED at a given on time and off time cycle, where the two times are taken from a file, the following steps can be followed:

1. **Read the on and off times from the file**: The first step is to read the on and off times from the file. This can be done using the appropriate file handling functions in the programming language being used.

2. **Set up the LED**: The next step is to set up the LED. This involves connecting the LED to the appropriate pin on the microcontroller and configuring the pin as an output.

3. **Turn the LED on and off**: The final step is to turn the LED on and off according to the on and off times read from the file. This can be done using a loop that turns the LED on, waits for the on time, turns the LED off, and then waits for the off time before repeating.

By following these steps, it is possible to flash an LED at a given on time and off time cycle, where the two times are taken from a file. This can be useful in a variety of applications, such as creating a flashing indicator light or a simple light show.



#### 5. a) Flash an LED based on cron output (acts as an alarm)

- Cron is a time-based job scheduler in Unix-like operating systems.
- It is used to schedule commands or scripts to run automatically at a specific time or date.
- To flash an LED based on cron output, you need to have a script that controls the LED and schedule it to run using cron.
- The script can be written in a programming language such as Python or Bash.
- The script should include commands to turn the LED on and off at the desired intervals.
- To schedule the script to run using cron, you need to edit the crontab file.
- The crontab file contains a list of commands and the times at which they should be executed.
- To edit the crontab file, use the `crontab -e` command.
- In the crontab file, add a new line with the desired schedule and the path to the script.
- The schedule is specified using the cron format, which consists of five fields separated by spaces.
- The fields represent, in order, the minute, hour, day of the month, month, and day of the week.
- For example, to schedule the script to run every day at 8:00 AM, the line in the crontab file would be `0 8 * * * /path/to/script`.
- Once the crontab file is saved, the cron daemon will automatically run the script at the specified time.
- As a result, the LED will flash based on the cron output, acting as an alarm.



#### b) Switch on a relay at a given time using cron, where the relay's contact terminals are connected to a load.

1. A relay is an electrically operated switch that can be used to control a circuit by a low-power signal.
2. Cron is a time-based job scheduler in Unix-like operating systems, which can be used to schedule tasks to run periodically at fixed times, dates, or intervals.
3. To switch on a relay at a given time using cron, the following steps can be followed:
    - Identify the relay and the load it is connected to.
    - Write a script that sends a signal to the relay to switch it on.
    - Use the `crontab` command to open the crontab file for editing.
    - Add a new line to the crontab file, specifying the time and date when the script should be executed.
    - Save and close the crontab file.
4. At the specified time, cron will execute the script, which will send a signal to the relay to switch it on, thereby connecting the load to the power source.



#### c) Get the status of a bulb at a remote place (on the LAN) through web.

To get the status of a bulb at a remote place on the LAN through the web, the following steps can be taken:

1. **Connect the bulb to the network**: The bulb must be connected to the local area network (LAN) and have an IP address assigned to it.

2. **Set up a web server**: A web server must be set up on the network to communicate with the bulb. This can be done using a computer or a dedicated device such as a Raspberry Pi.

3. **Create a web page**: A web page must be created on the web server to display the status of the bulb. This can be done using HTML, CSS, and JavaScript.

4. **Send a request to the bulb**: The web page must send a request to the bulb to get its status. This can be done using a protocol such as HTTP or MQTT.

5. **Receive the response from the bulb**: The bulb will send a response back to the web page with its current status (on or off).

6. **Display the status on the web page**: The web page must update to display the current status of the bulb.

By following these steps, it is possible to get the status of a bulb at a remote place on the LAN through the web. This can be useful for monitoring and controlling the bulb from a remote location.



#### Note: The Instructor may add/delete/modify/tune experiments

- It is important to note that the instructor has the authority to make changes to the experiments.
- This means that the instructor may add new experiments, delete existing ones, modify the details of the experiments, or tune them to better suit the needs of the class.
- As a student, it is important to be aware of these potential changes and to be prepared to adapt to them.
- This flexibility allows the instructor to tailor the course to the specific needs and interests of the students, and to ensure that the course remains up-to-date and relevant.
- It is recommended to regularly check for updates and to communicate with the instructor to stay informed about any changes to the experiments.



# KOT 552 INTERNET OF THINGS LAB KCS

- KOT 552 is a course on the Internet of Things (IoT) Lab.
- IoT refers to the interconnection of physical devices, vehicles, buildings, and other items embedded with electronics, software, sensors, and network connectivity, which enables these objects to collect and exchange data.
- The course covers the basics of IoT, including its architecture, protocols, and applications.
- Students will learn how to design, implement, and test IoT systems using various hardware and software platforms.
- The course also covers the security and privacy issues associated with IoT.
- The lab component of the course provides hands-on experience in building and deploying IoT systems.
- The course is offered by the KCS department.




# Course Outcome (CO) Bloom’s Knowledge Level (KL)

Course Outcome (CO) refers to the expected knowledge, skills, and attitudes that students should possess upon completion of a specific course. COs are derived from the course objectives and are used to assess the effectiveness of the course in achieving its intended goals.

Bloom’s Knowledge Level (KL) is a classification system used to categorize the cognitive skills and abilities required for learning. The system was developed by Benjamin Bloom and his colleagues in the 1950s and is commonly used in education to design curricula, assessments, and instructional strategies.

The Bloom’s Knowledge Level (KL) is divided into six levels, each representing a different type of cognitive skill:

1. **Remembering**: The ability to recall previously learned information.
2. **Understanding**: The ability to comprehend the meaning of the information.
3. **Applying**: The ability to use the information in a new situation.
4. **Analyzing**: The ability to break down complex information into its component parts.
5. **Evaluating**: The ability to make judgments about the value of the information.
6. **Creating**: The ability to combine information to form a new whole.

COs can be mapped to the different levels of Bloom’s KL to ensure that the course is designed to develop the appropriate cognitive skills in students. For example, a CO that requires students to apply a concept in a new situation would be mapped to the Applying level of Bloom’s KL.



# At the end of the course, the student will be able to:

1. Demonstrate a comprehensive understanding of the course material.
2. Apply the knowledge and skills acquired during the course to real-life situations.
3. Analyze and evaluate information critically and effectively.
4. Communicate effectively, both orally and in writing.
5. Work collaboratively with others to achieve common goals.
6. Demonstrate ethical and professional behavior.
7. Engage in lifelong learning and professional development.




# CO 1 Understand the concept of Internet of Things K3

- The Internet of Things (IoT) is a network of interconnected devices that can communicate with each other and share data.
- These devices can be anything from simple sensors to complex machines, and can be located anywhere in the world.
- IoT devices are equipped with sensors, processors, and communication hardware that allow them to collect, process, and transmit data.
- The data collected by IoT devices can be used to improve efficiency, reduce costs, and enhance the quality of life.
- IoT technology is being used in a wide range of applications, including smart homes, healthcare, transportation, and agriculture.
- The growth of IoT is being driven by advances in technology, such as the development of low-cost sensors and the increasing availability of high-speed internet.
- IoT is expected to have a significant impact on the economy, with some estimates suggesting that it could add trillions of dollars to global GDP over the next decade.
- There are, however, concerns about the security and privacy of data collected by IoT devices, and efforts are being made to address these issues.




# CO2 Implement interfacing of various sensors with Arduino/Raspberry Pi K4, K5

Interfacing sensors with Arduino or Raspberry Pi involves connecting the sensor to the microcontroller and writing code to read data from the sensor. Here are the steps to interface various sensors with Arduino/Raspberry Pi:

1. **Identify the sensor type and its communication protocol:** Sensors can communicate with microcontrollers using various protocols such as I2C, SPI, UART, or analog signals. It is important to identify the communication protocol used by the sensor to select the appropriate pins on the microcontroller for connection.

2. **Connect the sensor to the microcontroller:** Once the communication protocol is identified, the sensor can be connected to the microcontroller using the appropriate pins. For example, if the sensor uses I2C communication, it can be connected to the SDA and SCL pins on the microcontroller.

3. **Install necessary libraries:** Some sensors may require additional libraries to be installed on the microcontroller to facilitate communication. These libraries can be downloaded and installed using the Arduino Library Manager or by manually copying the library files to the appropriate directory.

4. **Write code to read data from the sensor:** Once the sensor is connected and the necessary libraries are installed, code can be written to read data from the sensor. This typically involves initializing the sensor, reading data from the sensor registers, and converting the raw data into meaningful values.

5. **Test and calibrate the sensor:** After the code is written, it is important to test the sensor to ensure that it is functioning correctly. This may involve comparing the sensor readings with known values or calibrating the sensor to improve its accuracy.

By following these steps, various sensors can be interfaced with Arduino or Raspberry Pi to read data and perform various tasks.



# CO 3 Demonstrate the ability to transmit data wirelessly between different devices. K4

- Wireless data transmission is the transfer of information between two or more devices without the use of wires or cables.
- There are several technologies that enable wireless data transmission, including Bluetooth, Wi-Fi, and cellular networks.
- Bluetooth is a short-range wireless technology that is commonly used to connect devices such as smartphones, tablets, and laptops to peripherals such as headphones, speakers, and keyboards.
- Wi-Fi is a wireless networking technology that uses radio waves to provide high-speed internet and network connections to devices within a local area network (LAN).
- Cellular networks use radio waves to transmit data over long distances, allowing mobile devices to connect to the internet and make phone calls from almost anywhere.
- To transmit data wirelessly between different devices, the devices must be equipped with the appropriate wireless technology and be within range of each other or a wireless access point.
- The process of transmitting data wirelessly involves encoding the data into a signal that can be transmitted over the airwaves, transmitting the signal, and then decoding the signal back into data at the receiving device.
- Wireless data transmission has many advantages, including increased mobility, flexibility, and convenience. However, it also has some challenges, such as security and interference.



# CO 4: Show an ability to upload/download sensor data on cloud and server K2

1. To upload sensor data to the cloud, one must first have access to a cloud storage service such as Amazon Web Services (AWS), Microsoft Azure, or Google Cloud Platform (GCP).
2. Once access to a cloud storage service is established, the user must configure the service to accept data uploads from the sensor device.
3. This can typically be done through the use of APIs (Application Programming Interfaces) provided by the cloud storage service.
4. The sensor device must then be programmed to send its data to the cloud storage service using the appropriate API.
5. To download sensor data from the cloud, the user must have access to the cloud storage service where the data is stored.
6. The user can then use the cloud storage service's interface or APIs to retrieve the data from the cloud and download it to a local device or server.
7. Server K2 is a specific type of server that may have its own protocols for uploading and downloading data.
8. To upload or download data to/from Server K2, the user must follow the specific instructions and protocols provided by the server's documentation.



# CO 5 Examine various SQL queries from MySQL database K4, K5

SQL (Structured Query Language) is a standard language used to manage and manipulate data stored in relational databases. MySQL is one of the most popular open-source relational database management systems that use SQL.

Here are some common SQL queries that can be used in a MySQL database:

1. **SELECT**: The SELECT statement is used to query a database and retrieve data from one or more tables. The basic syntax is `SELECT column1, column2, ... FROM table_name;`.

2. **INSERT**: The INSERT statement is used to add new records to a table. The basic syntax is `INSERT INTO table_name (column1, column2, ...) VALUES (value1, value2, ...);`.

3. **UPDATE**: The UPDATE statement is used to modify existing records in a table. The basic syntax is `UPDATE table_name SET column1 = value1, column2 = value2, ... WHERE condition;`.

4. **DELETE**: The DELETE statement is used to delete existing records from a table. The basic syntax is `DELETE FROM table_name WHERE condition;`.

5. **CREATE**: The CREATE statement is used to create a new table in a database. The basic syntax is `CREATE TABLE table_name (column1 data_type, column2 data_type, ...);`.

6. **ALTER**: The ALTER statement is used to add, modify, or delete columns in an existing table. The basic syntax is `ALTER TABLE table_name ADD column_name data_type;`.

7. **DROP**: The DROP statement is used to delete a table from a database. The basic syntax is `DROP TABLE table_name;`.

These are just a few examples of the many SQL queries that can be used in a MySQL database. It is important to have a good understanding of these queries in order to effectively manage and manipulate data in a MySQL database.



# DETAILED SYLLABUS

A detailed syllabus is a comprehensive outline of the topics, assignments, and assessments that will be covered in a course. It is an essential tool for students to understand the expectations and requirements of the course, and to plan their studies accordingly.

The following are some key points to consider when creating a detailed syllabus:

1. **Course Overview:** Provide a brief overview of the course, including its objectives, learning outcomes, and any prerequisites.

2. **Course Schedule:** Include a schedule of topics, assignments, and assessments, along with their due dates. This will help students to plan their studies and manage their time effectively.

3. **Assessment Criteria:** Clearly outline the criteria for grading, including the weight of each assignment and assessment.

4. **Course Policies:** Include information on course policies, such as attendance, late work, and academic integrity.

5. **Course Materials:** Provide a list of required and recommended course materials, including textbooks, articles, and online resources.

6. **Contact Information:** Include the instructor's contact information, office hours, and preferred method of communication.

7. **Additional Resources:** Provide information on additional resources, such as tutoring, academic support, and counseling services, that may be available to students.

A detailed syllabus is an important tool for both instructors and students, as it provides a clear roadmap for the course and helps to ensure that everyone is on the same page. It is important to review the syllabus regularly and make any necessary updates or changes.



# Hands-on Experience with Various Sensors

It is important for students to have hands-on experience with various sensors, including but not limited to:

1. **Temperature sensors**: These sensors measure the temperature of the environment or an object. They can be used in a variety of applications, such as monitoring the temperature of a room or a refrigerator.

2. **Humidity sensors**: These sensors measure the amount of moisture in the air. They can be used in applications such as monitoring the humidity level in a greenhouse or a storage room.

3. **Smoke sensors**: These sensors detect the presence of smoke in the air. They are commonly used in fire alarms and can help prevent fires from spreading.

4. **Light sensors**: These sensors measure the amount of light in the environment. They can be used in applications such as automatically turning on street lights at night or adjusting the brightness of a display.

By having hands-on experience with these sensors, students can learn how they work, how to use them in practical applications, and how to interpret the data they provide. This knowledge can be valuable in a variety of fields, including engineering, science, and technology.



# Using Control Web Camera, Network, and Relays Connected to the Pi

1. **Web Camera**: A web camera can be connected to the Raspberry Pi via USB. Once connected, the camera can be accessed and controlled using various software packages such as `fswebcam` or `motion`.
2. **Network**: The Raspberry Pi can be connected to a network via Ethernet or Wi-Fi. This allows the Pi to access the internet and communicate with other devices on the network. The network settings can be configured using the `raspi-config` tool or by editing the `/etc/network/interfaces` file.
3. **Relays**: Relays can be connected to the Raspberry Pi's GPIO pins to control external devices such as lights or motors. The relays can be controlled using various programming languages such as Python or C. Libraries such as `RPi.GPIO` or `wiringPi` can be used to interface with the GPIO pins.

It is important to note that the Raspberry Pi has limited power and processing capabilities, so it may not be suitable for all applications. It is also important to ensure that the connected devices are compatible with the Pi and that the necessary drivers and software are installed.



# Starting Raspberry Pi and trying various Linux commands in the command terminal window

1. **ls**: This command is used to list the contents of a directory. By default, it lists the contents of the current directory.
2. **cd**: This command is used to change the current directory. For example, `cd Documents` will change the current directory to the Documents directory.
3. **touch**: This command is used to create a new, empty file. For example, `touch newfile.txt` will create a new file named `newfile.txt`.
4. **mv**: This command is used to move or rename files or directories. For example, `mv file1.txt file2.txt` will rename `file1.txt` to `file2.txt`.
5. **rm**: This command is used to remove files or directories. For example, `rm file.txt` will remove the file named `file.txt`.
6. **man**: This command is used to display the manual page for a command. For example, `man ls` will display the manual page for the `ls` command.

These are some basic Linux commands that can be used in the command terminal window of a Raspberry Pi. It is important to familiarize oneself with these commands to effectively navigate and manipulate the file system on a Raspberry Pi.



# Linux Commands

Here is a brief overview of some commonly used Linux commands:

1. **mkdir**: This command is used to create a new directory. The syntax is `mkdir directory_name`.
2. **rmdir**: This command is used to remove an empty directory. The syntax is `rmdir directory_name`.
3. **tar**: This command is used to create or extract tar archives. The syntax is `tar [options] [archive_name] [file or directory to be archived]`.
4. **gzip**: This command is used to compress or decompress files. The syntax is `gzip [options] [file_name]`.
5. **cat**: This command is used to concatenate files and print their contents to the standard output. The syntax is `cat [options] [file_name]`.
6. **more**: This command is used to display the contents of a file one screen at a time. The syntax is `more [options] [file_name]`.
7. **less**: This command is similar to `more`, but it allows backward movement in the file as well as forward movement. The syntax is `less [options] [file_name]`.
8. **ps**: This command is used to report information about the current processes. The syntax is `ps [options]`.
9. **sudo**: This command is used to execute a command as another user, typically the superuser. The syntax is `sudo [options] [command]`.
10. **cron**: This command is used to schedule commands to be executed at a later time. The syntax is `crontab [options] [file]`.
11. **chown**: This command is used to change the owner of a file or directory. The syntax is `chown [options] [owner] [file or directory]`.
12. **chgrp**: This command is used to change the group ownership of a file or directory. The syntax is `chgrp [options] [group] [file or directory]`.
13. **ping**: This command is used to test the reachability of a host on an IP network. The syntax is `ping [options] [destination]`.




# Running Python Programs on Raspberry Pi

1. To run Python programs on a Raspberry Pi, you need to have the Python interpreter installed on the device. Most Raspberry Pi operating systems come with Python pre-installed, but you can also install it manually if needed.

2. Once you have Python installed, you can write your Python program using a text editor and save it with a `.py` file extension.

3. To run the program, open a terminal window and navigate to the directory where the program is saved. Then, type the command `python3 filename.py`, replacing `filename` with the name of your Python file.

4. The program will then run, and any output will be displayed in the terminal window.

5. You can also run Python programs on a Raspberry Pi using an Integrated Development Environment (IDE) such as Thonny or IDLE. These programs provide a graphical user interface for writing and running Python programs.

6. In addition to running standalone Python programs, you can also use Python to control the GPIO pins on a Raspberry Pi, allowing you to interact with external hardware such as sensors, motors, and displays.

7. There are many resources available online for learning how to run Python programs on a Raspberry Pi, including tutorials, example projects, and community forums.



# Read your name and print Hello message with name

1. To read your name, you can use the `input()` function in Python. This function prompts the user to enter some text and returns the text entered by the user as a string.
2. To print the Hello message with the name, you can use the `print()` function in Python. This function outputs the text passed to it as an argument to the console.
3. You can combine the `input()` and `print()` functions to read the user's name and print the Hello message with the name.

Here is an example code that demonstrates how to read the user's name and print the Hello message with the name:

```python
name = input("Enter your name: ")
print("Hello, " + name + "!")
```

In this code, the `input()` function prompts the user to enter their name. The entered name is then stored in the `name` variable. The `print()` function is then used to output the Hello message with the name to the console.

When you run this code, it will prompt you to enter your name. After you enter your name and press Enter, it will print the Hello message with your name. For example, if you enter "John" as your name, the output will be "Hello, John!".



# b) Read two numbers and print their sum, difference, product and division.

1. To read two numbers, you can use the `input()` function in Python. This function reads a line from input, converts it to a string, and returns it. You can then convert the string to an integer or float using the `int()` or `float()` functions, respectively.

```python
num1 = int(input("Enter the first number: "))
num2 = int(input("Enter the second number: "))
```

2. To print the sum of the two numbers, you can use the `+` operator.

```python
sum = num1 + num2
print("The sum of the two numbers is:", sum)
```

3. To print the difference of the two numbers, you can use the `-` operator.

```python
difference = num1 - num2
print("The difference of the two numbers is:", difference)
```

4. To print the product of the two numbers, you can use the `*` operator.

```python
product = num1 * num2
print("The product of the two numbers is:", product)
```

5. To print the division of the two numbers, you can use the `/` operator.

```python
division = num1 / num2
print("The division of the two numbers is:", division)
```



### Word and Character Count of a Given String

1. **Word Count**: The word count of a given string is the number of words present in the string. A word is defined as a sequence of characters separated by spaces or punctuation marks. For example, the string "Hello, world!" has a word count of 2.

2. **Character Count**: The character count of a given string is the number of characters present in the string, including spaces and punctuation marks. For example, the string "Hello, world!" has a character count of 13.

3. **Counting Words and Characters**: To count the number of words and characters in a given string, one can use the `split()` method to split the string into a list of words, and then use the `len()` function to find the length of the list and the length of the string. For example, the following code counts the number of words and characters in the string "Hello, world!":

```python
string = "Hello, world!"
words = string.split()
word_count = len(words)
character_count = len(string)
print("Word count:", word_count)
print("Character count:", character_count)
```

This code outputs the following:

```
Word count: 2
Character count: 13
```

4. **Additional Considerations**: When counting words, it is important to consider how words are separated in the given string. In the example above, the `split()` method splits the string at spaces, but it is also possible to split the string at other characters, such as punctuation marks, by providing an argument to the `split()` method. Additionally, when counting characters, it is important to consider whether or not to include spaces and punctuation marks in the count. This can be achieved by using string methods such as `replace()` to remove unwanted characters from the string before counting its length.



# Area of a given shape (rectangle, triangle and circle) reading shape and appropriate values from standard

The area of a given shape can be calculated using the appropriate formula for that shape. The formulas for calculating the area of a rectangle, triangle, and circle are as follows:

1. **Rectangle**: The area of a rectangle can be calculated by multiplying its length by its width. The formula for the area of a rectangle is `A = l * w`, where `A` is the area, `l` is the length, and `w` is the width.

2. **Triangle**: The area of a triangle can be calculated using the formula `A = (b * h) / 2`, where `A` is the area, `b` is the length of the base, and `h` is the height of the triangle.

3. **Circle**: The area of a circle can be calculated using the formula `A = π * r^2`, where `A` is the area, `π` is approximately equal to 3.14, and `r` is the radius of the circle.

To calculate the area of a given shape, the shape and the appropriate values must be read from the standard. For example, to calculate the area of a rectangle, the length and width of the rectangle must be read from the standard. Similarly, to calculate the area of a triangle, the base and height of the triangle must be read from the standard. To calculate the area of a circle, the radius of the circle must be read from the standard. Once the appropriate values have been read, the formula for the given shape can be used to calculate its area.



# Input

Input refers to the data or information that is entered into a computer system for processing. It can come from various sources, such as a keyboard, mouse, microphone, scanner, or other input devices. Here are some key points to remember about input:

1. Input devices are used to enter data into a computer system.
2. Common input devices include keyboards, mice, touchscreens, and microphones.
3. Input can also come from external sources, such as a file or a network connection.
4. The quality of the input data can affect the accuracy of the output.
5. Input data must be in a format that the computer can understand and process.
6. Input can be either manual or automatic.
7. Manual input involves a user entering data directly into the computer, while automatic input involves the computer receiving data from an external source without human intervention.




# Running Python Programs on Raspberry Pi

One of the programs that can be run on a Raspberry Pi using Python is printing a name 'n' times, where the name and the value of 'n' are read from standard input. Here is an example of how this can be done:

```python
name = input("Enter a name: ")
n = int(input("Enter the number of times to print the name: "))

for i in range(n):
    print(name)
```

In this program, the `input()` function is used to read the name and the value of 'n' from the user. The `int()` function is used to convert the value of 'n' from a string to an integer. The `for` loop is then used to print the name 'n' times.

This is just one example of the many programs that can be run on a Raspberry Pi using Python. The Raspberry Pi is a versatile device that can be used for a wide range of projects and applications.



# Using For and While Loops

For and while loops are used to iterate over a block of code multiple times. The difference between the two is that a for loop is used when the number of iterations is known, while a while loop is used when the number of iterations is unknown.

## For Loop
A for loop has the following syntax:
```
for variable in sequence:
    # code block
```
The `variable` takes on the value of each element in the `sequence` one by one, and the code block is executed for each value.

## While Loop
A while loop has the following syntax:
```
while condition:
    # code block
```
The code block is executed repeatedly as long as the `condition` is `True`. The `condition` is checked before each iteration.

# Handling Divide by Zero Exception

In Python, dividing by zero raises a `ZeroDivisionError`. This can be handled using a try-except block. Here is an example:
```
try:
    result = x / y
except ZeroDivisionError:
    print("Error: Cannot divide by zero")
```
In the above code, if `y` is zero, a `ZeroDivisionError` is raised and caught by the except block. The error message is then printed.

# Printing Current Time for 10 Times with an Interval

To print the current time for 10 times with an interval, we can use the `time` module. Here is an example:
```
import time

for i in range(10):
    print(time.ctime())
    time.sleep(1)
```
In the above code, the `time.ctime()` function returns the current time as a string. The `time.sleep(1)` function causes the program to pause for 1 second before the next iteration. This results in the current time being printed 10 times with an interval of 1 second between each print.



# Interval of 10 seconds

An interval of 10 seconds refers to a time period or duration of 10 seconds. It can be used in various contexts, such as measuring time, setting timers, or scheduling events.

# Reading a file line by line and printing the word count of each line

To read a file line by line and print the word count of each line, you can use the following steps:

1. Open the file in read mode using a file object.
2. Use a loop to iterate over the file object, which will return one line at a time.
3. For each line, use the `split()` method to split the line into a list of words.
4. Use the `len()` function to find the number of words in the list.
5. Print the word count for the current line.
6. Continue iterating over the file object until all lines have been processed.
7. Close the file object.

Here is an example code snippet in Python that demonstrates these steps:

```python
with open('file.txt', 'r') as f:
    for line in f:
        words = line.split()
        word_count = len(words)
        print(word_count)
```

This code opens a file named `file.txt` in read mode, iterates over each line in the file, splits the line into a list of words, finds the word count, and prints it. The `with` statement is used to automatically close the file object when the block of code is finished.



# 4. a) Light an LED through Python program

1. To light an LED through a Python program, you will need a few components: a Raspberry Pi, an LED, a resistor, and jumper wires.
2. Connect the LED to one of the GPIO pins on the Raspberry Pi using a resistor and jumper wires.
3. In your Python program, import the necessary libraries, such as `RPi.GPIO` and `time`.
4. Set the GPIO pin connected to the LED as an output pin using the `GPIO.setup()` function.
5. Use the `GPIO.output()` function to turn the LED on and off.
6. You can also use the `time.sleep()` function to control the duration of the LED being on or off.
7. Remember to clean up the GPIO pins using the `GPIO.cleanup()` function at the end of your program.

Here is an example Python program that lights an LED:

```python
import RPi.GPIO as GPIO
import time

LED_PIN = 18

GPIO.setmode(GPIO.BCM)
GPIO.setup(LED_PIN, GPIO.OUT)

try:
    while True:
        GPIO.output(LED_PIN, GPIO.HIGH)
        time.sleep(1)
        GPIO.output(LED_PIN, GPIO.LOW)
        time.sleep(1)
except KeyboardInterrupt:
    GPIO.cleanup()
```



# Get input from two switches and switch on corresponding LEDs

To get input from two switches and switch on corresponding LEDs, follow these steps:

1. Connect the two switches to the input pins of the microcontroller.
2. Connect the two LEDs to the output pins of the microcontroller.
3. Write a program to read the input from the switches.
4. In the program, use conditional statements to check the state of the switches.
5. If a switch is pressed, turn on the corresponding LED by setting the output pin to high.
6. If a switch is not pressed, turn off the corresponding LED by setting the output pin to low.
7. Upload the program to the microcontroller and run it.
8. Test the circuit by pressing the switches and observing the behavior of the LEDs.

This is a basic example of how to get input from two switches and switch on corresponding LEDs. The specific details may vary depending on the microcontroller and other components used. It is important to consult the datasheets and documentation for the specific components to ensure proper operation.



# Flashing an LED at a given on time and off time cycle

To flash an LED at a given on time and off time cycle, where the two times are taken from a file, the following steps can be followed:

1. **Read the on and off times from the file**: The on and off times can be stored in a file in a specific format, such as comma-separated values. The file can be read using a programming language such as Python or C, and the on and off times can be extracted and stored in variables.

2. **Set up the LED**: The LED can be connected to a microcontroller or a development board such as an Arduino or Raspberry Pi. The LED can be connected to a digital output pin of the microcontroller or development board.

3. **Write the code to flash the LED**: The code to flash the LED can be written in the programming language supported by the microcontroller or development board. The code should include a loop that turns the LED on for the on time, then turns it off for the off time. The on and off times can be controlled using delay functions or timers.

4. **Upload the code to the microcontroller or development board**: The code can be uploaded to the microcontroller or development board using a USB cable or wireless connection. Once the code is uploaded, the LED should start flashing at the given on and off times.



# Flash an LED based on cron output (acts as an alarm)

1. **Cron** is a time-based job scheduler in Unix-like operating systems. It is used to schedule commands or scripts to run automatically at a specific time or date.
2. To flash an LED based on cron output, you will need to have a **single-board computer** such as a Raspberry Pi, an **LED**, and a **resistor**.
3. Connect the LED to one of the GPIO pins on the Raspberry Pi and the resistor to the ground pin.
4. Write a **script** that turns the LED on and off at the desired interval.
5. Use the `crontab` command to **schedule** the script to run at the desired time or date.
6. The LED will flash based on the cron output, acting as an alarm.




# Switching on a Relay at a Given Time Using Cron

1. **Cron** is a time-based job scheduler in Unix-like operating systems. It is used to schedule commands or scripts to run automatically at a specified time and date.

2. **Relay** is an electrically operated switch. It is used to control a circuit by a low-power signal or to switch on and off a load connected to its contact terminals.

3. To switch on a relay at a given time using cron, the following steps can be followed:

    a. Connect the relay's contact terminals to the load that needs to be switched on.

    b. Write a script that sends a signal to the relay to switch it on. This can be done using a programming language such as Python or C.

    c. Open the crontab file by typing `crontab -e` in the terminal.

    d. In the crontab file, add a new line with the following format: `m h dom mon dow command`, where `m` is the minute, `h` is the hour, `dom` is the day of the month, `mon` is the month, `dow` is the day of the week, and `command` is the path to the script that switches on the relay.

    e. Save and close the crontab file.

4. At the specified time and date, the cron daemon will automatically run the script, which will send a signal to the relay to switch it on, thus switching on the load connected to its contact terminals.



#### Get the status of a bulb at a remote place (on the LAN) through web

1. To get the status of a bulb at a remote place on the LAN through the web, you need to have a smart bulb that is connected to the internet and can be controlled remotely.
2. The smart bulb should be connected to the same LAN as the device you are using to check its status.
3. You can use a web interface or an app provided by the manufacturer of the smart bulb to check its status.
4. Log in to the web interface or app using your credentials.
5. Navigate to the section where you can view the status of your smart devices.
6. Select the smart bulb whose status you want to check.
7. The status of the bulb, whether it is on or off, will be displayed on the screen.



# Note: The Instructor may add/delete/modify/tune experiments

- This statement indicates that the instructor has the authority to make changes to the experiments being conducted.
- The instructor may add new experiments to the curriculum to enhance the learning experience.
- The instructor may also delete experiments that are no longer relevant or necessary.
- The instructor may modify or tune existing experiments to better align with the learning objectives or to improve the effectiveness of the experiment.
- This flexibility allows the instructor to tailor the experiments to the needs of the students and the course.



# KOT 552 INTERNET OF THINGS LAB KCS

- Internet of Things (IoT) is a network of physical objects or "things" embedded with electronics, software, sensors, and connectivity to enable these objects to collect and exchange data.
- IoT allows objects to be sensed and controlled remotely across existing network infrastructure, creating opportunities for more direct integration of the physical world into computer-based systems, and resulting in improved efficiency, accuracy, and economic benefit.
- The IoT Lab course provides hands-on experience in designing and implementing IoT systems.
- The course covers the following topics:
  - Introduction to IoT
  - IoT Architecture
  - IoT Protocols
  - IoT Devices and Sensors
  - IoT Data Analytics
  - IoT Security
  - IoT Applications
- Students will work on projects to design and implement IoT systems using various hardware and software platforms.
- The course is designed for students with a background in computer science or a related field.
- The course is a combination of lectures, lab sessions, and project work.
- The course is graded based on lab assignments, project work, and a final exam.



## Course Outcome (CO) Bloom’s Knowledge Level (KL)

- Course Outcome (CO) refers to the expected knowledge, skills, and attitudes that students should possess upon completion of a specific course.
- COs are usually defined by the instructor or the institution and are used to evaluate the effectiveness of the course in achieving its intended goals.
- Bloom’s Taxonomy is a framework for categorizing educational goals and objectives into different levels of complexity and specificity.
- The Knowledge Level (KL) of Bloom’s Taxonomy refers to the lowest level of the hierarchy, where the focus is on the recall of information.
- At the Knowledge Level, students are expected to remember and recall basic facts, concepts, and principles related to the course material.
- COs can be mapped to different levels of Bloom’s Taxonomy, including the Knowledge Level, to ensure that the course is designed to facilitate the achievement of the desired learning outcomes.
- By aligning COs with Bloom’s Taxonomy, instructors can design assessments and instructional activities that target the appropriate level of cognitive complexity and promote deeper learning.



### At the end of the course, the student will be able to:

1. Demonstrate a comprehensive understanding of the course material.
2. Apply the knowledge and skills acquired during the course to real-life situations.
3. Analyze and evaluate information critically and effectively.
4. Communicate ideas and arguments clearly and effectively.
5. Work collaboratively with others to achieve common goals.
6. Demonstrate ethical and responsible behavior.
7. Engage in lifelong learning and professional development.




#### CO 1 Understand the concept of Internet of Things K3

- The Internet of Things (IoT) refers to the interconnection of physical devices, vehicles, buildings, and other items embedded with electronics, software, sensors, and network connectivity, which enables these objects to collect and exchange data.
- IoT allows objects to be sensed and controlled remotely across existing network infrastructure, creating opportunities for more direct integration of the physical world into computer-based systems, and resulting in improved efficiency, accuracy, and economic benefit.
- IoT is expected to offer advanced connectivity of devices, systems, and services that goes beyond machine-to-machine (M2M) communications and covers a variety of protocols, domains, and applications.
- IoT is a rapidly growing field, with an estimated 50 billion connected devices by 2020.
- IoT has the potential to impact various industries, including healthcare, transportation, and agriculture, among others.
- IoT raises concerns about security and privacy, as the large amount of data collected and transmitted by connected devices can be vulnerable to hacking and misuse.
- IoT requires the development of new technologies and standards to ensure interoperability and scalability of connected devices.




#### CO 2 Implement interfacing of various sensors with Arduino/Raspberry Pi K4, K5

Interfacing sensors with Arduino or Raspberry Pi involves connecting the sensor to the microcontroller and writing code to read data from the sensor. Here are the steps to interface various sensors with Arduino/Raspberry Pi:

1. **Identify the sensor and its communication protocol**: Different sensors use different communication protocols such as I2C, SPI, or UART. Identify the communication protocol used by the sensor.

2. **Connect the sensor to the microcontroller**: Connect the sensor to the microcontroller using the appropriate pins. For example, if the sensor uses I2C communication, connect the SDA and SCL pins of the sensor to the SDA and SCL pins of the microcontroller.

3. **Install necessary libraries**: Some sensors require specific libraries to be installed in order to interface with the microcontroller. Install the necessary libraries for the sensor.

4. **Write code to read data from the sensor**: Write code to read data from the sensor using the appropriate communication protocol. The code will vary depending on the sensor and the microcontroller being used.

5. **Test the sensor**: Test the sensor to ensure that it is working correctly and providing accurate data.

By following these steps, you can interface various sensors with Arduino or Raspberry Pi. It is important to consult the sensor's datasheet and the microcontroller's documentation to ensure proper interfacing.



#### CO 3 Demonstrate the ability to transmit data wirelessly between different devices. K4

1. Wireless data transmission is the transfer of information between two or more devices without the use of wires or cables.
2. There are several ways to transmit data wirelessly, including infrared, radio frequency, Bluetooth, Wi-Fi, and cellular networks.
3. Infrared technology uses light waves to transmit data between devices. It is commonly used in remote controls for televisions and other consumer electronics.
4. Radio frequency technology uses electromagnetic waves to transmit data. It is commonly used in wireless networking, satellite communication, and GPS.
5. Bluetooth technology uses short-range radio waves to transmit data between devices. It is commonly used in wireless headphones, speakers, and keyboards.
6. Wi-Fi technology uses radio waves to transmit data between devices on a local area network. It is commonly used in homes and businesses to connect devices to the internet.
7. Cellular networks use radio waves to transmit data between mobile devices and cell towers. It is commonly used in smartphones to connect to the internet and make phone calls.
8. To transmit data wirelessly between different devices, the devices must be equipped with the appropriate wireless technology and be within range of each other or a wireless network.
9. Wireless data transmission has several advantages, including increased mobility, flexibility, and convenience. However, it can also have security risks and may be subject to interference from other wireless devices.



#### CO 4 Show an ability to upload/download sensor data on cloud and server K2

- To upload sensor data to the cloud or server, one must first ensure that the data is in a format that is compatible with the cloud or server.
- The data can be uploaded using various methods such as FTP, HTTP, or through an API provided by the cloud or server.
- Once the data is uploaded, it can be accessed and downloaded by authorized users.
- To download the data, one can use the same methods as used for uploading, or use a web interface provided by the cloud or server.
- It is important to ensure that the data is secure during the upload and download process, and that only authorized users have access to the data.
- One can also automate the process of uploading and downloading data using scripts or programs.
- It is also important to keep track of the data being uploaded and downloaded, and to ensure that the data is backed up regularly.



#### CO 5 Examine various SQL queries from MySQL database K4, K5

Structured Query Language (SQL) is a standard language used to manage and manipulate data stored in relational databases. MySQL is one of the most popular open-source relational database management systems that use SQL.

Here are some common SQL queries used in MySQL:

1. **SELECT** - used to retrieve data from one or more tables.
```SQL
SELECT column1, column2, ...
FROM table_name;
```

2. **INSERT INTO** - used to insert new records into a table.
```SQL
INSERT INTO table_name (column1, column2, ...)
VALUES (value1, value2, ...);
```

3. **UPDATE** - used to modify existing records in a table.
```SQL
UPDATE table_name
SET column1 = value1, column2 = value2, ...
WHERE condition;
```

4. **DELETE** - used to delete existing records from a table.
```SQL
DELETE FROM table_name
WHERE condition;
```

5. **CREATE TABLE** - used to create a new table.
```SQL
CREATE TABLE table_name (
    column1 datatype,
    column2 datatype,
    ...
);
```

6. **ALTER TABLE** - used to add, modify, or delete columns in an existing table.
```SQL
ALTER TABLE table_name
ADD column_name datatype;
```

7. **DROP TABLE** - used to delete an existing table.
```SQL
DROP TABLE table_name;
```

8. **CREATE INDEX** - used to create an index on one or more columns.
```SQL
CREATE INDEX index_name
ON table_name (column1, column2, ...);
```

9. **DROP INDEX** - used to delete an existing index.
```SQL
ALTER TABLE table_name
DROP INDEX index_name;
```

These are just some of the many SQL queries that can be used in a MySQL database. It is important to have a good understanding of these queries to effectively manage and manipulate data in a MySQL database.



## DETAILED SYLLABUS

A detailed syllabus is an outline or summary of the topics that will be covered in a course. It typically includes the following information:

1. Course title and description: This section provides an overview of the course, including its objectives and goals.
2. Instructor information: This section includes the instructor's name, contact information, and office hours.
3. Course schedule: This section outlines the schedule of topics and assignments for the course.
4. Textbook and materials: This section lists the required and recommended textbooks and other materials for the course.
5. Grading policy: This section explains how grades will be determined, including the weighting of assignments and exams.
6. Course policies: This section outlines the policies for attendance, participation, late work, and academic integrity.
7. Assignments and exams: This section provides details on the assignments and exams for the course, including due dates and expectations.

A detailed syllabus is an important tool for students, as it helps them to understand the expectations and requirements of the course. It also provides a roadmap for the course, allowing students to plan their time and prepare for assignments and exams. Instructors may update the syllabus throughout the course to reflect changes in the schedule or course content. It is important for students to review the syllabus regularly to stay up-to-date with the course requirements.



### The student should have hands on experience in using various sensors like temperature, humidity, smoke, light, etc.

- **Temperature sensor**: A temperature sensor is a device that measures the temperature of its environment and converts the input data into electronic data to record, monitor, or signal temperature changes.
- **Humidity sensor**: A humidity sensor is a device that measures the relative humidity of the air around it. It works by detecting changes in electrical resistance or capacitance caused by moisture in the air.
- **Smoke sensor**: A smoke sensor is a device that detects the presence of smoke in the air. It works by using a light source and a light detector to measure the amount of light scattered by smoke particles in the air.
- **Light sensor**: A light sensor is a device that measures the amount of light in its environment. It works by converting light energy into an electrical signal that can be measured and recorded.

Having hands-on experience with these sensors can help students understand how they work and how to use them in practical applications. This can be useful in fields such as engineering, environmental science, and meteorology. It can also help students develop skills in data collection, analysis, and interpretation.



### Control Web Camera, Network, and Relays Connected to the Pi

1. **Web Camera**: The Raspberry Pi can be used to control a web camera. This can be done by connecting the camera to the Pi via USB and using software such as `motion` or `fswebcam` to capture images or video.
2. **Network**: The Raspberry Pi can be connected to a network via Ethernet or Wi-Fi. This allows the Pi to communicate with other devices on the network and access the internet. The `ifconfig` and `iwconfig` commands can be used to configure the network settings on the Pi.
3. **Relays**: Relays can be connected to the Raspberry Pi's GPIO pins to control devices such as lights or motors. The `gpio` command can be used to control the state of the GPIO pins and, in turn, the state of the connected relay.

By using these features, the Raspberry Pi can be used as a versatile and powerful tool for controlling a wide range of devices and systems.



#### 1. Start Raspberry Pi and try various Linux commands in command terminal window: ls, cd, touch, mv, rm, man

- **ls**: This command is used to list the contents of a directory. By default, it lists the contents of the current directory.
- **cd**: This command is used to change the current working directory. For example, `cd Documents` will change the current working directory to the `Documents` directory.
- **touch**: This command is used to create a new, empty file. For example, `touch newfile.txt` will create a new file named `newfile.txt` in the current working directory.
- **mv**: This command is used to move or rename files or directories. For example, `mv file1.txt file2.txt` will rename `file1.txt` to `file2.txt`, while `mv file.txt Documents/` will move `file.txt` to the `Documents` directory.
- **rm**: This command is used to remove files or directories. For example, `rm file.txt` will remove the file named `file.txt` from the current working directory.
- **man**: This command is used to display the manual page for a given command. For example, `man ls` will display the manual page for the `ls` command, which provides detailed information about the command and its usage.

These are some basic Linux commands that can be used in the command terminal window of a Raspberry Pi. It is recommended to try these commands and experiment with their usage to become more familiar with the Linux operating system.



#### mkdir, rmdir, tar, gzip, cat, more, less, ps, sudo, cron, chown, chgrp, ping etc.

- `mkdir`: This command is used to create a new directory.
- `rmdir`: This command is used to remove an empty directory.
- `tar`: This command is used to create, maintain, modify, and extract files that are archived in the tar format.
- `gzip`: This command is used to compress or expand files.
- `cat`: This command is used to concatenate files and print them on the standard output.
- `more`: This command is used to display the contents of a file one screen at a time.
- `less`: This command is similar to `more`, but it allows backward movement in the file as well as forward movement.
- `ps`: This command is used to report a snapshot of the current processes.
- `sudo`: This command is used to execute a command as another user, typically the superuser.
- `cron`: This command is used to schedule commands to be executed periodically.
- `chown`: This command is used to change the owner of a file or directory.
- `chgrp`: This command is used to change the group ownership of a file or directory.
- `ping`: This command is used to test the reachability of a host on an IP network.

These are some of the commonly used commands in Linux and Unix-like operating systems. They can be very useful for managing files, directories, processes, and system resources. It is important to understand their usage and syntax to be able to use them effectively.



#### 2. Run some python programs on Pi like:

1. **Blinking LED:** This is a simple program that can be used to control the blinking of an LED connected to the Raspberry Pi's GPIO pins.
2. **Temperature Sensor:** This program can be used to read data from a temperature sensor connected to the Raspberry Pi and display the temperature readings.
3. **Web Server:** This program can be used to set up a simple web server on the Raspberry Pi, allowing it to serve web pages and respond to HTTP requests.
4. **Camera Module:** This program can be used to control the Raspberry Pi camera module, allowing it to capture images and videos.
5. **GPIO Control:** This program can be used to control the Raspberry Pi's GPIO pins, allowing it to interact with various electronic components and sensors.

These are just a few examples of the many python programs that can be run on the Raspberry Pi. The possibilities are endless and the only limit is your imagination.



#### a) Read your name and print Hello message with name

To read your name and print a hello message with your name, you can follow these steps:

1. Declare a variable to store your name. For example, in Python, you can write `name = input("Enter your name: ")`.
2. Use the variable to print the hello message. For example, in Python, you can write `print("Hello, " + name + "!")`.

This will prompt the user to enter their name, store it in the `name` variable, and then print a hello message using the value of the `name` variable.

Here is an example of the complete code in Python:

```python
name = input("Enter your name: ")
print("Hello, " + name + "!")
```

When you run this code, it will prompt you to enter your name. After you enter your name and press enter, it will print a hello message with your name. For example, if you enter `John`, it will print `Hello, John!`.



#### b) Read two numbers and print their sum, difference, product and division.

To solve this problem, we can follow these steps:

1. Read the first number from the user and store it in a variable, let's say `num1`.
2. Read the second number from the user and store it in another variable, let's say `num2`.
3. Calculate the sum of `num1` and `num2` and store the result in a variable, let's say `sum`.
4. Calculate the difference between `num1` and `num2` and store the result in a variable, let's say `difference`.
5. Calculate the product of `num1` and `num2` and store the result in a variable, let's say `product`.
6. Calculate the division of `num1` by `num2` and store the result in a variable, let's say `division`.
7. Print the values of `sum`, `difference`, `product`, and `division`.

Here is an example of how the code might look like in Python:

```python
num1 = float(input("Enter the first number: "))
num2 = float(input("Enter the second number: "))

sum = num1 + num2
difference = num1 - num2
product = num1 * num2
division = num1 / num2

print("Sum:", sum)
print("Difference:", difference)
print("Product:", product)
print("Division:", division)
```




#### c) Word and character count of a given string.

- To count the number of words in a given string, we can split the string into a list of words using the `split()` method and then find the length of the list using the `len()` function.
- To count the number of characters in a given string, we can use the `len()` function directly on the string.
- For example, consider the string `"Hello, World!"`. The word count of this string can be found using the following code:
```python
string = "Hello, World!"
words = string.split()
word_count = len(words)
print(word_count)
```
This code will output `2`, indicating that there are 2 words in the given string.
- Similarly, the character count of the string can be found using the following code:
```python
string = "Hello, World!"
char_count = len(string)
print(char_count)
```
This code will output `13`, indicating that there are 13 characters in the given string (including spaces and punctuation).



#### d) Area of a given shape (rectangle, triangle and circle) reading shape and appropriate values from standard

The area of a given shape can be calculated by using the appropriate formula for the shape. The three shapes mentioned are rectangle, triangle, and circle.

1. **Rectangle**: The area of a rectangle can be calculated by multiplying its length by its width. The formula for the area of a rectangle is `A = l * w`, where `A` is the area, `l` is the length, and `w` is the width.

2. **Triangle**: The area of a triangle can be calculated using the formula `A = (b * h) / 2`, where `A` is the area, `b` is the length of the base, and `h` is the height of the triangle.

3. **Circle**: The area of a circle can be calculated using the formula `A = π * r^2`, where `A` is the area, `π` is approximately equal to 3.14, and `r` is the radius of the circle.

To calculate the area of a given shape, the shape and the appropriate values must be read from the standard. For example, if the shape is a rectangle, the length and width must be read from the standard. If the shape is a triangle, the base and height must be read from the standard. If the shape is a circle, the radius must be read from the standard. Once the appropriate values have been read, the formula for the shape can be used to calculate the area.



#### Input
- Input refers to the data or information that is entered into a computer system for processing.
- Input devices are the hardware components that allow users to enter data into a computer. Examples of input devices include keyboards, mice, touchscreens, and microphones.
- Input can also come from other sources, such as files, network connections, or sensors.
- The input data is processed by the computer's central processing unit (CPU) and then stored in memory or on a storage device.
- Input is an essential part of any computer system, as it allows users to interact with the computer and provide it with the data it needs to perform tasks.



#### 3. Run some python programs on Pi like: a) Print a name 'n' times, where name and n are read from standard input,

To run a Python program on a Raspberry Pi, you need to have Python installed on the Pi. Most Raspberry Pi operating systems come with Python pre-installed, but if not, you can install it using the following command:

```
sudo apt-get install python3
```

Once Python is installed, you can write a Python program to print a name 'n' times, where name and n are read from standard input. Here is an example program that does this:

```python
name = input("Enter a name: ")
n = int(input("Enter the number of times to print the name: "))

for i in range(n):
    print(name)
```

This program first prompts the user to enter a name and the number of times to print the name. The `input()` function is used to read the user's input, and the `int()` function is used to convert the string input to an integer. The program then uses a `for` loop to print the name the specified number of times.

To run this program on the Raspberry Pi, save it to a file with a `.py` extension, such as `print_name.py`, and then run it using the `python3` command:

```
python3 print_name.py
```

This will execute the program and prompt the user to enter the required inputs. Once the inputs are provided, the program will print the name the specified number of times.



#### Using for and while loops
- `for` and `while` loops are used to iterate over a block of code multiple times.
- `for` loops are used when the number of iterations is known beforehand.
- `while` loops are used when the number of iterations is not known beforehand and the loop continues until a certain condition is met.

#### Handle Divided by Zero Exception
- Dividing by zero is an illegal operation in mathematics and programming.
- In most programming languages, attempting to divide by zero will result in an error or exception being thrown.
- To handle this exception, you can use a `try` and `except` block to catch the exception and handle it gracefully.

#### Print current time for 10 times with an interval of 1 second
- To print the current time, you can use the `datetime` module in Python.
- To print the current time for 10 times with an interval of 1 second, you can use a `for` loop and the `sleep` function from the `time` module.
- Here is an example code snippet that achieves this:

```python
import datetime
import time

for i in range(10):
    print(datetime.datetime.now())
    time.sleep(1)
```



#### Interval of 10 seconds
- An interval of 10 seconds refers to a time period or duration of 10 seconds.
- This interval can be used in various contexts, such as setting a timer, scheduling a task, or measuring the time between two events.

#### Read a file line by line and print the word count of each line
- To read a file line by line, you can use a loop to iterate over each line in the file.
- For each line, you can use the `split()` method to split the line into a list of words.
- The length of this list represents the word count of the line.
- You can then print the word count of each line as you iterate through the file.

Here is an example of how this can be done in Python:

```python
with open('file.txt', 'r') as f:
    for line in f:
        words = line.split()
        word_count = len(words)
        print(word_count)
```



#### 4. a) Light an LED through Python program

1. To light an LED through a Python program, you will need a few components: a Raspberry Pi, an LED, a resistor, and jumper wires.
2. Connect the LED to the Raspberry Pi using the jumper wires and the resistor. The resistor is used to limit the current flowing through the LED to prevent it from burning out.
3. Once the LED is connected, you can write a Python program to control it. Start by importing the necessary libraries, such as the `RPi.GPIO` library which allows you to control the GPIO pins on the Raspberry Pi.
4. Set the GPIO pin connected to the LED as an output pin using the `GPIO.setup()` function.
5. Use the `GPIO.output()` function to turn the LED on and off by setting the output pin to `HIGH` or `LOW`.
6. You can also use a loop and the `time.sleep()` function to make the LED blink at a specific interval.

Here is an example Python program that will light an LED connected to GPIO pin 17 on the Raspberry Pi:

```python
import RPi.GPIO as GPIO
import time

# Set the pin numbering mode
GPIO.setmode(GPIO.BCM)

# Set the LED pin as an output pin
LED_PIN = 17
GPIO.setup(LED_PIN, GPIO.OUT)

# Turn the LED on
GPIO.output(LED_PIN, GPIO.HIGH)

# Wait for 5 seconds
time.sleep(5)

# Turn the LED off
GPIO.output(LED_PIN, GPIO.LOW)

# Clean up the GPIO pins
GPIO.cleanup()
```

This program will turn the LED on for 5 seconds and then turn it off. You can modify the program to make the LED blink or to control it in other ways.



#### b) Get input from two switches and switch on corresponding LEDs

To get input from two switches and switch on corresponding LEDs, the following steps can be followed:

1. Connect the two switches to the input pins of the microcontroller.
2. Connect the two LEDs to the output pins of the microcontroller.
3. Write a program to read the input from the switches.
4. In the program, use conditional statements to check the state of the switches.
5. If a switch is in the ON state, turn on the corresponding LED by sending a HIGH signal to the output pin connected to the LED.
6. If a switch is in the OFF state, turn off the corresponding LED by sending a LOW signal to the output pin connected to the LED.
7. Upload the program to the microcontroller and test the circuit.

This is a basic overview of how to get input from two switches and switch on corresponding LEDs. The specific details may vary depending on the microcontroller and other components used. It is important to consult the datasheets and documentation for the specific components to ensure proper operation.



#### c) Flash an LED at a given on time and off time cycle, where the two times are taken from a file.

To flash an LED at a given on time and off time cycle, where the two times are taken from a file, the following steps can be followed:

1. **Read the on and off times from the file:** The first step is to read the on and off times from the file. This can be done using file input/output operations in the programming language being used.

2. **Set up the LED:** The next step is to set up the LED. This involves connecting the LED to the appropriate pins on the microcontroller or development board being used.

3. **Create a loop:** A loop should be created to continuously turn the LED on and off. The loop should run indefinitely, or until a certain condition is met.

4. **Turn the LED on:** Within the loop, the first step is to turn the LED on. This can be done by setting the appropriate pin to a high voltage.

5. **Wait for the on time:** After turning the LED on, the program should wait for the on time specified in the file. This can be done using a delay function.

6. **Turn the LED off:** After the on time has elapsed, the LED should be turned off. This can be done by setting the appropriate pin to a low voltage.

7. **Wait for the off time:** After turning the LED off, the program should wait for the off time specified in the file. This can be done using a delay function.

8. **Repeat:** The loop should then repeat, turning the LED on and off according to the on and off times specified in the file.

By following these steps, an LED can be flashed at a given on time and off time cycle, where the two times are taken from a file. This can be useful for creating various lighting effects or for indicating the status of a system.



#### 5. a) Flash an LED based on cron output (acts as an alarm)

- Cron is a time-based job scheduler in Unix-like operating systems.
- It is used to schedule commands or scripts to run automatically at a specific time or date.
- To flash an LED based on cron output, you will need to write a script that controls the LED and schedule it to run using cron.
- The script can be written in a language such as Python or Bash and should include commands to turn the LED on and off.
- The script can be scheduled to run at a specific time or date using the `crontab` command.
- The `crontab` command allows you to edit the crontab file, which contains the schedule of cron jobs.
- To schedule the script to run at a specific time, you will need to add a new line to the crontab file with the appropriate time and date fields and the path to the script.
- Once the script is scheduled, cron will automatically run it at the specified time, causing the LED to flash and act as an alarm.



#### b) Switch on a relay at a given time using cron, where the relay's contact terminals are connected to a load.

1. A relay is an electrically operated switch that can be used to control the flow of current to a load.
2. Cron is a time-based job scheduler in Unix-like operating systems that can be used to schedule tasks to run at specific times.
3. To switch on a relay at a given time using cron, the following steps can be taken:
    1. Connect the relay's contact terminals to the load.
    2. Write a script that sends a signal to the relay to switch it on.
    3. Open the crontab file by typing `crontab -e` in the terminal.
    4. Add a new line to the crontab file in the format `m h dom mon dow command`, where `m` is the minute, `h` is the hour, `dom` is the day of the month, `mon` is the month, `dow` is the day of the week, and `command` is the path to the script that switches on the relay.
    5. Save and close the crontab file.
4. At the specified time, cron will run the script, sending a signal to the relay to switch it on, and the load will be powered.



#### c) Get the status of a bulb at a remote place (on the LAN) through web.

1. To get the status of a bulb at a remote place on the LAN through the web, you need to have a smart bulb that is connected to the internet and can be controlled remotely.
2. The smart bulb should be connected to the same LAN as the device you are using to check its status.
3. You can use a web interface or a mobile application provided by the manufacturer of the smart bulb to check its status.
4. Log in to the web interface or mobile application using your credentials.
5. Navigate to the section where you can view the status of your smart bulb.
6. The status of the bulb, whether it is on or off, will be displayed on the screen.
7. You can also use this interface to turn the bulb on or off remotely.




#### Note: The Instructor may add/delete/modify/tune experiments

- The instructor has the authority to make changes to the experiments.
- This includes adding new experiments, deleting existing ones, modifying the details of the experiments, or tuning them to better suit the needs of the course.
- These changes may be made at any time and are at the discretion of the instructor.
- It is important for students to be aware of this and to stay up to date with any changes made to the experiments.




# KOT 553 INTERNET OF THINGS LAB KCS

- KOT 553 is a course on the Internet of Things (IoT) Lab.
- IoT refers to the interconnection of physical devices, vehicles, buildings, and other items embedded with electronics, software, sensors, and network connectivity, which enables these objects to collect and exchange data.
- The course covers the basics of IoT, including its architecture, protocols, and applications.
- Students will learn how to design, implement, and test IoT systems using various hardware and software platforms.
- The course also covers the security and privacy issues associated with IoT.
- The lab component of the course provides hands-on experience in building and deploying IoT systems.
- KCS refers to the Knowledge and Competency Statement, which outlines the learning outcomes and competencies expected of students who complete the course.




# Course Outcome (CO) Bloom’s Knowledge Level (KL)

Course Outcome (CO) refers to the expected knowledge, skills, and attitudes that students should possess upon completion of a specific course. These outcomes are used to assess the effectiveness of the course and to guide the development of future courses.

Bloom’s Knowledge Level (KL) is a framework for categorizing educational learning objectives into levels of complexity and specificity. The levels are: Remembering, Understanding, Applying, Analyzing, Evaluating, and Creating. These levels can be used to assess the level of understanding that students have achieved in a course.

- COs are specific to each course and are developed by the instructor or course designer.
- COs should be measurable and achievable within the timeframe of the course.
- Bloom’s KL can be used to assess the level of understanding that students have achieved in a course.
- The levels of Bloom’s KL are hierarchical, with each level building upon the previous one.
- The levels of Bloom’s KL can be used to guide the development of course content and assessments.




# At the end of the course, the student will be able to:

1. Demonstrate a comprehensive understanding of the course material.
2. Apply the knowledge and skills acquired during the course to real-life situations.
3. Analyze and evaluate information critically and effectively.
4. Communicate ideas and arguments clearly and effectively, both orally and in writing.
5. Work collaboratively with others to achieve common goals.
6. Demonstrate the ability to think creatively and independently.
7. Demonstrate ethical and responsible behavior.
8. Demonstrate the ability to continue learning and adapting to new situations.




# CO 1 Understand the concept of Internet of Things K3

- The Internet of Things (IoT) describes the network of internet-connected physical objects (the “things”) that collect and transfer data using sensors.
- By collecting and transferring the data, these objects are improving user experience for things like wearable health monitors, connected appliances and smart home security systems.
- The IoT is the concept of connecting any device (so long as it has an on/off switch) to the Internet and to other connected devices.
- The IoT is a giant network of connected things and people – all of which collect and share data about the way they are used and about the environment around them.
- IoT is a system of interrelated computing devices, mechanical and digital machines, objects, animals or people that are provided with unique identifiers (UIDs) and the ability to transfer data over a network without requiring human-to-human or human-to-computer interaction.
- IoT describes physical objects embedded with sensors and actuators that communicate with computing systems via wired or wireless networks—allowing the physical world to be digitally monitored or even controlled.
- Everyday objects can be equipped with identifying, sensing, networking and processing capabilities that will allow them to communicate with one another and with other devices and services over the Internet to achieve some objective.



# CO2 Implement interfacing of various sensors with Arduino/Raspberry Pi K4, K5

Interfacing sensors with Arduino or Raspberry Pi involves connecting the sensor to the microcontroller and writing code to read data from the sensor. Here are the steps to interface various sensors with Arduino/Raspberry Pi:

1. **Identify the sensor and its communication protocol**: Different sensors use different communication protocols such as I2C, SPI, or UART. Identify the communication protocol used by the sensor.

2. **Connect the sensor to the microcontroller**: Connect the sensor to the microcontroller using the appropriate pins. For example, if the sensor uses I2C communication, connect the SDA and SCL pins of the sensor to the SDA and SCL pins of the microcontroller.

3. **Install necessary libraries**: Some sensors require specific libraries to be installed in order to interface with the microcontroller. Install the necessary libraries for the sensor.

4. **Write code to read data from the sensor**: Write code to read data from the sensor using the appropriate communication protocol. The code will vary depending on the sensor and the microcontroller used.

5. **Test the sensor**: Test the sensor to ensure that it is working correctly and providing accurate data.

Interfacing sensors with Arduino or Raspberry Pi allows for the collection of data from the physical world, which can be used in various applications such as monitoring environmental conditions, tracking movement, or detecting changes in light or sound. By following the above steps, various sensors can be interfaced with Arduino or Raspberry Pi to collect and analyze data.



# CO 3 Demonstrate the ability to transmit data wirelessly between different devices. K4

- Wireless data transmission is the transfer of information between two or more devices without the use of wires or cables.
- There are several technologies that enable wireless data transmission, including Bluetooth, Wi-Fi, and cellular networks.
- Bluetooth is a short-range wireless technology that is commonly used to connect devices such as smartphones, tablets, and laptops to peripherals such as headphones, speakers, and keyboards.
- Wi-Fi is a wireless networking technology that uses radio waves to provide high-speed internet and network connections to devices within a local area network (LAN).
- Cellular networks use radio waves to transmit data over long distances, allowing mobile devices to connect to the internet and make phone calls from almost anywhere.
- To transmit data wirelessly between different devices, the devices must be equipped with the appropriate wireless technology and be within range of each other or a wireless access point.
- The process of transmitting data wirelessly between different devices typically involves establishing a connection, sending the data, and confirming that the data has been received.
- Wireless data transmission has many advantages, including increased mobility, flexibility, and convenience. However, there are also potential security risks, and measures should be taken to protect data transmitted wirelessly.



# CO 4: Show an ability to upload/download sensor data on cloud and server K2

- To upload sensor data to a cloud or server, one must first ensure that the data is in a format that is compatible with the cloud or server.
- The data can then be transferred to the cloud or server using various methods such as FTP, HTTP, or through an API.
- To download sensor data from a cloud or server, one must first ensure that they have the necessary permissions to access the data.
- The data can then be retrieved using various methods such as FTP, HTTP, or through an API.
- It is important to ensure that the data is properly secured during the upload and download process to prevent unauthorized access.
- One must also ensure that the data is properly backed up to prevent data loss.



# CO 5 Examine various SQL queries from MySQL database K4, K5

SQL (Structured Query Language) is a standard language for managing and querying relational databases. MySQL is one of the most popular open-source relational database management systems that use SQL.

Here are some common SQL queries that can be used in a MySQL database:

1. **SELECT**: The SELECT statement is used to query a database and retrieve data from one or more tables. The basic syntax is `SELECT column1, column2, ... FROM table_name;`. You can also use the `WHERE` clause to filter the results based on a condition.

2. **INSERT**: The INSERT statement is used to add new records to a table. The basic syntax is `INSERT INTO table_name (column1, column2, ...) VALUES (value1, value2, ...);`.

3. **UPDATE**: The UPDATE statement is used to modify existing records in a table. The basic syntax is `UPDATE table_name SET column1 = value1, column2 = value2, ... WHERE condition;`.

4. **DELETE**: The DELETE statement is used to delete existing records from a table. The basic syntax is `DELETE FROM table_name WHERE condition;`.

5. **CREATE**: The CREATE statement is used to create a new table in the database. The basic syntax is `CREATE TABLE table_name (column1 datatype, column2 datatype, ...);`.

6. **ALTER**: The ALTER statement is used to add, modify, or delete columns in an existing table. The basic syntax is `ALTER TABLE table_name ADD column_name datatype;` to add a new column, `ALTER TABLE table_name MODIFY column_name datatype;` to modify an existing column, and `ALTER TABLE table_name DROP COLUMN column_name;` to delete a column.

7. **DROP**: The DROP statement is used to delete a table from the database. The basic syntax is `DROP TABLE table_name;`.

These are just a few examples of the many SQL queries that can be used in a MySQL database. It is important to have a good understanding of SQL and its syntax to effectively manage and query data in a MySQL database.



# DETAILED SYLLABUS

A detailed syllabus is a comprehensive outline of the topics, assignments, and assessments that will be covered in a course. It is an essential tool for students to understand the expectations and requirements of the course, and to plan their studies accordingly.

Some key points to consider when creating a detailed syllabus include:

1. **Course Overview:** Provide a brief overview of the course, including its objectives, learning outcomes, and any prerequisites.

2. **Course Schedule:** Include a schedule of topics, assignments, and assessments, along with their due dates.

3. **Assessment Criteria:** Clearly outline the criteria for grading, including the weight of each assignment or assessment.

4. **Course Policies:** Include information on attendance, participation, late work, and academic integrity.

5. **Course Materials:** List the required and recommended textbooks, readings, and other materials for the course.

6. **Contact Information:** Provide contact information for the instructor, teaching assistants, and any other relevant personnel.

7. **Additional Resources:** Include information on additional resources, such as tutoring, office hours, and online resources.

A detailed syllabus is an important tool for both students and instructors, as it provides a clear roadmap for the course and helps to ensure that everyone is on the same page. It is important to review the syllabus regularly and make any necessary updates or changes.



# Hands-on Experience with Sensors

It is important for students to have hands-on experience with various sensors, including but not limited to:

1. **Temperature sensors**: These sensors measure the temperature of the environment or an object. They can be used in a variety of applications, such as monitoring the temperature of a room or a refrigerator.

2. **Humidity sensors**: These sensors measure the amount of moisture in the air. They can be used in applications such as weather monitoring or controlling the humidity in a greenhouse.

3. **Smoke sensors**: These sensors detect the presence of smoke in the air. They are commonly used in fire alarms and can help prevent fires from spreading.

4. **Light sensors**: These sensors measure the amount of light in the environment. They can be used in applications such as automatic lighting control or monitoring the growth of plants.

By working with these sensors, students can gain practical knowledge and experience in using them. This can help them understand how sensors work and how they can be used in real-world applications. Additionally, hands-on experience with sensors can help students develop problem-solving and critical thinking skills.



# Using Control Web Camera, Network, and Relays Connected to the Pi

1. **Web Camera**: A web camera can be connected to the Raspberry Pi via USB. Once connected, the camera can be controlled using various software packages, such as `fswebcam` or `motion`. These packages allow the user to capture images, record video, and adjust camera settings.

2. **Network**: The Raspberry Pi can be connected to a network via Ethernet or Wi-Fi. This allows the user to access the internet, transfer files, and communicate with other devices on the network. The network settings can be configured using the `raspi-config` tool or by editing the `/etc/network/interfaces` file.

3. **Relays**: Relays can be connected to the Raspberry Pi's GPIO pins to control external devices, such as lights or motors. The `gpiozero` library can be used to control the relays using Python. The user can write a script to turn the relays on or off, or to trigger them based on certain conditions.

These are some of the ways in which a user can control a web camera, network, and relays connected to the Raspberry Pi. It is important to note that the specific steps and commands may vary depending on the hardware and software being used.



# Starting Raspberry Pi and trying various Linux commands in command terminal window

1. **ls**: This command is used to list the contents of a directory. By default, it lists the contents of the current directory.
2. **cd**: This command is used to change the current working directory. For example, `cd Documents` will change the current working directory to the `Documents` directory.
3. **touch**: This command is used to create a new, empty file. For example, `touch newfile.txt` will create a new file named `newfile.txt` in the current working directory.
4. **mv**: This command is used to move or rename files and directories. For example, `mv oldname.txt newname.txt` will rename the file `oldname.txt` to `newname.txt`.
5. **rm**: This command is used to remove files and directories. For example, `rm file.txt` will remove the file `file.txt` from the current working directory.
6. **man**: This command is used to display the manual page for a command. For example, `man ls` will display the manual page for the `ls` command, which provides detailed information about the command and its usage.

These are some basic Linux commands that can be used in the command terminal window of a Raspberry Pi. It is recommended to try these commands and experiment with their usage to become more familiar with the Linux operating system.



# Linux Commands

Here is a brief overview of some commonly used Linux commands:

1. **mkdir**: This command is used to create a new directory. The syntax is `mkdir directory_name`.
2. **rmdir**: This command is used to remove an empty directory. The syntax is `rmdir directory_name`.
3. **tar**: This command is used to create or extract tar archives. The syntax is `tar [options] [archive_name] [file or directory to be archived]`.
4. **gzip**: This command is used to compress or decompress files. The syntax is `gzip [options] [file_name]`.
5. **cat**: This command is used to concatenate files and print their contents to the standard output. The syntax is `cat [options] [file_name]`.
6. **more**: This command is used to display the contents of a file one screen at a time. The syntax is `more [options] [file_name]`.
7. **less**: This command is similar to `more`, but it allows backward movement in the file as well as forward movement. The syntax is `less [options] [file_name]`.
8. **ps**: This command is used to report information about the current processes. The syntax is `ps [options]`.
9. **sudo**: This command is used to execute a command as another user, typically the superuser. The syntax is `sudo [options] [command]`.
10. **cron**: This command is used to schedule commands to be executed at a later time. The syntax is `crontab [options] [file]`.
11. **chown**: This command is used to change the owner of a file or directory. The syntax is `chown [options] [owner] [file or directory]`.
12. **chgrp**: This command is used to change the group of a file or directory. The syntax is `chgrp [options] [group] [file or directory]`.
13. **ping**: This command is used to test the connectivity to a network host. The syntax is `ping [options] [host]`.




# Running Python Programs on Raspberry Pi

Raspberry Pi is a small, affordable computer that can be used to run various programs, including those written in Python. Here are some steps to run Python programs on Raspberry Pi:

1. **Install Python**: Make sure that Python is installed on your Raspberry Pi. You can do this by opening the terminal and typing `sudo apt-get install python3`.

2. **Write a Python Program**: Write a Python program using a text editor such as `nano` or `vi`. Save the file with a `.py` extension.

3. **Run the Program**: To run the program, open the terminal and navigate to the directory where the program is saved. Then, type `python3 filename.py` and press enter to run the program.

4. **Debugging**: If there are any errors in the program, the terminal will display error messages. Use these messages to debug the program and fix any issues.

5. **Running Programs on Startup**: If you want the Python program to run automatically when the Raspberry Pi starts up, you can add the command to run the program to the `/etc/rc.local` file.

These are some basic steps to run Python programs on Raspberry Pi. You can also use various libraries and modules to enhance the functionality of your programs.



# Reading your name and printing a Hello message with name

1. To read your name, you can use the `input()` function in Python. This function prompts the user to enter a value and returns the value entered by the user as a string.
2. To print a Hello message with the name entered by the user, you can use the `print()` function in Python. This function outputs the specified message to the screen.
3. Here is an example of how you can read your name and print a Hello message with the name in Python:

```python
name = input("Enter your name: ")
print("Hello, " + name + "!")
```

In this example, the `input()` function prompts the user to enter their name. The value entered by the user is then stored in the `name` variable. The `print()` function is then used to output a Hello message with the name entered by the user.



# b) Read two numbers and print their sum, difference, product and division.

1. To read two numbers, you can use the input function in Python. For example:
```python
num1 = int(input("Enter the first number: "))
num2 = int(input("Enter the second number: "))
```
2. To print the sum of the two numbers, you can use the `+` operator. For example:
```python
sum = num1 + num2
print("The sum of the two numbers is:", sum)
```
3. To print the difference of the two numbers, you can use the `-` operator. For example:
```python
difference = num1 - num2
print("The difference of the two numbers is:", difference)
```
4. To print the product of the two numbers, you can use the `*` operator. For example:
```python
product = num1 * num2
print("The product of the two numbers is:", product)
```
5. To print the division of the two numbers, you can use the `/` operator. For example:
```python
division = num1 / num2
print("The division of the two numbers is:", division)
```



### Word and Character Count of a Given String

1. **Word Count**: The word count of a given string is the number of words present in the string. A word is defined as a sequence of characters separated by spaces or punctuation marks. To count the number of words in a string, one can split the string using a delimiter such as space or punctuation marks and then count the number of elements in the resulting list.

2. **Character Count**: The character count of a given string is the number of characters present in the string. This includes all characters, including spaces and punctuation marks. To count the number of characters in a string, one can simply use the `len()` function in Python or the `.length` property in JavaScript.

3. **Example**: Consider the string `"Hello, world! This is a test."`. The word count of this string is 6, as there are 6 words separated by spaces or punctuation marks. The character count of this string is 25, as there are 25 characters in total, including spaces and punctuation marks.

4. **Applications**: Word and character counts are commonly used in text analysis and natural language processing. They can be used to measure the length of a document, to calculate the average word length, or to determine the readability of a text. They are also used in social media platforms to limit the length of posts or messages.



# Area of a given shape (rectangle, triangle and circle) reading shape and appropriate values from standard

The area of a given shape can be calculated by using the appropriate formula for that shape. Here are the formulas for calculating the area of a rectangle, triangle, and circle:

1. **Rectangle**: The area of a rectangle can be calculated by multiplying its length by its width. The formula for the area of a rectangle is `A = l * w`, where `A` is the area, `l` is the length, and `w` is the width.

2. **Triangle**: The area of a triangle can be calculated using the formula `A = (b * h) / 2`, where `A` is the area, `b` is the length of the base, and `h` is the height of the triangle.

3. **Circle**: The area of a circle can be calculated using the formula `A = π * r^2`, where `A` is the area, `π` is approximately equal to 3.14, and `r` is the radius of the circle.

To calculate the area of a given shape, one must first identify the shape and then use the appropriate formula to calculate its area. The values for the variables in the formula can be obtained from standard measurements of the shape. For example, to calculate the area of a rectangle, one must measure its length and width and then use the formula `A = l * w` to calculate its area. Similarly, to calculate the area of a triangle, one must measure its base and height and then use the formula `A = (b * h) / 2` to calculate its area. To calculate the area of a circle, one must measure its radius and then use the formula `A = π * r^2` to calculate its area.



# Input

- Input refers to the data or information that is entered into a computer system for processing.
- Input devices are the hardware components that allow users to enter data into a computer. Examples of input devices include keyboards, mice, touchscreens, and microphones.
- Input can also come from other sources, such as files, network connections, or sensors.
- The input data is typically processed by the computer's central processing unit (CPU) and stored in memory for later use.
- Input is an essential part of any computer system, as it allows users to interact with the system and provide the data necessary for the system to perform its functions.
- Input can take many forms, including text, numbers, images, audio, and video.
- The type of input required by a computer system depends on the tasks it is designed to perform. For example, a word processing program requires text input, while a graphics program requires image input.
- Input can be entered into a computer system in many ways, including typing on a keyboard, clicking a mouse, speaking into a microphone, or touching a touchscreen.
- Input can also be entered automatically, such as when a sensor detects a change in temperature and sends the data to the computer.
- Input is an important concept in computer science and is essential for the operation of any computer system.



# 3. Running Python Programs on Raspberry Pi

One of the programs that can be run on a Raspberry Pi using Python is printing a name 'n' times, where the name and the value of 'n' are read from standard input. Here is an example of how this can be done:

```python
name = input("Enter a name: ")
n = int(input("Enter the number of times to print the name: "))

for i in range(n):
    print(name)
```

In this program, the `input()` function is used to read the name and the value of 'n' from the user. The `int()` function is used to convert the value of 'n' from a string to an integer. The `for` loop is then used to print the name 'n' times.



# Using for and while loops
- `for` and `while` loops are used to iterate over a block of code multiple times.
- `for` loops are used when the number of iterations is known beforehand.
- `while` loops are used when the number of iterations is not known beforehand.

# Handling Divided by Zero Exception
- Dividing by zero is not allowed in mathematics and will result in an error in most programming languages.
- To handle this exception, you can use a conditional statement to check if the denominator is zero before performing the division.
- If the denominator is zero, you can handle the exception by displaying an error message or performing an alternative calculation.

# Printing current time for 10 times with an interval
- To print the current time for 10 times with an interval, you can use a `for` loop and the `time` module.
- Inside the `for` loop, use the `time` module's `ctime` function to get the current time and print it.
- Use the `time` module's `sleep` function to pause the execution of the program for the desired interval before the next iteration of the loop.

Here is an example code snippet in Python:

```python
import time

for i in range(10):
    print(time.ctime())
    time.sleep(1)
```



# Interval of 10 seconds

An interval of 10 seconds refers to a time duration or time period of 10 seconds. It can be used in various contexts such as measuring time, setting timers, scheduling tasks, etc.

# Reading a file line by line and printing the word count of each line

To read a file line by line and print the word count of each line, the following steps can be followed:

1. Open the file in read mode using a file object.
2. Use a loop to iterate over the file object. This will read the file line by line.
3. For each line, use the `split()` method to split the line into a list of words.
4. Use the `len()` function to find the number of words in the list.
5. Print the word count of the line.
6. Close the file object after all lines have been read and processed.

Here is an example code snippet in Python that demonstrates the above steps:

```python
# Open the file in read mode
with open('file.txt', 'r') as file:
    # Iterate over the file object line by line
    for line in file:
        # Split the line into a list of words
        words = line.split()
        # Find the number of words in the list
        word_count = len(words)
        # Print the word count of the line
        print(word_count)
```

This code will read the file `file.txt` line by line and print the word count of each line. The file object is opened using the `with` statement, which ensures that the file is properly closed after all lines have been read and processed. The `split()` method is used to split each line into a list of words, and the `len()` function is used to find the number of words in the list. The word count of each line is then printed using the `print()` function.



# 4. a) Light an LED through Python program

To light an LED through a Python program, you will need the following components:
1. A single-board computer such as a Raspberry Pi or a microcontroller such as an Arduino.
2. An LED (Light Emitting Diode).
3. A resistor (220-ohm is a common value).
4. Jumper wires to connect the components.

Here are the steps to light an LED through a Python program:
1. Connect the LED to the single-board computer or microcontroller. The longer leg of the LED (the anode) should be connected to a GPIO (General Purpose Input/Output) pin through a resistor. The shorter leg of the LED (the cathode) should be connected to a ground pin.
2. Write a Python program to control the GPIO pin. The program should set the GPIO pin to output mode and then turn it on and off to light the LED. Here is an example Python program that can be used to light an LED on a Raspberry Pi:

```python
import RPi.GPIO as GPIO
import time

LED_PIN = 18

GPIO.setmode(GPIO.BCM)
GPIO.setup(LED_PIN, GPIO.OUT)

while True:
    GPIO.output(LED_PIN, GPIO.HIGH)
    time.sleep(1)
    GPIO.output(LED_PIN, GPIO.LOW)
    time.sleep(1)
```

This program sets the GPIO pin to output mode using the `GPIO.setup()` function. It then enters an infinite loop that turns the LED on and off by setting the GPIO pin to high and low using the `GPIO.output()` function. The `time.sleep()` function is used to pause the program for one second between turning the LED on and off.

3. Run the Python program. The LED should light up and turn off at one-second intervals.

Note that the exact details of how to connect the LED and write the Python program may vary depending on the specific single-board computer or microcontroller you are using. Consult the documentation for your device for more information.



# Get input from two switches and switch on corresponding LEDs

To get input from two switches and switch on corresponding LEDs, you can follow these steps:

1. Connect the two switches to the input pins of a microcontroller, such as an Arduino or Raspberry Pi.
2. Connect the two LEDs to the output pins of the microcontroller.
3. Write a program for the microcontroller that reads the state of the switches and turns on the corresponding LED when the switch is pressed.
4. Upload the program to the microcontroller and run it.

When the program is running, pressing one of the switches will turn on the corresponding LED. This can be used to create a simple interactive system where the user can control the state of the LEDs using the switches.



# Flashing an LED at a given on time and off time cycle

To flash an LED at a given on time and off time cycle, where the two times are taken from a file, the following steps can be followed:

1. **Read the on and off times from the file**: The first step is to read the on and off times from the file. This can be done using various file handling techniques in the programming language of your choice.

2. **Set up the LED**: The next step is to set up the LED. This involves connecting the LED to the appropriate pins on the microcontroller or development board and configuring the pins as outputs.

3. **Turn the LED on and off**: Once the LED is set up, the next step is to turn it on and off according to the on and off times read from the file. This can be done using a loop that turns the LED on, waits for the on time, turns the LED off, and then waits for the off time before repeating the cycle.

4. **Repeat the cycle**: The final step is to repeat the cycle of turning the LED on and off according to the on and off times read from the file. This can be done using a loop that continues to run until the program is stopped or some other condition is met.

By following these steps, it is possible to flash an LED at a given on time and off time cycle, where the two times are taken from a file. This can be useful for creating various visual effects or for indicating the status of a system.



# Flash an LED based on cron output (acts as an alarm)

1. **Cron** is a time-based job scheduler in Unix-like operating systems. It is used to schedule commands or scripts to run automatically at a specific time or date.
2. To flash an LED based on cron output, you will need a **single-board computer** such as a Raspberry Pi, an **LED**, and a **resistor**.
3. Connect the LED to one of the GPIO pins on the Raspberry Pi and the resistor to the other end of the LED.
4. Write a **script** that turns the LED on and off at the desired interval.
5. Use the **crontab** command to schedule the script to run at the desired time or date.
6. When the cron job runs, the script will be executed, and the LED will flash, acting as an alarm.




# Switching on a Relay at a Given Time Using Cron

1. **Cron** is a time-based job scheduler in Unix-like operating systems. It is used to schedule commands or scripts to run automatically at a specified time and date.

2. A **relay** is an electrically operated switch. It uses an electromagnet to mechanically operate a switch, allowing a low power circuit to control a high power circuit.

3. To switch on a relay at a given time using cron, the relay's contact terminals must be connected to a load.

4. The first step is to write a script that will control the relay. This script will send a signal to the relay to switch it on or off.

5. Next, the script must be scheduled to run at the desired time using the `crontab` command. The `crontab` command is used to edit the cron table, which is a list of commands that are scheduled to run at specified times.

6. The syntax for the `crontab` command is as follows: `crontab -e`. This will open the cron table in the default text editor.

7. The cron table uses a specific syntax to schedule commands. Each line of the cron table represents a separate job, and follows the format: `m h dom mon dow command`.

    - `m`: minute (0-59)
    - `h`: hour (0-23)
    - `dom`: day of the month (1-31)
    - `mon`: month (1-12)
    - `dow`: day of the week (0-6, with 0 representing Sunday)
    - `command`: the command to be executed

8. To schedule the script to run at a specific time, the appropriate values must be entered for `m`, `h`, `dom`, `mon`, and `dow`. The `command` field should contain the path to the script that controls the relay.

9. Once the cron table has been edited and saved, the script will be automatically executed at the specified time, switching on the relay and allowing current to flow to the load.

10. It is important to ensure that the script is executable and that the user has the appropriate permissions to run the `crontab` command.



# Get the status of a bulb at a remote place (on the LAN) through web

1. To get the status of a bulb at a remote place on the LAN through the web, the bulb must be connected to a device that is capable of communicating its status over the network.
2. This can be achieved by using a smart bulb that is connected to a home automation system or by connecting a regular bulb to a smart plug or switch that is capable of communicating its status over the network.
3. Once the bulb is connected to a device that can communicate its status, the status can be accessed through a web interface or an API provided by the device or the home automation system.
4. The user can then log in to the web interface or use the API to retrieve the current status of the bulb, such as whether it is on or off.
5. This can be useful for remotely monitoring and controlling the lighting in a building or for automating lighting based on certain conditions or events.




# Note: The Instructor may add/delete/modify/tune experiments

- This statement indicates that the instructor has the authority to make changes to the experiments in a course or program.
- The instructor may add new experiments to the course to introduce new concepts or techniques.
- The instructor may delete experiments that are no longer relevant or necessary for the course.
- The instructor may modify or tune existing experiments to improve their effectiveness or relevance.
- These changes may be made to ensure that the course content is up-to-date and meets the learning objectives of the students.
- Students should be aware that the experiments in the course may change and should be prepared to adapt to these changes.



# KOT 553 INTERNET OF THINGS LAB KCS

- Internet of Things (IoT) is a network of physical objects or "things" embedded with electronics, software, sensors, and connectivity to enable these objects to collect and exchange data.
- IoT allows objects to be sensed and controlled remotely across existing network infrastructure, creating opportunities for more direct integration of the physical world into computer-based systems, and resulting in improved efficiency, accuracy, and economic benefit.
- The IoT Lab course (KOT 553) provides students with hands-on experience in designing, building, and deploying IoT systems.
- The course covers topics such as IoT architecture, sensors and actuators, communication protocols, data analysis, and security.
- Students will work on projects to develop IoT solutions for real-world problems, using tools and platforms such as Arduino, Raspberry Pi, and cloud services.
- The course is designed for students with a background in computer science or a related field, and assumes prior knowledge of programming and basic electronics.
- The course is part of the KCS (Korea Computer Science) program, which offers a range of courses in computer science and related fields.




## Course Outcome (CO) Bloom’s Knowledge Level (KL)

- Course Outcome (CO) refers to the expected knowledge, skills, and attitudes that students should possess upon completion of a specific course.
- COs are statements that describe the measurable skills and abilities that students should be able to demonstrate as a result of completing a course.
- Bloom’s Taxonomy is a framework for categorizing educational goals and objectives into different levels of complexity and specificity.
- Bloom’s Knowledge Level (KL) is the first and most basic level of Bloom’s Taxonomy, which focuses on the recall and recognition of information.
- At the Knowledge Level, students are expected to remember and recall basic facts, concepts, and principles related to the course material.
- COs can be mapped to different levels of Bloom’s Taxonomy, including the Knowledge Level, to ensure that the course is designed to meet the desired learning outcomes.
- By aligning COs with Bloom’s Taxonomy, instructors can design assessments and activities that target the appropriate level of cognitive complexity and help students achieve the desired learning outcomes.



### At the end of the course, the student will be able to:

1. Demonstrate a comprehensive understanding of the course material.
2. Apply the knowledge and skills acquired during the course to real-world scenarios.
3. Analyze and evaluate information critically and effectively.
4. Communicate ideas and arguments clearly and effectively, both orally and in writing.
5. Work collaboratively with others to achieve common goals.
6. Demonstrate ethical and professional behavior in academic and professional settings.
7. Engage in lifelong learning and professional development.




#### CO 1 Understand the concept of Internet of Things K3

- The Internet of Things (IoT) is a network of interconnected devices that can communicate with each other and exchange data over the internet.
- These devices can be anything from simple sensors to complex machines, and can be embedded in everyday objects such as home appliances, vehicles, and even clothing.
- IoT devices can collect and transmit data, allowing for real-time monitoring and control of various systems and processes.
- The data collected by IoT devices can be used to improve efficiency, reduce costs, and enhance the user experience.
- IoT technology has the potential to revolutionize many industries, including healthcare, transportation, and agriculture.
- Some common applications of IoT technology include smart homes, wearable technology, and industrial automation.
- IoT technology is still in its early stages, and there are many challenges to overcome, including security and privacy concerns.
- However, the potential benefits of IoT technology are vast, and it is expected to play an increasingly important role in our daily lives in the coming years.



#### CO 2 Implement interfacing of various sensors with Arduino/Raspberry Pi K4, K5

Interfacing sensors with Arduino or Raspberry Pi involves connecting the sensor to the microcontroller and writing code to read data from the sensor. Here are the steps to interface various sensors with Arduino/Raspberry Pi:

1. **Identify the sensor and its communication protocol:** Different sensors use different communication protocols such as I2C, SPI, or UART. Identify the sensor and its communication protocol before proceeding.

2. **Connect the sensor to the microcontroller:** Connect the sensor to the microcontroller using the appropriate pins. For example, if the sensor uses I2C communication, connect the SDA and SCL pins of the sensor to the SDA and SCL pins of the microcontroller.

3. **Install necessary libraries:** Some sensors require specific libraries to be installed in the Arduino or Raspberry Pi. Install the necessary libraries before writing the code.

4. **Write the code:** Write the code to read data from the sensor. The code will vary depending on the sensor and its communication protocol.

5. **Test the sensor:** Upload the code to the microcontroller and test the sensor to ensure that it is working correctly.

These are the basic steps to interface various sensors with Arduino or Raspberry Pi. It is important to refer to the sensor's datasheet and the microcontroller's documentation for specific details and instructions.



#### CO 3 Demonstrate the ability to transmit data wirelessly between different devices. K4

1. Wireless data transmission is the transfer of information between two or more devices without the use of wires or cables.
2. There are several technologies that enable wireless data transmission, including Bluetooth, Wi-Fi, and cellular networks.
3. Bluetooth is a short-range wireless technology that allows devices to communicate with each other over distances of up to 10 meters. It is commonly used for transmitting data between smartphones, tablets, and other personal devices.
4. Wi-Fi is a wireless networking technology that uses radio waves to provide high-speed internet and network connections. It is commonly used in homes, offices, and public places to provide wireless internet access.
5. Cellular networks use radio waves to transmit data between mobile devices and cell towers. These networks are commonly used to provide mobile internet access and to enable smartphones to make calls and send text messages.
6. To transmit data wirelessly between different devices, the devices must be equipped with the appropriate wireless technology and must be within range of each other or a wireless network.
7. The process of transmitting data wirelessly involves encoding the data into a signal that can be transmitted over the airwaves, transmitting the signal, and then decoding the signal back into data at the receiving device.
8. Wireless data transmission has many advantages, including increased mobility, flexibility, and convenience. However, it can also be less secure than wired data transmission and may be subject to interference from other wireless devices or networks.



#### CO 4 Show an ability to upload/download sensor data on cloud and server K2

- To upload sensor data to the cloud or server, one must first ensure that the sensor is properly connected to the device that will be used to upload the data.
- The device must have the necessary software and drivers installed to communicate with the sensor and the cloud or server.
- The data from the sensor can be read and processed using the appropriate programming language and libraries.
- The processed data can then be uploaded to the cloud or server using the appropriate protocols and methods, such as HTTP, FTP, or MQTT.
- To download sensor data from the cloud or server, one must have the necessary credentials and permissions to access the data.
- The data can be downloaded using the appropriate protocols and methods, such as HTTP, FTP, or MQTT.
- Once the data is downloaded, it can be processed and analyzed using the appropriate tools and software.
- It is important to ensure the security and integrity of the data during the upload and download process, by using encryption and secure communication protocols.



#### CO 5 Examine various SQL queries from MySQL database K4, K5

Structured Query Language (SQL) is a standard language used to manage and manipulate data stored in relational databases. MySQL is one of the most popular open-source relational database management systems that use SQL.

Here are some common SQL queries that can be used in a MySQL database:

1. **SELECT**: This query is used to retrieve data from a database table. The basic syntax is `SELECT column1, column2, ... FROM table_name;`. You can use the `WHERE` clause to filter the rows returned by the query.

2. **INSERT**: This query is used to insert new records into a database table. The basic syntax is `INSERT INTO table_name (column1, column2, ...) VALUES (value1, value2, ...);`.

3. **UPDATE**: This query is used to update existing records in a database table. The basic syntax is `UPDATE table_name SET column1 = value1, column2 = value2, ... WHERE condition;`.

4. **DELETE**: This query is used to delete records from a database table. The basic syntax is `DELETE FROM table_name WHERE condition;`.

5. **CREATE**: This query is used to create a new table in a database. The basic syntax is `CREATE TABLE table_name (column1 datatype, column2 datatype, ...);`.

6. **ALTER**: This query is used to add, modify, or delete columns in an existing table. The basic syntax is `ALTER TABLE table_name ADD column_name datatype;` to add a new column, `ALTER TABLE table_name MODIFY column_name datatype;` to modify an existing column, and `ALTER TABLE table_name DROP COLUMN column_name;` to delete a column.

7. **DROP**: This query is used to delete a table from a database. The basic syntax is `DROP TABLE table_name;`.

These are just a few examples of the many SQL queries that can be used in a MySQL database. It is important to have a good understanding of these queries and their syntax in order to effectively manage and manipulate data in a MySQL database.



## DETAILED SYLLABUS

A detailed syllabus is a comprehensive outline of the topics, assignments, and assessments that will be covered in a course. It is typically provided by the instructor at the beginning of the course and serves as a guide for students to understand the expectations and requirements of the course.

Some key components of a detailed syllabus may include:

1. Course description: A brief overview of the course, including its objectives and goals.
2. Course schedule: A list of topics and assignments, along with their due dates, that will be covered throughout the course.
3. Grading policy: An explanation of how grades will be calculated, including the weight of each assignment and assessment.
4. Course materials: A list of required and recommended textbooks, articles, and other resources for the course.
5. Attendance and participation policy: The expectations for attendance and participation in the course, including any penalties for absences or lack of participation.
6. Academic integrity policy: A statement on the importance of academic integrity and the consequences of academic dishonesty.
7. Contact information: The instructor's contact information, including office hours and preferred methods of communication.

A detailed syllabus is an important tool for students to succeed in a course, as it provides a clear roadmap of what is expected of them and how they will be evaluated. It is important for students to review the syllabus carefully and refer to it throughout the course to ensure they are meeting the course requirements.



### The student should have hands-on experience in using various sensors like temperature, humidity, smoke, light, etc.

- **Temperature sensor:** A temperature sensor is a device that measures the temperature of its environment and converts the input data into electronic data to record, monitor, or signal temperature changes.
- **Humidity sensor:** A humidity sensor is a device that measures the relative humidity of the air around it. It works by detecting changes in electrical resistance or capacitance caused by moisture in the air.
- **Smoke sensor:** A smoke sensor is a device that detects the presence of smoke in the air. It is commonly used in fire alarms and smoke detectors to alert people to the presence of a fire.
- **Light sensor:** A light sensor is a device that measures the amount of light in its environment. It is commonly used in automatic lighting systems to turn lights on or off based on the amount of natural light available.

Having hands-on experience with these sensors can help students understand how they work and how they can be used in various applications. This can be valuable knowledge for students pursuing careers in fields such as engineering, science, or technology.



### Control Web Camera, Network, and Relays Connected to the Pi

1. The Raspberry Pi is a versatile single-board computer that can be used for a variety of purposes, including controlling web cameras, networks, and relays.
2. To control a web camera connected to the Pi, you can use software such as `motion` or `fswebcam` to capture images or video from the camera.
3. To control a network connected to the Pi, you can use the `ifconfig` and `iwconfig` commands to configure network interfaces and wireless settings, respectively.
4. To control relays connected to the Pi, you can use the `gpio` command to control the GPIO pins on the Pi, which can be used to turn the relays on or off.
5. It is important to ensure that the Pi is properly configured and that the appropriate software and libraries are installed before attempting to control web cameras, networks, or relays.
6. With the right setup, the Raspberry Pi can be a powerful tool for controlling web cameras, networks, and relays, allowing for a wide range of applications and projects.



#### 1. Start Raspberry Pi and try various Linux commands in command terminal window: ls, cd, touch, mv, rm, man

- **ls**: This command is used to list the contents of a directory. By default, it lists the contents of the current directory.
- **cd**: This command is used to change the current directory. It is used in conjunction with a directory path to navigate to a specific directory.
- **touch**: This command is used to create a new, empty file. It can also be used to update the timestamps on an existing file.
- **mv**: This command is used to move or rename files and directories. It takes two arguments: the source file or directory, and the destination.
- **rm**: This command is used to remove files and directories. It can be used with the `-r` flag to remove directories and their contents recursively.
- **man**: This command is used to display the manual page for a given command. It provides detailed information on how to use the command and its various options.

These are some basic Linux commands that can be used in the command terminal window of a Raspberry Pi. It is recommended to try these commands and experiment with their various options to become familiar with the Linux command line.



#### mkdir, rmdir, tar, gzip, cat, more, less, ps, sudo, cron, chown, chgrp, ping etc.

- `mkdir`: This command is used to create a new directory.
- `rmdir`: This command is used to remove an empty directory.
- `tar`: This command is used to create and extract compressed archive files.
- `gzip`: This command is used to compress and decompress files.
- `cat`: This command is used to concatenate and display the contents of files.
- `more`: This command is used to display the contents of a file one screen at a time.
- `less`: This command is similar to `more`, but it allows backward movement in the file as well as forward movement.
- `ps`: This command is used to display information about the currently running processes.
- `sudo`: This command is used to execute a command with superuser privileges.
- `cron`: This command is used to schedule commands to be executed at a later time.
- `chown`: This command is used to change the owner of a file or directory.
- `chgrp`: This command is used to change the group ownership of a file or directory.
- `ping`: This command is used to test the connectivity between two network hosts.

These are some of the basic commands used in Linux and Unix-like operating systems. They can be very useful for managing files, directories, and processes on your system. It is important to learn and understand these commands to effectively use the command line interface.



#### 2. Run some python programs on Pi like:

1. **Blinking LED:** This is a simple program that can be run on the Raspberry Pi to blink an LED connected to one of the GPIO pins.
2. **Temperature Sensor:** Another program that can be run on the Raspberry Pi is to read data from a temperature sensor connected to the GPIO pins and display the temperature readings.
3. **Web Server:** A more advanced program that can be run on the Raspberry Pi is to set up a simple web server using Python's built-in `http.server` module.
4. **Home Automation:** Python can also be used to control home automation devices such as lights, thermostats, and security systems through the Raspberry Pi.
5. **Robotics:** Python can be used to program robots and other automated systems using the Raspberry Pi as the control unit.

These are just a few examples of the many programs that can be run on the Raspberry Pi using Python. The possibilities are endless and only limited by your imagination and programming skills.



#### a) Read your name and print Hello message with name
- To read your name, you can use the `input()` function in Python. This function prompts the user to enter a value and returns the value entered by the user as a string.
- To print a message with your name, you can use the `print()` function in Python. This function outputs the specified message to the screen.
- Here is an example of how you can read your name and print a Hello message with your name in Python:

```python
name = input("Enter your name: ")
print("Hello, " + name + "!")
```

- In the above example, the `input()` function prompts the user to enter their name. The value entered by the user is then stored in the `name` variable.
- The `print()` function is then used to output a Hello message with the user's name. The `+` operator is used to concatenate the strings "Hello, ", the value of the `name` variable, and "!" to form the final message.



#### b) Read two numbers and print their sum, difference, product and division.

To solve this problem, we need to follow these steps:

1. Read the first number from the user and store it in a variable.
2. Read the second number from the user and store it in another variable.
3. Calculate the sum of the two numbers by adding the values of the two variables and store the result in a new variable.
4. Calculate the difference of the two numbers by subtracting the value of the second variable from the value of the first variable and store the result in a new variable.
5. Calculate the product of the two numbers by multiplying the values of the two variables and store the result in a new variable.
6. Calculate the division of the two numbers by dividing the value of the first variable by the value of the second variable and store the result in a new variable.
7. Print the values of the sum, difference, product, and division variables.

Here is an example of how this can be done in Python:

```python
# Read the first number from the user
num1 = float(input("Enter the first number: "))

# Read the second number from the user
num2 = float(input("Enter the second number: "))

# Calculate the sum of the two numbers
sum = num1 + num2

# Calculate the difference of the two numbers
difference = num1 - num2

# Calculate the product of the two numbers
product = num1 * num2

# Calculate the division of the two numbers
division = num1 / num2

# Print the results
print("The sum of the two numbers is:", sum)
print("The difference of the two numbers is:", difference)
print("The product of the two numbers is:", product)
print("The division of the two numbers is:", division)
```



#### c) Word and character count of a given string.

- To count the number of words in a given string, one can split the string into a list of words using the `split()` method and then find the length of the list using the `len()` function.
- To count the number of characters in a given string, one can use the `len()` function directly on the string.
- It is important to note that the `len()` function counts the number of characters in the string, including spaces and punctuation.
- Here is an example in Python:

```python
string = "This is an example string."
word_count = len(string.split())
character_count = len(string)
print(f"Word count: {word_count}")
print(f"Character count: {character_count}")
```

- In the above example, the `split()` method is used to split the string into a list of words, and the `len()` function is used to find the length of the list and the length of the string.
- The output of the above code would be:

```
Word count: 5
Character count: 25
```

- As shown in the output, the word count is 5 and the character count is 25, including spaces and punctuation.



#### d) Area of a given shape (rectangle, triangle and circle) reading shape and appropriate values from standard

The area of a shape is the measure of the region enclosed by the shape. The formula for calculating the area of a shape depends on the type of shape. Here are the formulas for calculating the area of a rectangle, triangle, and circle:

1. **Rectangle**: The area of a rectangle is calculated by multiplying its length by its width. The formula is `A = l * w`, where `A` is the area, `l` is the length, and `w` is the width.

2. **Triangle**: The area of a triangle can be calculated using the formula `A = (b * h) / 2`, where `A` is the area, `b` is the length of the base, and `h` is the height of the triangle.

3. **Circle**: The area of a circle is calculated using the formula `A = π * r^2`, where `A` is the area, `π` is approximately equal to 3.14, and `r` is the radius of the circle.

To calculate the area of a given shape, one must first identify the type of shape and then use the appropriate formula with the given values. For example, to calculate the area of a rectangle with a length of 5 units and a width of 3 units, one would use the formula for the area of a rectangle and substitute the given values to get `A = 5 * 3 = 15` square units.



#### Input
- Input refers to the process of entering data or information into a computer or other electronic device.
- This can be done through various input devices such as a keyboard, mouse, touch screen, microphone, or scanner.
- Input devices translate physical actions or analog signals into digital data that can be processed by the computer.
- The type of input device used depends on the type of data being entered and the user's preferences.
- For example, a keyboard is commonly used for entering text, while a mouse or touch screen is used for navigating and selecting options on a graphical user interface.
- Some input devices, such as microphones and scanners, can capture analog signals and convert them into digital data.
- Input is a crucial part of the computing process, as it allows users to interact with and control the computer or device. Without input, a computer would not be able to receive instructions or data from the user.



#### 3. Run some python programs on Pi like: a) Print a name 'n' times, where name and n are read from standard input,

To run a Python program on a Raspberry Pi, you will need to have Python installed on the device. Most Raspberry Pi models come with Python pre-installed, but you can check if it is installed by opening a terminal and typing `python3 --version`. If Python is not installed, you can install it by running `sudo apt-get install python3`.

Here is an example of a Python program that will print a name 'n' times, where the name and n are read from standard input:

```python
name = input("Enter a name: ")
n = int(input("Enter the number of times to print the name: "))

for i in range(n):
    print(name)
```

To run this program on a Raspberry Pi, you can save it to a file with a `.py` extension, such as `print_name.py`, and then run it from the terminal by navigating to the directory where the file is saved and typing `python3 print_name.py`.

This will prompt you to enter a name and the number of times to print it, and then it will print the name the specified number of times. For example, if you enter "Alice" as the name and "3" as the number of times to print it, the program will output:

```
Alice
Alice
Alice
```



#### Using for and while loops
- `for` and `while` loops are used to iterate over a block of code repeatedly.
- `for` loops are used when the number of iterations is known beforehand.
- `while` loops are used when the number of iterations is not known beforehand.

#### Handle Divided by Zero Exception
- Dividing by zero is an illegal operation in mathematics and programming.
- In Python, attempting to divide by zero raises a `ZeroDivisionError` exception.
- This exception can be handled using a `try` and `except` block.

#### Print current time for 10 times with an interval of 1 second
- The `time` module in Python provides functions to work with time-related tasks.
- The `time.time()` function returns the current time in seconds since the epoch.
- The `time.sleep()` function can be used to pause the execution of the program for a specified number of seconds.

Here is an example code that prints the current time for 10 times with an interval of 1 second:
```python
import time

for i in range(10):
    print(time.ctime())
    time.sleep(1)
```



#### Interval of 10 seconds
An interval of 10 seconds refers to a time duration or a time period that lasts for 10 seconds. This interval can be used in various contexts, such as setting a timer, scheduling a task, or measuring the time between two events.

#### Read a file line by line and print the word count of each line
To read a file line by line and print the word count of each line, the following steps can be followed:

1. Open the file in read mode using a file object.
2. Use a loop to iterate over the file object, which will read the file line by line.
3. For each line, use the `split()` method to split the line into a list of words.
4. Use the `len()` function to find the number of words in the list, which is the word count of the line.
5. Print the word count of the line.
6. Continue the loop until all lines in the file have been read and the word count of each line has been printed.

Here is an example code snippet in Python that demonstrates how to read a file line by line and print the word count of each line:

```python
with open('file.txt', 'r') as file:
    for line in file:
        words = line.split()
        word_count = len(words)
        print(word_count)
```

This code opens the file `file.txt` in read mode, and uses a `for` loop to iterate over the file object. For each line, the `split()` method is used to split the line into a list of words, and the `len()` function is used to find the word count of the line. The word count is then printed using the `print()` function. This process is repeated for all lines in the file.



#### 4. a) Light an LED through Python program

1. To light an LED through a Python program, you will need a Raspberry Pi, an LED, a resistor, and some jumper wires.
2. Connect the LED to one of the GPIO pins on the Raspberry Pi using a resistor and jumper wires.
3. In your Python program, import the `RPi.GPIO` module, which allows you to control the GPIO pins on the Raspberry Pi.
4. Set the GPIO pin connected to the LED as an output pin using the `GPIO.setup()` function.
5. Use the `GPIO.output()` function to turn the LED on and off by setting the output pin to `GPIO.HIGH` or `GPIO.LOW`.
6. You can control the brightness of the LED by using pulse-width modulation (PWM) to vary the duty cycle of the output pin.
7. To use PWM, create a `GPIO.PWM` object for the output pin and use the `start()`, `ChangeDutyCycle()`, and `stop()` methods to control the duty cycle.

Here is an example Python program that lights an LED connected to GPIO pin 18 on a Raspberry Pi:

```python
import RPi.GPIO as GPIO
import time

LED_PIN = 18

GPIO.setmode(GPIO.BCM)
GPIO.setup(LED_PIN, GPIO.OUT)

pwm = GPIO.PWM(LED_PIN, 100)
pwm.start(0)

try:
    while True:
        for duty_cycle in range(0, 101, 5):
            pwm.ChangeDutyCycle(duty_cycle)
            time.sleep(0.1)
        for duty_cycle in range(100, -1, -5):
            pwm.ChangeDutyCycle(duty_cycle)
            time.sleep(0.1)
except KeyboardInterrupt:
    pass

pwm.stop()
GPIO.cleanup()
```

This program uses a loop to gradually increase and decrease the duty cycle of the output pin, causing the LED to fade in and out. The `try` and `except` statements are used to handle the `KeyboardInterrupt` exception, which is raised when the user presses `Ctrl-C` to stop the program. When the exception is caught, the `pwm.stop()` and `GPIO.cleanup()` functions are called to clean up the resources used by the program.



#### b) Get input from two switches and switch on corresponding LEDs

To get input from two switches and switch on corresponding LEDs, you can follow these steps:

1. Connect the two switches to the input pins of the microcontroller. Make sure to use the appropriate pull-up or pull-down resistors to ensure a stable input signal.
2. Connect the two LEDs to the output pins of the microcontroller. Make sure to use current-limiting resistors to protect the LEDs from excessive current.
3. In the program, configure the input pins as digital inputs and the output pins as digital outputs.
4. Continuously read the state of the input pins using the appropriate digital input function.
5. If the state of an input pin is high, set the corresponding output pin to high using the appropriate digital output function. This will turn on the corresponding LED.
6. If the state of an input pin is low, set the corresponding output pin to low using the appropriate digital output function. This will turn off the corresponding LED.

By following these steps, you can get input from two switches and switch on the corresponding LEDs. This can be useful in various applications, such as creating a simple user interface or controlling the state of external devices.



#### c) Flash an LED at a given on time and off time cycle, where the two times are taken from a file.

To flash an LED at a given on time and off time cycle, where the two times are taken from a file, the following steps can be followed:

1. Read the on time and off time values from the file. These values can be stored in variables for use in the program.
2. Set up the LED by configuring the appropriate pin as an output.
3. Use a loop to repeatedly turn the LED on and off. The on time and off time values read from the file can be used to control the duration of the LED being on and off.
4. Use a delay function to control the timing of the LED being on and off. The delay function can be called with the on time and off time values read from the file as arguments.
5. Continue the loop until the program is stopped or the desired number of cycles is completed.

This approach allows for the on time and off time values to be easily changed by updating the values in the file, without the need to modify the program code. This can be useful in situations where the on and off times need to be adjusted frequently.



#### 5. a) Flash an LED based on cron output (acts as an alarm)

Cron is a time-based job scheduler in Unix-like operating systems. It is used to schedule commands or scripts to run automatically at a specified time and date. Here are the steps to flash an LED based on cron output:

1. Connect an LED to a GPIO pin on your device.
2. Write a script that turns the LED on and off at the desired interval.
3. Use the `crontab` command to open the crontab file for editing.
4. Add a new line to the crontab file specifying the desired schedule and the path to the script.
5. Save and close the crontab file.

The LED will now flash at the specified interval based on the cron schedule. This can be used as an alarm to notify the user of a specific event or time.



#### b) Switch on a relay at a given time using cron, where the relay's contact terminals are connected to a load.

1. A relay is an electrically operated switch that can be used to control a circuit by a low-power signal.
2. Cron is a time-based job scheduler in Unix-like operating systems, which can be used to schedule tasks to run periodically at fixed times, dates, or intervals.
3. To switch on a relay at a given time using cron, the following steps can be followed:
    - Identify the relay and the load it is connected to.
    - Write a script that sends a signal to the relay to switch it on.
    - Use the `crontab` command to open the crontab file for editing.
    - Add a new line to the crontab file specifying the time and date when the script should be executed.
    - Save and close the crontab file.
4. At the specified time, cron will execute the script, which will send a signal to the relay to switch it on, thereby connecting the contact terminals to the load.



#### c) Get the status of a bulb at a remote place (on the LAN) through web.

To get the status of a bulb at a remote place on the LAN through the web, the following steps can be taken:

1. **Connect the bulb to the network:** The bulb must be connected to the local area network (LAN) through a wired or wireless connection.

2. **Assign an IP address to the bulb:** The bulb must be assigned a unique IP address on the LAN so that it can be accessed remotely.

3. **Set up a web server on the bulb:** A web server must be set up on the bulb to allow remote access to its status. This can be done using a microcontroller and appropriate firmware.

4. **Access the bulb's web server:** The bulb's web server can be accessed by entering its IP address into a web browser on a device connected to the LAN.

5. **View the bulb's status:** The bulb's web server should display the current status of the bulb, such as whether it is on or off.

By following these steps, it is possible to remotely access the status of a bulb on a LAN through the web. This can be useful for monitoring and controlling lighting in a remote location.



#### Note: The Instructor may add/delete/modify/tune experiments

- The instructor has the authority to make changes to the experiments.
- These changes may include adding new experiments, deleting existing experiments, modifying the details of the experiments, or tuning the experiments to better fit the needs of the course.
- It is important for students to be aware of this, as the experiments may change throughout the course.
- Students should regularly check for updates and changes to the experiments to ensure they are prepared for the course.



# KOT 554 INTERNET OF THINGS LAB KCS

- KOT 554 is a course on the Internet of Things (IoT) Lab.
- IoT refers to the interconnection of devices and objects through the internet, allowing them to collect and exchange data.
- The course covers the fundamentals of IoT, including its architecture, protocols, and applications.
- Students will learn how to design, implement, and test IoT systems.
- The course includes hands-on lab sessions where students will work with IoT devices and platforms.
- The course is offered by the KCS department.




# Course Outcome (CO) Bloom’s Knowledge Level (KL)

- Course Outcome (CO) refers to the expected knowledge, skills, and attitudes that students should possess upon completion of a specific course.
- Bloom’s Taxonomy is a framework for categorizing educational goals and objectives into different levels of complexity and specificity.
- The Knowledge Level (KL) of Bloom’s Taxonomy is the first and most basic level, which involves the recall of facts, terms, basic concepts, and answers.
- At the Knowledge Level, students are expected to remember or recognize information, but not necessarily understand, apply, analyze, evaluate, or create with it.
- Examples of verbs used to describe learning objectives at the Knowledge Level include: define, describe, identify, label, list, match, name, outline, recall, recognize, record, repeat, and state.
- Course Outcomes can be written to target different levels of Bloom’s Taxonomy, including the Knowledge Level, to ensure that students are challenged to develop a range of cognitive skills.
- By aligning Course Outcomes with Bloom’s Taxonomy, instructors can design assessments and learning activities that appropriately measure and promote student learning at the desired level of complexity.



# At the end of the course, the student will be able to:

1. Demonstrate a comprehensive understanding of the course material.
2. Apply the knowledge and skills acquired during the course to real-life situations.
3. Analyze and evaluate information critically and effectively.
4. Communicate ideas and arguments clearly and effectively in both written and oral forms.
5. Work collaboratively with others to achieve common goals.
6. Demonstrate ethical and professional behavior in academic and professional settings.
7. Engage in lifelong learning and continuous professional development.




# CO 1 Understand the concept of Internet of Things K3

The Internet of Things (IoT) is a network of interconnected devices that can communicate with each other and share data. These devices can range from simple sensors to complex machines, and can be found in a variety of settings, including homes, businesses, and public spaces.

Some key points to understand about the concept of IoT include:

1. IoT devices are connected to the internet and can communicate with other devices and systems.
2. IoT devices can collect and share data, allowing for improved decision-making and automation.
3. IoT technology can be used in a variety of applications, including smart homes, healthcare, transportation, and manufacturing.
4. IoT devices can improve efficiency, safety, and convenience in many aspects of daily life.
5. Security and privacy are important considerations in the design and implementation of IoT systems.

Overall, the concept of IoT involves the integration of physical devices with digital systems, allowing for improved communication, data sharing, and automation. This technology has the potential to revolutionize many aspects of daily life and industry.



# CO2 Implement interfacing of various sensors with Arduino/Raspberry Pi K4, K5

- Interfacing sensors with Arduino or Raspberry Pi involves connecting the sensor to the microcontroller and writing code to read data from the sensor.
- Both Arduino and Raspberry Pi have a range of compatible sensors that can be used for various applications.
- Some common sensors that can be interfaced with Arduino or Raspberry Pi include temperature sensors, humidity sensors, light sensors, and motion sensors.
- To interface a sensor with Arduino or Raspberry Pi, the first step is to connect the sensor to the microcontroller using the appropriate pins.
- The next step is to write code to read data from the sensor. This can be done using the programming language supported by the microcontroller, such as C++ for Arduino or Python for Raspberry Pi.
- Once the code is written and uploaded to the microcontroller, the sensor can be used to collect data and perform various actions based on the data collected.
- It is important to carefully read the documentation for the sensor and the microcontroller to ensure that the sensor is correctly connected and the code is written correctly.
- Interfacing sensors with Arduino or Raspberry Pi can be a useful skill for building various projects and applications.



# CO 3 Demonstrate the ability to transmit data wirelessly between different devices. K4

1. **Wireless data transmission** refers to the transfer of information between two or more devices without the use of physical connections such as cables or wires.
2. **Wireless technologies** such as Wi-Fi, Bluetooth, and cellular networks are commonly used for wireless data transmission.
3. **Wi-Fi** is a wireless networking technology that uses radio waves to provide high-speed internet and network connections to devices such as smartphones, laptops, and tablets.
4. **Bluetooth** is a short-range wireless technology that allows devices to communicate with each other over distances of up to 10 meters.
5. **Cellular networks** such as 3G, 4G, and 5G use radio waves to transmit data between mobile devices and cell towers, allowing for wireless internet access and communication over long distances.
6. To transmit data wirelessly between different devices, the devices must be equipped with the appropriate wireless technology and be within range of each other or a wireless network.
7. **Wireless data transmission** has many advantages, including increased mobility, flexibility, and convenience, as well as reduced costs and improved efficiency.
8. However, wireless data transmission can also have some challenges, such as security concerns and potential interference from other wireless devices or networks.



# CO 4: Show an ability to upload/download sensor data on cloud and server K2

- **Uploading sensor data to the cloud and server K2** involves sending data from a sensor device to a remote storage location for processing and analysis.
- **Downloading sensor data from the cloud and server K2** involves retrieving data from a remote storage location for use on a local device or system.
- **Cloud storage** is a service model in which data is maintained, managed, backed up remotely and made available to users over a network (typically the Internet).
- **Server K2** is a specific type of server that may be used for storing and managing sensor data.
- To **upload sensor data**, the data must first be collected and formatted in a way that is compatible with the cloud or server K2 storage system.
- This may involve using specific data formats, protocols, and APIs (Application Programming Interfaces) to ensure that the data can be transmitted and stored correctly.
- Once the data is prepared, it can be transmitted to the cloud or server K2 storage location using a variety of methods, such as HTTP (Hypertext Transfer Protocol), FTP (File Transfer Protocol), or other data transfer protocols.
- To **download sensor data**, the data must first be located and accessed on the cloud or server K2 storage system.
- This may involve using specific APIs or data access methods to retrieve the data from the remote storage location.
- Once the data is accessed, it can be transmitted back to the local device or system using a variety of methods, such as HTTP, FTP, or other data transfer protocols.
- The ability to upload and download sensor data to and from the cloud and server K2 is important for enabling remote data collection, analysis, and management, and can provide many benefits, such as improved data accessibility, scalability, and security.



# CO 5 Examine various SQL queries from MySQL database K4, K5

- SQL (Structured Query Language) is a standard language for managing and querying relational databases.
- MySQL is an open-source relational database management system that uses SQL.
- Some common SQL queries used in MySQL include:
  - SELECT: used to retrieve data from a database table.
  - INSERT: used to add new records to a database table.
  - UPDATE: used to modify existing records in a database table.
  - DELETE: used to remove records from a database table.
  - CREATE: used to create new database objects such as tables, views, and indexes.
  - ALTER: used to modify the structure of existing database objects.
  - DROP: used to delete database objects.
- These queries can be used in various combinations and with various clauses and conditions to perform complex database operations.
- It is important to carefully examine and test SQL queries to ensure that they produce the desired results and do not have unintended consequences.



# DETAILED SYLLABUS

A detailed syllabus is a comprehensive outline of the topics, concepts, and skills that will be covered in a course. It is typically provided by the instructor at the beginning of the course and serves as a guide for students to understand the expectations and requirements of the course.

The following are some key components of a detailed syllabus:

1. **Course Overview:** This section provides a brief description of the course, its objectives, and its relevance to the student's academic program.

2. **Course Schedule:** This section outlines the schedule of topics and assignments for the course, including due dates and exam dates.

3. **Learning Outcomes:** This section lists the specific skills and knowledge that students are expected to acquire by the end of the course.

4. **Assessment Methods:** This section describes the methods that will be used to evaluate student performance, such as exams, papers, and projects.

5. **Grading Policy:** This section explains how grades will be calculated and assigned, including the weighting of different assignments and the criteria for earning a particular grade.

6. **Course Materials:** This section lists the required and recommended textbooks, articles, and other resources for the course.

7. **Course Policies:** This section outlines the policies for attendance, participation, late work, and academic integrity.

8. **Contact Information:** This section provides the instructor's contact information, including office hours and preferred methods of communication.

A detailed syllabus is an important tool for students to succeed in a course. It provides a clear roadmap of what is expected and helps students to plan their time and efforts effectively. It is important for students to review the syllabus carefully and to ask the instructor for clarification if anything is unclear.



# Hands-on Experience with Sensors

It is important for students to have hands-on experience with various sensors, including but not limited to:

1. **Temperature sensors**: These sensors measure the temperature of the environment or an object. They can be used in a variety of applications, such as monitoring the temperature of a room or a refrigerator, or measuring the temperature of a patient's body.
2. **Humidity sensors**: These sensors measure the amount of moisture in the air. They can be used in applications such as monitoring the humidity level in a room or greenhouse, or measuring the moisture content of soil.
3. **Smoke sensors**: These sensors detect the presence of smoke in the air. They are commonly used in fire alarms and smoke detectors to alert people to the presence of a fire.
4. **Light sensors**: These sensors measure the amount of light in the environment. They can be used in applications such as automatically turning on lights when it gets dark, or adjusting the brightness of a display based on the ambient light.

Having hands-on experience with these sensors allows students to understand how they work and how they can be used in real-world applications. This knowledge can be valuable in a variety of fields, including engineering, science, and technology.



# Using Control Web Camera, Network, and Relays Connected to the Pi

1. **Web Camera**: A web camera can be connected to the Raspberry Pi via USB. Once connected, the camera can be controlled using various software packages, such as `fswebcam` or `motion`.
2. **Network**: The Raspberry Pi can be connected to a network via Ethernet or Wi-Fi. This allows the Pi to communicate with other devices on the network, as well as access the internet. The network can be configured using the `raspi-config` tool or by editing the `/etc/network/interfaces` file.
3. **Relays**: Relays can be connected to the Raspberry Pi's GPIO pins to control external devices. The `RPi.GPIO` library can be used to control the relays using Python. The relays can be used to switch on/off devices such as lights, motors, or other appliances.

These are some of the ways in which a Raspberry Pi can be used to control a web camera, network, and relays. It is important to note that the specific details of how to set up and use these components may vary depending on the specific hardware and software being used.



# Starting Raspberry Pi and trying various Linux commands in command terminal window

1. **ls**: This command is used to list the contents of a directory. By default, it lists the contents of the current directory.
2. **cd**: This command is used to change the current working directory. For example, `cd Documents` will change the current working directory to the `Documents` directory.
3. **touch**: This command is used to create a new, empty file. For example, `touch newfile.txt` will create a new file named `newfile.txt` in the current working directory.
4. **mv**: This command is used to move or rename files or directories. For example, `mv file1.txt file2.txt` will rename `file1.txt` to `file2.txt`.
5. **rm**: This command is used to remove files or directories. For example, `rm file.txt` will remove the file named `file.txt`.
6. **man**: This command is used to display the manual page for a command. For example, `man ls` will display the manual page for the `ls` command.

These are some basic Linux commands that can be used in the command terminal window of a Raspberry Pi. It is important to read the manual pages for each command to understand their usage and options.



# `mkdir`, `rmdir`, `tar`, `gzip`

- `mkdir` is a command used to create directories in a file system. The syntax for the command is `mkdir [options] directory_name`. Some common options include `-p` to create parent directories as needed and `-m` to set the file mode.

- `rmdir` is a command used to remove empty directories in a file system. The syntax for the command is `rmdir [options] directory_name`. Some common options include `-p` to remove parent directories if they become empty and `--ignore-fail-on-non-empty` to ignore errors if the directory is not empty.

- `tar` is a command used to create and manipulate archive files. The syntax for the command is `tar [options] [archive_file] [file...]`. Some common options include `-c` to create a new archive, `-x` to extract files from an archive, and `-f` to specify the archive file.

- `gzip` is a command used to compress and decompress files. The syntax for the command is `gzip [options] [file...]`. Some common options include `-d` to decompress files, `-k` to keep the original files, and `-l` to list the compressed file information.

These commands are commonly used in Linux and Unix-like operating systems for file and directory management and manipulation. They can be used in combination to perform complex tasks such as creating and extracting compressed archives of directories.

