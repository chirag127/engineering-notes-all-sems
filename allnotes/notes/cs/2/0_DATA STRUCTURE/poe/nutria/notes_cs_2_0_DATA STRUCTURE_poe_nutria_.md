


## Unit 1 - Introduction: Basic Terminology, Elementary Data Organization, Built in Data Types in C. Algorithm, Efficiency of an Algorithm, Time and Space Complexity, Asymptotic notations: Big Oh, Big Theta and Big Omega, Time-Space trade-off. Abstract Data Types (ADT).

1. Basic Terminology:
    * Data: Information that is stored and processed by a computer.
    * Data Organization: The way data is organized and stored in a computer system.
    * Built in Data Types in C: Data types that are built into the C programming language, such as int, char, and float.

2. Algorithm:
    * An algorithm is a set of instructions or steps that can be followed to solve a problem or complete a task.
    * Efficiency of an Algorithm: The amount of time and/or space required to complete a task.

3. Time and Space Complexity:
    * Time complexity: The amount of time taken to complete a task.
    * Space complexity: The amount of memory required to complete a task.
    * Asymptotic notations: Big Oh, Big Theta and Big Omega: These are used to measure the time and space complexity of an algorithm.

4. Time-Space trade-off:
    * The trade-off between time and space complexity. Increasing the amount of memory used can reduce the amount of time needed to complete a task.

5. Abstract Data Types (ADT):
    * A data type that is defined by its behavior rather than its implementation. Examples include stacks, queues and linked lists.




### Basic Terminology for the notes of the Unit 1 - Introduction: 

- **Basic Terminology**: Terms used to describe the fundamental concepts of data structure, such as data, information, structure, algorithm, etc.
- **Elementary Data Organization**: A way of organizing data in a computer, such as arrays, linked lists, binary trees, etc.
- **Built in Data Types in C**: A set of data types that are built into the C programming language, such as int, float, char, etc.
- **Algorithm**: A step-by-step procedure for solving a problem or performing a task.
- **Efficiency of an Algorithm**: A measure of how well an algorithm performs in terms of time and space complexity.
- **Time and Space Complexity**: A measure of how much time and space an algorithm requires to complete its task.
- **Asymptotic Notations**: A way of measuring the time and space complexity of an algorithm, such as Big-Oh, Big-Theta, and Big-Omega.
- **Time-Space Trade-off**: The idea that an algorithm can be made faster by using more space, or vice versa.
- **Abstract Data Types (ADT)**: A set of data types that are abstracted from their implementation, such as stacks, queues, heaps, etc.





### Elementary Data Organization 

* **Basic Terminology**: A data organization is the way data is organized and stored in a computer system. It involves the structure of the data, the relationships between the data elements and the rules governing the data. 

* **Elementary Data Organization**: Elementary data organizations are the basic ways of organizing data. These include flat files, arrays, linked lists, stacks, queues, trees, and graphs.

* **Built in Data Types in C**: C programming language provides a set of built-in data types to handle different kinds of data. These data types are used to define a variable before it is used in a program. The built-in data types are int, float, char, and void. 

* **Algorithm**: An algorithm is a set of instructions that tells a computer how to perform a task. It is a step-by-step procedure for solving a problem or performing a task. 

* **Efficiency of an Algorithm**: Efficiency of an algorithm is the measure of how well it performs. It is usually measured in terms of time and space complexity. 

* **Time and Space Complexity**: Time complexity is the amount of time taken by an algorithm to complete its task. Space complexity is the amount of memory required by an algorithm to complete its task. 

* **Asymptotic notations: Big Oh, Big Theta and Big Omega**: Asymptotic notations are used to measure the time complexity of an algorithm. Big Oh is used to represent the upper bound of an algorithm's time complexity. Big Theta is used to represent the average case of an algorithm's time complexity. Big Omega is used to represent the lower bound of an algorithm's time complexity. 

* **Time-Space trade-off**: Time-space trade-off is a technique used to reduce the time complexity of an algorithm at the cost of an increase in the space complexity. 

* **Abstract Data Types (ADT) in the subject of DATA STRUCTURE**: Abstract Data Types (ADT) are data structures that are used to store and organize data. Examples of ADT include arrays, linked lists, stacks, queues, trees, and graphs.




### Built in Data Types in C

Unit 1 - Introduction:
- Basic Terminology
- Elementary Data Organization
- Built in Data Types in C
- Algorithm
- Efficiency of an Algorithm
- Time and Space Complexity
- Asymptotic notations: Big Oh, Big Theta and Big Omega
- Time-Space trade-off
- Abstract Data Types (ADT) in the subject of DATA STRUCTURE





### Algorithm for the notes of the Unit 1 - Introduction 

1. Basic Terminology: 
    - Algorithms are a set of instructions used to solve a problem.
    - Elementary Data Organization: Data is arranged in a logical order to facilitate efficient storage and retrieval.
    - Built in Data Types in C: C language provides a set of built-in data types like int, float, char, etc.
2. Algorithm: 
    - Efficiency of an Algorithm: The efficiency of an algorithm is determined by the number of operations it performs to solve a problem.
    - Time and Space Complexity: Time complexity measures the amount of time an algorithm takes to execute, while space complexity measures the amount of memory it requires.
    - Asymptotic Notations: Big Oh, Big Theta and Big Omega are asymptotic notations used to measure the complexity of an algorithm.
    - Time-Space Trade-off: This refers to the process of trading off time for space or vice versa, depending on the problem at hand.
3. Abstract Data Types (ADT): ADT is a type of data structure used in data science to represent the data in a logical and organized manner. It is used to store and manipulate data efficiently. ADT is used in the subject of DATA STRUCTURE.




### Efficiency of an Algorithm

* An algorithm is a step-by-step procedure for solving a problem or accomplishing a task. 
* The efficiency of an algorithm is the amount of time and/or space it takes to execute the algorithm.
* Time complexity is a measure of the amount of time it takes for an algorithm to complete its task.
* Space complexity is a measure of the amount of memory required to execute the algorithm.
* Asymptotic notations are used to describe the efficiency of an algorithm. These include Big Oh (O), Big Theta (Θ), and Big Omega (Ω).
* Time-space trade-off is a technique used to improve the efficiency of an algorithm by trading off time complexity for space complexity, or vice versa.
* Abstract Data Types (ADTs) are data structures used in the subject of Data Structure to represent and manipulate data.





### Time and Space Complexity

* Algorithm efficiency is determined by its **time complexity**, which is the amount of time it takes for an algorithm to run, and its **space complexity**, which is the amount of memory it requires. 
* **Time complexity** is the measure of the amount of time it takes for an algorithm to run as a function of the input size. 
* **Space complexity** is the measure of the amount of memory it requires to run as a function of the input size.
* **Asymptotic notations** are used to measure the time and space complexity of an algorithm. The three main ones are **Big O**, **Big Theta**, and **Big Omega**. 
* **Big O** notation is used to measure the worst-case time complexity of an algorithm. 
* **Big Theta** notation is used to measure the average-case time complexity of an algorithm. 
* **Big Omega** notation is used to measure the best-case time complexity of an algorithm. 
* The **time-space trade-off** is an important concept in algorithm design. It is the idea that by increasing the amount of space used by an algorithm, the time complexity can be reduced. 
* **Abstract Data Types (ADT)** are used in the subject of Data Structures. An ADT is a data type that is defined in terms of the operations that can be performed on it.




### Asymptotic Notations: Big Oh, Big Theta and Big Omega

* Asymptotic notations are mathematical tools used to represent the time complexity of algorithms. 
* Big Oh (O) notation is used to represent the upper bound on the time complexity of an algorithm. It is the worst case time complexity of an algorithm.
* Big Theta (Θ) notation is used to represent the average time complexity of an algorithm. It is the average case time complexity of an algorithm.
* Big Omega (Ω) notation is used to represent the lower bound on the time complexity of an algorithm. It is the best case time complexity of an algorithm.
* Time complexity is the measure of the amount of time taken by an algorithm to execute and produce the result.
* Space complexity is the measure of the amount of memory taken by an algorithm to execute.
* Time-Space trade-off is the concept of reducing the time complexity of an algorithm by increasing its space complexity.
* Abstract Data Types (ADT) are the logical description of data structures and the operations on them. They are used to define the data structures in the subject of Data Structure.




### Time-Space Trade-Off

Time-space trade-off is an important concept in the subject of Data Structure. It is the balance between the time taken to complete a process and the space (memory) required to do so.

- **Basic Terminology**: The basic terminology used in the context of Time-Space trade-off includes Algorithm, Efficiency of an Algorithm, Time and Space Complexity, Asymptotic notations: Big Oh, Big Theta and Big Omega.

- **Elementary Data Organization**: Elementary data organization is the way data is organized in order to achieve the desired efficiency.

- **Built in Data Types in C**: Built in data types in C include int, char, float, double, etc.

- **Time and Space Complexity**: Time and Space Complexity are two factors which determine the efficiency of an algorithm. Time complexity is the amount of time taken by an algorithm to complete a process, while Space complexity is the amount of memory required by an algorithm to complete a process.

- **Asymptotic Notations**: Asymptotic notations are used to measure the running time of an algorithm. Big Oh, Big Theta and Big Omega are the three most commonly used asymptotic notations.

- **Time-Space Trade-Off**: Time-space trade-off is the balance between the time taken to complete a process and the space (memory) required to do so. It is important to understand this concept in order to design efficient algorithms.

- **Abstract Data Types (ADT)**: Abstract Data Types (ADT) are a set of data types which are used to represent data in a structured way. ADT is an important concept in the subject of Data Structure.




### Abstract Data Types (ADT)

* Basic Terminology: A set of fundamental concepts and principles used to describe and analyze a particular problem or domain.
* Elementary Data Organization: The way in which data is organized and stored in a computer system.
* Built-in Data Types in C: Primitive data types such as int, char, float, and double, as well as user-defined data types such as structs and unions.
* Algorithm: A step-by-step procedure for solving a problem.
* Efficiency of an Algorithm: The amount of time and/or space required to execute an algorithm.
* Time and Space Complexity: The amount of time and/or space required to execute an algorithm.
* Asymptotic Notations: Big Oh, Big Theta and Big Omega, which are used to describe the time and space complexity of an algorithm.
* Time-Space Trade-off: The idea that the time required to execute an algorithm can be reduced by increasing the amount of space required.
* Abstract Data Types (ADT): A type of data structure that is defined by the operations that can be performed on it, rather than by its physical implementation. Examples of ADTs include stacks, queues, and trees.




## Unit 2 - Arrays: 
* Arrays are data structures that store elements of the same type in a contiguous block of memory. 
* There are two types of arrays: single-dimensional and multidimensional. 
* Representation of arrays can be done in two ways: row major order and column major order. 
* Index formulae for 1-D,2-D,3-D and n-D arrays can be derived. 
* Arrays can be used in various applications. 
* Sparse matrices are a type of array that have many elements with zero values. 

## Linked Lists: 
* Linked lists are data structures that consist of nodes that are connected in a linear fashion. 
* Linked lists can be implemented using arrays or pointers. 
* There are three types of linked lists: singly linked lists, doubly linked lists, and circularly linked lists. 
* Operations on linked lists include insertion, deletion, and traversal. 

## Polynomials: 
* Polynomials are mathematical expressions that consist of variables and coefficients. 
* Polynomials can be represented as linked lists. 
* Addition, subtraction, and multiplication of single and two variable polynomials can be performed.




### Definition for the Notes of the Unit 2 - Data Structure

#### Arrays: 
* Definition: An array is a collection of items stored at contiguous memory locations. It can store data of any data type and can be of any size. 
* Single and Multidimensional Arrays: Arrays can be single-dimensional, multi-dimensional, or even jagged. 
* Representation of Arrays: Arrays can be represented in either row-major order or column-major order. 
* Derivation of Index Formulae for 1-D, 2-D, 3-D and n-D Array: Index formulae are used to calculate the position of each element in the array. 
* Application of Arrays: Arrays are used in many applications such as sorting, searching, and manipulation of data.
* Sparse Matrices and their Representations: Sparse matrices are matrices with a large number of zero elements. They can be represented using linked lists or hash tables. 

#### Linked Lists: 
* Array Implementation: Arrays can be used to implement linked lists. 
* Pointer Implementation of Singly Linked Lists: A singly linked list is a data structure in which each node contains a pointer to the next node in the list. 
* Doubly Linked List: A doubly linked list is a data structure in which each node contains a pointer to both the next and previous nodes in the list. 
* Circularly Linked List: A circularly linked list is a data structure in which the last node points back to the first node. 
* Operations on a Linked List: Operations on a linked list include insertion, deletion, traversal, and searching. 

#### Polynomial Representation and Addition Subtraction & Multiplications of Single Variable & Two Variables Polynomial: 
* Polynomial Representation: Polynomials can be represented using linked lists or arrays. 
* Addition, Subtraction and Multiplication of Single Variable Polynomials: Single variable polynomials can be added, subtracted and multiplied using the standard arithmetic operations. 
* Addition, Subtraction and Multiplication of Two Variables Polynomials: Two variables polynomials can also be added, subtracted and multiplied using the standard arithmetic operations.




### Single and Multidimensional Arrays

The topic of this unit is single and multidimensional arrays in data structure.

#### Definition of Arrays

An array is a data structure consisting of a collection of elements (values or variables), each identified by at least one array index or key. An array is stored such that the position of each element can be computed from its index tuple by a mathematical formula.

#### Single and Multidimensional Arrays

Arrays can be single or multidimensional. A single dimensional array is a linear array of elements, while a multidimensional array is an array of arrays.

#### Representation of Arrays

Arrays can be represented in two ways: row major order and column major order. In row major order, the elements of the array are stored in a row-wise fashion, while in column major order, the elements are stored in a column-wise fashion.

#### Derivation of Index Formulae

Index formulae can be derived for single dimensional, two dimensional, three dimensional, and n-dimensional arrays. These formulae are used to locate the position of the elements in the array.

#### Application of Arrays

Arrays can be used in various applications such as sorting, searching, and matrix operations.

#### Sparse Matrices

Sparse matrices are matrices in which most of the elements are zero. They can be represented using linked lists.

#### Linked Lists

Linked lists are data structures which store data in nodes, which are linked together by pointers. Linked lists can be implemented using arrays or pointers.

#### Operations on a Linked List

Common operations on a linked list include insertion, deletion, traversal, and searching.

#### Polynomial Representation

Polynomials can be represented using linked lists. This representation can be used to perform operations such as addition, subtraction, and multiplication of single-variable and two-variable polynomials.




### Representation of Arrays: Row Major Order, and Column Major Order

Arrays are a data structure that stores a collection of data items of the same type. Arrays can be single-dimensional (1-D), multi-dimensional (2-D, 3-D, etc.), and sparse. 

Row Major Order: In row major order, the elements of the array are stored in a sequence, row-wise. That is, the elements in each row are stored in consecutive memory locations. 

Column Major Order: In column major order, the elements of the array are stored in a sequence, column-wise. That is, the elements in each column are stored in consecutive memory locations. 

Derivation of Index Formulae for 1-D,2-D,3-D and n-D Array: The index formulae are used to calculate the location of the element in the array. For 1-D array, the index formula is given by: 

Index = (row * n) + column 

Where n is the number of columns in the array and row and column are the row and column numbers of the element. 

For 2-D array, the index formula is given by: 

Index = (row * n) + column 

Where n is the number of columns in the array and row and column are the row and column numbers of the element. 

For 3-D array, the index formula is given by: 

Index = (row * n * m) + (column * m) + depth 

Where n and m are the number of columns and rows in the array, respectively, and row, column and depth are the row, column and depth numbers of the element. 

For n-D array, the index formula is given by: 

Index = (row * n * m * ...) + (column * m * ...) + (depth * ...) + ... 

Where n, m, ... are the number of columns, rows, depths, etc. in the array, respectively, and row, column, depth, ... are the row, column, depth, ... numbers of the element. 

Application of Arrays: Arrays are used in various applications such as sorting, searching, data manipulation and storage, etc. 

Sparse Matrices and their Representations: Sparse matrices are matrices that contain mostly zeros. They can be represented in various ways such as linked list representation, array representation, etc. 

Linked Lists: Linked lists are a data structure that consists of nodes that are linked together. There are various types of linked lists such as singly linked list, doubly linked list, circularly linked list, etc. 

Array Implementation and Pointer Implementation of Singly Linked Lists: In array implementation of singly linked list, each node is represented by an array of data elements. In pointer implementation of singly linked list, each node is represented by a pointer to the data element and a pointer to the next node. 

Doubly Linked List: In doubly linked list, each node is represented by a pointer to the data element, a pointer to the previous node and a pointer to the next node. 

Circularly Linked List: In circularly linked list, each node is represented by a pointer to the data element, a pointer to the previous node and a pointer to the next node. The last node of the list points to the first node of the list. 

Operations on a Linked List: Operations on a linked list include insertion, deletion, traversal, etc. 

Insertion: Insertion is the process of adding a new node to a linked list. 

Deletion: Deletion is the process of removing a node from a linked list. 

Traversal: Traversal is the process of visiting each node of a linked list in a certain order. 

Polynomial Representation and Addition Subtraction & Multiplications of Single variable & Two variables Polynomial: Polynomial is a mathematical expression consisting of terms in which each term is a constant or a variable raised to a certain power. Polynomials can be represented in various ways such as array representation, linked list representation, etc. 

Single variable polynomial: Single variable polynomial is a polynomial which consists of terms with only one variable. Operations on single variable polynomials include addition, subtraction, and multiplication. 

Two variables polynomial: Two variables polynomial is a polynomial which consists of terms with two variables. Operations on two variables polynomials include addition, subtraction, and multiplication.





### Derivation of Index Formulae for 1-D,2-D,3-D and n-D Array

This section covers the derivation of index formulae for 1-D, 2-D, 3-D and n-D array. 

#### 1-D Array 
A 1-D array consists of a single row or column of elements. The index of an element in a 1-D array is given by the formula: 

Index = Element Number - 1 

#### 2-D Array 
A 2-D array consists of a matrix of elements. The index of an element in a 2-D array is given by the formula: 

Index = (Row Number - 1) * Number of Columns + (Column Number - 1) 

#### 3-D Array 
A 3-D array consists of a cube of elements. The index of an element in a 3-D array is given by the formula: 

Index = (Layer Number - 1) * Number of Rows * Number of Columns + (Row Number - 1) * Number of Columns + (Column Number - 1) 

#### n-D Array 
An n-D array consists of multiple layers of elements. The index of an element in an n-D array is given by the formula: 

Index = (Layer Number - 1) * Number of Rows * Number of Columns + (Row Number - 1) * Number of Columns + (Column Number - 1) + ... + (Element Number - 1)




### Application of Arrays

* Arrays are a type of data structure which store a collection of elements. They are used to store and manipulate data in an efficient manner.

* Arrays can be single-dimensional (1D), two-dimensional (2D), three-dimensional (3D) and n-dimensional (nD).

* Representation of Arrays: Row Major Order and Column Major Order. 
* Derivation of Index Formulae for 1-D,2-D,3-D and n-D Array Application of arrays. 

### Sparse Matrices
* Sparse matrices are matrices which contain a large number of zero elements. 
* They are used to represent data in a more efficient manner.

### Linked Lists
* Linked lists are a type of data structure which stores data in a linked list structure. 
* Array implementation and pointer implementation of singly linked lists, doubly linked list, and circularly linked list are possible. 
* Operations on a Linked List include insertion, deletion, and traversal.

### Polynomial Representation
* Polynomials can be represented in terms of their coefficients and powers. 
* Addition, subtraction and multiplication of single variable and two variables polynomials are possible.




### Sparse Matrices and their Representations 

Sparse matrices are a type of matrix in which most of the elements are zero. They are used to represent data in which the majority of the elements are zero, such as in graphs or networks.

#### Arrays 

An array is a data structure that is used to store a collection of items. Arrays can be single or multidimensional, and can be represented using row major order or column major order. 

The index formulae for 1-D, 2-D, 3-D and n-D arrays are derived from the row major order and column major order. Arrays can be used for a variety of applications, such as sorting, searching and manipulating data.

#### Sparse Matrices 

Sparse matrices are a type of matrix in which most of the elements are zero. They are used to represent data in which the majority of the elements are zero, such as in graphs or networks. Sparse matrices can be represented using a variety of data structures, such as linked lists, hash tables, and arrays.

#### Linked Lists 

Linked lists are a type of data structure that is used to store a collection of items. Linked lists can be implemented using either an array or a pointer. Singly linked lists, doubly linked lists and circularly linked lists are the three main types of linked lists. 

Linked lists can be used to perform a variety of operations, such as insertion, deletion, traversal, and addition/subtraction/multiplication of single and two variable polynomials. 

#### Polynomial Representation 

Polynomials can be represented using linked lists. Each node in the linked list contains a coefficient and an exponent. The coefficients and exponents of the polynomial can be added, subtracted, or multiplied to calculate the result.




### Linked Lists
Linked lists are a type of data structure which allow for the storage of data in a non-contiguous manner. They are composed of nodes which contain data and a pointer to the next node in the list. Linked lists are useful for storing data which will be accessed in a non-sequential manner, as they allow for fast insertion and deletion of nodes.

Array Implementation:
Array implementation of linked lists involves using an array to store the data of the nodes. This implementation is useful for static lists, as the size of the array must be predetermined.

Pointer Implementation:
Pointer implementation of linked lists involves using pointers to link the nodes together. This implementation is useful for dynamic lists, as the size of the list can be adjusted as needed.

Singly Linked Lists:
A singly linked list is a linked list in which each node contains a pointer to the next node in the list. It is the simplest form of a linked list, and is useful for storing data which will be accessed in a sequential manner.

Doubly Linked List:
A doubly linked list is a linked list in which each node contains a pointer to both the next and previous node in the list. It is useful for storing data which will be accessed in both a sequential and non-sequential manner.

Circularly Linked List:
A circularly linked list is a linked list in which the last node contains a pointer to the first node in the list. It is useful for storing data which will be accessed in a circular manner.

Operations on a Linked List:
The following are some common operations that can be performed on a linked list:
- Insertion: Inserting a node into a linked list.
- Deletion: Deleting a node from a linked list.
- Traversal: Traversing a linked list to access its nodes.

Polynomial Representation and Addition Subtraction & Multiplications of Single variable & Two variables Polynomial:
Polynomials can be represented as linked lists, with each node containing a coefficient and an exponent. This representation is useful for performing operations on polynomials, such as addition, subtraction, and multiplication. For single variable polynomials, the addition, subtraction, and multiplication operations can be performed using the standard algorithms. For two variable polynomials, the addition, subtraction, and multiplication operations can be performed using the algorithms developed by Gauss.





## Unit 3 - Searching

### Concept of Searching
Searching is a process of finding a particular item from a collection of items. It is a fundamental operation in computing and is used in many applications. Searching algorithms are used to search for an item in a collection of items. 

#### Sequential search
Sequential search is a type of search algorithm which searches for an item in a collection of items by comparing each item in the collection with the search item one by one. It is also known as linear search.

#### Index Sequential Search
Index sequential search is an improvement over sequential search. It uses an index to speed up the searching process. In this method, the collection is divided into multiple blocks and each block is indexed.

#### Binary Search
Binary search is an efficient search algorithm which searches for an item in a sorted collection of items by repeatedly dividing the collection into two halves. It is also known as logarithmic search.

### Concept of Hashing & Collision resolution Techniques used in Hashing
Hashing is a process of mapping a set of values to a fixed size data structure. It is used to store and retrieve data from a collection of items. There are various collision resolution techniques used in hashing such as linear probing, quadratic probing, chaining and double hashing.

### Sorting
Sorting is a process of arranging a collection of items in a specific order. There are various sorting algorithms such as insertion sort, selection sort, bubble sort, quick sort, merge sort, heap sort and radix sort.




### Concept of Searching
Searching is the process of locating an item from a collection of items. It is the most important operation in data structures, as it helps to find a specific record from a large collection of records quickly. There are various searching techniques, such as linear search, binary search, index sequential search, and hashing. 

#### Sequential Search
Sequential search is the simplest searching algorithm. It searches every element of the list one by one, until it finds the desired element or until it reaches the end of the list. It is also known as a linear search. 

#### Index Sequential Search
Index sequential search is an improvement over the sequential search. It is a two-step process. First, the index of the list is searched to find the desired element. If the element is not found, then a sequential search is performed on the list. 

#### Binary Search
Binary search is an efficient searching algorithm. It is also known as a divide and conquer algorithm. It divides the list into two parts and searches for the desired element in one of the two parts. It is the most efficient searching algorithm. 

#### Concept of Hashing & Collision Resolution Techniques used in Hashing
Hashing is a technique used to store and retrieve data from a data structure. It is a method of mapping a large set of data to a smaller set of data. It is used to store and access data quickly. Collision resolution techniques are used to resolve conflicts when two different keys map to the same location in the data structure. These techniques include linear probing, quadratic probing, and double hashing. 

#### Sorting
Sorting is the process of arranging data in a particular order. Common sorting algorithms include insertion sort, selection sort, bubble sort, quick sort, merge sort, heap sort, and radix sort. Insertion sort is a simple sorting algorithm that sorts an array by inserting each element into its correct position. Selection sort is an algorithm that sorts an array by repeatedly finding the minimum element from the unsorted part and placing it at the beginning. Bubble sort is an algorithm that repeatedly swaps adjacent elements if they are in the wrong order. Quick sort is a divide and conquer algorithm that sorts an array by partitioning it into two parts and then recursively sorting the two parts. Merge sort is a divide and conquer algorithm that sorts an array by dividing it into two parts and then merging them in a sorted order. Heap sort is a sorting algorithm that sorts an array by building a heap data structure and then extracting the elements in sorted order. Radix sort is a sorting algorithm that sorts an array by grouping the elements according to their digits.




### Sequential search for the notes of the Unit 3 - Searching: Concept of Searching, Sequential search, Index Sequential Search, Binary Search. Concept of Hashing & Collision resolution Techniques used in Hashing. Sorting: Insertion Sort, Selection, Bubble Sort, Quick Sort, Merge Sort, Heap Sort and Radix Sort. in the subject of DATA STRUCTURE

#### Searching

* **Concept of Searching**: Searching is a process of finding an element from a given set of elements. It is an important operation in data structure.

* **Sequential search**: It is a technique of searching an element in a list or an array. It involves searching an element by traversing the list sequentially.

* **Index Sequential Search**: It is an improvement over the sequential search. It involves indexing the elements of the list and then searching the element using the index.

* **Binary Search**: It is an efficient search technique that uses the divide and conquer approach. It involves dividing the list into two halves and then searching the element in the appropriate half.

#### Hashing

* **Concept of Hashing**: Hashing is a technique of mapping a data element to a unique identifier. It is used for storing and retrieving data from a data structure.

* **Collision resolution Techniques used in Hashing**: Collision resolution techniques are used to handle collisions in hashing. The techniques used in hashing are chaining, open addressing and linear probing.

#### Sorting

* **Insertion Sort**: It is a sorting technique that uses the divide and conquer approach. It involves dividing the list into two halves and then sorting the elements in each half.

* **Selection Sort**: It is a sorting technique that uses the divide and conquer approach. It involves selecting the smallest element from the list and then sorting the remaining elements.

* **Bubble Sort**: It is a sorting technique that uses the divide and conquer approach. It involves comparing adjacent elements and then swapping them if they are not in the correct order.

* **Quick Sort**: It is a sorting technique that uses the divide and conquer approach. It involves selecting a pivot element and then sorting the elements according to the pivot element.

* **Merge Sort**: It is a sorting technique that uses the divide and conquer approach. It involves dividing the list into two halves and then merging the sorted halves.

* **Heap Sort**: It is a sorting technique that uses the divide and conquer approach. It involves building a heap data structure and then sorting the elements according to the heap.

* **Radix Sort**: It is a sorting technique that uses the divide and conquer approach. It involves sorting the elements according to their digits.




### Index Sequential Search for the notes of the Unit 3 - Searching: Concept of Searching, Sequential search, Index Sequential Search, Binary Search. Concept of Hashing & Collision resolution Techniques used in Hashing. Sorting: Insertion Sort, Selection, Bubble Sort, Quick Sort, Merge Sort, Heap Sort and Radix Sort. in the subject of DATA STRUCTURE

- **Index Sequential Search** is a search algorithm used to search for a particular element in a given list or array. It is a combination of two searching techniques, namely, indexing and sequential searching. This search is used when the list is sorted and when the elements of the list are known in advance. 

- **Concept of Searching** refers to the process of finding an item in a list or array. Searching algorithms are used to find a particular item in a list or array. The time complexity of a search algorithm depends on the size of the list or array and the number of elements in it.

- **Sequential search** is a searching technique used to find a particular element in a list or array. It is a linear search algorithm which compares the elements of the list one by one until the desired element is found.

- **Index Sequential Search** is an improvement over the sequential search algorithm. It uses an index table to store the index of the elements in the list. This helps to reduce the search time as the index table can be used to directly access the elements in the list.

- **Binary Search** is a search algorithm which is used to search for an element in a sorted list or array. It is a divide and conquer algorithm which divides the list into two halves and searches for the element in one of the halves.

- **Concept of Hashing** is the process of mapping a set of values to a unique identifier. Hashing is used to store data in an efficient and secure manner.

- **Collision Resolution Techniques** used in Hashing are methods used to resolve collisions in a hash table. Collisions occur when two or more elements are mapped to the same hash value. The common collision resolution techniques are linear probing, quadratic probing, chaining and rehashing.

- **Sorting** is the process of arranging a list of elements in a particular order. Insertion sort, selection sort, bubble sort, quick sort, merge sort, heap sort and radix sort are some of the common sorting algorithms. Insertion sort is a simple sorting algorithm which works by inserting the elements one by one into their correct positions. Selection sort works by selecting the smallest element in the list and swapping it with the first element. Bubble sort is a comparison-based sorting algorithm which works by swapping adjacent elements if they are not in the correct order. Quick sort is a divide and conquer algorithm which works by partitioning the list into two halves and sorting them recursively. Merge sort is a divide and conquer algorithm which works by dividing the list into two halves and then merging them in the correct order. Heap sort is a comparison-based sorting algorithm which works by building a heap and then sorting the elements in the heap. Radix sort is a sorting algorithm which works by sorting the elements based on their digits.





### Binary Search

Binary Search is an efficient searching algorithm used to search for an element in a sorted array. It works by dividing the array into two halves and then comparing the element to be searched with the middle element of the array. If the element is equal to the middle element, the search is successful. Otherwise, the search continues in either the left or the right half of the array depending on the comparison.

#### Concept of Searching

Searching is a process of finding an element from a collection of elements. It is an important operation in data structures. The efficiency of searching depends on the data structure used to store the elements. Examples of data structures used for searching are arrays, linked lists, binary search trees, and hash tables.

#### Sequential search

Sequential search is a basic searching algorithm used to search for an element in an array. It works by comparing the element to be searched with each element in the array sequentially until the element is found.

#### Index Sequential Search

Index sequential search is an improvement of the sequential search algorithm. It uses an index table to store the elements in the array. The index table stores the elements in sorted order and contains the indices of the elements in the array. This helps in reducing the number of comparisons required for searching.

#### Binary Search

Binary search is an efficient searching algorithm used to search for an element in a sorted array. It works by dividing the array into two halves and then comparing the element to be searched with the middle element of the array. If the element is equal to the middle element, the search is successful. Otherwise, the search continues in either the left or the right half of the array depending on the comparison.

#### Concept of Hashing & Collision Resolution Techniques used in Hashing

Hashing is a technique used to store and retrieve elements from a collection. It works by mapping the elements to a unique key using a hash function. The hash function maps the elements to a unique key and the key is used to store and retrieve the elements. Collision resolution techniques are used to handle collisions, which occur when two elements map to the same key. Examples of collision resolution techniques are open addressing, chaining, and linear probing.

#### Sorting

Sorting is an important operation in data structures. It is used to arrange elements in a particular order. Examples of sorting algorithms are insertion sort, selection sort, bubble sort, quick sort, merge sort, heap sort, and radix sort. Each sorting algorithm has its own advantages and disadvantages.




### Concept of Hashing & Collision Resolution Techniques Used in Hashing

Hashing is a technique used to store and retrieve data from a data structure called a hash table. It is a type of data structure that uses a hash function to map keys to values. The hash function takes a key and computes an index in the hash table where the corresponding value is stored.

Collision resolution techniques are used to determine how to handle collisions, which occur when two different keys are mapped to the same index in the hash table. There are several collision resolution techniques, including linear probing, separate chaining, and open addressing.

#### Searching

Searching is a process of finding an element in a data structure. There are several searching techniques, including sequential search, index sequential search, and binary search.

Sequential search is a technique that searches for an element in a data structure by comparing each element in the data structure to the value being searched for.

Index sequential search is a technique that searches for an element in a data structure by first searching for the index of the element before searching for the element itself.

Binary search is a technique that searches for an element in a sorted data structure by repeatedly dividing the search space in half.

#### Sorting

Sorting is a process of arranging elements in a data structure in a particular order. There are several sorting algorithms, including insertion sort, selection sort, bubble sort, quick sort, merge sort, heap sort, and radix sort.

Insertion sort is a sorting algorithm that sorts elements in a data structure by repeatedly inserting elements into their correct position.

Selection sort is a sorting algorithm that sorts elements in a data structure by repeatedly selecting the smallest element in the unsorted portion of the data structure and swapping it with the first element in the unsorted portion.

Bubble sort is a sorting algorithm that sorts elements in a data structure by repeatedly swapping adjacent elements if they are in the wrong order.

Quick sort is a sorting algorithm that sorts elements in a data structure by repeatedly partitioning the data structure around a pivot element and recursively sorting the two partitions.

Merge sort is a sorting algorithm that sorts elements in a data structure by first dividing the data structure into smaller sub-arrays, sorting the sub-arrays, and then merging the sorted sub-arrays together.

Heap sort is a sorting algorithm that sorts elements in a data structure by repeatedly building a heap from the elements and extracting the largest element from the heap.

Radix sort is a sorting algorithm that sorts elements in a data structure by repeatedly sorting the elements based on their individual digits.




### Sorting: Insertion Sort, Selection, Bubble Sort, Quick Sort, Merge Sort, Heap Sort and Radix Sort

Insertion Sort: Insertion sort is an in-place comparison-based sorting algorithm. It works by taking elements from an unsorted list and inserting them into the correct position in a sorted list.

Selection Sort: Selection sort is an in-place comparison-based sorting algorithm. It works by taking the smallest element from the unsorted list and inserting it into the correct position in the sorted list.

Bubble Sort: Bubble sort is an in-place comparison-based sorting algorithm. It works by comparing adjacent elements in the unsorted list and swapping them if they are in the wrong order.

Quick Sort: Quick sort is an in-place comparison-based sorting algorithm. It works by picking an element from the unsorted list as the pivot, and then partitioning the list into two parts based on the pivot. The elements smaller than the pivot are placed to the left of the pivot and the elements larger than the pivot are placed to the right of the pivot.

Merge Sort: Merge sort is a divide-and-conquer sorting algorithm. It works by dividing the unsorted list into two smaller lists, sorting them and then merging them back together.

Heap Sort: Heap sort is an in-place comparison-based sorting algorithm. It works by creating a heap data structure out of the unsorted list and then sorting it by repeatedly removing the largest element from the heap and inserting it back into the list.

Radix Sort: Radix sort is a non-comparison-based sorting algorithm. It works by sorting the elements of the list based on the individual digits of their keys.




## Unit 4 - Graphs

- Graphs are collections of objects called **vertices** or **nodes**, connected by lines called **edges**.
- Graphs are used to represent relationships between objects, such as the connections between cities on a map.
- **Adjacency Matrices** are 2-dimensional arrays that represent a graph. The rows and columns of the matrix represent the vertices of the graph, and the entries in the matrix indicate the presence or absence of an edge between the vertices.
- **Adjacency Lists** are linked lists that represent a graph. Each vertex in the graph is represented by a linked list, with each element in the list representing a destination vertex.
- **Graph Traversal** algorithms are used to traverse a graph and visit every vertex in the graph.
    - **Depth First Search** is an algorithm that visits each vertex in the graph by recursively visiting each adjacent vertex until it reaches the end of the graph.
    - **Breadth First Search** is an algorithm that visits each vertex in the graph by visiting each adjacent vertex in a level-by-level manner.
- **Connected Component** is a set of connected vertices in a graph.
- **Spanning Trees** are subgraphs of a graph that connect all the vertices in the graph.
- **Minimum Cost Spanning Trees** are spanning trees that have the minimum cost possible.
    - **Prim's Algorithm** is an algorithm that finds a minimum cost spanning tree by adding the cheapest edge to the tree at each step.
    - **Kruskal's Algorithm** is an algorithm that finds a minimum cost spanning tree by adding the least expensive edge to the tree at each step.
- **Transitive Closure** is a matrix that shows the reachability of each vertex in a graph.
- **Shortest Path Algorithm** is an algorithm that finds the shortest path between two vertices in a graph.
    - **Warshall's Algorithm** is an algorithm that finds the transitive closure of a graph.
    - **Dijkstra's Algorithm** is an algorithm that finds the shortest path between two vertices in a graph.





### Terminology used with Graph

* Graph: A graph is a data structure used to represent a set of objects (called vertices) and the connections between them (called edges).

* Data Structure for Graph Representations: 
  * Adjacency Matrices: An adjacency matrix is a two-dimensional array that stores the information about the connections between the vertices. 
  * Adjacency List: An adjacency list is a one-dimensional array that stores the information about the connections between the vertices. 
  * Adjacency: Adjacency is the relationship between two vertices that are connected by an edge.

* Graph Traversal: 
  * Depth First Search: Depth first search is an algorithm used to traverse a graph. It begins at the root node and explores as far as possible along each branch before backtracking.
  * Breadth First Search: Breadth first search is an algorithm used to traverse a graph. It begins at the root node and explores each level of the graph in a breadth-first manner.

* Connected Component: A connected component is a set of vertices in a graph that are all connected to each other.

* Spanning Trees: A spanning tree is a subgraph of a graph that contains all the vertices and some of the edges of the original graph.

* Minimum Cost Spanning Trees: 
  * Prims Algorithm: Prim's algorithm is an algorithm used to find the minimum cost spanning tree of a graph.
  * Kruskal Algorithm: Kruskal's algorithm is an algorithm used to find the minimum cost spanning tree of a graph.

* Transitive Closure and Shortest Path algorithm:
  * Warshal Algorithm: Warshal's algorithm is an algorithm used to find the transitive closure of a graph.
  * Dijikstra Algorithm: Dijkstra's algorithm is an algorithm used to find the shortest path between two vertices in a graph.

