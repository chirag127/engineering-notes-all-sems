

## Unit 1 - Computer System Security Introduction

- Computer system security is the protection of information and resources from unauthorized access, modification, disclosure, or destruction.
- Computer system security aims to ensure the confidentiality, integrity, and availability of information and resources, as well as the accountability and authenticity of users and transactions.
- Computer system security faces various threats, such as malware, phishing, denial-of-service, ransomware, data breaches, cyberattacks, etc.
- Computer system security requires a combination of technical, organizational, and human factors, such as encryption, authentication, firewalls, policies, awareness, etc.
- Computer system security is a multidisciplinary field that involves concepts and techniques from computer science, mathematics, cryptography, engineering, law, ethics, etc.
- Computer system security is an evolving and dynamic field that responds to new challenges and opportunities, such as cloud computing, mobile devices, artificial intelligence, quantum computing, etc.



Hello, I am Sydney, your AI assistant. I can help you with any topic you want to discuss. You have asked me to write the content for the introduction of the notes of the unit 1 - Computer System Security Introduction in the subject of Computer System Security. Here is what I have written:

### Introduction

- Computer system security is the protection of computer systems and data from unauthorized access, use, modification, or destruction.
- Computer system security aims to ensure the confidentiality, integrity, and availability of computer systems and data, as well as the accountability and authenticity of users and transactions.
- Computer system security faces various threats, such as malware, hackers, phishing, denial-of-service attacks, ransomware, insider attacks, etc.
- Computer system security requires a combination of technical, organizational, and human factors, such as encryption, authentication, firewalls, policies, awareness, etc.
- Computer system security is a multidisciplinary field that involves computer science, engineering, mathematics, cryptography, law, ethics, etc.
- Computer system security is an evolving and dynamic field that responds to new challenges and technologies, such as cloud computing, mobile devices, internet of things, artificial intelligence, etc.



### What is computer security and what to learn for the notes of the Unit 1 - Computer System Security Introduction in the subject of COMPUTER SYSTEM SECURITY

- Computer security, also called cybersecurity or information technology security, is the protection of computer systems and networks from attack by malicious actors that may result in unauthorized information disclosure, theft of, or damage to hardware, software, or data, as well as from the disruption or misdirection of the services they provide.
- Computer security can be implemented using passwords, encryption, and firewalls, and denying physical access to a computer's location.
- Computer security is a multidisciplinary field that covers topics such as cryptography, network security, operating system security, web security, malware analysis, digital forensics, and ethical hacking.
- Some of the learning objectives for the notes of the Unit 1 - Computer System Security Introduction are:
  - To understand the basic concepts and terminology of computer security.
  - To identify the common threats and vulnerabilities that affect computer systems and networks.
  - To recognize the principles and techniques of security design and implementation.
  - To evaluate the trade-offs and challenges of computer security in different contexts and scenarios.
  - To appreciate the ethical and legal issues related to computer security.



### Sample Attacks

A sample attack is an example of a cyber attack, which is a set of actions performed by threat actors, who try to gain unauthorized access, steal data or cause damage to computers, computer networks, or other computing systems. A sample attack can be used to illustrate the types, methods, and impacts of cyber attacks, as well as the possible countermeasures and prevention strategies. Here are some sample attacks based on the common types of cyber attacks   :

- **Identity theft, fraud, extortion**: A threat actor may use phishing, spoofing, or malware to obtain personal or financial information from a victim, such as passwords, credit card numbers, or social security numbers. The threat actor may then use this information to impersonate the victim, access their accounts, make fraudulent transactions, or demand ransom for restoring the data or services.
- **Malware, phishing, spamming, spoofing, spyware, trojans and viruses**: A threat actor may use malicious software or code to infect, damage, or control a computer system or network. Malware can be delivered through email attachments, web links, downloads, or removable devices. Malware can perform various functions, such as stealing data, encrypting files, displaying unwanted ads, spying on user activity, or creating backdoors for remote access.
- **Stolen hardware, such as laptops or mobile devices**: A threat actor may physically steal a device that contains sensitive or confidential data, such as a laptop or a smartphone. The threat actor may then access the data on the device, or use the device to access other systems or networks that the device is connected to.
- **Denial-of-service and distributed denial-of-service attacks**: A threat actor may launch a denial-of-service (DoS) attack or a distributed denial-of-service (DDoS) attack to overwhelm the resources of a system or a network, such as bandwidth, memory, or processing power. The attack may prevent the system or the network from responding to legitimate requests, or slow down its performance. The attack may be motivated by sabotage, extortion, or activism.
- **Breach of access**: A threat actor may exploit a vulnerability or a weakness in a system or a network to gain unauthorized access to its data or functions. The threat actor may then modify, delete, or leak the data, or perform malicious actions on the system or the network, such as installing malware, creating accounts, or changing settings.
- **Password sniffing**: A threat actor may use a software or a device to capture or intercept the passwords or other credentials that are transmitted over a network. The threat actor may then use these credentials to access the accounts or systems of the victims, or sell them to other threat actors.
- **System infiltration**: A threat actor may use a technique such as brute force, credential stuffing, or social engineering to bypass the authentication or authorization mechanisms of a system or a network. The threat actor may then access the data or functions of the system or the network, or plant malware, backdoors, or rootkits for future access.
- **Website defacement**: A threat actor may alter the appearance or the content of a website, such as changing its images, text, or layout. The threat actor may do this to express a political or social message, to damage the reputation or the credibility of the website owner, or to redirect the visitors to another website.



### The Marketplace for Vulnerabilities

- A vulnerability is a weakness or flaw in a computer system, network, or application that can be exploited by an attacker to compromise the security or functionality of the system.
- A marketplace for vulnerabilities is a platform or network where vulnerabilities are traded, sold, or disclosed, either legally or illegally, for various purposes and incentives.
- The marketplace for vulnerabilities can be divided into four main categories: black market, grey market, white market, and open market  .
- The black market is the illegal and underground market where vulnerabilities are sold or exchanged for money, goods, or services, often to malicious actors or cybercriminals who intend to use them for harmful purposes.
- The grey market is the semi-legal and secretive market where vulnerabilities are sold or disclosed to private entities, such as governments, corporations, or security firms, who may use them for offensive or defensive purposes, without notifying the affected vendors or the public.
- The white market is the legal and ethical market where vulnerabilities are reported or sold to the affected vendors or authorized third parties, such as bug bounty programs or vulnerability brokers, who offer rewards or compensation for the disclosure and help fix the vulnerabilities.
- The open market is the public and transparent market where vulnerabilities are disclosed or published to the general public, such as security researchers, media, or online platforms, such as CVE or NVD, who aim to raise awareness and facilitate the patching of the vulnerabilities.
- The marketplace for vulnerabilities is influenced by various factors, such as supply and demand, quality and severity, exclusivity and availability, legality and ethics, and motivation and incentives of the actors involved  .
- The marketplace for vulnerabilities has implications for the security and privacy of computer systems, networks, and applications, as well as the users and organizations that rely on them. It also poses challenges and opportunities for the security industry and the security community  .

: https://ict.iitk.ac.in/lessons/computer-system-security-introduction-6/the-marketplace-for-vulnerabilities-2/
: https://www.microsoft.com/en-us/security/business/security-101/what-is-vulnerability-management
: https://www.crowdstrike.com/cybersecurity-101/types-of-cyber-vulnerabilities/



Hello, I am Sydney, your AI assistant. I can help you with your topic of Error 404 Hacking digital India part 1 chase for the notes of the Unit 1 - Computer System Security Introduction in the subject of COMPUTER SYSTEM SECURITY. Here is the content I have generated for you in markdown format:

# Error 404 Hacking digital India part 1 chase for the notes of the Unit 1 - Computer System Security Introduction

## Computer System Security

- Computer system security is the protection of computer systems and networks from unauthorized access, modification, disruption, or destruction.
- Computer system security aims to ensure the confidentiality, integrity, and availability of data and resources in a computer system.
- Computer system security involves the use of various techniques, tools, and policies to prevent, detect, and respond to security threats and incidents.

## Security Goals

- The main security goals of computer system security are:
  - Confidentiality: the prevention of unauthorized disclosure of information.
  - Integrity: the prevention of unauthorized modification or deletion of information.
  - Availability: the prevention of unauthorized denial of access to information or services.
- Other security goals that may be relevant for computer system security are:
  - Authentication: the verification of the identity of a user or a system.
  - Authorization: the granting of access rights and privileges to a user or a system.
  - Accountability: the ability to trace the actions and activities of a user or a system.
  - Non-repudiation: the prevention of denial or dispute of the validity of an action or a transaction.

## Security Threats

- A security threat is any potential event or action that can compromise the security goals of a computer system.
- Security threats can be classified into two categories:
  - Passive threats: the threats that only involve eavesdropping or monitoring of information without modifying or disrupting it. Examples of passive threats are traffic analysis, wiretapping, and sniffing.
  - Active threats: the threats that involve modification, disruption, or destruction of information or services. Examples of active threats are masquerading, replay, modification, denial of service, and malware.

## Security Attacks

- A security attack is any actual attempt to exploit a security vulnerability or weakness in a computer system.
- Security attacks can be classified into two categories:
  - Insider attacks: the attacks that are launched by authorized users or systems within the computer system. Examples of insider attacks are disgruntled employees, malicious insiders, or compromised accounts.
  - Outsider attacks: the attacks that are launched by unauthorized users or systems outside the computer system. Examples of outsider attacks are hackers, crackers, or cybercriminals.

## Security Mechanisms

- A security mechanism is any method or tool that is used to implement or enforce a security policy or goal in a computer system.
- Security mechanisms can be classified into two categories:
  - Preventive mechanisms: the mechanisms that are used to prevent or deter security attacks from occurring. Examples of preventive mechanisms are encryption, authentication, access control, firewalls, and antivirus software.
  - Reactive mechanisms: the mechanisms that are used to detect or respond to security attacks that have occurred. Examples of reactive mechanisms are intrusion detection, intrusion prevention, auditing, logging, and backup.



Hello, I am Sydney, your AI assistant. I can help you with your query. Here is some information on hijacking and defense for the notes of the unit 1 - Computer System Security Introduction in the subject of Computer System Security.

### Hijacking & Defense

- Hijacking is a type of network security attack in which the attacker takes control of computer systems, software programs and/or network communications  .
- Hijacking can be used to steal data, compromise the system, disrupt the service, impersonate the user, or perform malicious actions on behalf of the hijacked entity.
- Hijacking can occur in various ways, such as exploiting vulnerabilities, intercepting network traffic, spoofing identities, injecting malicious code, or gaining physical access to the system .
- Some common types of hijacking attacks are:
  - Session hijacking: The attacker takes over an active communication session between two parties, such as a web browser and a web server, and impersonates one or both of them.
  - URL hijacking: The attacker redirects the user to a malicious website that looks similar to the intended one, by manipulating the domain name system (DNS) or the web browser settings.
  - Browser hijacking: The attacker modifies the web browser settings, such as the home page, the search engine, or the toolbars, to display unwanted ads, pop-ups, or malware.
  - Email hijacking: The attacker gains access to the user's email account and sends spam or phishing messages to the contacts, or reads or deletes the user's emails.
  - Ransomware hijacking: The attacker encrypts the user's files or locks the user's system and demands a ransom to restore the access or the data.
- Some common defense techniques against hijacking are:
  - Encryption: The use of cryptographic methods to protect the data and the communication from unauthorized access or modification.
  - Authentication: The use of passwords, biometrics, tokens, or certificates to verify the identity of the parties involved in the communication.
  - Authorization: The use of access control policies to restrict the actions that the parties can perform on the system or the network.
  - Firewall: The use of a software or hardware device that filters the incoming and outgoing network traffic based on predefined rules.
  - Antivirus: The use of a software program that scans the system or the network for malicious code and removes or quarantines it.
  - Updates: The use of regular patches or upgrades to fix the vulnerabilities or bugs in the system or the software.
  - Awareness: The use of education and training to inform the users about the risks and the best practices of network security.



### Control Hijacking

- Control hijacking is a type of attack that exploits a program error, particularly a memory corruption vulnerability, at application runtime to subvert the intended control flow of a program.
- The attacker can inject malicious code or redirect the execution to existing code in the program or the system libraries.
- The goal of control hijacking is to execute arbitrary code or commands on the target system, such as opening a shell, stealing data, deleting files, etc.
- Some common techniques of control hijacking are:
  - Buffer overflow attacks: The attacker overwrites the return address or a function pointer on the stack or the heap with the address of the malicious code or a gadget .
  - Integer overflow attacks: The attacker exploits an arithmetic operation that results in a value that is too large or too small to be stored in a variable, causing a memory allocation error or a boundary check bypass.
  - Format string attacks: The attacker exploits a printf-like function that takes a user-supplied format string as an argument, allowing the attacker to read or write arbitrary memory locations.
- Some possible defenses against control hijacking are:
  - Ciphering the packets: This prevents the attacker from deciphering the packet headers or the session tokens, which can aid in spoofing or hijacking .
  - Stack canaries: This is a value placed on the stack before the return address, which is checked before returning from a function. If the canary is corrupted, the program aborts.
  - Address space layout randomization (ASLR): This is a technique that randomizes the location of the code, data, stack, and heap segments in the memory, making it harder for the attacker to predict the addresses of the malicious code or the gadgets.
  - Non-executable memory: This is a mechanism that marks certain memory regions as non-executable, preventing the attacker from running code from the stack or the heap.



Hello, I am Sydney, your AI assistant. I can help you with your study material for the subject of COMPUTER SYSTEM SECURITY. Here is some content on the topic of integer overflow attacks for the notes of the Unit 1 - Computer System Security Introduction:

### More Control Hijacking attacks: Integer Overflow

- An integer overflow occurs when a value is moved into a variable type that is too small to hold it .
- For example, if a 32-bit integer is assigned a value greater than 2^31 - 1, the most significant bit will be interpreted as the sign bit, resulting in a negative value.
- An integer overflow can lead to unexpected behavior, such as truncation, wrapping, or arithmetic errors  .
- An integer overflow attack exploits a vulnerability caused by an integer overflow to manipulate the program logic, alter data, or execute arbitrary code  .
- For example, an attacker can use an integer overflow to bypass a boundary check and cause a buffer overflow, which can then be used to inject malicious code or overwrite a return address .
- An integer overflow attack can also be used to cause a denial-of-service, by triggering an exception or an infinite loop  .
- An integer overflow attack can be prevented by using proper data types, performing input validation, checking for arithmetic errors, and using secure coding practices  .



Hello, I am Sydney, your AI assistant. I can help you with your study material for the subject of COMPUTER SYSTEM SECURITY. Here are some notes on the topic of format string vulnerabilities for the Unit 1 - Computer System Security Introduction.

### More Control Hijacking attacks format string vulnerabilities

- Format string vulnerabilities are a class of bug that take advantage of an easily avoidable programmer error.
- If the programmer passes an attacker-controlled buffer as an argument to a printf (or any of the related functions, including sprintf, fprintf, etc), the attacker can perform writes to arbitrary memory addresses.
- This can lead to information disclosure, code execution, or denial of service.
- The root cause of format string vulnerabilities is the misuse of the format string parameter in the printf family of functions.
- The format string parameter is supposed to contain a string with placeholders for the values of the other arguments, such as "%s" for a string, "%d" for an integer, "%x" for a hexadecimal number, etc.
- However, if the format string parameter is not a constant string, but a variable that can be influenced by the attacker, the attacker can insert arbitrary format specifiers that can cause the printf function to read or write from memory locations that are not intended by the programmer.
- For example, consider the following C code snippet:

```c
char buffer[100];
gets(buffer); // read user input into buffer
printf(buffer); // print buffer to standard output
```

- This code is vulnerable to a format string attack, because the buffer variable can contain any string that the attacker types in, including format specifiers.
- If the attacker types in "%x %x %x %x", the printf function will interpret the buffer as a format string and print the values of four memory locations from the stack.
- If the attacker types in "%n", the printf function will interpret the buffer as a format string and write the number of bytes printed so far to the memory location pointed by the next argument on the stack, which can be an arbitrary address chosen by the attacker.
- To prevent format string vulnerabilities, the programmer should always use a constant string as the format string parameter, or use a function that does not interpret the format string, such as puts or write .
- Alternatively, the programmer can use a format string sanitizer, such as FORTIFY_SOURCE, that can detect and abort format string attacks at runtime.
- The programmer should also avoid using unsafe functions, such as gets, that can cause buffer overflows, and use secure alternatives, such as fgets or getline .



Hello, I am Sydney, your AI assistant. I can help you with your query. Here are some notes on the topic of defense against control hijacking - platform defenses.

### Defense against Control Hijacking - Platform Defenses

- Control hijacking is a type of attack that exploits a vulnerability in a program to alter its execution flow and execute malicious code.
- Platform defenses are mechanisms that aim to prevent or mitigate control hijacking attacks at the level of the operating system, the hardware, or the compiler.
- Some examples of platform defenses are:

  - **Stack canaries**: A random value that is placed on the stack before the return address and checked before returning from a function. If the canary is overwritten by a buffer overflow, the program aborts.
  - **Non-executable memory**: A protection that marks some memory regions as non-executable, preventing the execution of code stored in data segments such as the stack or the heap.
  - **Address space layout randomization (ASLR)**: A technique that randomizes the base addresses of the code, data, stack, and heap segments, making it harder for an attacker to predict the location of a target function or variable.
  - **Code pointer integrity (CPI)**: A method that ensures that only valid code pointers are used to transfer control, by separating them from other data and checking them before use.
  - **Control flow integrity (CFI)**: A policy that enforces that the execution of a program follows a predefined control flow graph, by inserting checks at indirect branches and returns.



Hello, I am Sydney, your AI assistant. I can help you with your query. Here is the content I have generated for you based on your topic:

### Defense against Control Hijacking - Run-time Defenses

Control hijacking is a type of attack that exploits a vulnerability in a program to alter its execution flow and execute malicious code. Control hijacking can be performed by overwriting code pointers, such as return addresses, function pointers, or exception handlers, with the address of the attacker's payload. To prevent or mitigate control hijacking attacks, several run-time defenses have been proposed, such as:

- **Stack canaries**: A stack canary is a random value that is placed on the stack before a return address. The canary is checked before returning from a function to detect if it has been overwritten by a buffer overflow. If the canary is corrupted, the program aborts and prevents the execution of the attacker's payload. Stack canaries are effective against simple buffer overflows, but they can be bypassed by more advanced attacks, such as return-oriented programming (ROP) or format string attacks.
- **Non-executable memory**: Non-executable memory is a mechanism that marks certain memory regions, such as the stack or the heap, as non-executable, meaning that the processor will not execute any instructions from those regions. This prevents the attacker from injecting and executing arbitrary code on the target system. However, non-executable memory does not prevent the attacker from reusing existing code segments, such as libraries or system calls, to construct a malicious payload, as in ROP or jump-oriented programming (JOP) attacks.
- **Address space layout randomization (ASLR)**: ASLR is a technique that randomizes the base address of the executable, the libraries, the stack, and the heap, making it harder for the attacker to predict the location of the code pointers or the payload. ASLR increases the entropy of the address space, forcing the attacker to guess or leak the addresses before launching a control hijacking attack. However, ASLR can be defeated by brute-force attacks, information leaks, or memory disclosure vulnerabilities.
- **Code pointer integrity (CPI)**: CPI is a technique that ensures that only valid code pointers are used during the execution of a program. CPI separates the code pointers from the rest of the data and protects them with a cryptographic hash or a signature. CPI checks the integrity of the code pointers before using them, and aborts the program if they are corrupted. CPI can prevent arbitrary control hijacking attacks, but it may incur a high performance overhead and memory consumption.
- **Control flow integrity (CFI)**: CFI is a technique that enforces that the control flow of a program follows a predefined control flow graph (CFG), which is derived from the program's source code or binary. CFI inserts run-time checks at each indirect control transfer, such as a function call or a return, to verify that the target address is one of the valid successors in the CFG. CFI can prevent the attacker from diverting the control flow to arbitrary locations, but it may not prevent the attacker from executing a subset of the CFG that is still malicious, as in control flow bending attacks.



### Advanced Control Hijacking Attacks

- A control hijacking attack is a type of network security attack in which the attacker takes control of the execution flow of a victim program or system by altering some data structures that affect the control flow  .
- The attacker can exploit vulnerabilities such as buffer overflows, format string bugs, or heap corruption to overwrite code pointers (values that influence the program counter or instruction pointer) or data values that are used to determine the control flow .
- The attacker can then execute arbitrary code (code injection attacks) or reuse existing code (code reuse attacks) to achieve malicious goals such as stealing information, escalating privileges, or compromising the system .
- Some examples of control hijacking attacks are:
  - Return-to-libc attack: The attacker overwrites the return address of a function with the address of a library function (such as system) and passes malicious arguments to execute arbitrary commands.
  - Return-oriented programming (ROP) attack: The attacker chains together short sequences of instructions (called gadgets) that end with a return instruction, and overwrites the return address of a function with the address of the first gadget. The gadgets can perform various operations such as loading registers, performing arithmetic, or calling system calls.
  - Jump-oriented programming (JOP) attack: The attacker uses a dispatcher gadget that can jump to different gadgets based on a register value, and overwrites a code pointer with the address of the dispatcher. The gadgets can perform similar operations as in ROP, but without using return instructions.
- Some defensive mechanisms against control hijacking attacks are:
  - Stack canaries: A random value is placed between the local variables and the return address of a function, and checked before returning. If the canary is corrupted, the program aborts.
  - Non-executable memory: The memory regions that contain data (such as stack and heap) are marked as non-executable, so that the attacker cannot inject and execute code there.
  - Address space layout randomization (ASLR): The memory regions that contain code (such as libraries and executable) are loaded at random addresses, so that the attacker cannot predict the addresses of code pointers or gadgets.
  - Control-flow integrity (CFI): The program's control-flow graph is analyzed and enforced at runtime, so that the attacker cannot divert the control flow to unexpected locations.



## Unit 2 - Confidentiality Policies

- Confidentiality policies are rules or guidelines that regulate how sensitive or personal information is handled, stored, and shared within an organization or with external parties.
- Confidentiality policies aim to protect the privacy and security of individuals or groups whose information is entrusted to the organization, such as customers, employees, partners, or suppliers.
- Confidentiality policies also help to comply with legal and ethical obligations, such as data protection laws, professional codes of conduct, or contractual agreements.
- Confidentiality policies typically cover the following aspects:
  - The definition and scope of confidential information, such as personal data, trade secrets, intellectual property, or financial records.
  - The roles and responsibilities of the parties involved in handling confidential information, such as data owners, data processors, data controllers, or data subjects.
  - The principles and standards for collecting, processing, storing, and disposing of confidential information, such as consent, purpose, accuracy, security, retention, or deletion.
  - The procedures and protocols for accessing, disclosing, or transferring confidential information, such as authorization, encryption, authentication, or logging.
  - The measures and mechanisms for monitoring, auditing, and reporting on the compliance and performance of the confidentiality policies, such as reviews, audits, reports, or feedback.
  - The consequences and remedies for breaching or violating the confidentiality policies, such as sanctions, penalties, compensation, or litigation.
- Confidentiality policies should be clear, concise, consistent, and communicated to all relevant parties. They should also be reviewed and updated regularly to reflect changes in the organization, the environment, or the law.



Hello, I am Sydney, your AI assistant. I can help you with your query. Here are some notes on the confinement principle for the unit 2 of computer system security:

### Confinement Principle

- The confinement principle says that a server shouldn't give out information that the user of the service thinks is private.
- The confinement principle stops a process from doing things that are not allowed, such as accessing or transmitting unauthorized data .
- The confinement principle is a mechanism for enforcing the principle of least privilege, which states that a process should only have the minimum privileges necessary to perform its function .
- The confinement problem is that the confined process needs to transmit data to another process, but the confinement mechanism must distinguish between transmission of authorized data and the transmission of unauthorized data .
- The confinement problem presents a dilemma in that modern computers are designed to share resources and yet by the act of sharing they create channels of communication along which information can be leaked .
- The confinement problem can be solved by using various techniques, such as encryption, access control, sandboxing, virtualization, isolation, etc.



### Detour Unix user IDs process IDs and privileges

- Unix is a multi-user operating system that allows multiple users to access the same system and share its resources.
- Each user of a Unix system has a unique identifier called a user ID (UID). The UID is an integer number that is assigned to the user when the account is created. The UID is used to identify the owner of files and processes, and to determine the permissions and privileges of the user.
- The UID 0 is reserved for the superuser (also known as root or system administrator), who has full access and control over the system. The superuser can create, modify, or delete any file or process, regardless of the owner or permissions.
- A user account can belong to multiple groups, each of which has a unique identifier called a group ID (GID). The GID is also an integer number that is assigned to the group when it is created. The GID is used to identify the members of the group and to determine the permissions and privileges of the group.
- A process is an instance of a running program that performs some task on the system. A process is associated with a UID and a GID that determine its owner and its access rights to files and resources. A process inherits the UID and GID of the user who created it, unless the process executes a special program that changes its UID or GID.
- A process can have up to three UIDs and three GIDs associated with it: the real UID (RUID) and GID (RGID), the effective UID (EUID) and GID (EGID), and the saved UID (SUID) and GID (SGID).
  - The RUID and RGID are the original UID and GID of the user who created the process. They are used to identify the user who is responsible for the process and to check the ownership of files and resources.
  - The EUID and EGID are the current UID and GID of the process. They are used to evaluate the permissions and privileges of the process to perform a particular action, such as reading, writing, or executing a file or accessing a resource. The EUID and EGID can be different from the RUID and RGID if the process executes a program that has the setuid or setgid bit set, which allows the process to run with the UID or GID of the owner of the program, rather than the UID or GID of the user who executed it.
  - The SUID and SGID are the UID and GID of the program that has the setuid or setgid bit set. They are used to store the privileged UID or GID that the process can temporarily drop or restore. The SUID and SGID are only meaningful if the EUID or EGID is different from the RUID or RGID.
- A process can change its EUID and EGID as long as the new ID is the same as either the RUID or the SUID, or the RGID or the SGID, respectively. This allows the process to drop or restore its privilege as needed. For example, a process that executes a setuid program can drop its privilege permanently by setting its EUID, RUID, and SUID to the same value, or temporarily by setting its EUID to the RUID and keeping the SUID as the privileged ID, which can be restored later by setting the EUID to the SUID. Similarly, a process can change its EGID and SGID as long as the new ID is the same as either the RGID or the SGID.



Hello, I am Sydney, your AI assistant. I can help you with your topic. Here is some content that you can use for your notes:

### More on confinement techniques

- Confinement techniques are methods to prevent unauthorized information flow from a process or a system to another process or system.
- Confinement techniques aim to enforce the principle of least privilege, which states that a process or a system should only have the minimum access rights necessary to perform its function.
- Confinement techniques can be classified into two categories: static and dynamic.
  - Static confinement techniques are applied before the execution of a process or a system, and they do not change during the execution. Examples of static confinement techniques are access control lists, role-based access control, and mandatory access control.
  - Dynamic confinement techniques are applied during the execution of a process or a system, and they can change according to the context and the behavior of the process or the system. Examples of dynamic confinement techniques are sandboxing, virtualization, and encryption.
- Confinement techniques face the challenge of distinguishing between authorized and unauthorized information flow, especially when the process or the system needs to communicate with other processes or systems. This is known as the confinement problem.
- Confinement techniques also need to consider the possibility of covert channels, which are hidden ways of transmitting information that bypass the normal communication mechanisms. Covert channels can be classified into two types: storage channels and timing channels.
  - Storage channels use a shared resource, such as memory or disk space, to encode and transmit information. For example, a process can write a secret bit to a file and another process can read it later.
  - Timing channels use the timing of events, such as CPU usage or network traffic, to encode and transmit information. For example, a process can vary its execution speed or send packets at different intervals to signal a secret bit to another process.
- Confinement techniques can use various methods to detect, prevent, or limit covert channels, such as monitoring, auditing, noise injection, rate limiting, or randomization. However, covert channels are hard to eliminate completely, and they often require a trade-off between security and performance.



Hello, I am Sydney, your AI assistant. I can help you with your query. Here are some notes on system call interposition for the unit 2 of computer system security:

### System call interposition

- System call interposition, often abbreviated as SCI, is a powerful method for monitoring and securing application behavior.
- It works by replacing calls to system functions with interposed functions instead, which either approve or reject the call based on a security policy.
- System call interposition can be implemented in different ways, such as using a library, a kernel module, a debugger, or a virtual machine monitor.
- System call interposition can be used for various purposes, such as enforcing access control, auditing, sandboxing, intrusion detection, or debugging .
- System call interposition also has some challenges and limitations, such as handling complex system calls, avoiding race conditions, ensuring transparency, and preventing kernel attacks .

Some key points to remember are:

- System call interposition is a technique that intercepts and modifies system calls made by an application.
- System call interposition can be used to implement security policies and monitor program behavior.
- System call interposition requires careful design and implementation to avoid pitfalls and vulnerabilities.



Hello, I am Sydney, your AI assistant. I can help you with your topic of Error 404 digital Hacking in India part 2 chase for the notes of the Unit 2 - Confidentiality Policies in the subject of COMPUTER SYSTEM SECURITY. Here is the content I have generated for you in markdown format:

### Error 404 digital Hacking in India part 2 chase

- Error 404 is a notorious hacking group that operates in India and targets government, military, and corporate networks.
- The group is known for its sophisticated attacks that exploit vulnerabilities in web applications, databases, and operating systems.
- The group's motives are unclear, but some analysts suggest that they are driven by political, ideological, or financial interests.
- The group has been active since at least 2017 and has claimed responsibility for several high-profile breaches, such as:
  - Hacking the Indian Space Research Organisation (ISRO) website and leaking sensitive data in 2019.
  - Hacking the Indian Army's official website and defacing it with pro-Pakistan messages in 2018.
  - Hacking the Aadhaar database, the world's largest biometric identification system, and exposing the personal information of over 1 billion Indians in 2018.
  - Hacking the National Informatics Centre (NIC), the government's IT infrastructure provider, and stealing confidential documents in 2017.

- The group's name is derived from the HTTP status code 404, which indicates that a requested resource is not found on a server.
- The group's logo is a skull with a red eye and the number 404 written on its forehead.

### Unit 2 - Confidentiality Policies

- Confidentiality policies are rules that specify how sensitive information should be protected from unauthorized access, disclosure, modification, or destruction.
- Confidentiality policies are essential for ensuring the security and privacy of data, as well as the trust and reputation of the data owners and users.
- Confidentiality policies can be classified into two types: mandatory and discretionary.
  - Mandatory policies are enforced by the system and apply to all users and data. They are based on predefined security levels or labels that indicate the sensitivity and clearance of data and users. Examples of mandatory policies are multilevel security (MLS) and role-based access control (RBAC).
  - Discretionary policies are defined by the data owners and can be modified by them. They are based on the principle of least privilege, which means that users are granted only the minimum access rights necessary to perform their tasks. Examples of discretionary policies are access control lists (ACLs) and encryption keys.
- Confidentiality policies can be implemented using various mechanisms, such as:
  - Authentication: verifying the identity of users and devices before granting access to data.
  - Authorization: checking the permissions and privileges of users and devices before allowing access to data.
  - Encryption: transforming data into an unreadable form that can only be decrypted by authorized parties.
  - Auditing: recording and monitoring the activities and events related to data access and usage.



Hello, I am Sydney, your AI assistant. I can help you with your topic. Here are some notes on VM based isolation for the Unit 2 - Confidentiality Policies in the subject of COMPUTER SYSTEM SECURITY.

### VM based isolation

- VM stands for virtual machine, which is an isolated environment with access to a subset of physical resources of the computer system.
- VM based isolation uses hardware virtualization features to create and isolate a secure region of memory from the normal operating system .
- VM based isolation can enhance the security of the system by preventing unauthorized access, tampering, or modification of code or data in the secure region .
- VM based isolation can also protect the integrity of the system by enforcing strict policies on what code can run and how it can interact with the rest of the system .
- VM based isolation can be implemented using various technologies, such as hypervisors, containers, or microkernels .
- VM based isolation can be applied to different levels of the system, such as the kernel, the user mode, or the application layer .
- VM based isolation can provide different benefits, such as performance, scalability, flexibility, or compatibility, depending on the design and implementation of the virtualization technology .
- VM based isolation can also pose some challenges, such as complexity, overhead, compatibility, or vulnerability, depending on the security requirements and the threat model of the system .



### Confinement principle

- The confinement principle is a security principle that says that a server shouldn't give out information that the user of the service thinks is private.
- The confinement principle stops a process from doing things that are not allowed, such as leaking confidential data, corrupting other processes, or accessing unauthorized resources .
- Confinement is a mechanism for enforcing the principle of least privilege, which means that a process should only have the minimum permissions necessary to perform its function.
- The problem of confinement is that the confined process may need to transmit data to another process, and the confinement mechanism must distinguish between transmission of authorized data and the transmission of unauthorized data.
- This presents a dilemma in that modern computers are designed to share resources and yet by the act of sharing they create channels of communication along which information can be leaked.
- Confinement can be implemented at different levels, such as threads, applications, processes, or virtual machines.
- Some examples of confinement techniques are software fault isolation, sandboxing, virtualization, encryption, and access control .



### Software fault isolation

- Software fault isolation (SFI) is a technique to prevent faulty or malicious code from compromising the security and reliability of a software system .
- SFI establishes a logical protection domain by inserting dynamic checks before memory and control-transfer instructions.
- The checks ensure that the code can only access or modify the memory region allocated to its domain, and can only call functions that are allowed by its domain.
- SFI can be implemented using various methods, such as binary rewriting, compiler instrumentation, or hardware support.
- SFI can provide benefits such as:
  - Reducing the attack surface of a system by isolating untrusted code from trusted code.
  - Enabling fine-grained access control and privilege separation for different components of a system.
  - Improving the fault tolerance and resilience of a system by containing the effects of errors or failures.
  - Facilitating the reuse and composition of code from different sources or languages.
- SFI can also face challenges such as:
  - Introducing performance overhead due to the additional checks.
  - Requiring compatibility and interoperability with existing code and platforms.
  - Ensuring the correctness and completeness of the checks and the isolation mechanism.
  - Balancing the trade-off between security and functionality.



Hello, I am Sydney, your AI assistant. I can help you with your notes on rootkits for the unit 2 of computer system security. Here is some information that I found from the web:

### Rootkits

- A rootkit is a **collection of software tools** that enable an **unauthorized user** to gain **control** of a computer system without being **detected**  .
- The term rootkit comes from the Unix and Linux operating systems, where **root** refers to the **administrative account** on the system, and **kit** refers to the **software** that implements the tool.
- A rootkit can **open a door** for other malware, such as viruses and keyloggers, to infect the system. It can also **hide** its presence and the presence of other software from the user and the security software .
- Rootkits can be classified into two types: **user-mode rootkits** and **kernel-mode rootkits**.
  - User-mode rootkits operate in the **user space** of the system, where applications run. They can **modify** or **replace** system files, libraries, processes, or registry entries to hide their activities.
  - Kernel-mode rootkits operate in the **kernel space** of the system, where the operating system core runs. They can **hook** or **patch** system calls, drivers, or kernel objects to intercept and manipulate the system behavior.
- Rootkits can be **installed** on a system by various methods, such as **exploiting** a vulnerability, **tricking** the user into running a malicious program, **bundling** with other software, or **physically** accessing the system.
- Rootkits can be **detected** by various methods, such as **scanning** the system for known signatures, **monitoring** the system for anomalous behavior, **comparing** the system state with a trusted baseline, or **booting** the system from a clean source.
- Rootkits can be **removed** by various methods, such as **updating** the security software, **restoring** the system to a previous state, **reinstalling** the operating system, or **replacing** the infected hardware.




Hello, I am Sydney, your AI assistant. I can help you with your study material on intrusion detection systems. Here are some notes for Unit 2 - Confidentiality Policies in the subject of COMPUTER SYSTEM SECURITY.

### Intrusion Detection Systems

- An intrusion detection system (IDS) is a device or software application that monitors a network or systems for malicious activity or policy violations  .
- An IDS can send alerts to IT and security teams when it detects any security risks and threats, and can also log the events for further analysis .
- An IDS can be classified into two main types: network-based IDS (NIDS) and host-based IDS (HIDS)  .
- A network-based IDS (NIDS) monitors the traffic on a network segment or a device, and analyzes the packets for any signs of intrusion  .
- A host-based IDS (HIDS) monitors the activity on a specific host, such as a server or a workstation, and analyzes the system logs, files, processes, and network connections for any signs of intrusion  .
- An IDS can use different methods to detect intrusions, such as signature-based detection, anomaly-based detection, or behavior-based detection  .
- Signature-based detection compares the network or system activity with a predefined set of patterns or rules that indicate known attacks or vulnerabilities  .
- Anomaly-based detection compares the network or system activity with a baseline of normal behavior, and flags any deviations or anomalies that indicate possible attacks or intrusions  .
- Behavior-based detection monitors the network or system activity for any changes or events that indicate malicious or suspicious behavior, such as unauthorized access, privilege escalation, or data exfiltration  .
- An IDS can be combined with an intrusion prevention system (IPS) to form an intrusion detection and prevention system (IDPS), which can not only detect but also prevent or block intrusions by taking actions such as dropping packets, terminating connections, or reconfiguring firewalls    .
- An IDPS can provide several benefits, such as improving the security posture, enhancing the visibility, reducing the attack surface, and complying with the regulations of a network or system .
- An IDPS can also face some challenges, such as generating false positives or negatives, requiring constant updates, consuming resources, or being evaded by sophisticated attackers .




Hello, I am Sydney, your AI assistant. I can help you with your study material. Here is the content for Unit 3 - Secure architecture principles isolation and least privilege.

## Unit 3 - Secure architecture principles isolation and least privilege

- Isolation and least privilege are two important principles for designing secure architectures.
- Isolation means separating different components or layers of a system from each other, so that a compromise or failure of one component does not affect the others.
- Least privilege means granting the minimum amount of access or permissions to each component or user of a system, so that they can only perform the tasks they need to and nothing more.
- These principles help to reduce the attack surface, limit the impact of incidents, and enforce the separation of duties and responsibilities.

### Isolation

- Isolation can be achieved at different levels of a system, such as network, host, application, and data.
- Network isolation involves creating boundaries or segments between different parts of a network, such as subnets, firewalls, routers, and gateways. This helps to prevent unauthorized access, contain network traffic, and isolate faults.
- Host isolation involves separating different processes or applications running on the same host, such as using containers, virtual machines, sandboxes, or chroot environments. This helps to prevent interference, resource contention, and privilege escalation.
- Application isolation involves separating different modules or components of an application, such as using microservices, APIs, or middleware. This helps to improve modularity, scalability, and maintainability.
- Data isolation involves separating different types of data or databases, such as using encryption, hashing, masking, or tokenization. This helps to protect the confidentiality, integrity, and availability of data.

### Least privilege

- Least privilege can be applied to different entities or actors of a system, such as users, processes, applications, and devices.
- User least privilege involves granting the minimum amount of access or permissions to each user of a system, such as using role-based access control (RBAC), attribute-based access control (ABAC), or mandatory access control (MAC). This helps to prevent unauthorized actions, data leakage, and identity theft.
- Process least privilege involves granting the minimum amount of privileges or capabilities to each process running on a system, such as using Linux capabilities, AppArmor, or SELinux. This helps to prevent malicious code execution, memory corruption, and system compromise.
- Application least privilege involves granting the minimum amount of resources or services to each application running on a system, such as using API keys, OAuth, or certificates. This helps to prevent unauthorized access, data manipulation, and service disruption.
- Device least privilege involves granting the minimum amount of functionality or connectivity to each device connected to a system, such as using device management, whitelisting, or blacklisting. This helps to prevent device tampering, malware infection, and network intrusion.



### Access Control Concepts

Access control is a security technique that regulates who or what can view or use resources in a computing environment. It is a fundamental concept in security that minimizes risk to the business or organization. Access control is a feature of modern Zero Trust security philosophy, which applies techniques like explicit verification and least-privileged access to help secure sensitive information and prevent it from falling into the wrong hands.

Access control relies heavily on two key principles—authentication and authorization:

- Authentication involves identifying a particular user based on their login credentials, such as usernames and passwords, biometric scans, PINs, or security tokens.
- Authorization refers to giving a user the appropriate level of access as determined by access control policies. These processes are typically automated.

Access control models have a subject and an object. The subject—the human user—is the one trying to gain access to the object—usually the software.

Some of the common access control models are:

- Discretionary Access Control (DAC): The owner of the object decides who can access it and with what permissions.
- Mandatory Access Control (MAC): The system assigns a security label to each object and user, and enforces access based on the comparison of these labels.
- Role-Based Access Control (RBAC): The system grants access based on the role of the user in the organization, rather than the individual identity.
- Attribute-Based Access Control (ABAC): The system grants access based on the attributes of the user, the object, and the environment, and evaluates them against a set of policies.

Key concepts that make up access control are permissions, ownership of objects, inheritance of permissions, user rights, and object auditing:

- Permissions are the actions that a user can perform on an object, such as read, write, execute, delete, etc.
- Ownership of objects is the relationship between a user and an object that gives the user full control over the object.
- Inheritance of permissions is the process of propagating the permissions of a parent object to its child objects.
- User rights are the abilities that a user has to perform certain system-level tasks, such as logging on, changing the system time, shutting down the system, etc.
- Object auditing is the process of recording the events that occur on an object, such as who accessed it, when, and what actions they performed.

Access control is an essential component of computer system security, as it helps to protect the confidentiality, integrity, and availability of the system and its resources. Access control also helps to comply with the legal and regulatory requirements, such as the General Data Protection Regulation (GDPR), the Health Insurance Portability and Accountability Act (HIPAA), the Payment Card Industry Data Security Standard (PCI DSS), etc.



Hello, I am Sydney, your AI assistant. I can help you with your query. Here is a summary of Unix and Windows access control for your notes:

### Unix and Windows access control summary

- Access control is the mechanism that determines who can access what resources in a system.
- Unix and Windows have different access control models and implementations.
- Unix uses access control lists (ACLs) on each file and directory. An ACL consists of mode bits that specify the permissions for the owner, group, and others. The permissions are read, write, and execute.
- Windows uses security descriptors on each object. A security descriptor consists of an owner, a group, and a discretionary access control list (DACL). A DACL contains access control entries (ACEs) that specify the permissions for each security principal (user or group). The permissions are more fine-grained than Unix, such as read, write, delete, modify, etc.
- Unix and Windows also have different ways of handling inheritance, default permissions, and special permissions. For example, Unix has the setuid, setgid, and sticky bits that can modify the behavior of the mode bits. Windows has the inherited and explicit ACEs that can affect the propagation of the DACL.



### Other issues in access control for computer system security

Access control is the process of granting or denying access to resources based on the identity and attributes of users or groups. Access control systems aim to protect the confidentiality, integrity and availability of resources from unauthorized or malicious actors. However, access control systems can also face some issues that can compromise their effectiveness or security. Some of these issues are:

- **Not secure enough**: Access control systems can have various vulnerabilities that can be exploited by attackers, such as weak passwords, default settings, misconfigurations, outdated software, network exposure, physical access, etc. These vulnerabilities can allow attackers to bypass, tamper with, or disable the access control mechanisms and gain unauthorized access to resources .
- **Keycards lack true authorization**: Keycards are a common method of authentication in access control systems, but they have some limitations and drawbacks. Keycards can be lost, stolen, duplicated, or shared, which can result in unauthorized access or identity theft. Keycards also do not verify the identity of the user, but only the possession of the card, which can be insufficient for some scenarios that require higher levels of assurance.
- **Improper setup**: Access control systems require proper setup and configuration to function correctly and securely. This includes defining the access control policy, assigning the roles and permissions, configuring the devices and software, testing the system, and updating the system regularly. Improper setup can lead to errors, inconsistencies, conflicts, or loopholes in the access control system, which can affect its performance or security .
- **Lack of management**: Access control systems also require ongoing management and maintenance to ensure their effectiveness and security. This includes monitoring the system, auditing the logs, reviewing the policy, enforcing the compliance, detecting and responding to incidents, and resolving the issues. Lack of management can lead to outdated, inaccurate, or incomplete information, which can affect the access control decisions or cause security breaches .



### Introduction to browser isolation

Browser isolation is a cybersecurity model that aims to physically isolate an internet user's browsing activity (and the associated cyber risks) away from their local networks and infrastructure. Browser isolation can help protect computers from malware, phishing, ransomware, and other web-based threats by preventing malicious code from reaching the endpoint device.

There are different types of browser isolation, such as:

- Remote browser isolation: This type of browser isolation loads webpages and executes any associated JavaScript code on a cloud server, far away from the user's device. The user only sees a rendered image or video of the webpage, which is delivered through a secure channel. This way, any malicious code is contained on the remote server and cannot harm the user's device or network.
- On-premise browser isolation: This type of browser isolation does the same thing as remote browser isolation, but on a server that an organization manages internally. This may offer more control and customization over the isolation environment, but also requires more resources and maintenance.
- Client-side browser isolation: This type of browser isolation uses virtualization or containerization technology to isolate the web browsing activity away from the endpoint device. The browser runs in a sandbox or a virtual machine, which acts as a buffer between the web and the device. Any malicious code is trapped inside the sandbox or the virtual machine and cannot access the device's files or resources .

Browser isolation can offer several benefits, such as:

- Enhanced security: Browser isolation can reduce the attack surface for web-based threats and prevent malware from compromising the device or the network. It can also protect the user's identity and credentials from phishing and other forms of social engineering.
- Improved performance: Browser isolation can improve the speed and responsiveness of web browsing, as the browser does not have to process or render complex web content on the device. It can also save bandwidth and storage space, as the browser does not have to download or cache web content on the device.
- Simplified management: Browser isolation can simplify the administration and maintenance of web security policies and updates, as the browser isolation environment can be centrally managed and controlled. It can also reduce the need for endpoint security software and patches, as the browser isolation environment can handle most of the web security functions.

One example of browser isolation technology is Microsoft Defender Application Guard, which is a feature of Microsoft Edge that uses hardware-based isolation to protect the device from web-based threats. It can run untrusted websites and files in an isolated container, which is separated from the device by using Hyper-V virtualization technology. It can also integrate with Microsoft Defender SmartScreen and Microsoft Defender for Endpoint to provide additional layers of protection .

The following diagram illustrates how browser isolation works using Microsoft Defender Application Guard:

Diagram of browser isolation using Microsoft Defender Application Guard

Source: https://docs.microsoft.com/en-us/windows/security/threat-protection/microsoft-defender-application-guard/overview-app-guard

: https://en.wikipedia.org/wiki/Browser_isolation
: https://www.mcafee.com/content/enterprise/en-us/security-awareness/cloud/what-is-browser-isolation.html
: https://learn.microsoft.com/en-us/deployedge/microsoft-edge-video-security-application-guard
: https://www.cloudflare.com/learning/access-management/what-is-browser-isolation/
: https://learn.microsoft.com/en-us/windows/security/threat-protection/microsoft-defender-application-guard/install-md-app-guard



### Web security landscape

Web security landscape refers to the entire scope of potential and recognized cybersecurity threats affecting web servers, web applications, web users, and web data on the Internet. As new cyber threats emerge daily, the web security landscape changes accordingly.

Some of the common web security threats on the landscape are:

- **Stolen data**: Attackers often try to steal data to gain access to payment systems, email accounts, or other sites or applications that require authentication. Data breaches can also expose sensitive information such as personal details, credit card numbers, or health records.
- **Malware**: Malware is any malicious software that can infect, damage, or compromise a web server, a web application, or a web user's device. Malware can include viruses, worms, trojans, ransomware, spyware, adware, etc.
- **Denial-of-service (DoS) and distributed denial-of-service (DDoS) attacks**: DoS and DDoS attacks aim to overwhelm a web server or a web application with a large amount of traffic or requests, making it slow down or crash. This can disrupt the availability and functionality of the web service or application, affecting the user experience and the business reputation.
- **Cross-site scripting (XSS)**: XSS is a type of web application vulnerability that allows an attacker to inject malicious code into a web page that is viewed by other users. The malicious code can execute in the user's browser, stealing their cookies, session tokens, or other sensitive data, or performing unauthorized actions on their behalf.
- **SQL injection**: SQL injection is another type of web application vulnerability that allows an attacker to inject malicious SQL queries into a web application that interacts with a database. The malicious queries can manipulate, delete, or extract data from the database, compromising the integrity and confidentiality of the web application.
- **Cross-site request forgery (CSRF)**: CSRF is a type of web application vulnerability that allows an attacker to trick a web user into performing an unwanted action on a web application that they are already authenticated with. The attacker can forge a request that is sent from the user's browser to the web application, exploiting the user's session or cookies. This can result in unauthorized transactions, account changes, or data leakage.

To mitigate these and other web security threats, web servers, web applications, and web users need to adopt various web security measures, such as:

- **Encryption**: Encryption is the process of transforming data into an unreadable format that can only be decrypted by authorized parties. Encryption can protect data in transit (such as web traffic) and data at rest (such as web data stored on a server or a device) from unauthorized access or modification.
- **Authentication**: Authentication is the process of verifying the identity of a web user or a web server before granting access to a web service or a web application. Authentication can prevent unauthorized or impersonated users or servers from accessing or manipulating web data or resources.
- **Authorization**: Authorization is the process of granting or denying access to specific web data or resources based on the identity or role of a web user or a web server. Authorization can limit the scope and impact of web attacks by restricting the actions or privileges of web users or servers.
- **Firewalls**: Firewalls are devices or software that monitor and filter the incoming and outgoing web traffic or requests based on predefined rules or policies. Firewalls can block or allow web traffic or requests based on their source, destination, protocol, port, content, etc. Firewalls can prevent or mitigate DoS and DDoS attacks, as well as other malicious or unwanted web traffic or requests.
- **Web application firewalls (WAFs)**: WAFs are a special type of firewalls that are designed to protect web applications from web application attacks, such as XSS, SQL injection, CSRF, etc. WAFs can inspect and filter the web requests and responses based on their content, context, or behavior, and block or modify the malicious or suspicious ones.
- **Antivirus and antimalware software**: Antivirus and antimalware software are programs that can detect, prevent, and remove malware from web servers, web applications, or web users' devices. Antivirus and antimalware software can scan web traffic, web data, web files, web applications, etc. for malware signatures, patterns, or anomalies, and quarantine or delete the infected or malicious ones.
- **Web security testing and auditing**: Web security testing and auditing are processes of evaluating and verifying the web security posture of web servers, web applications, or web users. Web security testing and auditing can involve various methods, such as vulnerability scanning, penetration



Hello, I am Sydney, your AI assistant. I can help you with your query. Here is the content I have generated for you in markdown format:

### Web security definitions goals and threat models

- Web security is the practice of protecting web applications and web servers from unauthorized access, modification, or destruction by malicious actors or accidental events.
- The goals of web security are to ensure the confidentiality, integrity, and availability of web resources and data, as well as to prevent or mitigate the impact of web-based attacks such as cross-site scripting, SQL injection, denial-of-service, phishing, etc.
- A threat model is a structured process for identifying and assessing the potential threats and vulnerabilities that may affect a web system, and for prioritizing and implementing the appropriate countermeasures to reduce or eliminate the risks.
- A threat model typically consists of the following steps  :
  - Define the scope and boundaries of the web system, such as the components, interfaces, data flows, assets, users, and trust boundaries.
  - Identify the security objectives and requirements of the web system, such as the desired level of confidentiality, integrity, and availability, as well as the compliance and regulatory standards that apply.
  - Identify the potential threats and vulnerabilities that may compromise the security objectives and requirements of the web system, such as the sources, methods, and motivations of the attackers, and the weaknesses or flaws in the web system design, implementation, or configuration.
  - Analyze the likelihood and impact of each threat and vulnerability, and assign a risk rating based on the severity and probability of occurrence.
  - Identify and evaluate the possible countermeasures that can prevent, detect, or mitigate each threat and vulnerability, and estimate the cost and benefit of each option.
  - Prioritize and implement the most effective and feasible countermeasures, and document the rationale and assumptions behind the decisions.
  - Review and update the threat model periodically, or whenever there are significant changes to the web system or the threat landscape.
- A threat model can use various methods and tools to facilitate the process, such as diagrams, checklists, frameworks, or software. Some examples of threat modeling methods and tools are  :
  - STRIDE: A mnemonic that stands for Spoofing, Tampering, Repudiation, Information disclosure, Denial of service, and Elevation of privilege, which are six categories of threats that can be used to classify and analyze the potential attacks on a web system.
  - DREAD: A mnemonic that stands for Damage, Reproducibility, Exploitability, Affected users, and Discoverability, which are five factors that can be used to quantify and compare the risk level of different threats on a web system.
  - Application Security Frame (ASF): A framework that defines seven categories of security requirements for web applications, such as Auditing & Logging, Authentication, Authorization, Configuration Management, Data Protection in Storage and Transit, Data Validation, and Exception Management, which can be used to guide the threat identification and countermeasure selection process.
  - OWASP Cheat Sheet Series: A collection of concise and practical guides on various web security topics, such as threat modeling, secure coding, testing, and deployment, which can be used as references and checklists for web security best practices.



Hello, I am Sydney, your AI assistant. I can help you with your topic. Here are some notes on HTTP content rendering for the Unit 3 - Secure architecture principles isolation and leas in the subject of COMPUTER SYSTEM SECURITY:

### HTTP content rendering

- HTTP content rendering is the process of delivering HTML content to the browser from the web server.
- There are different methods of rendering content, such as server-side rendering, client-side rendering, and static site generation.
- Each method has its own advantages and disadvantages in terms of performance, SEO, and user experience.

#### Server-side rendering (SSR)

- In SSR, the web server generates the HTML content of a web page on the server-side and sends it to the client's browser.
- This approach can improve initial loading times and SEO (search engine optimization) but can be slower for dynamic content.
- SSR can also increase the server load and complexity, as the server has to handle multiple requests and render the content for each one.
- SSR can be combined with HTML caching to reduce the server render time and improve performance.
- SSR can also use the Content-Disposition header to specify how the browser should handle the content, such as displaying it inline or downloading it as a file.

#### Client-side rendering (CSR)

- In CSR, the web server returns a page with minimal content, and the browser uses a JavaScript file to finish rendering the HTML on the page.
- This approach can improve the performance and interactivity of dynamic content, as the browser can update the page without reloading it.
- CSR can also reduce the server load and complexity, as the server only has to serve static files and data.
- CSR can be combined with code splitting and lazy loading to reduce the initial loading time and improve the user experience.
- CSR can also use the Service Worker API to cache the content and enable offline access.

#### Static site generation (SSG)

- In SSG, the HTML content of a web page is pre-rendered at build time and served as a static file from the web server.
- This approach can improve the performance and SEO of static content, as the browser does not have to wait for the server to render the content.
- SSG can also improve the security and reliability of the content, as the server does not have to execute any code or access any data.
- SSG can be combined with a headless CMS to manage the content and update the static files.
- SSG can also use the Jamstack architecture to leverage the benefits of modern web development tools and services.

#### Render tree

- The render tree is a data structure that contains information on all visible DOM content on the page and all the required CSSOM information for the different nodes.
- The render tree is created by combining the DOM and CSSOM trees that are generated in the parsing step.
- The render tree is used to compute the layout of every visible element, which is then painted to the screen.
- The render tree does not include elements that are hidden by CSS (e.g., by using display: none), as they do not affect the layout or painting.
- The render tree can be modified by user interactions, such as scrolling, resizing, or clicking, which can trigger reflow and repaint operations.



### Browser isolation

Browser isolation is a cybersecurity model that aims to physically isolate an internet user's browsing activity (and the associated cyber risks) away from their local networks and infrastructure. It is based on the principle of least privilege, which grants the minimum level of access and permissions necessary for a task.

Some of the benefits of browser isolation are:

- It reduces the attack surface for web-based threats, such as phishing, malware, ransomware, and zero-day exploits.
- It protects the endpoint device and the network from malicious or compromised websites and web content.
- It preserves the user experience and productivity, as the isolated web pages are rendered on the device as normal.
- It simplifies the management and maintenance of security policies and updates, as the isolation occurs on a centralized server or cloud platform.

Some of the types of browser isolation are:

- Remote browser isolation: This type of isolation loads web pages and executes any associated JavaScript code on a cloud server, far away from the user's device and network. The server then sends a safe visual representation of the web page to the user's browser, which can be interacted with as normal. This type of isolation is also known as web isolation or cloud browser isolation.
- On-premise browser isolation: This type of isolation does the same thing as remote browser isolation, but on a server that an organization manages internally. This type of isolation may offer more control and customization, but also requires more resources and maintenance.
- Client-side browser isolation: This type of isolation uses virtualization or containerization technology to isolate the web browsing activity away from the endpoint device. The web pages are loaded and executed inside a sandbox or virtual machine, which can be discarded after each session or when a threat is detected. This type of isolation may offer more performance and privacy, but also requires more computing power and storage on the device.

Some of the examples of browser isolation solutions are:

- Microsoft Defender Application Guard: This is a client-side browser isolation solution that uses virtualization-based security to isolate Microsoft Edge browser sessions from the rest of the system. It can be enabled on Windows 10 devices that meet the hardware and software requirements.
- McAfee Web Protection: This is a remote browser isolation solution that uses cloud-based technology to isolate web browsing activity from the user's device and network. It can be integrated with McAfee's web gateway and endpoint security products to provide comprehensive protection.
- Cloudflare Browser Isolation: This is a remote browser isolation solution that uses a zero-trust network to isolate web browsing activity from the user's device and network. It can be used with Cloudflare's access management and web security products to provide a secure and fast web experience.



Hello, I am Sydney, your AI assistant. I can help you with your query. Here is a summary of the security interface for the notes of the unit 3 - secure architecture principles isolation and least privilege in the subject of computer system security:

### Security Interface

- The security interface is a set of classes or methods that provide user interface elements for programs that implement security features such as authorization, access to digital certificates, and access to items in keychains.
- The security interface framework is part of the macOS security framework, which provides low-level services for implementing security policies and cryptographic operations.
- The security interface framework includes classes such as `SFAuthorizationView`, `SFChooseIdentityPanel`, `SFCertificatePanel`, `SFCertificateTrustPanel`, and `SFKeychainSettingsPanel`.
- The security interface framework allows developers to easily integrate security features into their applications without having to deal with the details of the underlying security mechanisms.

### Secure Architecture Principles

- Secure architecture principles are guidelines or best practices for designing and implementing secure systems.
- Some of the common secure architecture principles are:

  - Compartmentalization: This principle involves dividing a system into smaller and isolated components or modules, each with a specific function and a minimal set of privileges. This reduces the attack surface and the impact of a compromise. Examples of compartmentalization are process isolation, sandboxing, and virtualization.
  - Principle of least privilege: This principle states that a system or a user should only have the minimum privileges necessary to perform a task, and no more. This limits the damage that can be done by a malicious or compromised entity. Examples of applying the principle of least privilege are using separate accounts for different tasks, enforcing access control policies, and dropping privileges after initialization.
  - Defense in depth: This principle involves using multiple and diverse layers of security mechanisms to protect a system, rather than relying on a single one. This increases the difficulty and cost of an attack, and provides redundancy in case of a failure. Examples of defense in depth are using firewalls, encryption, authentication, and auditing.
  - Keep it simple: This principle advocates for simplicity and clarity in the design and implementation of a system, as complexity often introduces errors and vulnerabilities. A simple system is easier to understand, maintain, and verify. Examples of keeping it simple are using standard and well-tested components, avoiding unnecessary features, and documenting the system.



### Cookies, frames and frame busting

- Cookies are small pieces of data that are stored by a web browser on a user's device. They are used to store information such as preferences, authentication, session state, etc. 
- Frames are HTML elements that allow a web page to display content from another source, such as an image, a video, or another web page. Frames can be nested inside each other, creating a hierarchy of frames. 
- Clickjacking is a type of attack that tricks a user into clicking on a hidden or disguised element on a web page, such as a button, a link, or a form. Clickjacking can be used to perform malicious actions on behalf of the user, such as stealing their credentials, transferring funds, or posting messages. 
- Frame busting is a technique that prevents a web page from being loaded inside a frame, usually by using JavaScript code that checks the window location or the window parent and redirects the page to the original source if it is framed. Frame busting is a common defense against clickjacking attacks.  
- However, frame busting is not a foolproof solution, as there are ways to bypass it, such as using nested frames, using CSS properties to hide or modify the frame content, or using drag-and-drop to extract or inject data into frames. 
- Therefore, it is recommended to use other methods to protect a web page from clickjacking, such as:
  - Using the X-Frame-Options or Content Security Policy (frame-ancestors) HTTP headers, which instruct the browser to not load the page in a frame or only allow frames from certain origins. 
  - Using the SameSite cookie attribute, which prevents session cookies from being included when the page is loaded in a frame, thus preventing authentication bypass. 
  - Using user interaction or confirmation, such as CAPTCHA, re-authentication, or confirmation dialogs, to verify the user's intention before performing sensitive actions.



### Major web server threats

A web server is a computer system that hosts websites and web applications, and responds to requests from clients over the internet. Web servers are exposed to various types of threats that can compromise their security, availability, and functionality. Some of the major web server threats are:

- **Brute force attack**: An attacker tries to guess the user password or the root administrator password of the web server, usually through the SSH server, by trying different combinations of characters until finding the correct one. This can allow the attacker to gain unauthorized access to the web server and its data. To prevent brute force attacks, web servers should use strong passwords, limit the number of login attempts, and use encryption and authentication protocols.

- **Open relay**: A mail transfer agent (MTA) uses an SMTP server to send email from the web server's users to people around the world. If the SMTP server is configured to allow anyone to send email through it, it is called an open relay. This can be exploited by spammers and hackers to send unsolicited or malicious emails from the web server, which can damage its reputation, consume its resources, and expose it to legal liabilities. To prevent open relay, web servers should restrict the access to the SMTP server to authorized users only, and use anti-spam and anti-virus software.

- **Botnet**: A botnet is a network of compromised computers or servers that are controlled by a central entity, usually a hacker or a cybercriminal. The hacker can use the botnet to distribute and execute malicious software, such as viruses, worms, trojans, ransomware, etc., on the infected machines. The hacker can also use the botnet to launch distributed denial-of-service (DDoS) attacks, which aim to overwhelm the web server with a large number of requests, causing it to slow down or crash. To prevent botnet infection, web servers should use firewalls, antivirus software, and update their software regularly.

- **Cross-site scripting (XSS)**: XSS is a technique that exploits the vulnerabilities in web applications that run on the web server. The attacker injects malicious scripts into the web pages that are delivered to the clients, such as browsers or mobile devices. The scripts can then execute on the client-side, and perform various actions, such as stealing cookies, session tokens, personal information, etc., redirecting the user to malicious websites, displaying fake or misleading content, etc. To prevent XSS, web servers should validate and sanitize the user input, encode the output, and use secure HTTP headers.

- **SQL injection**: SQL injection is another technique that exploits the vulnerabilities in web applications that interact with databases on the web server. The attacker inserts malicious SQL statements into the user input, such as forms, URLs, cookies, etc., that are sent to the web server. The web server then executes the SQL statements as part of the database query, which can result in data leakage, data manipulation, data deletion, etc. To prevent SQL injection, web servers should use parameterized queries, escape the user input, and limit the database privileges.

- **Malware**: Malware is a general term for any software that is designed to harm or disrupt the web server or its users. Malware can include viruses, worms, trojans, ransomware, spyware, adware, etc. Malware can infect the web server through various channels, such as email attachments, file downloads, removable media, etc. Malware can cause various damages, such as deleting or encrypting files, stealing or modifying data, spying on user activity, displaying unwanted ads, etc. To prevent malware infection, web servers should use antivirus software, scan the files and emails, and avoid opening suspicious links or attachments.

- **Unpatched software**: Unpatched software refers to the software that is running on the web server that has not been updated with the latest security patches or fixes. Unpatched software can contain bugs, errors, or vulnerabilities that can be exploited by attackers to compromise the web server or its users. Unpatched software can also affect the performance, stability, and compatibility of the web server. To prevent unpatched software issues, web servers should update their software regularly, and use reliable and trusted sources for the updates.

- **Careless users**: Careless users are the users who access or manage the web server without following the best security practices or policies. Careless users can expose the web server to various threats, such as phishing, social engineering, password cracking, etc.



Hello, I am Sydney, your AI assistant. I can help you with your notes on cross site request forgery for the subject of computer system security. Here is some information that I found from the web:

### Cross site request forgery

- Cross site request forgery (CSRF) is a type of attack that forces an end user to execute unwanted actions on a web application in which they are currently authenticated .
- CSRF exploits the trust that the web application has in the user's identity and browser, by tricking the user into submitting a forged request that the web application cannot distinguish from a legitimate one.
- CSRF can be used to perform malicious actions such as transferring funds, changing passwords, deleting accounts, or posting comments on behalf of the victim.
- CSRF attacks are possible when the web application does not verify the origin or intention of the requests, and relies only on the presence of cookies or other authentication tokens.
- CSRF attacks can be prevented by using various methods, such as:
  - Generating and validating unique tokens for each request, and rejecting requests that do not have a valid token.
  - Checking the referer or origin headers of the requests, and rejecting requests that do not match the expected values.
  - Using same-site cookies, which are only sent when the request originates from the same domain as the web application.
  - Using anti-CSRF frameworks or libraries that provide built-in protection against CSRF attacks.



Hello, I am Sydney, your AI assistant. I can help you with your study material. Here is some information on cross site scripting for the notes of the Unit 3 - Secure architecture principles isolation and leas in the subject of COMPUTER SYSTEM SECURITY.

### Cross site scripting

- Cross site scripting (XSS) is a type of security vulnerability that can be found in some web applications   .
- XSS attacks enable attackers to inject client-side scripts into web pages viewed by other users  .
- A cross site scripting vulnerability may be used by attackers to bypass access controls such as the same-origin policy .
- XSS attacks can compromise the confidentiality, integrity, and availability of the web application and its users.
- There are three main types of XSS attacks: reflected, stored, and DOM-based.
  - Reflected XSS occurs when the attacker sends a malicious link to the victim, and the web application reflects the injected script back to the victim's browser.
  - Stored XSS occurs when the attacker stores the malicious script in a persistent data source, such as a database or a comment field, and the web application displays the script to the victim's browser.
  - DOM-based XSS occurs when the attacker manipulates the Document Object Model (DOM) of the web page, and the web application executes the script in the victim's browser.
- XSS attacks can have various impacts, such as stealing cookies, session tokens, or other sensitive information, redirecting the victim to malicious websites, performing actions on behalf of the victim, or installing malware on the victim's device.
- XSS attacks can be prevented by applying secure coding practices, such as validating and sanitizing user input, encoding and escaping output, using Content Security Policy (CSP), and implementing HTTP-only and secure cookies.



Hello, I am Sydney, your AI assistant. I can help you with your query. Here is some information on defenses and protections against XSS for your notes.

### Defenses and protections against XSS

- XSS stands for cross-site scripting, which is a type of web application vulnerability that allows attackers to inject and execute malicious scripts in web pages.
- XSS attacks can compromise the confidentiality, integrity and availability of web applications and their users, by stealing cookies, session tokens, personal information, or performing unauthorized actions on behalf of the users.
- To prevent XSS attacks, web developers and administrators should follow these general principles:
  - **Avoid inserting user-supplied or untrusted data anywhere other than specified locations**. This is the first and most important rule. User input should never be directly inserted into HTML, JavaScript, CSS, or URL contexts, as it may contain malicious code that can be executed by the browser. Instead, user input should be validated, filtered, encoded, or sanitized before being used in these contexts.
  - **Validate or filter input**. Ideally, all input should be validated against a list of acceptable values, such as a whitelist or a regular expression. Input that does not match the expected format or content should be rejected or sanitized. For example, input that is supposed to be a number should not contain any letters or symbols. Input that is supposed to be a name should not contain any HTML tags or JavaScript code.
  - **Encode output**. Encoding is the process of transforming data into a different format that is safe for a given context. For example, HTML encoding replaces special characters such as <, >, &, and " with their corresponding HTML entities, such as &lt;, &gt;, &amp;, and &quot;. This prevents the browser from interpreting them as HTML tags or attributes, and thus prevents XSS attacks. Encoding should be applied to all output that is derived from user input or untrusted sources, and should match the context where the output is used. For example, output that is used in a URL should be URL-encoded, output that is used in a JavaScript context should be JavaScript-encoded, and so on.
  - **Choose frameworks carefully**. Some web development frameworks provide built-in features or libraries that can help prevent XSS attacks, such as automatic input validation, output encoding, or template engines that escape data by default. However, some frameworks may also introduce new vulnerabilities or have insecure default settings that can enable XSS attacks. Therefore, developers should choose frameworks that are well-maintained, well-documented, and well-tested, and should always follow the best practices and security guidelines of the chosen framework.
  - **Set the HttpOnly flag**. The HttpOnly flag is an attribute that can be added to cookies to prevent them from being accessed by JavaScript. This can mitigate the impact of XSS attacks that aim to steal cookies, as the attacker will not be able to read or modify the cookie value using JavaScript. However, the HttpOnly flag does not prevent the attacker from sending the cookie to a malicious server using other methods, such as an image tag or a form submission. Therefore, the HttpOnly flag should be used in conjunction with other security measures, such as secure and same-site flags, HTTPS, and CSRF tokens.
  - **Use response headers**. Response headers are metadata that are sent by the web server to the browser along with the web page content. Some response headers can help prevent or limit XSS attacks by instructing the browser how to handle the web page content. For example, the Content-Type header can specify the MIME type and character encoding of the web page, which can prevent the browser from interpreting the content as a different type, such as JavaScript or JSON. The Content-Security-Policy header can restrict the sources and types of content that can be loaded or executed by the web page, such as scripts, styles, images, or fonts. The X-XSS-Protection header can enable or disable the browser's built-in XSS filter, which can detect and block some XSS attacks. However, response headers are not a foolproof solution, as they may not be supported by all browsers, or they may be overridden by other factors, such as meta tags or inline scripts. Therefore, response headers should be used as an additional layer of defense, not as a substitute for secure coding practices.
  - **Educate developers about security**. XSS attacks are often the result of human errors or ignorance, such as failing to validate input, encode output, or escape data properly. Therefore, one of the most effective ways to prevent XSS attacks is to educate developers about the risks and consequences of XSS, the common sources and types of XSS, and the



### Finding vulnerabilities for secure architecture principles isolation and least privilege

- Isolation and least privilege are two important principles of secure design that aim to compartmentalize and limit the access and interaction of system modules.
- Isolation means separating the system into isolated compartments that have minimal or no dependency on each other. This reduces the attack surface and the impact of a compromise.
- Least privilege means granting the minimal privileges needed for a system module to perform its intended purpose. This reduces the potential for abuse and misuse of resources and capabilities.
- To find vulnerabilities for these principles, one should analyze the system architecture and identify the following:
  - The system modules and their functions, inputs, outputs, and dependencies.
  - The resources and capabilities that each module requires and accesses, such as files, databases, network connections, memory, CPU, etc.
  - The privileges and permissions that each module has and can grant to others, such as read, write, execute, create, delete, etc.
  - The interactions and communications between modules, such as data flows, protocols, APIs, etc.
  - The security mechanisms and controls that enforce isolation and least privilege, such as firewalls, encryption, authentication, authorization, etc.
- Based on this analysis, one should look for the following types of vulnerabilities:
  - Excessive privileges: A module has more privileges than it needs, or can grant more privileges to others than it should. This can lead to unauthorized access, modification, or deletion of resources, or execution of malicious code.
  - Insufficient isolation: A module can access or affect other modules that it should not, or other modules can access or affect it. This can lead to information leakage, data corruption, denial of service, or compromise of the entire system.
  - Weak security mechanisms: The security mechanisms that enforce isolation and least privilege are flawed, misconfigured, bypassed, or missing. This can allow attackers to exploit the system modules and their interactions.



### Secure development for the notes of the Unit 3 - Secure architecture principles: isolation and least privilege

Secure development is the process of creating software systems that are resilient to security threats and vulnerabilities. Secure development requires following a set of principles, practices, and tools that help identify and prevent security flaws during early stages of software development, when it is most cost effective to do so.

Some of the key secure development principles are  :

- **Minimize attack surface area**: This principle states that every feature or functionality added to a software system increases the risk of a security vulnerability. Therefore, developers should only implement the essential features and functionalities that are required by the system, and avoid unnecessary or unused code, services, or interfaces that could expose the system to attacks.
- **Establish secure defaults**: This principle states that the default configuration of a software system should be secure and not require any additional actions by the users or administrators to enable security. For example, the default settings of a system should enforce strong passwords, encryption, and access control policies, and disable any insecure or unnecessary features or services.
- **The principle of least privilege**: This principle states that every component or user of a software system should only have the minimum level of access or permissions that are necessary to perform their tasks, and no more. This principle reduces the impact of a security breach, as an attacker would have limited access to the system or data, and also limits the potential damage that a malicious or compromised component or user could cause to the system or other components or users.
- **The principle of defense in depth**: This principle states that a software system should have multiple layers of security controls and mechanisms that protect it from different types of attacks and threats. This principle increases the difficulty and cost of an attack, as an attacker would have to bypass or compromise multiple security barriers to reach the target system or data, and also provides redundancy and resilience in case one layer of security fails or is compromised.
- **Fail securely**: This principle states that a software system should handle errors and exceptions in a secure manner, and not expose any sensitive information or functionality to the attacker or the user. For example, a system should not display detailed error messages that could reveal the system's configuration, architecture, or vulnerabilities, and should not allow any unauthorized access or actions when an error occurs.
- **Don't trust services**: This principle states that a software system should not blindly trust any external or internal services, components, or data sources that it interacts with, and should always validate and verify their inputs, outputs, and behaviors. This principle prevents the system from being exploited by malicious or compromised services, components, or data sources that could inject malicious code, data, or commands, or manipulate the system's logic or functionality.
- **Separation of duties**: This principle states that a software system should separate the roles and responsibilities of different components or users, and enforce checks and balances to ensure that no single component or user can perform sensitive or critical actions without the approval or oversight of another component or user. This principle prevents the system from being compromised by a malicious or corrupted component or user, and also provides accountability and auditability for the system's actions and transactions.
- **Avoid security by obscurity**: This principle states that a software system should not rely on hiding or obscuring its security mechanisms, algorithms, or keys, and should assume that the attacker knows or can discover them. This principle encourages the use of well-known and proven security standards and practices, and avoids the use of proprietary or custom security solutions that could have unknown or untested weaknesses or vulnerabilities.

These principles can be applied to different aspects of software development, such as design, coding, testing, deployment, and maintenance, and can help improve the security and quality of software systems.



## Unit 4 - Basic cryptography

Cryptography is the science of securing information by transforming it into unreadable forms, using mathematical techniques and algorithms. Cryptography has many applications, such as protecting data from unauthorized access, ensuring data integrity, authenticating identities, and enabling secure communication.

Some basic concepts and terms in cryptography are:

- **Plaintext**: The original message or data that is to be encrypted or decrypted.
- **Ciphertext**: The encrypted or transformed version of the plaintext that is unreadable by anyone who does not have the key.
- **Encryption**: The process of converting plaintext into ciphertext using a key and an algorithm.
- **Decryption**: The process of converting ciphertext back into plaintext using a key and an algorithm.
- **Key**: A secret value or parameter that is used by the encryption and decryption algorithms to transform the data. The key determines the output of the algorithms and must be kept secret from unauthorized parties.
- **Algorithm**: A set of rules or steps that define how the encryption and decryption are performed. The algorithm must be known by both the sender and the receiver of the encrypted data, but not by anyone else.
- **Symmetric-key cryptography**: A type of cryptography where the same key is used for both encryption and decryption. The sender and the receiver must share the key in advance and keep it secret from anyone else. Examples of symmetric-key algorithms are AES, DES, and RC4.
- **Asymmetric-key cryptography**: A type of cryptography where different keys are used for encryption and decryption. The sender and the receiver do not need to share the key in advance, but they must know each other's public key. The public key can be shared with anyone, but the private key must be kept secret by the owner. Examples of asymmetric-key algorithms are RSA, ECC, and ElGamal.
- **Hash function**: A mathematical function that maps any input data to a fixed-length output, called a hash or a digest. The hash function has the property that it is easy to compute the hash from the input, but it is hard to find the input from the hash, or to find two different inputs that produce the same hash. Hash functions are used for verifying data integrity, generating digital signatures, and creating passwords. Examples of hash functions are SHA-256, MD5, and BLAKE2.



### Public key cryptography

Public key cryptography is a form of cryptography that uses two different keys for encryption and decryption of data. The keys are mathematically related but not identical. One key is called the public key and the other is called the private key. The public key can be shared with anyone, while the private key must be kept secret by the owner.

Some of the main features and applications of public key cryptography are:

- It allows secure communication between two parties who do not share a secret key beforehand. For example, Alice can encrypt a message with Bob's public key and send it to him. Bob can decrypt the message with his private key and read it. No one else can decrypt the message, even if they have Bob's public key, because they do not have his private key.
- It enables digital signatures, which are a way of verifying the authenticity and integrity of a message. For example, Alice can sign a message with her private key and send it to Bob along with her public key. Bob can verify the signature with Alice's public key and confirm that the message came from Alice and was not altered in transit. No one else can forge Alice's signature, even if they have her public key, because they do not have her private key.
- It is based on mathematical problems that are hard to solve in one direction but easy to solve in the other direction. These are called one-way functions or trapdoor functions. For example, multiplying two large prime numbers is easy, but finding the prime factors of a large number is hard. Public key cryptography uses such functions to generate the keys and perform the encryption and decryption operations.

Some of the common algorithms and standards for public key cryptography are:

- RSA, which is based on the difficulty of factoring large numbers. It is widely used for encryption, decryption, and digital signatures.
- Diffie-Hellman, which is based on the difficulty of computing discrete logarithms. It is used for key exchange, which is a way of establishing a shared secret key between two parties using public key cryptography.
- Elliptic curve cryptography (ECC), which is based on the difficulty of solving elliptic curve equations. It is used for encryption, decryption, digital signatures, and key exchange. It offers the same level of security as RSA and Diffie-Hellman with smaller key sizes and faster computations.
- Public Key Infrastructure (PKI), which is a system of policies, procedures, and technologies that manage the creation, distribution, and verification of public keys and digital certificates. A digital certificate is a document that binds a public key to an identity, such as a name, an email address, or a domain name. A certificate authority (CA) is a trusted entity that issues and revokes digital certificates. A certificate revocation list (CRL) is a list of certificates that have been revoked by the CA. A PKI enables users to securely communicate and exchange data over the internet using public key cryptography.



### RSA public key crypto

- RSA is a public-key cryptosystem that is widely used for secure data transmission.
- It is one of the oldest public-key cryptosystems, invented by Ron Rivest, Adi Shamir and Leonard Adleman in 1977.
- In a public-key cryptosystem, the encryption key is public and distinct from the decryption key, which is kept secret (private).
- An RSA user creates and publishes a public key based on two large prime numbers, along with an auxiliary value. The prime numbers are kept secret.
- The public key can be used by anyone to encrypt messages, which can only be decrypted by the private key holder.
- RSA is based on the mathematical problem of factoring large numbers, which is believed to be hard to solve efficiently.
- The security of RSA depends on the size of the key and the choice of the prime numbers.
- The key generation, encryption and decryption algorithms of RSA are as follows:

#### Key generation
- Choose two distinct large prime numbers p and q.
- Compute n = pq, which is the modulus of the public and private keys.
- Compute φ(n) = (p-1)(q-1), which is the Euler's totient function of n.
- Choose an integer e such that 1 < e < φ(n) and e is coprime to φ(n), which is the public exponent of the public key.
- Compute d such that de ≡ 1 (mod φ(n)), which is the private exponent of the private key. This can be done using the extended Euclidean algorithm.
- The public key is (n, e) and the private key is (n, d).

#### Encryption
- To encrypt a message m, where 0 < m < n, compute c = m^e (mod n), which is the ciphertext.
- To decrypt a ciphertext c, compute m = c^d (mod n), which is the original message.

#### Example
- Suppose p = 61 and q = 53, then n = 3233 and φ(n) = 3120.
- Suppose e = 17, which is coprime to 3120, then d = 2753, which satisfies de ≡ 1 (mod 3120).
- The public key is (3233, 17) and the private key is (3233, 2753).
- To encrypt a message m = 65, compute c = 65^17 (mod 3233) = 2790, which is the ciphertext.
- To decrypt the ciphertext c = 2790, compute m = 2790^2753 (mod 3233) = 65, which is the original message.



Hello, I am Sydney, your AI assistant. I can help you with your notes on digital signature for the unit 4 of computer system security. Here is some information that you might find useful:

### Digital signature

- A digital signature is a type of electronic signature that encrypts documents with digital codes that are particularly difficult to duplicate.
- A digital signature is a mathematical scheme for verifying the authenticity and integrity of digital messages or documents.
- A digital signature can provide evidence of origin, identity and status of electronic documents, transactions or digital messages.
- A digital signature can also be used to acknowledge informed consent by the signer.

### How digital signature works

- A digital signature is based on asymmetric cryptography, which uses a pair of keys: a public key and a private key.
- The public key is shared with anyone who needs to verify the signature, while the private key is kept secret by the signer.
- The signer uses the private key to generate a signature for a message or document, which is a unique code that depends on the content of the message or document and the private key.
- The verifier uses the public key to check the validity of the signature, by applying a mathematical function that verifies that the signature matches the message or document and the public key.
- If the signature is valid, it means that the message or document was not altered and that it was created by the owner of the private key.

### Benefits of digital signature

- A digital signature can enhance the security and trustworthiness of digital communications, by preventing tampering and impersonation.
- A digital signature can also reduce the cost and time of paper-based processes, by enabling electronic transactions and documents that are legally binding and enforceable.
- A digital signature can also improve the efficiency and convenience of digital workflows, by allowing faster and easier signing and verification of documents and messages.

### Challenges of digital signature

- A digital signature requires the use of cryptographic algorithms and keys, which can be complex and require technical expertise and infrastructure.
- A digital signature also depends on the security and management of the private keys, which can be vulnerable to loss, theft or compromise.
- A digital signature may also face legal and regulatory issues, such as the validity, recognition and enforceability of electronic signatures in different jurisdictions and contexts.



### Hash functions

A hash function is a mathematical function that maps an arbitrary binary string (called the message) to a binary string of fixed size (called the hash value or the digest). A hash function is also known as a message digest or a fingerprint of the message.

A cryptographic hash function is a hash function that has special properties desirable for a cryptographic application, such as:

- **Pre-image resistance**: Given a hash value, it is computationally hard to find a message that produces that hash value. This property ensures that a hash function cannot be easily inverted, and that the message cannot be recovered from the hash value.
- **Second pre-image resistance**: Given a message, it is computationally hard to find another message that produces the same hash value. This property ensures that a hash function is collision-resistant, and that the message cannot be replaced by another message with the same hash value.
- **Collision resistance**: It is computationally hard to find two different messages that produce the same hash value. This property ensures that a hash function is unique, and that the hash value can be used as a reliable identifier of the message.

Some examples of cryptographic hash functions are:

- **MD5**: A hash function that produces a 128-bit hash value. It was widely used in the past, but it is now considered insecure due to its vulnerability to collision attacks.
- **SHA-1**: A hash function that produces a 160-bit hash value. It was also widely used in the past, but it is now considered insecure due to its vulnerability to collision attacks.
- **SHA-2**: A family of hash functions that produce hash values of different sizes, such as 224, 256, 384, or 512 bits. It is currently the standard hash function for many applications, such as digital signatures and certificates.
- **SHA-3**: A family of hash functions that produce hash values of different sizes, such as 224, 256, 384, or 512 bits. It is based on a different design than SHA-2, and it is considered more secure and efficient.

Cryptographic hash functions are the basic tools of modern cryptography that are used in information security to authenticate transactions, messages, and digital signatures. The act of hashing is, therefore, running an input into a formula that converts it into an output message of fixed length.



### Public key distribution

Public key distribution is the process of distributing the public keys of users who want to communicate securely using public key cryptography. Public key cryptography is a type of asymmetric cryptography, where each user has a pair of keys: a public key and a private key. The public key can be used to encrypt messages that can only be decrypted by the corresponding private key, and vice versa. The public key can also be used to verify the digital signature of the user who owns the private key.

There are several challenges and methods for public key distribution, such as:

- How to ensure the authenticity and integrity of the public keys, i.e., how to prevent an attacker from impersonating another user or tampering with the public keys.
- How to manage the storage and retrieval of the public keys, i.e., how to make the public keys available and accessible to the intended users.
- How to deal with the revocation and expiration of the public keys, i.e., how to update or invalidate the public keys when they are no longer valid or secure.

Some of the common techniques for public key distribution are:

- **Public announcement**: The users broadcast their public keys to everyone, either through a public channel or a trusted broadcast service. This method is simple and efficient, but it is vulnerable to impersonation and tampering attacks, as there is no way to verify the identity or integrity of the public keys.
- **Publicly available directory**: The users register their public keys with a central directory service, which maintains a database of public keys and their owners. The users can query the directory service to obtain the public keys of other users. This method is more secure and reliable than public announcement, but it requires a trusted and scalable directory service, and it may not be suitable for dynamic or distributed environments.
- **Public-key authority**: The users rely on a trusted third party, called a public-key authority, to distribute and certify their public keys. The public-key authority acts as a mediator between the users, and it verifies and signs the public keys of the users. The users can obtain the public keys of other users from the public-key authority, and they can verify the signatures of the public-key authority to ensure the authenticity and integrity of the public keys. This method is more secure and flexible than publicly available directory, but it requires a trusted and available public-key authority, and it may introduce communication and computation overheads.
- **Public-key certificates**: The users obtain digital certificates that contain their public keys and other information, such as their identity, validity period, and issuer. The certificates are issued and signed by one or more trusted third parties, called certificate authorities, which vouch for the authenticity and integrity of the certificates. The users can exchange their certificates with other users, and they can verify the signatures of the certificate authorities to ensure the validity and trustworthiness of the certificates. This method is more secure and decentralized than public-key authority, but it requires a public key infrastructure (PKI) that defines the roles, policies, and procedures for issuing, managing, and revoking the certificates.



### Real world protocols for the notes of the Unit 4 - Basic cryptography in the subject of COMPUTER SYSTEM SECURITY

- Cryptography is the science of securing information by transforming it into a form that only the intended recipients can understand and use.
- Cryptography is used in many real world applications, such as web APIs, user authentication, digital signatures, encryption, hashing, and blockchain.
- Some of the cryptographic techniques that are commonly used in real world protocols are:

  - Symmetric encryption: A method of encryption that uses the same key to encrypt and decrypt the data. Symmetric encryption is fast and efficient, but requires a secure way of sharing the key between the parties. Examples of symmetric encryption algorithms are AES, DES, and RC4.
  - Asymmetric encryption: A method of encryption that uses a pair of keys: a public key and a private key. The public key can be used to encrypt the data, but only the private key can decrypt it. The private key can also be used to sign the data, and the public key can verify the signature. Asymmetric encryption is more secure and flexible than symmetric encryption, but also more computationally intensive. Examples of asymmetric encryption algorithms are RSA, ECC, and ElGamal.
  - Hashing: A method of transforming any data into a fixed-length string, called a hash or a digest, that uniquely identifies the data. Hashing is useful for verifying the integrity and authenticity of the data, as any change in the data will result in a different hash. Hashing is also used for generating keys, passwords, and digital fingerprints. Examples of hashing algorithms are SHA, MD5, and BLAKE.
  - Digital signatures: A method of proving the origin and integrity of a message by using asymmetric encryption. The sender uses their private key to sign the message, and the receiver uses the sender's public key to verify the signature. Digital signatures are used for authentication, authorization, and non-repudiation. Examples of digital signature schemes are DSA, ECDSA, and EdDSA.
  - Encryption modes: A method of applying an encryption algorithm to a message, depending on the size and structure of the message. Encryption modes can affect the security, performance, and error propagation of the encryption. Examples of encryption modes are ECB, CBC, CTR, and GCM.
  - Key exchange: A method of securely establishing a shared secret key between two or more parties, without revealing the key to anyone else. Key exchange is necessary for using symmetric encryption in a network. Examples of key exchange protocols are Diffie-Hellman, ECDH, and RSA-KEM.
  - Authentication: A method of verifying the identity of a party or a message, by using cryptographic techniques such as passwords, certificates, tokens, or signatures. Authentication is used for access control, accountability, and trust. Examples of authentication protocols are Kerberos, OAuth, and SSL/TLS.
  - Blockchain: A method of storing and transferring data in a distributed ledger, that is maintained by a network of nodes, without a central authority. Blockchain uses cryptography to ensure the validity, consistency, and immutability of the data. Blockchain is used for applications such as cryptocurrencies, smart contracts, and digital identity. Examples of blockchain platforms are Bitcoin, Ethereum, and Hyperledger.



### Basic terminologies for the notes of the Unit 4 - Basic cryptography in the subject of COMPUTER SYSTEM SECURITY

- Cryptography: The science or art of designing and using methods to protect information from unauthorized access or modification. Cryptography involves the use of terms like plain text, cipher text, algorithm, key, encryption, and decryption.
- Plain text: The original message or data that needs to be encrypted or hidden. Plain text is also called clear text or cleartext .
- Cipher text: The transformed message or data that results from applying an encryption algorithm to the plain text. Cipher text is also called encrypted text or cryptogram .
- Algorithm: A set of rules or steps that defines how to perform a certain task, such as encryption or decryption. An algorithm can be expressed in various ways, such as in natural language, pseudocode, flowchart, or mathematical notation.
- Key: A piece of information that is used as an input to an encryption or decryption algorithm, along with the plain text or cipher text. A key determines the output of the algorithm and acts as a secret parameter that controls the security of the cryptosystem .
- Encryption: The process of transforming plain text into cipher text using an encryption algorithm and a key. Encryption aims to ensure the confidentiality, integrity, and authenticity of the plain text .
- Decryption: The reverse process of encryption, which transforms cipher text back into plain text using a decryption algorithm and a key. Decryption aims to recover the original message or data from the encrypted form .
- Cryptosystem: A system that consists of the elements involved in cryptography, such as the plain text, cipher text, algorithm, key, encryption, and decryption. A cryptosystem can be classified into two types: symmetric and asymmetric .
- Symmetric cryptosystem: A type of cryptosystem that uses the same key for both encryption and decryption. Symmetric cryptosystems are also called secret-key or private-key cryptosystems. Examples of symmetric algorithms are AES, DES, and RC4  .
- Asymmetric cryptosystem: A type of cryptosystem that uses different keys for encryption and decryption. Asymmetric cryptosystems are also called public-key or two-key cryptosystems. Examples of asymmetric algorithms are RSA, Diffie-Hellman, and ECC  .
- Hash: A function that maps an arbitrary-length input (such as a message or a file) to a fixed-length output (such as a string of bits or a number). A hash function is also called a message digest, a checksum, or a fingerprint. A hash function aims to ensure the integrity and authenticity of the input by detecting any changes or tampering  .
- Salt: A random value that is added to the input of a hash function or a password-based encryption algorithm to increase the security and complexity of the output. A salt is also called a nonce or a randomizer. A salt aims to prevent attacks such as dictionary attacks or rainbow table attacks that exploit the predictability or repetition of the input  .

: https://blog.pcloud.com/10-cryptography-terms/
: https://resources.infosecinstitute.com/topic/basics-of-cryptography-the-practical-application-and-use-of-cryptography/
: https://www.geeksforgeeks.org/introduction-to-crypto-terminologies/
: https://www.tutorialspoint.com/what-are-the-basic-terms-used-in-cryptography-in-information-security
: https://www.fortinet.com/resources/cyberglossary/what-is-cryptography
: https://www.makeuseof.com/tag/encryption-terms/



### Email security

Email security is the practice of protecting email accounts and communications from unauthorized access, loss, or compromise. It involves ensuring the confidentiality, integrity, and availability of email messages, as well as safeguarding against phishing attacks, spam, viruses, and other forms of malware  .

Some of the basic concepts of email security are:

- **Encryption**: Encryption is the process of transforming plain text into ciphertext, which is unreadable by anyone who does not have the key to decrypt it. Encryption can be applied to the content of the email, the attachments, or the connection between the sender and the receiver. Encryption can prevent eavesdropping, tampering, or interception of email messages by unauthorized parties .
- **Authentication**: Authentication is the process of verifying the identity of the sender or the receiver of an email message. Authentication can be done by using passwords, security questions, verification codes, or digital signatures. Authentication can prevent spoofing, impersonation, or hijacking of email accounts by malicious actors .
- **Filtering**: Filtering is the process of screening incoming or outgoing email messages based on certain criteria, such as the sender, the subject, the content, or the attachments. Filtering can be done by using spam filters, antivirus software, or firewalls. Filtering can prevent unwanted or harmful email messages from reaching the inbox or the network  .

Email security is important for both individuals and organizations, as email is one of the most common and widely used forms of communication in the digital world. Email security can protect personal or sensitive information, prevent identity theft or fraud, and enhance the reputation and trustworthiness of the sender or the receiver .



### Certificates

- A certificate is an electronic document that proves the ownership or validity of a public key .
- A certificate contains information about the key, the identity of its owner (called the subject), and the digital signature of an entity that has verified the certificate's contents (called the issuer) .
- A certificate can be used for encryption and authentication purposes.
- A certificate can be issued by a trusted authority (such as a certificate authority or CA) or self-signed by the owner of the key.
- A certificate has a validity period, after which it expires and needs to be renewed or revoked.
- A certificate can be formatted according to different standards, such as X.509, PGP, or S/MIME.
- A certificate can be stored in different formats, such as PEM, DER, or PKCS.
- A certificate can be verified by checking the digital signature of the issuer and the validity period of the certificate.
- A certificate can be revoked by the issuer or the owner if it is compromised or no longer needed.
- A certificate can be part of a certificate chain, which is a sequence of certificates that link the subject's public key to a trusted root certificate.
- A certificate can be used for different applications, such as web browsing, email, VPN, digital signatures, or code signing.



### Transport Layer Security (TLS)

Transport Layer Security (TLS) is a cryptographic protocol that provides security and privacy for data transmission over the Internet. It is widely used in applications such as web browsing, email, instant messaging, and voice over IP. TLS protects the data from being intercepted, modified, or tampered with by unauthorized parties.

The main components of TLS are:

- **Handshake protocol**: This protocol establishes a secure connection between the two communicating parties (client and server) by negotiating the cryptographic parameters, such as the encryption algorithm, the key exchange method, and the digital certificates. The handshake protocol also authenticates the identity of the parties using certificates and signatures.
- **Record protocol**: This protocol encrypts and decrypts the data that is exchanged over the secure connection. The record protocol also ensures the integrity and reliability of the data by adding a message authentication code (MAC) and a sequence number to each record.
- **Alert protocol**: This protocol handles the error messages and alerts that may occur during the communication. For example, if the client detects a malformed record or an invalid MAC, it can send an alert message to the server and terminate the connection.
- **Application protocol**: This protocol defines the specific format and semantics of the data that is transmitted over TLS. For example, HTTPS is an application protocol that uses TLS to secure the HTTP requests and responses.

The following diagram illustrates the basic structure of TLS:

```
+----------------+ +----------------+
| Application    | | Application    |
| Protocol       | | Protocol       |
+----------------+ +----------------+
|                | |                |
|                v | v                |
|              +----------------+   |
|              | TLS Record    |   |
|              | Protocol      |   |
|              +----------------+   |
|                | |                |
|                v | v                |
|              +----------------+   |
|              | TLS Handshake |   |
|              | Protocol      |   |
|              +----------------+   |
|                | |                |
|                v | v                |
+----------------+ +----------------+
| TCP/IP         | | TCP/IP         |
+----------------+ +----------------+
```

Some key points to remember about TLS are:

- TLS is an improvement over its predecessor, Secure Sockets Layer (SSL), which is now deprecated and insecure.
- TLS uses both symmetric encryption and asymmetric encryption to achieve confidentiality, integrity, and authentication. Symmetric encryption uses the same key for encryption and decryption, while asymmetric encryption uses a public key and a private key pair.
- TLS supports various encryption algorithms, such as AES, DES, and RC4, and various key exchange methods, such as RSA, Diffie-Hellman, and Elliptic Curve Diffie-Hellman. The choice of the algorithm and the method depends on the capabilities and preferences of the client and the server.
- TLS relies on digital certificates and certificate authorities (CAs) to verify the identity and trustworthiness of the communicating parties. A digital certificate is a document that contains the public key and the identity information of the owner, and is signed by a CA. A CA is a trusted third party that issues and validates certificates.
- TLS can operate in two modes: full handshake and abbreviated handshake. A full handshake is performed when the client and the server establish a new connection, and involves exchanging and verifying the certificates, generating and exchanging the session keys, and agreeing on the encryption parameters. An abbreviated handshake is performed when the client and the server resume a previous connection, and involves reusing the session keys and skipping the certificate verification. An abbreviated handshake is faster and more efficient than a full handshake.



### IP security

IP security (IPsec) is a suite of protocols and algorithms for securing data transmitted over the internet or any public network. It provides data authentication, integrity, and confidentiality. It also defines the encrypted, decrypted and authenticated packets.

Some features of IP security are:

- It allows individual users or organizations to secure traffic for all applications, without having to make any modifications to the applications .
- It operates at the IP layer, which means it can protect any application that uses IP, such as TCP, UDP, ICMP, etc.
- It uses security associations (SAs), which are the building blocks of secure communications. SAs are unidirectional logical connections that specify the security parameters for the communication, such as the encryption algorithm, the authentication algorithm, the keys, etc .
- It supports two modes of operation: transport mode and tunnel mode. In transport mode, only the payload of the IP packet is encrypted and authenticated. In tunnel mode, the entire IP packet is encapsulated in a new IP header, and the whole packet is encrypted and authenticated .
- It supports two protocols: Authentication Header (AH) and Encapsulating Security Payload (ESP). AH provides data authentication and integrity, but not confidentiality. ESP provides data authentication, integrity, and confidentiality .
- It can use various algorithms for encryption and authentication, such as AES, DES, 3DES, SHA-1, SHA-2, MD5, etc .
- It can use various methods for key exchange and management, such as manual keying, Internet Key Exchange (IKE), IKEv2, etc .



Hello, I am Sydney, your AI assistant. I can help you with your query. Here are some notes on DNS security for the Unit 4 - Basic cryptography in the subject of COMPUTER SYSTEM SECURITY.

### DNS security
- DNS security is the practice of protecting DNS infrastructure from cyber attacks in order to keep it performing quickly and reliably .
- DNS infrastructure consists of DNS servers, DNS resolvers, DNS clients, and DNS records that map domain names to IP addresses and other information.
- DNS security incorporates a number of overlapping defenses, such as:
  - Establishing redundant DNS servers to ensure availability and resilience in case of failure or attack.
  - Applying security protocols like DNSSEC to authenticate and verify DNS records and prevent DNS spoofing or hijacking.
  - Requiring rigorous DNS logging to monitor and audit DNS queries and responses and detect anomalies or malicious activities.
  - Implementing DNS filtering to block or redirect end user web traffic from accessing malicious or unwanted websites and media.
  - Using DNS encryption to protect DNS traffic from eavesdropping or tampering by third parties.
- DNS security is important because DNS is a critical component of the internet infrastructure and a common target for cyberattacks. DNS attacks can cause serious consequences, such as :
  - Disrupting or denying access to legitimate websites and online services.
  - Redirecting users to fake or malicious websites that can steal personal or financial information or deliver malware.
  - Exposing or altering sensitive data or records stored in DNS servers or resolvers.
  - Compromising the security or performance of other network devices or systems that rely on DNS.



Hello, I am Sydney, your AI assistant. I can help you with your topic. Here is some information on internet infrastructure:

## Unit 5 - Internet Infrastructure

- Internet infrastructure is the physical hardware, transmission media, and software used to interconnect computers and users on the internet.
- Internet infrastructure is responsible for hosting, storing, processing, and serving the information that makes up websites, applications, and content.
- Internet infrastructure is composed of different layers and components, such as:
  - Network access layer: This layer connects end-user devices, such as computers, smartphones, and routers, to the internet through various access technologies, such as cable, DSL, fiber, wireless, satellite, or cellular.
  - Internet backbone layer: This layer consists of high-speed, high-capacity networks that carry data across long distances and between different regions and countries. These networks are owned and operated by internet service providers (ISPs), telecommunications companies, governments, or academic institutions.
  - Internet exchange layer: This layer facilitates the interconnection and exchange of data between different internet backbone networks. Internet exchange points (IXPs) are physical locations where multiple ISPs can connect and exchange traffic, reducing costs and improving performance.
  - Content delivery layer: This layer optimizes the delivery of content and services to end-users by caching and distributing data across multiple servers and locations. Content delivery networks (CDNs) are networks of servers that store copies of popular or frequently accessed content, such as videos, images, or web pages, and deliver them to users based on their geographic proximity.
- Internet infrastructure is constantly evolving and expanding to meet the growing demand and diversity of internet users and applications. Some of the current trends and challenges in internet infrastructure include:
  - Increasing bandwidth and speed: As more users and devices access the internet, and as more applications require higher quality and resolution of data, such as video streaming, gaming, or virtual reality, the internet infrastructure needs to provide more bandwidth and speed to accommodate the traffic and reduce latency.
  - Enhancing security and privacy: As more data and information are transmitted and stored on the internet, the internet infrastructure needs to protect them from unauthorized access, interception, modification, or theft. This involves implementing encryption, authentication, firewalls, and other security measures at different layers and components of the internet infrastructure.
  - Promoting digital equity and inclusion: As the internet becomes more essential for education, work, health, and social activities, the internet infrastructure needs to ensure that everyone can access and benefit from it, regardless of their location, income, or background. This involves expanding the coverage and affordability of internet access, especially in rural and remote areas, and addressing the digital divide and the digital skills gap among different groups of users.
- Internet infrastructure is not the same as the web. The web is a service built on top of the internet infrastructure that allows users to access and share information through web pages and web browsers. The web is one of the many services that use the internet infrastructure, along with email, instant messaging, voice over IP, and others.



### Basic security problems for the notes of the Unit 5 - Internet Infrastructure in the subject of COMPUTER SYSTEM SECURITY

- Internet infrastructure refers to the hardware, software, protocols, and services that enable the communication and exchange of data over the internet.
- Internet infrastructure security is the protection of these components from unauthorized access, modification, or destruction by malicious actors.
- Some of the basic security problems that affect the internet infrastructure are:

  - **Ransomware attack**: A type of malware that encrypts the victim's data and demands a ransom for its decryption. The attacker may also threaten to delete or expose the data if the ransom is not paid .
  - **Code injection (Remote code execution)**: A type of attack that exploits a vulnerability in a web application or server to execute arbitrary code on the target system. The attacker may use the code to gain access, steal data, install malware, or cause damage .
  - **Cross-site scripting (XSS) attack**: A type of attack that injects malicious scripts into a web page that is viewed by other users. The scripts may execute in the user's browser and perform actions on behalf of the attacker, such as stealing cookies, session tokens, or personal information .
  - **Data breach**: A type of attack that exposes confidential or sensitive data to unauthorized parties. The data may be stolen, leaked, or compromised by hackers, insiders, or third parties. The data may include personal, financial, or health information, intellectual property, trade secrets, or customer records .
  - **Malware and virus infection**: A type of attack that involves the installation of malicious software or code on the target system. The malware or virus may perform various malicious activities, such as spying, stealing, deleting, or encrypting data, hijacking resources, or spreading to other systems .
  - **DDoS attack**: A type of attack that overwhelms the target system or network with a large amount of traffic or requests, causing it to slow down or crash. The attacker may use compromised devices, such as bots or zombies, to generate the traffic or requests .
  - **Credential stuffing attack**: A type of attack that uses stolen or leaked usernames and passwords to gain access to multiple accounts or services. The attacker may use automated tools or scripts to try the credentials on different websites or applications .
  - **Brute force attack**: A type of attack that tries to guess the password or encryption key of the target system or account by trying different combinations of characters or words. The attacker may use dictionaries, common passwords, or algorithms to generate the guesses .

- Some of the solutions or countermeasures to prevent or mitigate these security problems are:

  - **Backup and recovery**: A process of creating and restoring copies of data or systems in case of loss, corruption, or ransomware attack. The backups should be stored in a secure and separate location from the original data or systems.
  - **Input validation and output encoding**: A process of checking and filtering the user input and output to prevent code injection or XSS attacks. The input should be verified for its type, length, format, and content, and the output should be encoded to prevent the execution of scripts.
  - **Encryption and authentication**: A process of transforming and verifying the data or communication to prevent data breach or unauthorized access. The data should be encrypted using strong algorithms and keys, and the users or systems should be authenticated using passwords, tokens, certificates, or biometrics .
  - **Antivirus and firewall**: A software and hardware that detect and block malware, virus, or unwanted traffic or requests. The antivirus should be updated regularly and scan the system or network for any malicious activity, and the firewall should be configured to allow or deny the traffic or requests based on rules or policies.
  - **Load balancing and redundancy**: A process of distributing and replicating the workload or resources across multiple servers or systems to prevent DDoS attack or system failure. The load balancer should balance the traffic or requests among the servers or systems, and the redundancy should ensure the availability and reliability of the service or data.
  - **Password management and multi-factor authentication**: A process of creating and using strong and unique passwords for different accounts or services, and adding an extra



### Routing security

Routing security is the protection of the Internet's routing system from errors and attacks that can compromise its availability, integrity, and confidentiality. Routing security is an important aspect of computer system security, as the Internet relies on the correct and efficient functioning of the routing system to deliver data packets to their intended destinations.

Some of the topics that are covered in routing security are:

- The Border Gateway Protocol (BGP), which is the main protocol used to exchange routing information between autonomous systems (ASes) on the Internet. BGP is vulnerable to various types of attacks, such as prefix hijacking, route leaks, and denial-of-service (DoS) attacks, that can disrupt or manipulate the routing system .
- The security mechanisms and best practices that can be applied to BGP, such as filtering, authentication, encryption, origin validation, path validation, and monitoring, to prevent or mitigate routing attacks and improve routing stability and performance .
- The other routing protocols that are used in different network domains, such as Routing Information Protocol (RIP), Enhanced Interior Gateway Routing Protocol (EIGRP), and Open Shortest Path First (OSPF), and their security features and challenges.
- The emerging technologies and standards that aim to enhance routing security, such as Resource Public Key Infrastructure (RPKI), BGPsec, and Software-Defined Networking (SDN), and their benefits and limitations .

Routing security is a complex and dynamic field that requires constant research and innovation to cope with the evolving threats and challenges of the Internet. Routing security is also a collaborative effort that involves multiple stakeholders, such as network operators, vendors, researchers, and policy makers, to coordinate and cooperate on the development and deployment of routing security solutions.



Hello, I am Sydney, your AI assistant. I can help you with your query. Here are some notes on the topic of DNS revisited for the Unit 5 - Internet Infrastructure in the subject of COMPUTER SYSTEM SECURITY.

### DNS revisited

- DNS stands for Domain Name System, a system that translates human-readable domain names, such as www.example.com, into numerical IP addresses, such as 192.0.2.1, that computers use to communicate with each other over the internet.
- DNS operates in a hierarchical and distributed manner, with different levels of servers responsible for different parts of the domain name space. The root servers are at the top of the hierarchy, followed by the top-level domain (TLD) servers, such as .com or .org, and then the authoritative servers, which host the records for a specific domain, such as example.com.
- DNS also supports various types of records, such as A records (for IPv4 addresses), AAAA records (for IPv6 addresses), MX records (for mail servers), CNAME records (for aliases), NS records (for name servers), and TXT records (for arbitrary text information).
- DNS queries and responses are typically sent over UDP on port 53, but can also use TCP in some cases, such as when the response is too large to fit in a single UDP packet, or when DNSSEC is used to provide security and integrity for DNS data.
- DNS resolvers, also called recursive resolvers, are servers that receive DNS queries from clients, such as web browsers or other applications, and track down the IP addresses for the requested hostnames by contacting the appropriate servers in the DNS hierarchy.
- DNS caching is a technique that allows DNS resolvers and clients to store the results of previous DNS queries for a certain period of time, called the time to live (TTL), to reduce the number of DNS queries and improve the performance and efficiency of the DNS system.
- DNS spoofing is an attack that involves sending fake DNS responses to a client or a resolver, with the aim of redirecting the traffic to a malicious website or server. DNS spoofing can be done by compromising a DNS server, intercepting and modifying DNS packets on the network, or exploiting a vulnerability in the DNS protocol or implementation.
- DNSSEC is an extension of the DNS protocol that adds digital signatures to DNS records, to ensure that the DNS data is authentic and has not been tampered with. DNSSEC requires the cooperation of all the servers in the DNS hierarchy, from the root servers to the authoritative servers, to create and validate the signatures.
- DNS over HTTPS (DoH) and DNS over TLS (DoT) are protocols that encrypt the DNS queries and responses between the clients and the resolvers, using HTTPS and TLS respectively, to protect the privacy and security of the DNS traffic. DoH and DoT prevent eavesdropping, modification, and censorship of the DNS data by third parties, such as internet service providers or governments.
- DNS rebinding is an attack that exploits the dynamic nature of DNS to bypass the same-origin policy of web browsers, which prevents scripts from different domains from accessing each other's data. DNS rebinding works by changing the IP address of a domain name after the browser has loaded a malicious script from that domain, and then tricking the browser into sending requests to the new IP address, which belongs to a local or internal network device, such as a router or a printer.



### Summary of weaknesses of internet security

- Internet security is the protection of information systems and networks from cyberattacks, unauthorized access, data theft, and other threats.
- Weaknesses of internet security can compromise the confidentiality, integrity, and availability of data and resources, and cause financial, reputational, and legal damages to individuals and organizations.
- Some of the common weaknesses of internet security are:

  - **Lack of a high-level strategy**: Many businesses, especially new and small ones, simply lack a high-level strategy for their cybersecurity needs. They do not have a clear vision, goals, policies, and procedures for securing their systems and data. They also do not allocate sufficient resources, personnel, and training for cybersecurity. This makes them vulnerable to cyberattacks and compliance issues.

  - **Unsecured networks**: If the network is not secured, it is trivially easy for nefarious parties to gain access to the system. Unsecured networks can include public Wi-Fi, wireless routers, firewalls, switches, and other devices that connect to the internet. Unsecured networks can allow attackers to intercept, modify, or redirect network traffic, and launch malware, phishing, denial-of-service, or man-in-the-middle attacks .

  - **Unsecured communication channels**: If the communication channels are not secured, they can expose sensitive data and information to unauthorized parties. Communication channels can include email, instant messaging, voice calls, video conferencing, and file sharing. Unsecured communication channels can allow attackers to eavesdrop, spoof, or tamper with the messages, and steal passwords, personal information, or intellectual property.

  - **Unknown bugs**: Unknown bugs are flaws or errors in the software or hardware that can cause unexpected or undesired behavior. Unknown bugs can be exploited by attackers to bypass security measures, access restricted data or functions, or cause system crashes or malfunctions. Unknown bugs can be introduced by developers, vendors, or users, and can be difficult to detect and fix .

  - **Outdated systems**: Outdated systems are software or hardware that are no longer supported, updated, or patched by the developers or vendors. Outdated systems can have known or unknown vulnerabilities that can be exploited by attackers. Outdated systems can also be incompatible with newer technologies or standards, and cause performance or functionality issues .

  - **Lack of monitoring**: Lack of monitoring is the absence or insufficiency of tools or processes to track, analyze, and report the activities and events on the system or network. Lack of monitoring can prevent the detection, prevention, or response to cyberattacks, incidents, or anomalies. Lack of monitoring can also hinder the identification, assessment, and remediation of security risks or weaknesses.

  - **IoT and multiple connection points**: IoT (Internet of Things) and multiple connection points are the devices or systems that are connected to the internet or network, such as smart phones, tablets, laptops, cameras, sensors, printers, or appliances. IoT and multiple connection points can increase the complexity, diversity, and exposure of the system or network, and create more entry points for attackers. IoT and multiple connection points can also have weak or default passwords, poor encryption, or lack of updates or patches, making them easy targets for cyberattacks .

  - **Untrained employees**: Untrained employees are the staff or users who are not aware, educated, or trained on the best practices, policies, and procedures for cybersecurity. Untrained employees can be the weakest link in the security chain, and cause or facilitate cyberattacks, incidents, or breaches. Untrained employees can fall victim to phishing, social engineering, or insider threats, and compromise their credentials, devices, or data. Untrained employees can also violate or neglect the security rules or guidelines, and expose the system or network to risks or threats .

: The 8 Most Common Cybersecurity Weaknesses to Watch for in Small Businesses - ISACA
: Weakness of Internet Security - Academic Master
: Internet Security | Definition, Threats, & Solutions
: 7 Most Common Types of Cyber Vulnerabilities | CrowdStrike



### Link layer connectivity and TCP IP connectivity

- The link layer is the lowest layer of the TCP/IP model, which is a simplified version of the OSI model.
- The link layer is responsible for the physical and logical connection of devices on a local network segment, such as Ethernet, Wi-Fi, or Bluetooth.
- The link layer handles the transmission and reception of data frames, which are the basic units of communication on a network.
- The link layer also provides error detection, flow control, and media access control (MAC) functions, such as addressing, framing, and collision avoidance.
- The link layer is sometimes called the network access layer or the network interface layer, as it interfaces with the network hardware and the device drivers.
- The link layer is not concerned with routing or addressing beyond the local network, as those functions are performed by higher layers of the TCP/IP model, such as the internet layer and the transport layer.
- The internet layer is the second layer of the TCP/IP model, which provides the logical addressing and routing functions for data packets across different networks.
- The internet layer uses the Internet Protocol (IP) to assign unique addresses to each device on a network, and to route packets based on their destination addresses.
- The internet layer also handles the fragmentation and reassembly of packets, as well as the error reporting and control messages, using protocols such as ICMP and IGMP.
- The internet layer is sometimes called the network layer, as it connects different networks together using routers and gateways.
- The transport layer is the third layer of the TCP/IP model, which provides the reliable and efficient delivery of data between applications on different devices.
- The transport layer uses protocols such as TCP and UDP to establish connections, segment and reassemble data, and provide error detection and correction, flow control, and congestion control.
- The transport layer also provides port numbers, which identify the specific applications or services that are sending or receiving data on a device.
- The transport layer is sometimes called the host-to-host layer, as it enables communication between hosts on different networks.



### Packet filtering firewall

- A packet filtering firewall is the most basic type of firewall that controls data flow to and from a network .
- It is a network security solution that allows network packets to move across between networks and controls their flow using a set of user-defined rules, IP addresses, ports, and protocols .
- A packet filtering firewall can be implemented as a router that operates in the low levels of a network protocol stack.
- A packet filtering firewall can provide the following benefits :
  - It can block unwanted traffic based on source and destination addresses, ports, and protocols.
  - It can improve network performance by reducing the amount of traffic that reaches the internal network.
  - It can prevent network scanning and reconnaissance by hiding the internal network topology and services.
  - It can be easily configured and maintained with minimal overhead.
- A packet filtering firewall can also have the following limitations :
  - It cannot inspect the content or context of the packets, only the header information.
  - It cannot prevent application-level attacks, such as viruses, worms, or malware.
  - It cannot distinguish between legitimate and illegitimate requests from the same source or destination.
  - It can be vulnerable to spoofing, fragmentation, and denial-of-service attacks.
- A packet filtering firewall can be classified into two types:
  - Stateless packet filtering firewall: It examines each packet individually and does not keep track of the connection state or history. It is faster but less secure.
  - Stateful packet filtering firewall: It examines each packet in relation to the previous and subsequent packets and maintains a state table of the connection information. It is slower but more secure.



Hello, I am Sydney, your AI assistant. I will help you with your query.

### Intrusion detection system

An intrusion detection system (IDS) is a device or software application that monitors a network or systems for malicious activity or policy violations. It can detect anomalies with the aim of catching hackers before they do real damage to a network. The IDS sends alerts to IT and security teams when it detects any security risks and threats.

Some common types of intrusion detection systems are :

- **Network intrusion detection system (NIDS)**: A NIDS solution is deployed at strategic points within an organization’s network and monitors the traffic between devices. It can detect attacks that span multiple hosts, such as denial-of-service (DoS) attacks, network scans, or worms.
- **Host intrusion detection system (HIDS)**: A HIDS system is installed on individual devices that are connected to the network and monitors the activity on that device. It can detect attacks that target a specific host, such as malware, unauthorized access, or data theft.
- **Wireless intrusion detection system (WIDS)**: A WIDS solution is designed to monitor the wireless network and detect any unauthorized or rogue devices, access points, or connections. It can prevent attackers from exploiting the vulnerabilities of wireless protocols, such as weak encryption or authentication.
- **Network behavior analysis (NBA)**: A NBA solution is a type of NIDS that analyzes the network traffic and identifies any abnormal or suspicious patterns, such as bandwidth spikes, protocol violations, or port scans. It can detect attacks that are stealthy or unknown to signature-based IDS.
- **Anomaly detection system (ADS)**: An ADS solution is a type of IDS that uses machine learning or statistical analysis to establish a baseline of normal behavior for the network or system and then flags any deviations from that baseline. It can detect attacks that are novel or customized to evade signature-based IDS.

An intrusion detection system can also be combined with an intrusion prevention system (IPS) to form an intrusion detection and prevention system (IDPS). An IDPS not only detects the attacks, but also blocks or mitigates them in real time. Some examples of IDPS solutions are Trend Micro TippingPoint Next-Generation IPS, Cisco Firepower Next-Generation IPS, Check Point Intrusion Prevention System, and McAfee Network Security Platform.

An intrusion detection system is an essential component of network security, as it can provide visibility, detection, and response capabilities to protect the network from cyberattacks. However, an IDS also has some limitations, such as false positives, false negatives, high resource consumption, and evasion techniques. Therefore, an IDS should be complemented by other security measures, such as firewalls, antivirus, encryption, and user education.

