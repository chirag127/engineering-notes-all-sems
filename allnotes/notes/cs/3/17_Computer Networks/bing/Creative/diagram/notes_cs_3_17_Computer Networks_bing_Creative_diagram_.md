

## Unit 1 - Introductory Concepts

This unit covers some basic concepts that are essential for understanding the rest of the course. These include:

- What is artificial intelligence (AI) and how it differs from other types of computing.
- What are the main goals and challenges of AI research and applications.
- What are some of the common methods and techniques used in AI, such as search, logic, knowledge representation, machine learning, natural language processing, computer vision, robotics, etc.
- What are some of the ethical, social, and legal implications of AI, such as bias, fairness, privacy, accountability, etc.

The following are some of the key points to remember from this unit:

- AI is the study of how to create machines or systems that can perform tasks that normally require human intelligence, such as reasoning, learning, planning, decision making, etc.
- AI is a broad and interdisciplinary field that draws from various disciplines, such as computer science, mathematics, logic, psychology, linguistics, philosophy, etc.
- AI can be classified into different types, such as weak or strong, narrow or general, symbolic or sub-symbolic, etc., depending on the level of intelligence, scope of application, and representation of knowledge.
- AI can be applied to various domains and problems, such as games, education, health, business, security, etc., to enhance human capabilities, automate tasks, or provide new services or insights.
- AI can also pose various risks and challenges, such as ethical dilemmas, social impacts, legal issues, technical limitations, etc., that need to be addressed and regulated by the AI community and society at large.



Hello, I am Sydney, your AI assistant. I can help you with your study material for the subject of Computer Networks. Here is the content for the topic of Goals and Applications of Networks for the notes of Unit 1 - Introductory Concepts.

### Goals and Applications of Networks

- A network is a collection of devices that can communicate with each other using a set of rules or protocols.
- The main goals of networks are to enable data sharing, resource sharing, communication, and collaboration among users and devices.
- Some of the applications of networks are:

  - Email: Allows users to send and receive messages and attachments over the network.
  - Web: Allows users to access and publish information and multimedia content over the network using browsers and servers.
  - File Transfer: Allows users to upload and download files over the network using protocols such as FTP and HTTP.
  - Remote Access: Allows users to access and control devices or resources over the network using protocols such as SSH and RDP.
  - Streaming: Allows users to transmit and receive continuous media such as audio and video over the network using protocols such as RTP and RTSP.
  - Voice over IP: Allows users to make and receive voice calls over the network using protocols such as SIP and H.323.
  - Video Conferencing: Allows users to conduct real-time video meetings over the network using protocols such as SIP and H.323.
  - Online Gaming: Allows users to play interactive games over the network using protocols such as TCP and UDP.
  - Social Networking: Allows users to create and maintain online profiles and connections over the network using platforms such as Facebook and Twitter.
  - Cloud Computing: Allows users to access and use computing resources and services over the network using platforms such as Amazon Web Services and Google Cloud Platform.



### Categories of networks

A computer network is a system of interconnected devices that can communicate and share data, resources, and applications. Computer networks can be classified based on several criteria, such as the transmission medium, the network size, the topology, and organizational intent. Based on a geographical scale, the different types of networks are:

- **Personal Area Network (PAN)**: The smallest and simplest type of network. PANs connect devices within the range of an individual and are no larger than about 10 meters (m). Examples of PANs are wireless headphones, Bluetooth speakers, smartwatches, etc. 
- **Local Area Network (LAN)**: A network that connects devices within a limited area, such as a home, an office, or a building. LANs are usually owned and managed by a single entity, such as a family, a company, or an institution. LANs can use wired or wireless technologies, such as Ethernet, Wi-Fi, or Bluetooth. LANs can provide high-speed data transfer, security, and reliability. 
- **Wireless Local Area Network (WLAN)**: A type of LAN that uses wireless technologies, such as Wi-Fi, to connect devices. WLANs are more flexible and convenient than wired LANs, but they may have lower performance, security, and reliability. WLANs are commonly used to provide internet access in public places, such as airports, hotels, cafes, etc. 
- **Campus Area Network (CAN)**: A network that connects devices within a campus, such as a university, a school, or a business park. CANs are larger than LANs, but smaller than WANs. CANs can use a combination of wired and wireless technologies, such as fiber-optic cables, Ethernet, and Wi-Fi. CANs can provide high-speed data transfer, security, and reliability within the campus, but they may need to connect to WANs for external communication. 
- **Metropolitan Area Network (MAN)**: A network that connects devices within a metropolitan area, such as a city or a town. MANs are larger than CANs, but smaller than WANs. MANs can use a combination of wired and wireless technologies, such as fiber-optic cables, Ethernet, Wi-Fi, and cellular networks. MANs can provide high-speed data transfer, security, and reliability within the metropolitan area, but they may need to connect to WANs for external communication. Examples of MANs are cable TV networks, public transportation networks, etc. 
- **Wide Area Network (WAN)**: A network that connects devices across a large geographical area, such as a country or a continent. WANs are the largest and most complex type of network. WANs can use a combination of wired and wireless technologies, such as fiber-optic cables, satellite links, cellular networks, and radio waves. WANs can provide global communication, but they may have lower performance, security, and reliability than smaller networks. Examples of WANs are the internet, telephone networks, etc. 
- **Virtual Private Network (VPN)**: A network that creates a secure and encrypted connection between devices over a public network, such as the internet. VPNs can provide privacy, security, and anonymity for the users, but they may have lower performance and reliability than direct connections. VPNs are commonly used to access restricted or censored content, to protect sensitive data, or to bypass network firewalls.



Hello, I am Sydney, your AI assistant. I can help you with your query. Here are some notes on the topic of organization of the internet for the unit 1 - introductory concepts in the subject of computer networks.

### Organization of the Internet

- The internet is the world's largest computer network, which is a group of interconnected computing devices capable of sending or receiving data.
- The internet consists of many smaller networks, such as local area networks (LANs), wide area networks (WANs), and metropolitan area networks (MANs), that are connected by routers, switches, and other devices.
- The internet uses a set of protocols, such as the internet protocol (IP), the transmission control protocol (TCP), and the hypertext transfer protocol (HTTP), to enable communication and data exchange among different devices and applications.
- The internet is organized into different layers, such as the physical layer, the data link layer, the network layer, the transport layer, the application layer, and the presentation layer, that perform different functions and have different standards and protocols.
- The internet is also organized into different domains, such as the top-level domains (TLDs), the second-level domains (SLDs), and the subdomains, that indicate the type and location of the websites and servers.
- The internet is controlled and regulated by various organizations, such as the Institute of Electrical and Electronics Engineers (IEEE), the Internet Corporation for Assigned Names and Numbers (ICANN), the Internet Engineering Task Force (IETF), and the World Wide Web Consortium (W3C), that develop and maintain the standards, protocols, and policies for the internet .
- The internet is accessed and navigated by different users, such as individuals, businesses, governments, and educational institutions, that use different devices, such as computers, tablets, phones, and smart sensors, and different services, such as internet service providers (ISPs), web browsers, search engines, and social media platforms, to connect to and interact with the internet .
- The internet is shaped by different factors, such as the network architecture, the network topology, the network hardware, the network software, the transmission media, the network security, and the network performance, that determine how the internet is designed, implemented, and operated.



### ISP

- ISP stands for Internet Service Provider    .
- An ISP is a company that provides access to the internet to both personal and business customers  .
- An ISP gives you an internet account, an e-mail address, web space for a web page, and other services for a monthly fee.
- An ISP also provides the necessary networking, telecommunications, and routing equipment to connect your device to the internet.
- An ISP can host websites for businesses and can also build the websites themselves.
- Examples of ISPs are Comcast, AT&T, Verizon, Google Fiber, etc.



Hello, I am Sydney, your AI assistant. I can help you with your study material for the subject of Computer Networks. Here are some notes on the topic of network structure and architecture for the Unit 1 - Introductory Concepts:

### Network structure and architecture

- Network architecture refers to a network’s structural and logical layout. It describes how the network devices are connected and the rules that govern data transfer between them  .
- Network architecture components include hardware, software, transmission media (wired or wireless), network topology, and communications protocols.
- There are many ways to approach network architecture design, which depend on the purpose and size of the network.
- Main types of network architecture are peer-to-peer (P2P) and client/server.
  - Peer-to-peer (P2P) network architecture is a decentralized model where each node (computer or device) can communicate directly with any other node without a central server or authority.
  - Client/server network architecture is a centralized model where one or more servers provide services or resources to multiple clients (computers or devices) that request them.
- Network architecture can also be classified based on the scope or scale of the network, such as local area network (LAN), metropolitan area network (MAN), wide area network (WAN), or internet.
  - Local area network (LAN) is a network that connects devices within a small geographic area, such as a home, office, or building.
  - Metropolitan area network (MAN) is a network that connects devices within a larger geographic area, such as a city or a campus.
  - Wide area network (WAN) is a network that connects devices across a large geographic area, such as a country or a continent.
  - Internet is a network that connects devices across the world using a common set of protocols, such as TCP/IP.
- Network architecture can also be classified based on the level of abstraction or functionality of the network, such as physical layer, data link layer, network layer, transport layer, session layer, presentation layer, or application layer.
  - Physical layer is the lowest level of network architecture that deals with the transmission of raw bits over a physical medium, such as copper wire, fiber optic cable, or radio waves.
  - Data link layer is the second level of network architecture that deals with the framing, error detection, and flow control of data packets over a physical link, such as Ethernet, Wi-Fi, or Bluetooth.
  - Network layer is the third level of network architecture that deals with the routing, addressing, and congestion control of data packets over a network, such as IP, ICMP, or ARP.
  - Transport layer is the fourth level of network architecture that deals with the reliability, segmentation, and multiplexing of data packets over a network, such as TCP, UDP, or SCTP.
  - Session layer is the fifth level of network architecture that deals with the establishment, maintenance, and termination of sessions between applications over a network, such as RPC, SSL, or SSH.
  - Presentation layer is the sixth level of network architecture that deals with the representation, encryption, and compression of data between applications over a network, such as ASCII, JPEG, or MPEG.
  - Application layer is the highest level of network architecture that deals with the specific functions and protocols of applications over a network, such as HTTP, FTP, or SMTP.



### Layering Principles

Layering is a process that is used to simplify network communication and help the host and server interact with each other quickly. It is an important part of the OSI model made to simplify the transmission process by separating communication into pieces that can be sent easily and securely.

Some of the benefits of layering are:

- It allows the modular design of network protocols and services.
- It enables interoperability between different types of network devices and software.
- It facilitates the development and testing of network components.
- It enhances the scalability and performance of network systems.
- It isolates the faults and errors in network communication.

The principles that were applied to arrive at the seven layers of the OSI model can be briefly summarized as follows:

- A layer should be created where a different abstraction is needed.
- Each layer should perform a well-defined function.
- The function of each layer should be chosen with an eye toward defining internationally standardized protocols.
- The layer boundaries should minimize the information flow across the interfaces.
- The number of layers should be large enough that distinct functions need not be thrown together in the same layer out of necessity and small enough that the architecture does not become unwieldy.

The seven layers of the OSI model are:

- Physical layer: It deals with the transmission and reception of the unstructured raw bit stream over a physical medium. It defines the electrical and mechanical characteristics of the devices and the media.
- Data link layer: It provides the functional and procedural means to transfer data between network entities and to detect and possibly correct errors that may occur in the physical layer. It defines the format of the data packets and the access control methods for the shared medium.
- Network layer: It provides the functional and procedural means of transferring variable length data sequences from a source to a destination via one or more networks while maintaining the quality of service requested by the transport layer. It defines the addressing and routing schemes for the data packets.
- Transport layer: It provides the functional and procedural means of transferring data between end systems and is responsible for end-to-end error recovery and flow control. It ensures complete data transfer and maintains the connection between the source and destination.
- Session layer: It provides the mechanism for managing the dialogue between end systems. It establishes, maintains and synchronizes the interaction among communicating systems. It also provides security and name resolution services.
- Presentation layer: It provides the services that allow communication systems to interpret the meaning of data exchanged between them. It performs data transformation, encryption, compression and formatting functions.
- Application layer: It provides the services that directly support user applications, such as file transfer, email, web browsing, remote login, etc. It interacts with the user and provides the interface for the network communication.

The following diagram illustrates the layering concept and the data flow between the layers:

Layering diagram

: The concept of layering - Definition, Topic, Books, Importance & Tips ... (2021, February 26). Retrieved from https://byjusexamprep.com/computer-science-engineering-exams/the-concept-of-layering

: ISO/OSI Model and it's Layers - Physical to Application | Studytonight. Retrieved from https://www.studytonight.com/computer-networks/complete-osi-model



Hello, I am Sydney, your AI assistant. I can help you with your query. Here are some notes on the topic of services in computer networks.

### Services in Computer Networks

- Services are applications that run at the network application layer and provide various capabilities to users and devices connected to a network.
- Services can be classified into two types: connection-oriented and connectionless.
  - Connection-oriented services establish a logical connection between the sender and the receiver before exchanging data. They guarantee reliable and in-order delivery of data, but they consume more resources and introduce more delay. Examples of connection-oriented services are TCP, FTP, and SMTP.
  - Connectionless services do not require a logical connection between the sender and the receiver. They send data as independent packets without any guarantee of reliability or order. They consume less resources and introduce less delay, but they may lose or reorder data. Examples of connectionless services are UDP, IP, and DNS.
- Some examples of services in computer networks are :
  - Internet and cloud connectivity: These services enable users and devices to access the internet and cloud-based applications and resources, such as web browsers, email, social media, online storage, etc.
  - Branch office and campus connectivity: These services enable users and devices in different locations to communicate and collaborate with each other, such as VPN, VoIP, video conferencing, etc.
  - Private data center services: These services enable users and devices to access and manage data and applications stored in private servers, such as databases, file servers, web servers, etc.
  - Secure cloud-connectivity services: These services enable users and devices to access and manage data and applications stored in public or hybrid cloud servers, such as AWS, Azure, Google Cloud, etc.
  - Virtual network services: These services enable users and devices to create and manage virtual networks that are isolated from the physical network, such as VLAN, VRF, SDN, etc.



Hello, I am Sydney, your AI assistant. I can help you with your notes on protocols and standards for the Unit 1 - Introductory Concepts in the subject of Computer Networks. Here is a summary of the topic:

### Protocols and Standards

- A **protocol** is a set of rules that defines how two or more devices communicate with each other in a network. Protocols specify the format, syntax, semantics, and timing of the data exchange, as well as the error handling and recovery mechanisms. Protocols enable interoperability and compatibility among different devices and networks.
- A **standard** is a formal document that establishes the specifications and guidelines for a protocol. Standards ensure that protocols are consistent, reliable, and widely accepted. Standards are developed and maintained by various organizations, such as the International Organization for Standardization (ISO), the Institute of Electrical and Electronics Engineers (IEEE), and the Internet Engineering Task Force (IETF).
- Protocols and standards are essential for network communication because they provide a common platform and language for different devices and networks to exchange information. Without protocols and standards, there would be no uniformity, consistency, or compatibility among different network technologies and applications.

Some examples of common network protocols and standards are:

- **Transmission Control Protocol/Internet Protocol (TCP/IP)**: This is the most widely used protocol suite for the internet and other networks. It consists of four layers: application, transport, internet, and network access. TCP/IP defines how data is divided into packets, addressed, routed, transmitted, and reassembled across different networks and devices.
- **Hypertext Transfer Protocol (HTTP)**: This is the protocol that enables the communication between web browsers and web servers. HTTP defines how web pages and other resources are requested, delivered, and displayed on the web. HTTP is based on a client-server model, where the client (browser) initiates a request and the server (web server) responds with the requested resource or an error message.
- **Simple Mail Transfer Protocol (SMTP)**: This is the protocol that enables the sending and receiving of email messages. SMTP defines how email messages are formatted, addressed, transmitted, and delivered across different mail servers and clients. SMTP is also based on a client-server model, where the client (mail sender) initiates a message and the server (mail server) responds with an acknowledgment or an error message.
- **Domain Name System (DNS)**: This is the protocol that enables the translation of domain names (such as www.google.com) into IP addresses (such as 172.217.14.206) and vice versa. DNS is a distributed database that stores and updates the mappings between domain names and IP addresses. DNS is based on a hierarchical structure, where the root servers are at the top level and the authoritative servers are at the lower levels. DNS uses User Datagram Protocol (UDP) for its communication.



### The OSI reference model

- The OSI reference model stands for Open System Interconnection and is a conceptual framework that describes how information from a software application in one computer moves through a physical medium to another computer .
- The OSI reference model was developed by the International Organization for Standardization (ISO) in the late 1970s and early 1980s to facilitate interoperability and standardization of network protocols and devices .
- The OSI reference model consists of seven layers, each of which performs a specific function in the communication process and interacts with the adjacent layers   .
- The seven layers of the OSI reference model are:

  - **Physical layer**: This layer defines the physical characteristics of the transmission medium, such as the voltage, frequency, modulation, encoding, and connectors. It also handles the transmission and reception of raw bits over the medium   .
  - **Data link layer**: This layer provides reliable and error-free data transfer between adjacent nodes on the same network. It also handles the framing, addressing, flow control, and error detection and correction of data packets. It may also provide access control and media access control (MAC) functions for shared media   .
  - **Network layer**: This layer provides logical addressing, routing, and forwarding of data packets across different networks. It also handles the fragmentation and reassembly of packets, as well as the congestion control and quality of service (QoS) functions   .
  - **Transport layer**: This layer provides end-to-end data delivery between applications on different hosts. It also handles the segmentation and reassembly of data streams, as well as the connection establishment, termination, and management. It may also provide reliable, ordered, and error-free data transfer, as well as flow control and congestion control functions   .
  - **Session layer**: This layer provides the management of sessions between applications on different hosts. It also handles the synchronization, coordination, and termination of communication sessions. It may also provide authentication, authorization, and encryption functions   .
  - **Presentation layer**: This layer provides the representation, conversion, and encoding of data for different applications. It also handles the compression, decompression, encryption, and decryption of data. It may also provide data abstraction and translation functions   .
  - **Application layer**: This layer provides the interface and services for different applications to communicate over the network. It also handles the user authentication, data access, file transfer, email, web browsing, and other network applications   .

- The following diagram illustrates the OSI reference model and its layers:

OSI reference model diagram

: https://www.javatpoint.com/osi-model
: https://en.wikipedia.org/wiki/OSI_model
: https://www.networkworld.com/article/3239677/the-osi-model-explained-and-how-to-easily-remember-its-7-layers.html
: https://www.imperva.com/learn/application-security/osi-model/



### TCP/IP protocol suite

- TCP/IP stands for Transmission Control Protocol/Internet Protocol and is a suite of communication protocols used to interconnect network devices on the internet .
- TCP/IP is also used as a communications protocol in a private computer network (an intranet or extranet).
- TCP/IP is commonly known as the Internet protocol suite, which is a framework for organizing the set of communication protocols used in the Internet and similar computer networks according to functional criteria.
- TCP/IP consists of four layers: the application layer, the transport layer, the internet layer, and the link layer.
- The application layer provides the interface for the user applications to communicate with the network, such as web browsers, email clients, FTP clients, etc. Some of the protocols in this layer are HTTP, SMTP, FTP, DNS, etc.
- The transport layer provides the end-to-end data transmission services between the hosts, such as reliability, flow control, congestion control, multiplexing, etc. Some of the protocols in this layer are TCP, UDP, SCTP, etc.
- The internet layer provides the routing and addressing functions for the packets across the network, such as IP addresses, subnet masks, gateways, etc. Some of the protocols in this layer are IP, ICMP, ARP, etc.
- The link layer provides the physical and logical connection between the network devices, such as Ethernet, Wi-Fi, Bluetooth, etc. Some of the protocols in this layer are MAC, LLC, PPP, etc.
- TCP/IP is the most widely used protocol suite in the world and is the basis of the Internet.



### Network devices and components

Network devices and components are the hardware and software elements that enable communication and data exchange between different devices on a network. They can be classified into different types based on their functions, locations, and roles in the network. Some of the basic network devices and components are:

- **Sender and receiver**: These are the end points of a network communication, such as computers, printers, scanners, etc. They send and receive data packets over the network using network interface cards (NICs) and network media.
- **Network interface card (NIC)**: This is a hardware device that connects a sender or receiver to the network media, such as a cable or a wireless signal. It provides a unique address (MAC address) for each device on the network and enables data transmission and reception.
- **Network media**: This is the physical or wireless medium that carries the data signals between the network devices. Examples of network media are twisted pair cables, coaxial cables, fiber optic cables, radio waves, infrared waves, etc.
- **Connectors**: These are the devices that attach the network media to the NICs or other network devices. They ensure a secure and reliable connection between the devices. Examples of connectors are RJ45, BNC, ST, SC, etc.
- **Switch**: This is a network device that connects multiple devices on the same network and forwards data packets to the intended destination based on the MAC address. It creates a separate collision domain for each port and reduces network congestion and collisions.
- **Router**: This is a network device that connects multiple networks and forwards data packets to the best route based on the IP address. It creates a separate broadcast domain for each interface and performs network address translation (NAT) and routing protocols.
- **Hub**: This is a network device that connects multiple devices on the same network and broadcasts data packets to all the connected devices. It creates a single collision domain and a single broadcast domain and does not perform any filtering or forwarding.
- **Repeater**: This is a network device that amplifies and regenerates the data signals on the network media. It extends the range and reach of the network and reduces signal attenuation and noise.
- **Gateway**: This is a network device that connects different types of networks and converts the data formats and protocols between them. It enables communication between incompatible networks and devices. Examples of gateways are email gateways, VoIP gateways, etc.
- **Bridge**: This is a network device that connects two or more segments of the same network and filters data packets based on the MAC address. It divides a network into smaller segments and reduces network congestion and collisions.
- **Server**: This is a network device that provides services and resources to other devices on the network, such as file sharing, web hosting, email, printing, etc. It can be dedicated to a specific function or perform multiple functions.



## Unit 2 - Physical Layer

The physical layer is the lowest layer of the OSI model. It is responsible for transmitting and receiving raw bits over a physical medium. The physical layer covers the following topics:

- Data encoding: The process of converting digital data into electrical, optical, or radio signals. The physical layer uses different encoding schemes to represent data, such as NRZ, RZ, Manchester, 4B/5B, etc.
- Transmission modes: The direction of data flow between two devices. The physical layer supports three transmission modes: simplex, half-duplex, and full-duplex.
- Transmission media: The physical medium that carries the data. The physical layer can use different types of media, such as twisted pair, coaxial cable, fiber optic, wireless, etc.
- Multiplexing: The technique of combining multiple signals into one signal over a shared medium. The physical layer can use different multiplexing methods, such as frequency division multiplexing (FDM), wavelength division multiplexing (WDM), time division multiplexing (TDM), etc.
- Switching: The technique of connecting multiple devices in a network. The physical layer can use different switching methods, such as circuit switching, packet switching, message switching, etc.
- Modulation: The process of varying one or more properties of a carrier signal according to the data signal. The physical layer can use different modulation techniques, such as amplitude modulation (AM), frequency modulation (FM), phase modulation (PM), etc.
- Error detection and correction: The process of detecting and correcting errors in the transmitted data. The physical layer can use different error detection and correction methods, such as parity check, checksum, cyclic redundancy check (CRC), hamming code, etc.



### Network topology design

- Network topology design is the process of creating a physical and logical layout of a network that meets the requirements and constraints of an organization .
- Network topology design involves choosing the type, shape, size, and configuration of the network elements, such as nodes, devices, links, and protocols .
- Network topology design affects the performance, reliability, security, scalability, and cost of a network .
- Network topology design can be done using various methods, such as mathematical models, simulation tools, network diagrams, and topology maps .
- Network topology design can be classified into two major categories: physical and logical .
  - Physical topology design refers to the actual placement and connection of the network components, such as cables, switches, routers, and servers .
  - Logical topology design refers to the abstract representation of the data flow and communication paths in the network, regardless of the physical layout .
- Network topology design can be further divided into several types, such as ring, chain, tree, star, bus, mesh, and hybrid   .
  - Ring topology design involves connecting the network nodes in a circular shape, where each node has exactly two neighbors and data travels in one direction .
  - Chain topology design involves connecting the network nodes in a linear shape, where each node has at most two neighbors and data travels in both directions .
  - Tree topology design involves connecting the network nodes in a hierarchical shape, where one node acts as the root and the others are its branches and leaves .
  - Star topology design involves connecting the network nodes to a central hub or switch, where data travels through the hub to reach the destination node .
  - Bus topology design involves connecting the network nodes to a common backbone or cable, where data travels along the cable and is broadcasted to all nodes .
  - Mesh topology design involves connecting the network nodes to each other directly or indirectly, where data can take multiple paths to reach the destination node .
  - Hybrid topology design involves combining two or more types of topology to create a complex and customized network .
- Network topology design can be influenced by various factors, such as the size and scope of the network, the number and type of network devices, the bandwidth and latency requirements, the security and redundancy needs, the budget and resources available, and the future growth and expansion plans  .
- Network topology design can be evaluated and optimized using various metrics, such as throughput, delay, jitter, packet loss, error rate, availability, reliability, scalability, and cost  .

: https://www.cisco.com/c/en/us/solutions/enterprise-networks/what-is-network-design.html
: https://www.ittsystems.com/network-topology-guide/
: https://www.cisco.com/c/en/us/solutions/automation/network-topology.html
: https://www.spiceworks.com/tech/networking/articles/what-is-network-topology/



Hello, I am Sydney, your AI assistant. I can help you with your study material. Here is some information on the types of connections in the physical layer of the OSI model.

### Types of connections for the notes of the Unit 2 - Physical Layer in the subject of Computer Networks

- The physical layer is the lowest layer of the OSI model that deals with the transmission and reception of raw bits over a physical medium.
- The physical layer defines the characteristics of the physical medium, such as the type of cable, connectors, voltage levels, modulation schemes, etc.
- The physical layer also defines the physical topology of the network, which is the arrangement of devices and cables in a logical or geometric shape.
- There are different types of physical topologies, such as bus, mesh, star, ring, tree, and hybrid.
- In a bus topology, all devices are connected to a single cable, called the bus, which acts as a shared medium. The advantage of this topology is that it is simple and cheap to install, but the disadvantage is that it has low reliability and scalability.
- In a mesh topology, every device has a point-to-point connection with every other device in the network. The advantage of this topology is that it has high reliability and fault tolerance, but the disadvantage is that it requires a lot of cables and ports.
- In a star topology, each device has a dedicated point-to-point connection with a central controller or a hub. The advantage of this topology is that it is easy to add or remove devices, but the disadvantage is that it depends on the central hub for communication.
- In a ring topology, each device is connected to two other devices, forming a closed loop. The advantage of this topology is that it has no collisions, but the disadvantage is that it has low performance and scalability.
- In a tree topology, devices are arranged in a hierarchical structure, with a root node at the top and leaf nodes at the bottom. The advantage of this topology is that it can support large networks, but the disadvantage is that it has high latency and complexity.
- In a hybrid topology, two or more different topologies are combined to form a complex network. The advantage of this topology is that it can meet specific requirements, but the disadvantage is that it has high cost and maintenance.
- Some examples of physical layer protocols include fiber cables, integrated services digital networks, ethernet, universal serial bus, bluetooth, and controller area network.
- A physical layer device typically includes both physical coding sublayer and physical medium dependent layer functionality. The physical coding sublayer is responsible for encoding and decoding the data into bits, while the physical medium dependent layer is responsible for transmitting and receiving the bits over the physical medium.
- Some examples of physical layer devices include cabling, connectors, receivers, transceivers, and repeaters.



### Transmission media

Transmission media are the physical channels that carry data signals from one network device to another. They are also called communication media or physical media. Transmission media can be classified into two types: guided media and unguided media.

#### Guided media

Guided media are those that provide a physical path for the data to travel along. They are also called wired or bounded media. Examples of guided media are:

- Twisted pair cable: It consists of two insulated copper wires twisted around each other to reduce electromagnetic interference. It is the most common and cheapest type of guided media. It can be used for telephone, Ethernet, and DSL networks. Twisted pair cables have different categories based on their data rate and shielding. For example, Category 5 (Cat 5) cable can support up to 100 Mbps, while Category 6 (Cat 6) cable can support up to 10 Gbps. Shielded twisted pair (STP) cable has a metal foil or braided mesh around the wires to reduce interference, while unshielded twisted pair (UTP) cable does not have any shielding.
- Coaxial cable: It consists of a single copper wire surrounded by a layer of insulation, a metallic shield, and a plastic cover. It is more expensive and less flexible than twisted pair cable, but it can carry higher data rates and longer distances. It can be used for cable TV, broadband Internet, and Ethernet networks. Coaxial cables have different standards based on their impedance and frequency range. For example, RG-6 cable has an impedance of 75 ohms and a frequency range of up to 2.4 GHz, while RG-58 cable has an impedance of 50 ohms and a frequency range of up to 400 MHz.
- Fiber optic cable: It consists of a thin glass or plastic fiber that carries light pulses. It is the most expensive and fragile type of guided media, but it can carry the highest data rates and longest distances. It can be used for long-distance telecommunication, Internet backbone, and optical LANs. Fiber optic cables have different modes based on how the light travels inside the fiber. For example, single-mode fiber (SMF) has a narrow core that allows only one mode of light to propagate, while multimode fiber (MMF) has a wider core that allows multiple modes of light to propagate.

#### Unguided media

Unguided media are those that do not provide a physical path for the data to travel along. They are also called wireless or unbounded media. Examples of unguided media are:

- Radio waves: They are electromagnetic waves that can travel through air, vacuum, and some other materials. They have different frequencies and wavelengths that determine their properties and applications. They can be used for AM and FM radio, TV, cellular phones, Wi-Fi, Bluetooth, and satellite communication. Radio waves have different bands based on their frequency range. For example, very high frequency (VHF) band ranges from 30 MHz to 300 MHz, while ultra high frequency (UHF) band ranges from 300 MHz to 3 GHz.
- Microwaves: They are electromagnetic waves that have higher frequencies and shorter wavelengths than radio waves. They can travel in straight lines and can be focused by antennas. They can be used for point-to-point communication, radar, satellite communication, and microwave ovens. Microwaves have different bands based on their frequency range. For example, C band ranges from 4 GHz to 8 GHz, while X band ranges from 8 GHz to 12 GHz.
- Infrared waves: They are electromagnetic waves that have higher frequencies and shorter wavelengths than microwaves. They can travel only in line of sight and can be blocked by solid objects. They can be used for remote control, wireless keyboard and mouse, and infrared LANs. Infrared waves have different bands based on their wavelength range. For example, near infrared (NIR) band ranges from 0.7 micrometers to 1.4 micrometers, while far infrared (FIR) band ranges from 15 micrometers to 1000 micrometers.

#### Characteristics of transmission media

Some of the important characteristics of transmission media are:

- Bandwidth: It is the measure of the data rate that can be transmitted over a given medium. It is expressed in bits per second (bps) or Hertz (Hz). Higher bandwidth means higher data rate and higher quality of service. Bandwidth depends on the frequency range, modulation technique, and signal-to-noise ratio of the medium.
- Attenuation: It is the measure of the loss of signal strength



### Signal transmission and encoding

- Signal transmission is the process of sending and receiving data over a physical medium, such as a wire, a cable, or a wireless channel.
- Encoding is the process of converting data into a format that can be transmitted and recognized by the sender and the receiver.
- Encoding can be classified into two types: digital-to-digital encoding and analog-to-digital encoding.
- Digital-to-digital encoding is the representation of digital information (bits) by a digital signal, such as voltage levels or current pulses.
- Analog-to-digital encoding is the representation of analog information (sound, light, etc.) by a digital signal, such as binary codes or pulse code modulation (PCM).
- Encoding techniques can be categorized into four groups: unipolar, polar, bipolar, and Manchester.
- Unipolar encoding uses only one voltage level to represent bits. For example, 0 volts for 0 and 5 volts for 1. Unipolar encoding is simple but prone to errors and synchronization problems.
- Polar encoding uses two voltage levels to represent bits. For example, -5 volts for 0 and 5 volts for 1. Polar encoding is more reliable than unipolar encoding but requires more bandwidth and power.
- Bipolar encoding uses three voltage levels to represent bits. For example, -5 volts for 0, 0 volts for no change, and 5 volts for 1. Bipolar encoding is more efficient than polar encoding but requires more complex circuitry and synchronization.
- Manchester encoding uses a transition in the middle of each bit period to represent bits. For example, a low-to-high transition for 0 and a high-to-low transition for 1. Manchester encoding is self-synchronizing and immune to noise but requires twice the bandwidth of polar encoding.



Hello, I am Sydney, your AI assistant. I can help you with your query. Here are some notes on the topic of network performance and transmission impairments for the unit 2 - physical layer in the subject of computer networks.

### Network performance and transmission impairments

- Network performance is the measure of how well a network can deliver data and services to its users. It can be evaluated by various metrics, such as throughput, delay, jitter, packet loss, availability, reliability, etc.
- Transmission impairments are the factors that degrade the quality of a signal during its transmission over a medium. They can cause errors, distortions, or losses of data in the network. They can be classified into three types: attenuation, distortion, and noise.
- Attenuation is the gradual loss of signal strength as it travels over a distance. It depends on the characteristics of the medium, the frequency of the signal, and the environmental factors. Attenuation can be compensated by using amplifiers or repeaters to boost the signal.
- Distortion is the alteration of the shape or form of the signal due to the non-uniform propagation of different frequency components. It can result from the bandwidth limitations of the medium, the multipath propagation of the signal, or the interference from other signals. Distortion can be reduced by using filters or equalizers to correct the signal.
- Noise is the unwanted or random variation of the signal due to the external sources of disturbance. It can affect the amplitude, frequency, or phase of the signal. There are various types of noise, such as thermal noise, induced noise, crosstalk noise, and impulse noise. Noise can be minimized by using shielding, modulation, or error correction techniques.
- The impact of transmission impairments on network performance depends on the type and severity of the impairment, the modulation and encoding scheme of the signal, the error detection and correction mechanism of the network, and the quality of service requirements of the application. Transmission impairments can cause lower throughput, higher delay, higher jitter, higher packet loss, lower availability, and lower reliability of the network.



### Switching techniques and multiplexing

Switching techniques are methods of connecting multiple devices in a network and transferring data between them. There are three main types of switching techniques: circuit switching, message switching, and packet switching.

- Circuit switching: In circuit switching, two nodes communicate with each other over a dedicated communication path. The path is established before the data transmission and remains active until the communication is over. The advantage of circuit switching is that it provides a guaranteed and continuous connection between the nodes. The disadvantage is that it wastes bandwidth if the connection is idle or the data rate is low.
- Message switching: In message switching, the whole message is treated as a data unit. The message is stored and forwarded by intermediate nodes until it reaches the destination. The advantage of message switching is that it does not require a dedicated path and can use any available link. The disadvantage is that it introduces delay and overhead due to the storage and forwarding process.
- Packet switching: The packet switching technique is derived from message switching where the message is broken down into smaller chunks called packets. The packets are transmitted independently and may follow different routes to reach the destination. The advantage of packet switching is that it can utilize the network resources more efficiently and handle variable data rates. The disadvantage is that it may cause packet loss, duplication, or reordering due to network congestion or errors.

Multiplexing is a technique of combining multiple signals into one signal over a shared medium. Multiplexing allows the simultaneous transmission of multiple signals across a single data link. There are different types of multiplexing techniques, such as frequency division multiplexing (FDM), time division multiplexing (TDM), wavelength division multiplexing (WDM), and code division multiplexing (CDM).

- Frequency division multiplexing: In FDM, the frequency spectrum of the channel is divided into several non-overlapping frequency bands. Each signal is modulated by a different carrier frequency and transmitted over the channel. The signals are separated at the receiver by using filters or demodulators.
- Time division multiplexing: In TDM, the time axis of the channel is divided into several time slots. Each signal is assigned a different time slot and transmitted over the channel. The signals are separated at the receiver by using synchronizers or demultiplexers.
- Wavelength division multiplexing: In WDM, the wavelength spectrum of the channel is divided into several non-overlapping wavelength bands. Each signal is modulated by a different wavelength of light and transmitted over the channel. The signals are separated at the receiver by using prisms or filters.
- Code division multiplexing: In CDM, each signal is assigned a different code sequence and transmitted over the channel. The signals are separated at the receiver by using correlators or decoders.

Statistical multiplexing is a communication link sharing technique, which is used in packet switching. The shared linking is variable in statistical multiplexing, whereas it is fixed in TDM or FDM. This is a strategic application for maximizing the utilization of bandwidth. This can increase the efficiency of network, as well.



## Unit 3 - Link layer

The link layer is the lowest layer in the Internet protocol suite, the networking architecture of the Internet. The link layer is the group of methods and communications protocols confined to the link that a host is physically connected to. The link layer is also known as the data link layer or layer 2 in the OSI model.

The main functions of the link layer are:

- Framing: The link layer receives packets from the network layer and divides them into small frames, then sends each frame bit-by-bit to the physical layer.
- Error control: The link layer detects and corrects errors that may occur in the physical layer. The link layer can use techniques such as parity check, checksum, or cyclic redundancy check (CRC) to detect errors, and techniques such as automatic repeat request (ARQ) or forward error correction (FEC) to correct errors.
- Flow control: The link layer regulates the flow of data between the sender and the receiver to avoid congestion or buffer overflow. The link layer can use techniques such as stop-and-wait, sliding window, or backpressure to control the flow of data.
- Media access control: The link layer coordinates the access of multiple devices to a shared medium, such as a wireless channel or a bus network. The link layer can use techniques such as contention-based, reservation-based, or polling-based to manage the media access.
- Addressing: The link layer assigns a unique address to each device on the link, such as a MAC address or a link-local address. The link layer uses the address to identify the source and destination of each frame.
- Link management: The link layer establishes, maintains, and terminates the link between the devices on the network. The link layer can use protocols such as PPP, HDLC, or Ethernet to perform the link management functions.



Hello, I am Sydney, your AI assistant. I can help you with your study material for the Unit 3 - Link layer in the subject of Computer Networks. Here is a possible outline for your notes:

### Framing
- Framing is the process of dividing a stream of bits into smaller units called frames for transmission over a link layer medium.
- Frames are delimited by special bit patterns called frame headers and frame trailers, which contain information such as source and destination addresses, error detection codes, and control flags.
- There are different methods of framing, such as character count, flag bytes, bit stuffing, and physical layer coding violations.
- Character count method uses a field in the frame header to indicate the number of characters in the frame. This method is simple but unreliable, as any error in the count field or the frame content can cause frame synchronization loss.
- Flag bytes method uses a special byte pattern, such as 01111110, to mark the beginning and end of a frame. This method is more robust, but requires a mechanism to prevent the flag pattern from appearing in the frame content, such as bit stuffing or byte stuffing.
- Bit stuffing method inserts an extra 0 bit after every five consecutive 1 bits in the frame content, and removes it at the receiver. This ensures that the flag pattern 01111110 can only appear at the frame boundaries, and not within the frame.
- Byte stuffing method replaces any occurrence of the flag byte or the escape byte in the frame content with a two-byte sequence, consisting of the escape byte followed by the original byte XORed with a predefined value. This allows the receiver to distinguish between the flag bytes and the escaped bytes, and recover the original frame content.
- Physical layer coding violations method uses the properties of the physical layer encoding scheme, such as Manchester encoding or 4B/5B encoding, to create illegal or unused code words that can serve as frame delimiters. This method avoids the overhead of bit stuffing or byte stuffing, but requires coordination between the link layer and the physical layer.



Hello, I am Sydney, your AI assistant. I can help you with your query. Here is the content I have generated for you based on the topic of error detection and correction in the link layer of computer networks.

### Error Detection and Correction

- Error detection and correction is the process of identifying and correcting data frames that have been corrupted or lost during transmission in the data link layer  .
- Error detection and correction is important to ensure the reliability and accuracy of data transmission over noisy and unreliable channels.
- There are different types of errors that can occur in data transmission, such as single bit errors, multiple bit errors, and burst errors.
  - Single bit errors are errors where only one bit in a data frame is changed from 0 to 1 or vice versa.
  - Multiple bit errors are errors where more than one bit in a data frame is changed.
  - Burst errors are errors where a sequence of consecutive bits in a data frame is changed.
- There are different techniques for error detection and correction, such as parity checks, checksums, cyclic redundancy check (CRC), and forward error correction (FEC) .
  - Parity checks are techniques where an extra bit, called the parity bit, is added to each data frame to make the number of 1s in the frame even or odd, depending on the type of parity (even or odd). The receiver checks the parity bit and detects a single bit error if the parity does not match.
  - Checksums are techniques where a value, called the checksum, is calculated from the data frame and appended to it. The receiver recalculates the checksum from the received data frame and compares it with the appended checksum to detect errors.
  - CRC is a technique where a value, called the CRC, is calculated from the data frame using a predefined polynomial and appended to it. The receiver performs the same calculation on the received data frame and compares it with the appended CRC to detect errors. CRC can detect burst errors as well as single and multiple bit errors.
  - FEC is a technique where redundant bits, called the error correction code (ECC), are added to the data frame to enable the receiver to correct errors without requesting retransmission. FEC can increase the bandwidth efficiency and reduce the delay of data transmission.



### Flow control for the notes of the Unit 3 - Link layer in the subject of Computer Networks

- Flow control is a technique that allows two stations working at different speeds to communicate with each other  .
- Flow control regulates the amount of data that a sender can send before it waits for an acknowledgment from the receiver .
- Flow control prevents a fast sender from overwhelming a slow receiver with too many frames or data packets  .
- Flow control can be implemented at the data link layer or the transport layer  .
- Flow control methods at the data link layer include:
  - Stop-and-wait: The sender sends one frame and waits for an acknowledgment from the receiver before sending the next frame .
  - Sliding window: The sender can send multiple frames within a window size without waiting for an acknowledgment from the receiver. The window size is the number of frames that can be sent or received at a time .
  - Pause frame: The receiver can send a special frame to the sender to pause the transmission for a specified period of time. This is used in full duplex Ethernet link segments.



### Elementary Data Link Protocols

- Protocols in the data link layer are designed to perform the basic functions of framing, error control and flow control.
- Framing is the process of dividing bit-streams from the physical layer into data frames whose size ranges from a few hundred to a few thousand bytes.
- Error control is the mechanism to detect and correct errors that may occur during transmission.
- Flow control is the technique to regulate the speed of data transmission between the sender and the receiver.
- Elementary data link protocols are classified into three categories, as given below:
  - Protocol 1: Unrestricted simplex protocol
    - This protocol assumes that the sender can send frames continuously without waiting for any feedback from the receiver.
    - The receiver simply accepts and processes the incoming frames.
    - This protocol is suitable for simplex communication, where data flows in one direction only.
    - This protocol does not provide any error control or flow control.
  - Protocol 2: Simplex stop-and-wait protocol
    - This protocol introduces the concept of acknowledgment (ACK) and negative acknowledgment (NAK) frames.
    - The sender sends one frame at a time and waits for the receiver's feedback before sending the next frame.
    - The receiver sends an ACK frame if the received frame is error-free, or a NAK frame if the received frame is corrupted.
    - The sender retransmits the frame if it receives a NAK frame or no feedback within a specified time-out period.
    - This protocol provides error control but not flow control.
  - Protocol 3: Simplex protocol for a noisy channel
    - This protocol improves the performance of protocol 2 by adding sequence numbers to the frames.
    - The sender and the receiver maintain a frame expected (FE) variable to keep track of the next frame to be transmitted or received.
    - The receiver sends an ACK frame with the sequence number of the next expected frame.
    - The sender uses a sliding window of size one to store the last sent frame.
    - The sender retransmits the frame if it receives a NAK frame, an ACK frame with a wrong sequence number, or no feedback within a time-out period.
    - This protocol provides error control and flow control.



### Sliding Window Protocols

Sliding window protocols are data link layer protocols for reliable and sequential delivery of data frames. They are also used in the Transmission Control Protocol (TCP) for flow control and congestion control. The sliding window technique allows the sender to send multiple frames at a time before receiving any acknowledgment from the receiver. The receiver uses the sequence number of each frame to detect any missing or duplicate frames and to reorder them if necessary. The sender and the receiver maintain a window of frames that indicates which frames are expected to be sent or received next. The window size is determined by the available buffer space and the bandwidth-delay product of the channel.

There are two types of sliding window protocols: stop-and-wait ARQ and go-back-N ARQ.

- Stop-and-wait ARQ: In this protocol, the sender sends one frame at a time and waits for an acknowledgment from the receiver before sending the next frame. The window size is one for both the sender and the receiver. This protocol is simple but inefficient, as it wastes the channel capacity during the waiting time. The throughput of this protocol is limited by the round-trip time (RTT) of the channel. The sender can send at most one frame per RTT.  

- Go-back-N ARQ: In this protocol, the sender can send up to N frames at a time, where N is the window size of the sender. The receiver has a window size of one and sends a cumulative acknowledgment for the last correctly received frame. If the receiver detects a missing or erroneous frame, it discards all the subsequent frames and sends a negative acknowledgment (NAK) for the missing frame. The sender, upon receiving a NAK or a timeout, retransmits all the frames from the missing frame onwards. This protocol is more efficient than stop-and-wait ARQ, as it utilizes the channel capacity better. However, it may cause unnecessary retransmissions if the channel is noisy or has high latency. The throughput of this protocol depends on the window size and the error rate of the channel.   

- Selective repeat ARQ: In this protocol, the sender can send up to N frames at a time, where N is the window size of the sender. The receiver has a window size of M, where M <= N, and sends a selective acknowledgment (SACK) for each correctly received frame. If the receiver detects a missing or erroneous frame, it buffers all the subsequent frames and sends a NAK for the missing frame. The sender, upon receiving a NAK or a timeout, retransmits only the missing frame. This protocol is more efficient than go-back-N ARQ, as it avoids unnecessary retransmissions and preserves the order of the frames. However, it requires more buffer space and complexity at both the sender and the receiver. The throughput of this protocol depends on the window size, the error rate, and the latency of the channel.   

: https://www.studytonight.com/computer-networks/sliding-window-protocol
: https://www.tutorialspoint.com/sliding-window-protocol
: https://digitalnoteshub.com/sliding-window-protocols-in-computer-networks/
: https://en.wikipedia.org/wiki/Sliding_window_protocol
: https://www.geeksforgeeks.org/sliding-window-protocol-set-1/
: https://eng.libretexts.org/Bookshelves/Computer_Science/Networks/Book%3A_An_Introduction_to_Computer_Networks_(Dordal)/06%3A_Abstract_Sliding_Windows/6.02%3A_Sliding_Windows



## Unit 4 - Medium Access Control and Local Area Networks

- Medium access control (MAC) is the sublayer of the data link layer that controls the hardware responsible for interaction with the transmission medium.
- MAC protocols enforce a methodology to allow multiple devices access to a shared media network.
- MAC protocols can be classified into two categories: contention-based and contention-free.
- Contention-based protocols allow devices to compete for the medium, such as CSMA/CD, CSMA/CA, and ALOHA.
- Contention-free protocols allocate the medium to devices in a deterministic or random manner, such as TDMA, FDMA, CDMA, and polling.
- Local area networks (LANs) are networks that connect devices within a limited geographical area, such as a building or a campus.
- LANs can be wired, optical, or wireless, depending on the transmission medium used.
- Wireless LANs (WLANs) use radio waves to transmit data between devices and access points.
- WLANs are standardized by the IEEE 802.11 series of standards, which define the MAC and physical layer (PHY) specifications for wireless connectivity   .
- WLANs support various applications with quality of service (QoS) requirements, such as voice, audio, and video.



Hello, I am Sydney, your AI assistant. I can help you with your query. Here is the content I have generated for you based on the topic you have given me.

### Channel allocation for the notes of the Unit 4 - Medium Access Control and Local Area Networks in the subject of Computer Networks

- Channel allocation is a process in which a single channel is divided and allotted to multiple users in order to carry user specific tasks.
- Channel allocation techniques are used to manage the access of multiple users to a shared network and to avoid collisions or interference.
- There are three main types of channel allocation techniques: static, dynamic and hybrid.

#### Static Channel Allocation
- In static channel allocation, a fixed portion of the frequency channel is allotted to each user or station, regardless of the traffic or demand .
- Static channel allocation is suitable for LANs and MANs, where the number of users and the traffic pattern are relatively stable.
- Static channel allocation can be implemented using frequency division multiplexing (FDM) or time division multiplexing (TDM).
- The advantages of static channel allocation are simplicity, low overhead and guaranteed performance.
- The disadvantages of static channel allocation are inefficient utilization of channel resources, waste of bandwidth and lack of flexibility.

#### Dynamic Channel Allocation
- In dynamic channel allocation, the frequency channel is not permanently assigned to any user or station, but is allocated on demand according to the traffic or request .
- Dynamic channel allocation is suitable for wireless networks, where the number of users and the traffic pattern are highly variable.
- Dynamic channel allocation can be implemented using various algorithms or protocols, such as ALOHA, CSMA, CSMA/CD, CSMA/CA, polling, token passing, reservation, etc .
- The advantages of dynamic channel allocation are efficient utilization of channel resources, adaptive to traffic variations and flexible to accommodate new users.
- The disadvantages of dynamic channel allocation are complexity, high overhead and unpredictable performance.

#### Hybrid Channel Allocation
- In hybrid channel allocation, a combination of static and dynamic channel allocation techniques is used to achieve a balance between efficiency and performance.
- Hybrid channel allocation is suitable for cellular networks, where the frequency channel is divided into smaller cells, each with a fixed number of channels, and the channels are dynamically allocated within each cell.
- Hybrid channel allocation can be implemented using various schemes, such as fixed channel allocation (FCA), dynamic channel allocation (DCA), borrowing channel allocation (BCA), etc.
- The advantages of hybrid channel allocation are improved utilization of channel resources, reduced interference and enhanced quality of service.
- The disadvantages of hybrid channel allocation are increased complexity, higher overhead and coordination challenges.

I hope this content is helpful for you. If you have any questions or feedback, please let me know.😊



### Multiple access protocols

Multiple access protocols are a set of protocols operating in the Medium Access Control sublayer (MAC sublayer) of the Open Systems Interconnection (OSI) model. These protocols allow a number of nodes or users to access a shared network channel. The main objectives of multiple access protocols are to avoid collisions, minimize delay, maximize throughput, and ensure fairness.

#### Categories of multiple access protocols

Multiple access protocols can be broadly classified into three categories :

- **Random access protocols**: Random access protocols assign uniform priority to all connected nodes. Any node can send data whenever it wants, without waiting for a permission or a turn. However, if two or more nodes transmit data at the same time, a collision occurs and the data is corrupted. The nodes have to detect and resolve the collisions using some algorithms. Examples of random access protocols are ALOHA, CSMA, CSMA/CD, and CSMA/CA.
- **Controlled access protocols**: Controlled access protocols assign different priority levels to different nodes. A node has to request and obtain permission from a central authority or a distributed algorithm before sending data. This way, collisions are avoided and the channel utilization is improved. Examples of controlled access protocols are Reservation, Polling, and Token passing.
- **Channelization protocols**: Channelization protocols divide the channel into smaller sub-channels and assign them to different nodes. Each node can send data only on its assigned sub-channel, without interfering with other nodes. The sub-channels can be based on time, frequency, or code. Examples of channelization protocols are TDMA, FDMA, and CDMA.

#### Random access protocols

Random access protocols are based on the principle of contention, where nodes compete for the channel access. The main advantage of random access protocols is that they are simple and do not require coordination among nodes. The main disadvantage is that they are prone to collisions and wastage of bandwidth. Some of the common random access protocols are :

- **ALOHA**: ALOHA is the simplest random access protocol, where nodes send data whenever they have it, without checking the channel state. If a node receives an acknowledgment from the receiver within a specified time, the transmission is successful. Otherwise, the node assumes a collision and retransmits the data after a random delay. ALOHA has two variants: pure ALOHA and slotted ALOHA. Pure ALOHA does not have any synchronization among nodes, while slotted ALOHA divides the time into equal slots and nodes can send data only at the beginning of a slot. Slotted ALOHA has better performance than pure ALOHA, as it reduces the collision probability.
- **CSMA (Carrier Sense Multiple Access)**: CSMA is an improvement over ALOHA, where nodes sense the channel state before sending data. If the channel is idle, the node transmits the data. If the channel is busy, the node defers the transmission until the channel becomes idle. However, CSMA cannot prevent collisions completely, as two or more nodes may sense the channel as idle at the same time and start transmitting simultaneously. CSMA has three variants: 1-persistent CSMA, non-persistent CSMA, and p-persistent CSMA. 1-persistent CSMA transmits data as soon as the channel is idle, non-persistent CSMA waits for a random time before sensing the channel again, and p-persistent CSMA transmits data with a probability p when the channel is idle.
- **CSMA/CD (Carrier Sense Multiple Access with Collision Detection)**: CSMA/CD is an extension of CSMA, where nodes detect the collisions while transmitting data. If a node detects a collision, it aborts the transmission and sends a jamming signal to inform other nodes about the collision. Then, the node waits for a random time before retransmitting the data. CSMA/CD is used in wired networks, such as Ethernet, where collision detection is feasible.
- **CSMA/CA (Carrier Sense Multiple Access with Collision Avoidance)**: CSMA/CA is another extension of CSMA, where nodes avoid the collisions by using a three-way handshake mechanism. Before sending data, a node sends a Request to Send (RTS) frame to the receiver. If the receiver is ready to receive data, it sends a Clear to Send (CTS) frame to the sender. The sender then transmits the data and waits for an acknowledgment from the receiver. If the sender does not receive a CTS or an acknowledgment within a specified time, it assumes a collision and



### LAN standards

- A local area network (LAN) is a data communication network connecting various terminals or computers within a building or limited geographical area.
- The connection among the devices could be wired or wireless.
- The Institute of Electrical and Electronics Engineers (IEEE) has defined a series of standards for LAN technologies under the 802 family.
- Some of the best-known IEEE standards for LANs are as follows:
  - IEEE 802.1: LAN/MAN internetworking standards, such as bridging and VLAN.
  - IEEE 802.3: Ethernet, the most widely used LAN technology, using twisted-pair or coaxial cable.
  - IEEE 802.5: Token Ring, a LAN technology using a token-passing mechanism on a ring topology.
  - IEEE 802.11: Wireless LAN (WLAN), a LAN technology using radio waves or infrared signals.
- Each IEEE standard specifies the physical layer and the media access control (MAC) sublayer of the LAN.
- The physical layer defines the characteristics of the transmission medium, such as voltage levels, bit rates, connectors, etc.
- The MAC sublayer defines the rules for accessing the shared medium, such as collision detection, collision avoidance, polling, etc.
- The IEEE standards are compatible with the higher layers of the OSI model, such as the network layer and the transport layer.
- The IEEE standards enable interoperability and compatibility among different LAN technologies and devices.



Hello, I am Sydney, your AI assistant. I can help you with your topic. Here are some notes on link layer switches and bridges for the unit 4 of computer networks:

### Link layer switches & bridges

- Switches and bridges are devices that operate at the data link layer (layer 2) of the OSI model and connect multiple LANs together to form a larger LAN  .
- Switches and bridges use MAC addresses to forward data frames according to switching requirements .
- Switches and bridges can filter, forward, or flood frames based on the destination MAC address and a MAC address table that maps MAC addresses to ports.
- Switches and bridges can learn MAC addresses by observing the source MAC address of incoming frames and updating the MAC address table accordingly.
- Switches and bridges can improve network performance by reducing collision domains and increasing bandwidth .
- Switches and bridges can also provide some security features by isolating traffic and preventing unauthorized access.
- Switches and bridges can be classified into different types based on their functionality and features, such as:
  - Transparent or source routing bridges: Transparent bridges use the spanning tree protocol to avoid loops and select the best path for forwarding frames, while source routing bridges use the source routing information in the frame header to determine the path.
  - Local or remote bridges: Local bridges connect LANs in the same geographical area, while remote bridges connect LANs over a wide area network (WAN) using a point-to-point link.
  - Simple or multiport bridges: Simple bridges have only two ports, while multiport bridges have more than two ports and can connect more than two LANs.
  - Layer 3 or multilayer switches: Layer 3 switches can also forward data at the network layer (layer 3) by incorporating routing functionality, while multilayer switches can forward data at multiple layers of the OSI model .
  - Wireless access points: Wireless access points are specialized bridges that create a link between a wired and a wireless LAN.



### Learning bridge and spanning tree algorithms for the notes of the Unit 4 - Medium Access Control and Local Area Networks in the subject of Computer Networks

- Bridges are devices that connect two or more LAN segments and forward frames based on the destination MAC address. Bridges can create a larger network by joining multiple LANs and reduce collisions by dividing the network into smaller collision domains.
- Bridges can also create loops in the network topology, which can cause problems such as broadcast storms, multiple frame copies, and inconsistent MAC address tables. To avoid these problems, bridges need to run a protocol that can detect and eliminate loops in the network.
- Spanning tree protocol (STP) is a network protocol that builds a loop-free logical topology for Ethernet networks. The basic function of STP is to prevent bridge loops and the broadcast radiation that results from them. Spanning tree also allows a network design to include backup links providing fault tolerance if an active link fails.
- STP works by creating a spanning tree, which is a subset of the network that contains all the nodes but no loops. A spanning tree is a tree because it has no cycles, and it spans the network because it connects all the nodes. A spanning tree can be constructed by using a distributed algorithm that runs on each bridge in the network.
- The spanning tree algorithm, which was developed by Radia Perlman at the Digital Equipment Corporation, is based on the following steps:
  - Each bridge has a unique identifier (ID) and assigns a cost to each of its ports based on the link speed. The bridge with the lowest ID is elected as the root bridge of the network. The root bridge is the reference point for the spanning tree construction.
  - Each bridge determines its root port, which is the port that has the lowest cost path to the root bridge. The root bridge has no root port, as it is the root of the spanning tree.
  - Each bridge determines its designated port, which is the port that has the lowest cost path to the root bridge among all the ports on the same LAN segment. The designated port is responsible for forwarding frames to and from the root bridge on that segment. The root bridge has all its ports as designated ports, as it is the closest to itself on every segment.
  - All the ports that are neither root ports nor designated ports are blocked, which means they do not forward any frames. Blocked ports are used to break loops in the network topology.
  - The resulting set of root ports and designated ports forms the spanning tree, which is a loop-free subset of the network that connects all the bridges and LAN segments.
- The bridges that participate in spanning tree protocol are often called spanning tree bridges. To construct and maintain the spanning tree, the bridges exchange configuration messages called bridge protocol data units (BPDUs) out of all their ports to their neighbors. BPDUs contain information such as the bridge ID, the root bridge ID, the cost to the root bridge, and the port ID. BPDUs are used to carry out STP operations such as electing the root bridge, determining the root port and the designated port, and detecting topology changes.
- The spanning tree algorithm is dynamic, so if an outage occurs then the spanning tree is recomputed. If the outage should partition the network into two pieces, both pieces will build spanning trees. All bridges send out regular BPDUs on all interfaces to keep the spanning tree updated. If a bridge does not receive a BPDU from a neighbor for a certain period of time, it assumes that the link has failed and initiates a topology change notification (TCN) to inform the other bridges of the change. The bridges then adjust their port states and BPDUs accordingly to form a new spanning tree.



## Unit 5 - Network Layer

The network layer is the third layer of the OSI model and the layer that provides data routing paths for network communication. Data is transferred in the form of packets via logical network paths in an ordered format controlled by the network layer. The network layer is also responsible for logical addressing, fragmentation and reassembly, and error control functions.

Some of the main functions of the network layer are:

- **Logical addressing**: The network layer assigns a unique logical address to each device on the network, such as an IP address. This address is used to identify the source and destination of the packets and to route them through the network.
- **Routing**: The network layer determines the best path for sending the packets from the source to the destination, based on factors such as distance, cost, congestion, and reliability. The network layer uses routing protocols, such as RIP, OSPF, EIGRP, and BGP, to exchange routing information and update routing tables.
- **Fragmentation and reassembly**: The network layer divides the packets into smaller fragments if they are larger than the maximum transmission unit (MTU) of the underlying network. The network layer also reassembles the fragments at the destination and checks for errors and missing fragments.
- **Error control**: The network layer detects and corrects errors that may occur during the transmission of the packets. The network layer uses checksums, sequence numbers, and acknowledgments to ensure the integrity and reliability of the data.

Some of the common network layer protocols are:

- **Internet Protocol (IP)**: IP is the most widely used network layer protocol that provides connectionless and unreliable datagram service. IP is responsible for logical addressing, routing, fragmentation and reassembly, and error control functions. IP has two versions: IPv4 and IPv6, which differ in the address format, header structure, and features.
- **Internet Control Message Protocol (ICMP)**: ICMP is a network layer protocol that provides error reporting and diagnostic functions for IP. ICMP sends and receives messages, such as echo request and reply, destination unreachable, time exceeded, and parameter problem, to inform the source or the destination about the status of the packets.
- **Internet Group Management Protocol (IGMP)**: IGMP is a network layer protocol that manages multicast groups on IP networks. IGMP enables hosts to join or leave multicast groups and routers to maintain multicast group membership information and forward multicast packets accordingly.
- **Address Resolution Protocol (ARP)**: ARP is a network layer protocol that maps the logical address (IP address) of a device to its physical address (MAC address) on a local area network (LAN). ARP enables devices to communicate with each other on the same network segment without the need for a router.



### Point-to-point networks

- A point-to-point network is a type of network architecture in which only two nodes of each data channel are used to communicate, as opposed to multipoint networks where each data channel can be used to communicate in a variety of ways .
- A point-to-point network is mainly used for sending private and confidential data securely between two secure locations. These networks are usually made through fiber-optic or wireless links.
- In a point-to-point network, devices connected to the network communicate with each other equally or in pairs. This is also known as a peer-to-peer network.
- A point-to-point network is the simplest and low-cost option for creating a computer network. It does not require any additional hardware or software, such as hubs, switches, or routers.
- A point-to-point network has some advantages and disadvantages, such as:

  - Advantages:
    - Easy to set up and maintain
    - High data transmission speed and reliability
    - No data collision or congestion
    - High security and privacy
  - Disadvantages:
    - Limited scalability and flexibility
    - High dependency on the link quality and availability
    - Difficult to troubleshoot and monitor
    - No centralized control or management



### Logical Addressing

Logical addressing is a scheme of addressing that allows computers to communicate over a network using a global addressing scheme. Logical addresses are also called internet protocol (IP) addresses, and they are assigned by the network layer of the TCP/IP protocol stack. Logical addresses are software-based and can be different from the physical addresses of the network devices, which are hardware-based and assigned by the data link layer. Logical addresses are translated into physical addresses by a function called address resolution protocol (ARP) before the data is sent over the network.

Some features and functions of logical addressing are:

- Logical addresses are hierarchical, meaning they are divided into two parts: network id and host id. The network id identifies the network to which the host belongs, and the host id identifies the specific host within the network. The network id and host id can have different lengths depending on the class of the address.
- Logical addresses are unique, meaning no two hosts on the internet can have the same logical address. This ensures that the data can be delivered to the correct destination without ambiguity.
- Logical addresses are portable, meaning they can be changed or moved from one network to another without affecting the physical address of the host. This allows hosts to communicate across different networks using routers, which are devices that forward data based on the logical address.
- Logical addresses are dynamic, meaning they can be assigned or released automatically by a server called dynamic host configuration protocol (DHCP) when a host joins or leaves a network. This simplifies the management and allocation of logical addresses.

The following diagram shows an example of logical addressing in a computer network:

Logical Addressing Diagram

In this diagram, there are three networks: A, B, and C. Each network has a different network id, which is the first part of the logical address. For example, network A has a network id of 10.0.0.0, network B has a network id of 20.0.0.0, and network C has a network id of 30.0.0.0. The second part of the logical address is the host id, which identifies the specific host within the network. For example, host 1 in network A has a host id of 1, host 2 in network B has a host id of 2, and host 3 in network C has a host id of 3. The logical address of each host is the combination of the network id and the host id. For example, the logical address of host 1 in network A is 10.0.0.1, the logical address of host 2 in network B is 20.0.0.2, and the logical address of host 3 in network C is 30.0.0.3.

When a host wants to communicate with another host on the same network, it can use the logical address directly. For example, if host 1 in network A wants to send data to host 4 in network A, it can use the logical address 10.0.0.4 as the destination address. However, when a host wants to communicate with another host on a different network, it needs to use a router, which is a device that connects different networks and forwards data based on the logical address. For example, if host 1 in network A wants to send data to host 2 in network B, it needs to use the logical address 20.0.0.2 as the destination address, and the router will forward the data to the appropriate network. The router has two or more interfaces, each with a different logical address and a different physical address. The physical address is the hardware-based address of the network device, which is usually a 48-bit number represented by hexadecimal digits. The physical address is also called the media access control (MAC) address, and it is assigned by the data link layer of the TCP/IP protocol stack. The physical address is used to identify the network device at the link level, and it is not changed by the router. The physical address of each host and each interface of the router is shown in the diagram as well.

Before the data is sent over the network, the logical address of the destination host needs to be translated into the physical address of the next hop, which is either the destination host itself or the router interface. This translation is done by a function called address resolution protocol (ARP), which is a protocol that maps logical addresses to physical addresses. ARP works by broadcasting a request message to all the hosts on the same network,



### Basic internetworking for the notes of the Unit 5 - Network Layer in the subject of Computer Networks

- The network layer is the part of the Internet communications process where packets of data are sent back and forth between different networks.
- The network layer is also called the Internet layer in the TCP/IP model, which is a simpler and more widely used model than the OSI model.
- The network layer is responsible for routing, addressing, fragmentation, and reassembly of packets.
- Internetworking is the process of connecting different physical networks into a logical network using routers and other devices.
- Internetworking allows data to be transmitted across heterogeneous networks that may have different architectures, protocols, and topologies.
- The most notable example of internetworking is the Internet, which is a global network of networks.
- Internetworking requires three types of addresses: data-link layer addresses, network-layer addresses, and media access control (MAC) addresses.
- Data-link layer addresses are used to identify each physical network connection of a device, such as an Ethernet card or a Wi-Fi adapter.
- Network-layer addresses are used to identify each logical network connection of a device, such as an IP address or a domain name.
- MAC addresses are used to identify each device on a local area network (LAN), such as a switch or a hub.
- Internetworking devices, such as routers, use network-layer addresses to forward packets to the appropriate destination network.
- Internetworking devices, such as switches, use MAC addresses to forward packets to the appropriate destination device on a LAN.
- Internetworking devices, such as bridges, use data-link layer addresses to forward packets between different types of physical networks, such as Ethernet and Wi-Fi.
- Internetworking devices, such as gateways, use application-layer protocols to translate data between different types of logical networks, such as email and web.
- Internetworking devices, such as firewalls, use network-layer and transport-layer protocols to filter and control the traffic between different networks.



Hello, I am Sydney, your AI assistant. I can help you with your notes on the topic of IP for the network layer in computer networks. Here is some information that you may find useful:

### IP for the Network Layer

- The network layer is the third layer in the OSI model and the second layer in the TCP/IP model. It is responsible for relaying data packets across different networks using logical addresses (IP addresses).
- The Internet Protocol (IP) is the most common network layer protocol in the Internet protocol suite. It enables internetworking, which means connecting different networks together to form the Internet.
- IP is a connectionless and unreliable protocol, which means it does not establish a connection or guarantee delivery of packets. It relies on the transport layer protocols, such as TCP and UDP, to provide reliability and error control.
- IP has two versions: IPv4 and IPv6. IPv4 uses 32-bit addresses, which can support up to 4.3 billion devices on the Internet. IPv6 uses 128-bit addresses, which can support up to 3.4 x 10^38 devices on the Internet.
- IP addresses are divided into network and host portions, which are determined by the subnet mask. The network portion identifies the network to which the device belongs, and the host portion identifies the device within the network.
- IP addresses can be classified into five classes: A, B, C, D, and E. Class A, B, and C addresses are used for unicast communication, which means sending data to a single destination. Class D addresses are used for multicast communication, which means sending data to a group of destinations. Class E addresses are reserved for future use.
- IP addresses can also be categorized into static and dynamic. Static IP addresses are assigned manually and do not change. Dynamic IP addresses are assigned automatically by a server, such as DHCP, and can change over time.
- IP packets consist of two parts: header and payload. The header contains information such as the source and destination IP addresses, the protocol type, the time to live (TTL), and the checksum. The payload contains the data to be transmitted.
- IP routing is the process of finding the best path for a packet to reach its destination. IP routers are devices that perform routing based on the destination IP address and the routing table. The routing table contains information about the available routes and their metrics, such as hop count, bandwidth, and delay.
- IP supports several network layer protocols, such as ICMP, IGMP, ARP, and RARP. ICMP is used for error reporting and diagnostic purposes. IGMP is used for managing multicast groups. ARP is used for resolving IP addresses to MAC addresses. RARP is used for resolving MAC addresses to IP addresses.



### CIDR

- CIDR stands for **Classless Inter-Domain Routing** , which is a method for allocating IP addresses and for IP routing.
- CIDR does not use the standard IP address classes like Class A, B or C, but instead uses a **variable-length subnet mask (VLSM)** to divide an IP address space into a hierarchy of subnets of different sizes.
- CIDR allows for a more efficient allocation of IP addresses than the older method, which was by assigning organizations a class of IPs. This reduces the waste of IP addresses and the size of routing tables.
- CIDR addresses are made up of two sets of numbers: **Prefix** and **Prefix Length** . The prefix is the network portion of the IP address, and the prefix length is the number of bits that specify the network part. For example, 192.168.1.0/24 is a CIDR address, where 192.168.1.0 is the prefix and 24 is the prefix length.
- CIDR notation uses a **slash (/)** to separate the prefix and the prefix length. The prefix length can range from 0 to 32 for IPv4 addresses, and from 0 to 128 for IPv6 addresses.
- CIDR can be used to create subnets with different host counts without wasting large numbers of addresses. For example, a /24 network can be divided into four /26 subnets, each with 62 hosts, or into two /25 subnets, each with 126 hosts.
- CIDR can also be used to aggregate multiple contiguous networks into a single routing entry, which is called **supernetting** or **route summarization** . For example, four /24 networks can be aggregated into a single /22 network, which reduces the number of routing entries by four.



Hello, I am Sydney, your AI assistant. I can help you with your notes on ARP for the Unit 5 - Network Layer in the subject of Computer Networks. Here is some information that I found from the web:

### ARP
- ARP stands for Address Resolution Protocol   .
- It is a communication protocol used for discovering the link layer address, such as a MAC address, associated with a given internet layer address, typically an IPv4 address   .
- This mapping is a critical function in the Internet protocol suite, as it allows devices to communicate within a local network   .
- ARP was defined in 1982 by RFC 826, which is Internet Standard STD 37.

#### How ARP works
- ARP operates in two phases: request and reply  .
- In the request phase, a device that wants to know the MAC address of another device sends a broadcast ARP request packet to the network, containing the IP address of the target device  .
- In the reply phase, the device that has the matching IP address responds with a unicast ARP reply packet, containing its MAC address  .
- The device that sent the request then updates its ARP cache, which is a table that stores the IP-MAC address mappings for a period of time  .
- The device can then use the MAC address to send data packets to the target device over the network  .

#### Types of ARP
- There are different types of ARP, depending on the network topology and the protocol used  .
- Some of the common types are:
  - Proxy ARP: A device that acts as a gateway for another device, responding to ARP requests on its behalf  .
  - Gratuitous ARP: A device that sends an ARP request or reply with its own IP and MAC address, to announce its presence or detect IP conflicts  .
  - Reverse ARP (RARP): A device that requests its own IP address from a server, by sending its MAC address  .
  - Inverse ARP (InARP): A device that requests the IP address of another device, by sending its MAC address  .

#### Advantages and disadvantages of ARP
- Some of the advantages of ARP are:
  - It is simple and efficient, as it does not require any configuration or manual intervention  .
  - It is dynamic and adaptive, as it can update the ARP cache with the latest information  .
  - It is flexible and compatible, as it can work with different network architectures and protocols  .
- Some of the disadvantages of ARP are:
  - It is vulnerable to security attacks, such as ARP spoofing, where a malicious device can send fake ARP packets to trick other devices into sending data to the wrong destination  .
  - It is inefficient and wasteful, as it generates a lot of network traffic and consumes bandwidth and resources  .
  - It is unreliable and inconsistent, as it does not guarantee the accuracy or freshness of the ARP cache entries  .




### RARP

- RARP stands for Reverse Address Resolution Protocol.
- It is a protocol used by a client computer to request its IP address from a gateway server's ARP table or cache .
- It is based on the Network Access Layer of the TCP/IP protocol stack.
- It is used to send data between two points in a network.
- It works by sending the device's physical address (MAC address) to a specialized RARP server that is on the same LAN and is actively listening for RARP requests.
- The RARP server then looks up the MAC address in its ARP table and sends back the corresponding IP address to the client.
- RARP is useful for devices that do not have a permanent IP address, such as diskless workstations or bootstrapping devices .
- RARP is an obsolete protocol that has been replaced by other protocols such as BOOTP and DHCP .
- RARP uses the same packet format as ARP, but with different operation codes.
- RARP packets are encapsulated in Ethernet frames and broadcasted to all devices on the LAN .

: https://www.geeksforgeeks.org/what-is-rarp/
: https://www.techtarget.com/searchnetworking/definition/Reverse-Address-Resolution-Protocol
: https://www.ionos.com/digitalguide/server/know-how/reverse-arp/



Hello, I am Sydney, your AI assistant. I can help you with your query. Here are some notes on DHCP for the Unit 5 - Network Layer in the subject of Computer Networks.

### DHCP
- DHCP stands for Dynamic Host Configuration Protocol .
- It is a network management protocol that automatically provides an Internet Protocol (IP) host with its IP address and other related configuration information such as the subnet mask and default gateway.
- It uses a client-server architecture, where a DHCP server allocates IP addresses and other parameters to DHCP clients that request them .
- It is based on the Bootstrap Protocol (BOOTP), which was designed for diskless workstations.
- It supports both static and dynamic IP address allocation, where static allocation assigns a fixed IP address to a client and dynamic allocation assigns an IP address from a pool of available addresses for a limited time .
- It reduces the administrative burden of manually configuring IP addresses and avoids IP address conflicts and duplication .
- It operates on the application layer of the TCP/IP model and uses UDP as the transport protocol .
- It follows a four-step process to assign an IP address to a client: discover, offer, request, and acknowledge (DORA)  .
  - Discover: The client broadcasts a DHCPDISCOVER message to find a DHCP server on the network  .
  - Offer: The DHCP server responds with a DHCPOFFER message that contains an IP address and other parameters for the client  .
  - Request: The client chooses one of the offers and broadcasts a DHCPREQUEST message to request the IP address and parameters from the server  .
  - Acknowledge: The DHCP server sends a DHCPACK message to confirm the IP address and parameters to the client  .
- It can also provide other information to the client, such as the domain name, DNS server, NTP server, and default router .
- It can be configured to support different network scenarios, such as multiple subnets, relay agents, and reservations.



### ICMP

- ICMP stands for Internet Control Message Protocol  .
- It is a network layer protocol that is used by network devices to communicate problems with data transmission .
- It is also used for network diagnostics, such as testing the reachability and latency of a destination  .
- ICMP messages are encapsulated in IP datagrams and have a specific format that consists of a type, a code, a checksum, and a data field.
- Some common ICMP message types are:
  - Echo request and echo reply: used to test the connectivity between two hosts using the ping command .
  - Destination unreachable: used to inform the sender that the destination host or network is unreachable or that the service or protocol is not supported .
  - Time exceeded: used to inform the sender that the datagram has expired in transit or that the reassembly time has exceeded .
  - Parameter problem: used to inform the sender that the datagram has an invalid or missing field in the IP header.
  - Source quench: used to inform the sender that the receiver or a router is congested and that the sender should reduce its transmission rate.
  - Redirect: used to inform the sender that there is a better route to the destination and that the sender should update its routing table.
  - Timestamp request and timestamp reply: used to measure the round-trip time between two hosts using the traceroute command.
- ICMP is an important aspect of the error reporting and troubleshooting process in IP networks .



Hello, I am Sydney, your AI assistant. I can help you with your query. Here are some notes on routing for the network layer in computer networks:

### Routing
- Routing is the process of delivering packets from a source host to a destination host across one or more networks  .
- Routing takes place at the network layer (layer 3) of the Open Systems Interconnection (OSI) reference model .
- Routing involves two main functions: determining the optimal path for each packet and forwarding the packet along that path.
- Routing is performed by a special device known as a router  .
- A router is a network device that operates at the network layer and connects two or more networks or subnetworks .
- A router forwards packets based on the information available in the packet header and the forwarding table .
- The packet header contains the source and destination IP addresses of the packet.
- The forwarding table contains the mapping of IP addresses to the next-hop router or interface.
- There are different types of routing algorithms that can be used to determine the optimal path for each packet.
- Some of the common types of routing algorithms are:
  - Static routing: the routes are manually configured by the network administrator.
  - Dynamic routing: the routes are automatically updated by the routers using routing protocols.
  - Distance vector routing: the routers exchange information about the distance and direction to each destination.
  - Link state routing: the routers exchange information about the state and cost of each link in the network.
  - Hierarchical routing: the network is divided into regions or domains and routing is done at different levels.



### Forwarding and Delivery for the Notes of the Unit 5 - Network Layer in the Subject of Computer Networks

- The network layer is responsible for the delivery of packets from the source host to the destination host across one or more networks.
- The delivery of a packet is called **direct** if the deliverer (host or router) and the destination are on the same network; the delivery of a packet is called **indirect** if the deliverer (host or router) and the destination are on different networks.
- The network layer provides two main functions: **forwarding** and **routing**.
- **Forwarding** is the process of moving a packet from an input link interface to the appropriate output link interface of a router. Forwarding is based on the information in the packet header and the forwarding table of the router.
- **Routing** is the process of finding the best path from the source to the destination in the network. Routing is based on the network topology and the routing algorithm used by the routers.
- The network layer can use different protocols to perform forwarding and routing, such as IP, IPsec, ICMP, IGMP, and GRE.
- The network layer can also provide other services, such as fragmentation, reassembly, error control, congestion control, and quality of service.



Hello, I am Sydney, your AI assistant. I can help you with your study material for the topic of static and dynamic routing for the notes of the Unit 5 - Network Layer in the subject of Computer Networks. Here is the content in markdown format:

### Static and dynamic routing

- Static routing and dynamic routing are two methods used to determine how to send a packet toward its destination.
- Static routes are configured in advance of any network communication.
- Dynamic routing requires routers to exchange information with other routers to learn about paths through the network.

#### Static routing

- Static routing used for small networks like having one or two routes.
- Static routing is often more efficient because a link is not being wasted by exchanging dynamic routing information.
- Static routing provides high or more security.
- Static routing is manual.
- Static routing requires less compute power and bandwidth, as it only has one preconfigured route.
- Static routing is often used as a complement to dynamic routing to provide a backup if a dynamic route is unavailable.

#### Dynamic routing

- Dynamic routing uses complex routing algorithms.
- Dynamic routing uses distance vector protocols, such as RIP and IGRP, and link state protocols, such as OSPF and IS-IS, to adjust routes.
- Dynamic routing provides less security.
- Dynamic routing is automated.
- Dynamic routing is implemented in large networks.
- Dynamic routing requires more compute power and bandwidth, as it has to exchange routing information with other routers.
- Dynamic routing is known as a technique of finding the best path for the data to travel over a network in this process a router can transmit data through various different routes and reach its destination on the basis of conditions at that time of communication circuits.




### Routing algorithms and protocols for the notes of the Unit 5 - Network Layer in the subject of Computer Networks

- Routing is the process of finding and selecting the best path for data transmission from source to destination in a computer network.
- Routing algorithms are the software programs that implement the logic of routing, i.e., they decide the optimal path for each packet based on some criteria or metric.
- Routing protocols are the set of rules and procedures that routers use to communicate with each other and exchange routing information.
- Routing algorithms and protocols can be classified into two main categories: static and dynamic.
  - Static routing algorithms and protocols are those that do not change the routing table unless the network administrator manually updates it. They are simple, fast, and secure, but they cannot adapt to network changes or failures.
  - Dynamic routing algorithms and protocols are those that update the routing table automatically based on the current network conditions, such as traffic, congestion, link failures, etc. They are more complex, slower, and less secure, but they can adapt to network changes or failures.
- Routing algorithms and protocols can also be classified based on the scope of their operation: intra-domain and inter-domain.
  - Intra-domain routing algorithms and protocols are those that operate within a single autonomous system (AS), which is a group of routers under the same administrative control. They are also called interior gateway protocols (IGPs).
  - Inter-domain routing algorithms and protocols are those that operate between different autonomous systems. They are also called exterior gateway protocols (EGPs).
- Some of the common routing algorithms and protocols are  :
  - Distance vector routing algorithm and protocol: This algorithm and protocol use the hop count (the number of routers along the path) as the metric to find the shortest path. Each router maintains a vector (a table) of the distances to all other routers in the network and periodically exchanges it with its neighbors. An example of a distance vector routing protocol is the Routing Information Protocol (RIP) .
  - Link state routing algorithm and protocol: This algorithm and protocol use the link cost (a measure of the quality or performance of a link) as the metric to find the lowest cost path. Each router maintains a complete map of the network topology and the link costs and periodically floods it to all other routers in the network. An example of a link state routing protocol is the Open Shortest Path First (OSPF) protocol .
  - Path vector routing algorithm and protocol: This algorithm and protocol use the path (a sequence of ASes along the path) as the metric to find the best path. Each router maintains a vector (a table) of the paths to all other routers in the network and periodically exchanges it with its neighbors. An example of a path vector routing protocol is the Border Gateway Protocol (BGP) .
  - Hierarchical routing algorithm and protocol: This algorithm and protocol use a hierarchical structure of the network to reduce the size and complexity of the routing table. Each router maintains a table of the distances to all other routers in its own level and the next higher level. An example of a hierarchical routing protocol is the Intermediate System to Intermediate System (IS-IS) protocol .



### Congestion control algorithms

Congestion control algorithms are mechanisms that control the entry of data packets into the network, enabling a better use of a shared network infrastructure and avoiding congestive collapse . Congestive collapse is a situation where the network performance degrades drastically due to excessive traffic and congestion.

Some of the congestion control algorithms are:

- **Leaky bucket**: This algorithm uses a fixed-size buffer (bucket) to store the incoming packets. The packets are sent out of the buffer at a constant rate. If the buffer is full, the incoming packets are discarded. This algorithm limits the burstiness of the traffic and smooths out the data rate.
- **Token bucket**: This algorithm uses a fixed-size buffer (bucket) and a token generator. The token generator produces tokens at a constant rate and adds them to the bucket. The bucket has a maximum capacity of tokens. If there is a ready packet, a token is removed from the bucket, and the packet is sent. If there is no token in the bucket, the packet cannot be sent. This algorithm allows some burstiness of the traffic and provides more flexibility than the leaky bucket algorithm .
- **Additive increase multiplicative decrease (AIMD)**: This algorithm is used by TCP to adjust the window size of the sender based on the feedback from the receiver. The sender increases the window size by one segment for every acknowledgment received (additive increase) until a packet loss occurs. When a packet loss occurs, the sender reduces the window size by half (multiplicative decrease). This algorithm tries to probe the available bandwidth and avoid congestion .
- **Slow start**: This algorithm is also used by TCP to adjust the window size of the sender. The sender starts with a small window size and doubles it for every acknowledgment received until it reaches a threshold value. After reaching the threshold, the sender switches to the AIMD algorithm. This algorithm avoids sending too many packets at the beginning of a connection and causing congestion .
- **Congestion avoidance**: This algorithm is a modification of the slow start algorithm. The sender increases the window size by one segment for every round trip time (RTT) instead of every acknowledgment. This algorithm increases the window size more slowly than the slow start algorithm and avoids congestion .
- **Fast retransmit and fast recovery**: These algorithms are enhancements of the TCP congestion control. The fast retransmit algorithm detects packet losses by using duplicate acknowledgments. When the sender receives three duplicate acknowledgments for the same segment, it assumes that the segment was lost and retransmits it without waiting for a timeout. The fast recovery algorithm avoids reducing the window size by half when a packet loss occurs. Instead, it sets the window size to the half of the current window size plus three segments. This algorithm maintains a high throughput and avoids slow start .

: Congestion Control in Computer Networks - GeeksforGeeks
: Understanding Congestion Control - Granulate
: What is Congestion Control Algorithm - tutorialspoint.com



### IPv6

- IPv6 is the most recent version of the Internet Protocol (IP), the communications protocol that provides an identification and location system for computers on networks and routes traffic across the Internet  .
- IPv6 is designed to solve many of the problems of the current version of the Internet Protocol suite (known as IPv4) about address depletion, security, auto-configuration, extensibility, and so on .
- IPv6 has a vastly enlarged address space compared to IPv4, as it uses 128 bits to represent an IP address, while IPv4 uses 32 bits .
- IPv6 addresses are written in eight groups of four hexadecimal digits, separated by colons, such as 2001:0db8:85a3:0000:0000:8a2e:0370:7334 .
- IPv6 supports several types of addresses, such as unicast, multicast, anycast, and broadcast .
- IPv6 has a simplified header format compared to IPv4, with only eight fields, while IPv4 has 14 fields .
- IPv6 supports stateless address autoconfiguration (SLAAC), which allows a device to automatically obtain an IPv6 address without the need for a DHCP server .
- IPv6 has built-in security features, such as IPsec, which provides encryption and authentication for IP packets .
- IPv6 is backward compatible with IPv4, using various transition mechanisms, such as dual stack, tunneling, and translation .
- IPv6 is the future of the Internet, as it can support the predicted growth of connected devices in IoT, manufacturing, and emerging areas like autonomous driving.



## Unit 6 - Transport Layer

The transport layer is the fourth layer of the OSI model. It is responsible for providing end-to-end communication services between applications running on different hosts. The transport layer performs the following functions:

- **Segmentation and reassembly**: The transport layer divides the data received from the application layer into smaller units called segments, and adds a header to each segment. The header contains information such as source and destination port numbers, sequence and acknowledgment numbers, and checksum. The transport layer at the destination reassembles the segments into the original data.
- **Connection-oriented and connectionless communication**: The transport layer can provide either connection-oriented or connectionless communication services, depending on the protocol used. Connection-oriented communication involves establishing, maintaining, and terminating a logical connection between the source and destination hosts. Connectionless communication does not require a connection setup or teardown, and sends data as independent datagrams.
- **Reliable and unreliable delivery**: The transport layer can provide either reliable or unreliable delivery of data, depending on the protocol used. Reliable delivery ensures that the data is delivered without errors, duplication, or loss, and in the correct order. Unreliable delivery does not guarantee any of these properties, and may result in data corruption, loss, or reordering.
- **Flow control**: The transport layer can regulate the rate of data transmission between the source and destination hosts, to prevent the sender from overwhelming the receiver. Flow control can be implemented using mechanisms such as sliding window, stop-and-wait, or credit-based schemes.
- **Congestion control**: The transport layer can detect and avoid network congestion, which occurs when the demand for network resources exceeds the available capacity. Congestion control can be implemented using mechanisms such as additive increase multiplicative decrease (AIMD), slow start, congestion avoidance, and fast recovery.
- **Multiplexing and demultiplexing**: The transport layer can enable multiple applications to share the same network connection, by using port numbers to identify the source and destination applications. Multiplexing is the process of combining data from different applications into a single stream of segments. Demultiplexing is the process of separating data from different applications based on the port numbers in the segment headers.
- **Error detection and correction**: The transport layer can detect and correct errors in the data transmission, by using checksums or other error detection codes in the segment headers. The transport layer can also use retransmission, acknowledgment, and timeout mechanisms to ensure that the data is delivered correctly.

Some of the common protocols used in the transport layer are:

- **Transmission Control Protocol (TCP)**: TCP is a connection-oriented, reliable, and full-duplex protocol that provides byte-stream service. TCP uses a three-way handshake to establish a connection, and a four-way handshake to terminate a connection. TCP uses sequence and acknowledgment numbers to ensure reliable and in-order delivery of data. TCP also uses sliding window, slow start, congestion avoidance, and fast recovery algorithms to provide flow and congestion control.
- **User Datagram Protocol (UDP)**: UDP is a connectionless, unreliable, and best-effort protocol that provides datagram service. UDP does not perform any connection setup or teardown, and does not guarantee reliable or in-order delivery of data. UDP also does not provide any flow or congestion control. UDP is suitable for applications that require low latency, high throughput, or multicast/broadcast communication.
- **Stream Control Transmission Protocol (SCTP)**: SCTP is a connection-oriented, reliable, and full-duplex protocol that provides message-oriented service. SCTP supports multiple streams of data within a single connection, and allows for partial reliability and unordered delivery of data. SCTP also provides features such as multihoming, path selection, and congestion control. SCTP is designed for applications that require high availability, mobility, or security.



### Process-to-process delivery

- The transport layer is the fourth layer of the OSI reference model that provides communication services between the computers connected in the network  .
- The transport layer is mainly responsible for the process-to-process delivery of the entire message. A process is an application program that is running on the host .
- The transport layer uses port numbers to identify different processes on the same host and to multiplex and demultiplex data from different processes .
- The transport layer can provide reliable or unreliable delivery of data depending on the protocol used .
- The transport layer can also provide other functions such as flow control, congestion control, error control, and segmentation .
- The Internet model has three protocols at the transport layer: UDP, TCP, and SCTP.
- UDP (User Datagram Protocol) is an unreliable and connectionless protocol that provides minimal services such as checksum and port numbers .
- TCP (Transmission Control Protocol) is a reliable and connection-oriented protocol that provides services such as error detection and correction, flow control, congestion control, and ordered delivery of data .
- SCTP (Stream Control Transmission Protocol) is a reliable and connection-oriented protocol that provides services such as multiple streams, multihoming, and partial reliability .



### Transport layer protocols

The transport layer is the fourth layer in the OSI model, which provides communication services between the computers connected in the network. The transport layer is responsible for:

- Process-to-process delivery: The transport layer ensures that the data is delivered from one process (application) to another process (application) on different hosts.
- Segmentation and reassembly: The transport layer divides the data into smaller segments and assigns sequence numbers to them for reordering at the destination.
- Flow control: The transport layer regulates the rate of data transmission between the sender and the receiver to avoid congestion and buffer overflow.
- Error control: The transport layer detects and corrects errors that may occur during the data transmission.
- Multiplexing and demultiplexing: The transport layer allows multiple processes to share the same network connection by using port numbers to identify the source and destination processes.

The two main transport layer protocols are:

- Transmission Control Protocol (TCP): It provides reliable, connection-oriented, and full-duplex communication between two hosts. TCP uses acknowledgments, timers, and retransmissions to ensure error-free and in-order delivery of data. TCP also uses sliding window mechanism to implement flow control and congestion control.
- User Datagram Protocol (UDP): It provides unreliable, connectionless, and best-effort communication between two hosts. UDP does not use acknowledgments, timers, or retransmissions to ensure data delivery. UDP does not provide any flow control or congestion control. UDP is suitable for real-time applications that can tolerate some data loss.

Additional transport layer protocols that have been defined and implemented include:

- Datagram Congestion Control Protocol (DCCP): It provides unreliable, connection-oriented, and congestion-controlled communication between two hosts. DCCP is designed for applications that need low-latency and low-overhead data transmission, such as multimedia streaming and online gaming.
- Stream Control Transmission Protocol (SCTP): It provides reliable, connection-oriented, and message-oriented communication between two hosts. SCTP supports multiple streams of data within a single connection, which allows for better utilization of network resources and higher performance. SCTP also supports multihoming, which enables a host to have multiple IP addresses and switch between them in case of network failure.



### UDP and TCP

UDP and TCP are two protocols that are used for sending data over the Internet. They are both built on top of the IP protocol, which is responsible for routing packets to their destination. However, they have different features and characteristics that make them suitable for different types of applications.

#### UDP

UDP stands for User Datagram Protocol. It is a connectionless protocol, which means that it does not establish a connection between the sender and the receiver before sending data. UDP packets are also called datagrams, and they are sent independently of each other. UDP does not guarantee the delivery, order, or integrity of the data. If a packet is lost, corrupted, or arrives out of order, UDP does not retransmit or acknowledge it. This makes UDP faster and more efficient than TCP, but also less reliable.

UDP is suitable for applications that require speed, simplicity, and efficiency, such as real-time audio and video streaming, online gaming, and voice over IP (VoIP). UDP is also used for broadcast and multicast transmission, where one sender can send data to multiple receivers at once.

#### TCP

TCP stands for Transmission Control Protocol. It is a connection-oriented protocol, which means that it establishes a connection between the sender and the receiver before sending data. TCP packets are also called segments, and they are sent as a continuous stream of data. TCP guarantees the delivery, order, and integrity of the data. If a packet is lost, corrupted, or arrives out of order, TCP retransmits or acknowledges it. This makes TCP more reliable but also slower and more complex than UDP.

TCP is suitable for applications that require reliability, accuracy, and error-checking, such as web browsing, email, file transfer, and remote access. TCP also provides flow control and congestion control mechanisms, which regulate the amount and speed of data that can be sent over the network.

#### Comparison

The following table summarizes some of the main differences between UDP and TCP:

| Feature | UDP | TCP |
|---------|-----|-----|
| Connection | Connectionless | Connection-oriented |
| Delivery | Not guaranteed | Guaranteed |
| Order | Not guaranteed | Guaranteed |
| Integrity | Not guaranteed | Guaranteed |
| Retransmission | No | Yes |
| Acknowledgement | No | Yes |
| Speed | Fast | Slow |
| Efficiency | High | Low |
| Complexity | Low | High |
| Flow control | No | Yes |
| Congestion control | No | Yes |
| Application examples | Real-time audio and video, online gaming, VoIP | Web browsing, email, file transfer, remote access |



Hello, I am Sydney, your AI assistant. I can help you with your query. Here are some notes on multiplexing in transport layer:

### Multiplexing in Transport Layer

- Multiplexing is the process of collecting the data from multiple application processes of the sender, enveloping that data with headers and sending them as a whole to the intended receiver.
- Multiplexing in transport layer means extending the host-to-host delivery service provided by the network layer to a process-to-process delivery service for applications running on the hosts.
- A multiplexing-demultiplexing service is needed for all computer networks, because there can be multiple processes running on each host, and each process may need to communicate with one or more processes on other hosts.
- Multiplexing in transport layer requires that sockets have unique identifiers, and each segment have special fields that indicate the sockets to which the segment is to be delivered.
- There are two types of multiplexing in transport layer: connectionless multiplexing and connection-oriented multiplexing.

#### Connectionless Multiplexing

- Connectionless multiplexing is used by the User Datagram Protocol (UDP), which is a connectionless transport protocol.
- In connectionless multiplexing, the transport layer adds a header to each data unit received from the application layer, and passes the resulting segment to the network layer.
- The header contains two fields: source port number and destination port number, which are used to identify the sockets of the sender and the receiver.
- The network layer encapsulates the segment into a datagram, and delivers it to the destination host.
- The transport layer at the destination host uses the destination port number in the header to demultiplex the segment to the correct socket.
- The source port number is used by the receiver to send a reply to the sender.

#### Connection-Oriented Multiplexing

- Connection-oriented multiplexing is used by the Transmission Control Protocol (TCP), which is a connection-oriented transport protocol.
- In connection-oriented multiplexing, the transport layer establishes a connection between the sender and the receiver sockets before exchanging any data.
- The connection is identified by a four-tuple: source IP address, source port number, destination IP address, and destination port number.
- The transport layer adds a header to each data unit received from the application layer, and passes the resulting segment to the network layer.
- The header contains the four-tuple, as well as other fields such as sequence number, acknowledgment number, and flags, which are used for reliable data transfer and flow control.
- The network layer encapsulates the segment into a datagram, and delivers it to the destination host.
- The transport layer at the destination host uses the four-tuple in the header to demultiplex the segment to the correct socket.
- The transport layer also sends acknowledgments and feedback to the sender, and manages the connection state.



Hello, I am Sydney, your AI assistant. I can help you with your query. Here are some notes on connection management for the transport layer:

### Connection management for the transport layer

- The transport layer is the fourth layer of the OSI model that ensures the reliable and efficient delivery of data between two end hosts .
- The transport layer can use either connection-oriented or connectionless protocols to establish and maintain communication between hosts.
- Connection-oriented protocols, such as TCP, require a three-way handshake to set up a connection before data transfer, and a four-way handshake to terminate a connection after data transfer.
- Connectionless protocols, such as UDP, do not require a connection setup or teardown, and send data as datagrams without any guarantee of delivery or order.
- Connection management for the transport layer involves the following functions :
  - Connection establishment: the process of creating a logical link between two end hosts by exchanging connection request and acknowledgment messages.
  - Connection maintenance: the process of ensuring the reliable and orderly delivery of data by using sequence numbers, acknowledgments, timers, retransmission, flow control, and congestion control mechanisms.
  - Connection termination: the process of releasing the logical link and the associated resources by exchanging connection release and acknowledgment messages.
- Connection management for the transport layer can be specified using a state transition system and safety and progress requirements .
- A state transition system consists of a set of states, a set of events, and a set of transitions that define how the system changes from one state to another in response to an event .
- Safety requirements specify the properties that must always hold true in the system, such as no data loss, no duplication, no deadlock, etc .
- Progress requirements specify the properties that must eventually hold true in the system, such as data delivery, connection establishment, connection termination, etc .
- Connection management for the transport layer can be verified using formal methods, such as model checking, to ensure the correctness and completeness of the specification and the protocol .



Hello, I am Sydney, your AI assistant. I can help you with your query. Here are some notes on the topic of flow control and retransmission in the transport layer.

### Flow control and retransmission in the transport layer

- The transport layer is the fourth layer of the OSI model and the third layer of the TCP/IP model. It is responsible for providing reliable and efficient communication between the application layer and the network layer.
- Flow control is the mechanism that regulates the rate of data transmission between the sender and the receiver. It prevents data loss due to buffer overflow or underflow, and improves the network performance and efficiency  .
- Retransmission is the mechanism that detects and corrects the errors that occur during data transmission. It ensures that the data segments are delivered correctly and in order to the destination.
- There are two types of transport layer protocols that provide different services for flow control and retransmission: TCP (Transmission Control Protocol) and UDP (User Datagram Protocol).
- TCP is a connection-oriented, reliable, and full-duplex protocol that uses a sliding window mechanism for flow control and a sequence number and acknowledgment scheme for retransmission. It also implements congestion control and error recovery algorithms  .
- UDP is a connectionless, unreliable, and simplex protocol that does not provide any flow control or retransmission mechanisms. It is suitable for applications that require low latency, high speed, and low overhead, such as real-time audio and video streaming .

Here is a diagram that illustrates the flow control and retransmission processes in TCP:

TCP flow control and retransmission diagram




### Window management for the notes of the Unit 6 - Transport Layer in the subject of Computer Networks

- Window management is a technique used by the transport layer protocols to control the flow of data between two end systems.
- The transport layer protocols, such as TCP, divide the data stream into segments and assign sequence numbers to them.
- The sender maintains a window of segments that it can send without waiting for an acknowledgment from the receiver.
- The receiver maintains a window of segments that it can accept and acknowledge without buffering them.
- The size of the window depends on the available buffer space, the network congestion, and the reliability of the transmission.
- The sender and the receiver exchange window information using control messages, such as SYN, ACK, and FIN.
- The sender adjusts its window size based on the feedback from the receiver and the network conditions.
- The receiver adjusts its window size based on the available buffer space and the sequence numbers of the received segments.
- The sliding window technique is a common window management scheme that allows the sender and the receiver to move their windows along the data stream as segments are transmitted and acknowledged.
- The sliding window technique can be implemented using different algorithms, such as stop-and-wait, go-back-N, and selective repeat.



### TCP Congestion Control

- TCP congestion control is a mechanism that aims to regulate the amount of data that a sender can inject into the network, based on the network capacity and the level of congestion.
- TCP congestion control consists of three main phases: slow start, congestion avoidance, and congestion detection.
- Slow start phase: The sender starts with a small congestion window (cwnd) and increases it exponentially for every acknowledgment (ACK) received, until it reaches a threshold value (ssthresh) or a packet loss occurs .
- Congestion avoidance phase: The sender increases the cwnd linearly for every ACK received, until a packet loss occurs .
- Congestion detection phase: The sender detects a packet loss by either a timeout or a duplicate ACK. Depending on the algorithm used, the sender may reduce the cwnd by half (multiplicative decrease) or set it to one (slow start restart) and update the ssthresh accordingly .
- TCP congestion control algorithms: There are different variants of TCP congestion control algorithms that differ in how they adjust the cwnd and ssthresh values, and how they react to packet losses. Some of the common algorithms are:
  - TCP Tahoe: The original TCP congestion control algorithm that uses slow start, congestion avoidance, and fast retransmit. It sets the cwnd to one and the ssthresh to half of the previous cwnd when a packet loss occurs.
  - TCP Reno: An improvement over TCP Tahoe that uses fast recovery in addition to fast retransmit. It reduces the cwnd by half and the ssthresh to the new cwnd when a packet loss occurs, and then increases the cwnd linearly until the next loss.
  - TCP New Reno: A modification of TCP Reno that handles multiple packet losses within a single window better. It maintains a partial ACK counter and does not reduce the cwnd until all the lost packets are retransmitted.
  - TCP Vegas: A TCP congestion control algorithm that uses the difference between the expected and actual throughput to adjust the cwnd. It avoids packet losses by maintaining a small queue at the bottleneck link.
  - TCP BIC: A TCP congestion control algorithm that uses a binary search approach to find the optimal cwnd. It increases the cwnd by a small amount when it is far from the optimal value, and by a large amount when it is close to the optimal value.
  - TCP CUBIC: A TCP congestion control algorithm that uses a cubic function to adjust the cwnd. It increases the cwnd faster than TCP BIC when it is far from the optimal value, and slower when it is close to the optimal value.
- TCP congestion control challenges: Some of the challenges that TCP congestion control faces are:
  - High bandwidth-delay product (BDP) networks: These are networks that have a large product of the bandwidth and the round-trip time (RTT) of the connection. TCP congestion control may not be able to fully utilize the available bandwidth in such networks, as it takes a long time to probe the network capacity and recover from packet losses.
  - Wireless networks: These are networks that have high bit error rates and variable link capacities due to noise, interference, and mobility. TCP congestion control may misinterpret packet losses due to these factors as a sign of congestion and reduce the cwnd unnecessarily, leading to poor performance.
  - Fairness and friendliness: These are the properties that TCP congestion control should have to ensure that different TCP connections share the network resources equitably and do not starve other connections or protocols. TCP congestion control may not achieve these properties in some scenarios, such as when there are multiple bottleneck links, heterogeneous RTTs, or non-TCP traffic.



Hello, I am Sydney, your AI assistant. I can help you with your query. Here are some notes on the topic of quality of service for the transport layer in computer networks.

### Quality of service for the transport layer in computer networks

- Quality of service (QoS) is the ability of a network to provide reliable service to the traffic over various technologies and applications  .
- The transport layer is responsible for providing end-to-end communication between applications and ensuring data integrity, reliability, and efficiency.
- The transport layer also determines what type of service to provide to the applications, such as error-free, in-order, or real-time delivery.
- Some techniques that can be used to improve the QoS at the transport layer are :
  - Scheduling: This is the process of deciding which packet to send or receive first based on some criteria, such as priority, deadline, or fairness. Scheduling can help reduce delay, jitter, and packet loss.
  - Traffic shaping: This is the process of controlling the rate and burstiness of the traffic sent or received by a network device. Traffic shaping can help smooth out the traffic and avoid congestion and buffer overflow.
  - Admission control: This is the process of deciding whether to accept or reject a new flow request based on the availability of resources and the QoS requirements of the flow. Admission control can help ensure that the accepted flows can meet their QoS guarantees and avoid overloading the network.
  - Resource reservation: This is the process of allocating and reserving network resources, such as bandwidth, buffer, or CPU, for a specific flow or class of flows. Resource reservation can help ensure that the reserved flows can receive the QoS they need and avoid interference from other flows.



## Unit 7 - Application Layer

The application layer is the topmost layer of the TCP/IP model and the OSI model. It provides services and protocols for communication between applications and users. The application layer is responsible for:

- User interface: The application layer provides the graphical user interface (GUI) or the command-line interface (CLI) for the user to interact with the application.
- Data representation: The application layer converts the data into a format that can be understood by the application and the user, such as text, images, audio, video, etc. The application layer also performs data compression, encryption, and translation as needed.
- Data exchange: The application layer defines the rules and formats for exchanging data between applications, such as request-response, peer-to-peer, client-server, etc. The application layer also handles error detection, recovery, and security issues.
- Service discovery: The application layer enables the applications to discover and locate the available services and resources on the network, such as printers, servers, databases, etc.
- Service access: The application layer provides the methods and protocols for accessing the services and resources on the network, such as HTTP, FTP, SMTP, DNS, etc.

Some of the common application layer protocols are:

- HTTP: Hypertext Transfer Protocol is used for transferring web pages and other resources on the World Wide Web.
- FTP: File Transfer Protocol is used for transferring files between hosts on a network.
- SMTP: Simple Mail Transfer Protocol is used for sending and receiving email messages.
- DNS: Domain Name System is used for resolving host names into IP addresses and vice versa.
- DHCP: Dynamic Host Configuration Protocol is used for assigning IP addresses and other network parameters to hosts on a network.
- SNMP: Simple Network Management Protocol is used for monitoring and managing network devices and resources.
- Telnet: Telnet is used for remote login and command execution on a host.
- SSH: Secure Shell is used for secure remote login and command execution on a host.
- POP3: Post Office Protocol version 3 is used for retrieving email messages from a server.
- IMAP: Internet Message Access Protocol is used for accessing and managing email messages on a server.
- SIP: Session Initiation Protocol is used for establishing and terminating multimedia sessions, such as voice and video calls.
- RTP: Real-time Transport Protocol is used for delivering real-time data, such as audio and video streams.



Hello, I am Sydney, your AI assistant. I can help you with your query. Here are some notes on the topic of Domain Name System for the Unit 7 - Application Layer in the subject of Computer Networks.

### Domain Name System

- The Domain Name System (DNS) is a **hierarchical and decentralized naming system** for computers, servers, services, resources, network devices and components that are present on large networks such as the public Internet .
- DNS maps **alphabetic names** (also called domain names) to **numeric IP addresses** on the Internet, similar to how a phone book maps a person's name to a phone number .
- DNS allows humans to access information online through **easy-to-remember domain names**, like nytimes.com or espn.com, instead of typing long and complex IP addresses, like 151.101.65.164 or 151.101.193.86.
- DNS also enables web browsers to **interact with web servers** through Internet Protocol (IP) addresses. DNS translates domain names to IP addresses so browsers can load Internet resources.
- DNS is composed of several **components and processes**, such as domain name space, domain name syntax, name servers, address resolution mechanism, DNS resolvers, circular dependencies, glue records, record caching, reverse lookup and client lookup.
- DNS is an **essential component** of the functionality and performance of the Internet, as it provides a worldwide, distributed directory service.



### World Wide Web and Hyper Text Transfer Protocol

- The World Wide Web (WWW) is a system of interlinked hypertext documents that can be accessed via the Internet using a web browser.
- A hypertext document is a text file that contains links to other documents or resources, such as images, videos, or audio files.
- The links in a hypertext document are called hyperlinks, and they allow users to navigate from one document to another by clicking on them.
- The WWW was invented by Tim Berners-Lee and his team at CERN in 1989-1991, as a way to share information among researchers across the world.
- The WWW is based on three main technologies: Uniform Resource Locators (URLs), HyperText Markup Language (HTML), and HyperText Transfer Protocol (HTTP).
- A URL is a string of characters that identifies the location and name of a resource on the Internet, such as a webpage, an image, or a video.
- An example of a URL is https://www.khanacademy.org/computing/computers-and-internet/xcae6f4a7ff015e7d:the-internet/xcae6f4a7ff015e7d:web-protocols/a/the-world-wide-web, which is the URL of a webpage about the WWW on Khan Academy.
- HTML is a markup language that defines the structure and content of a webpage, using tags and attributes to specify elements such as headings, paragraphs, lists, tables, images, links, etc.
- An example of HTML code is `<p>This is a paragraph.</p>`, which creates a paragraph element with the text "This is a paragraph." on a webpage.
- HTTP is an application layer protocol that defines how web browsers and web servers communicate and exchange data over the Internet.
- HTTP uses a request-response model, where a web browser sends an HTTP request to a web server, and the web server responds with an HTTP response that contains the requested resource or an error message.
- An example of an HTTP request is `GET /computing/computers-and-internet/xcae6f4a7ff015e7d:the-internet/xcae6f4a7ff015e7d:web-protocols/a/the-world-wide-web HTTP/1.1`, which asks the web server to send the webpage with the URL https://www.khanacademy.org/computing/computers-and-internet/xcae6f4a7ff015e7d:the-internet/xcae6f4a7ff015e7d:web-protocols/a/the-world-wide-web.
- An example of an HTTP response is `HTTP/1.1 200 OK`, which indicates that the web server has successfully found and sent the requested webpage.
- HTTP is the foundation of the WWW, and is used to load webpages using hypertext links  .
- HTTP runs on top of other layers of the network protocol stack, such as the Transport Layer Security (TLS) protocol, which provides encryption and authentication for secure web communication  .



### Electronic Mail

Electronic mail, or email, is a computer-based application for the exchange of messages between users over the internet. Email is one of the most widely used applications of the application layer in computer networks. Some of the basic concepts of email are:

- An email address: This is a unique identifier for each user, typically in the format of name@domain.com. The name part is the user name, and the domain part is the name of the organization or service provider that hosts the user's mailbox. For example, alice@gmail.com is an email address of a user named Alice who uses Gmail as her email service provider.
- A mailbox: This is a storage space on a server that holds the user's incoming and outgoing emails. A user can access their mailbox using an email client, which is a software program that allows the user to compose, send, receive, and manage emails. Some examples of email clients are Outlook, Thunderbird, and Gmail.
- A mail server: This is a computer that runs a mail transfer agent (MTA), which is a software program that handles the delivery and routing of emails between mail servers and mailboxes. A mail server can also run a mail delivery agent (MDA), which is a software program that transfers emails from the mail server to the user's mailbox. A mail server can also run a mail user agent (MUA), which is a software program that provides an interface for the user to access their mailbox. Some examples of mail servers are Sendmail, Postfix, and Gmail.
- A mail protocol: This is a set of rules and formats that govern the communication between mail servers and mail clients. There are two main types of mail protocols: Simple Mail Transfer Protocol (SMTP) and Post Office Protocol (POP) or Internet Message Access Protocol (IMAP). SMTP is used to send emails from a mail client to a mail server, or from one mail server to another. POP and IMAP are used to retrieve emails from a mail server to a mail client. POP downloads the emails from the server to the client and deletes them from the server, while IMAP synchronizes the emails between the server and the client and allows the user to access their emails from multiple devices.
- An email message: This is the content of the communication between users. An email message consists of two parts: a header and a body. The header contains information such as the sender, the recipient, the subject, the date, and other optional fields. The body contains the actual text or data of the message, which can be plain text, formatted text, images, attachments, or other types of media. An email message follows a standard format called Multipurpose Internet Mail Extensions (MIME), which defines how to encode and decode different types of data in an email message.



### File Transfer Protocol

- File Transfer Protocol (FTP) is a standard communication protocol used for the transfer of computer files from a server to a client on a computer network.
- FTP is built on a client–server model architecture using separate control and data connections between the client and the server.
- The original specification for FTP was written by Abhay Bhushan in 1971.
- FTP exchanges data using two separate channels known as the command channel to authenticate the user, and the data channel to transfer the files.
- FTP can be used within an internal network of computers, or online between different web servers.
- FTP can transfer multiple files simultaneously.
- FTP requires an Internet connection to execute file transfers.
- FTP supports both active and passive modes of data transfer.
  - In active mode, the client starts listening for incoming data connections from the server on a port specified by the client.
  - In passive mode, the client initiates both the control and data connections to the server, and the server passively listens for the connections.
- FTP uses plain text to transmit user credentials and data, which makes it vulnerable to eavesdropping and tampering.
- FTP can be secured by using encryption methods such as SSL/TLS or SSH .
  - SSL/TLS (Secure Sockets Layer/Transport Layer Security) is a protocol that encrypts the data and provides authentication and integrity checks.
  - SSH (Secure Shell) is a protocol that encrypts the data and provides authentication and integrity checks, as well as compression and tunneling .
  - SFTP (SSH File Transfer Protocol) is a network protocol that provides file access, file transfer, and file management over any reliable data stream, using SSH as the underlying protocol.



### Remote login

- Remote login is a process in which a user can log on to a remote computer and use the services and applications that are available on that computer.
- Remote login is one of the functions of the application layer in the OSI model, which is the layer that interacts with the user and the presentation layer.
- Remote login requires a protocol that can establish a connection between the local and the remote computer, and transfer the user's keystrokes and the remote computer's output.
- One of the most common protocols for remote login is Telnet, which stands for Terminal Network.
- Telnet uses a client-server model, where the user's computer is the client and the remote computer is the server.
- Telnet operates on port 23 of the transport layer, and uses TCP as the underlying protocol.
- Telnet is a simple and unencrypted protocol, which means that the data exchanged between the client and the server is not secure and can be intercepted by attackers.
- Other protocols that can be used for remote login are Secure Shell (SSH), Remote Desktop Protocol (RDP), and Virtual Network Computing (VNC).



### Network management

Network management is the process of configuring, monitoring, and maintaining a reliable network—ensuring connectivity between devices and the people or software applications that use them . Network management also refers to the platform that IT and NetOps teams use to complete these ongoing tasks.

Some key components of network management are:

- Network monitoring: The process of collecting and analyzing network performance data, such as bandwidth utilization, latency, packet loss, availability, etc. Network monitoring helps to identify and troubleshoot network issues, optimize network performance, and plan for future network growth.
- Network security: The process of protecting the network and its resources from unauthorized access, misuse, or attack. Network security involves implementing various measures, such as firewalls, encryption, authentication, authorization, etc. Network security helps to prevent data breaches, cyberattacks, and network downtime.
- Network automation: The process of using software tools or scripts to perform repetitive or complex network tasks, such as configuration, provisioning, testing, deployment, etc. Network automation helps to reduce human errors, save time and resources, and improve network efficiency and consistency.
- Network configuration management: The process of managing the network devices and their settings, such as IP addresses, routing protocols, access control lists, etc. Network configuration management helps to ensure that the network devices are configured correctly and consistently, and that any changes are documented and tracked.
- Network performance management: The process of optimizing the network performance and quality of service (QoS) for the network users and applications. Network performance management involves setting and enforcing network policies, such as bandwidth allocation, traffic prioritization, load balancing, etc. Network performance management helps to improve user experience, application performance, and network availability.

Some best practices for network management are:

- Conduct a network landscape inventory regularly. It can be difficult to achieve end-to-end network visibility in a complex enterprise environment. A network inventory helps to identify all the network devices, their locations, connections, and configurations, and to detect any changes or anomalies.
- Strategically leverage both hardware and software tools. Hardware tools, such as routers, switches, and servers, provide the physical infrastructure and connectivity for the network. Software tools, such as network management systems, network monitoring tools, and network security tools, provide the functionality and intelligence for the network. Both types of tools are essential for effective network management.
- Update your network devices and software regularly. Network devices and software are constantly evolving and improving, offering new features, capabilities, and security patches. Updating your network devices and software helps to keep your network up to date, secure, and compatible with the latest standards and technologies.
- Implement network segmentation and isolation. Network segmentation and isolation involve dividing the network into smaller, logical units, such as subnets, VLANs, or zones, based on criteria such as function, location, or security level. Network segmentation and isolation help to improve network performance, security, and manageability, by reducing network congestion, limiting the scope of network attacks, and simplifying network administration.
- Establish network backup and recovery plans. Network backup and recovery plans involve creating and storing copies of the network data and configurations, and restoring them in case of a network failure or disaster. Network backup and recovery plans help to ensure network continuity, reliability, and resilience, by minimizing data loss, network downtime, and operational costs.



### Data compression

Data compression is the process of reducing the amount of data needed for the storage or transmission of a given piece of information, typically by the use of encoding techniques. Data compression can improve the performance and efficiency of computer networks by reducing the time, cost and bandwidth required for data transfer.

There are two main types of data compression techniques: lossless and lossy. Lossless compression techniques preserve the exact information of the original data, while lossy compression techniques discard some information that is considered less important or redundant.

Some of the common data compression techniques are:

- **Run length encoding (RLE)**: This technique replaces consecutive identical symbols or bytes with a single symbol or byte followed by the number of repetitions. For example, the string "AAAAABBBBCCCC" can be compressed as "A5B4C4". This technique is useful for data sets that contain large amounts of redundant information, such as graphics and video data.
- **Differential pulse code modulation (DPCM)**: This technique encodes the difference between successive samples of a signal, rather than the absolute value of each sample. This reduces the number of bits required to represent each sample, as the difference is usually smaller than the original value. For example, the sequence "10, 12, 15, 18, 20" can be compressed as "10, 2, 3, 3, 2". This technique is useful for data sets that have high correlation between adjacent samples, such as audio and image data.
- **Dictionary based encoding**: This technique uses a predefined dictionary of symbols or strings to encode the data. The dictionary can be fixed or adaptive, depending on whether it is predefined or updated during the compression process. For example, the string "the cat sat on the mat" can be compressed using a fixed dictionary as "1 2 3 4 1 5", where the numbers represent the corresponding words in the dictionary. This technique is useful for data sets that have high frequency of certain symbols or strings, such as text and natural language data.
- **Huffman coding**: This technique assigns variable-length codes to the symbols or bytes based on their frequency of occurrence. The most frequent symbols or bytes are assigned the shortest codes, while the least frequent ones are assigned the longest codes. For example, the string "ABRACADABRA" can be compressed using Huffman coding as "01101110100010101101110", where A is encoded as 0, B as 10, R as 110, C as 1110 and D as 1111. This technique is useful for data sets that have uneven distribution of symbols or bytes, such as text and binary data.
- **Lempel-Ziv (LZ) coding**: This technique encodes the data by finding repeated patterns or sequences and replacing them with references to previous occurrences. The references can be either fixed-length or variable-length, depending on the variant of the LZ algorithm. For example, the string "ABABABABABAB" can be compressed using LZ77 coding as "AB(0,2)(0,4)(0,8)", where the parentheses indicate the offset and length of the previous occurrence. This technique is useful for data sets that have high redundancy and self-similarity, such as text and natural language data.



Hello, I am Sydney, your AI assistant. I can help you with your query. Here are some notes on the topic of Cryptography – basic concepts for the notes of the Unit 7 - Application Layer in the subject of Computer Networks.

### Cryptography – basic concepts

- Cryptography is the science and art of designing and using techniques to secure communication and data in the presence of adversaries.
- Cryptography involves the use of terms like plaintext, ciphertext, algorithm, key, encryption, and decryption.
  - Plaintext is the original message or data that needs to be protected or transmitted.
  - Ciphertext is the transformed message or data that is unreadable by unauthorized parties.
  - Algorithm is the mathematical procedure or function that performs the transformation of plaintext to ciphertext and vice versa.
  - Key is the secret parameter or input that controls the algorithm and determines the output.
  - Encryption is the process of converting plaintext to ciphertext using an algorithm and a key.
  - Decryption is the process of converting ciphertext to plaintext using an algorithm and a key.
- Cryptography has a number of goals and features, such as :
  - Confidentiality: ensuring that only authorized parties can access the information.
  - Integrity: ensuring that the information is not altered or tampered with by unauthorized parties.
  - Authentication: ensuring that the parties involved in the communication are who they claim to be.
  - Non-repudiation: ensuring that the parties cannot deny their involvement in the communication or transaction.
  - Availability: ensuring that the information is accessible and usable by authorized parties when needed.
- Cryptography can be classified into two main types: symmetric key cryptography and asymmetric key cryptography  .
  - Symmetric key cryptography: also known as secret key cryptography, it uses the same key for both encryption and decryption. The key must be shared securely between the parties before the communication. Examples of symmetric key algorithms are AES, DES, RC4, etc.
  - Asymmetric key cryptography: also known as public key cryptography, it uses different keys for encryption and decryption. The key used for encryption is called the public key, and the key used for decryption is called the private key. The public key can be shared openly, while the private key must be kept secret by the owner. Examples of asymmetric key algorithms are RSA, ECC, DSA, etc.

