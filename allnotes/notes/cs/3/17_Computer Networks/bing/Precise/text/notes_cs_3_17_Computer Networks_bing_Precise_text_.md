

## Unit 1 - Introductory Concepts

1. Definition of Economics: Economics is the social science that studies the production, distribution, and consumption of goods and services.
2. Scarcity: Scarcity refers to the limited availability of resources to satisfy unlimited wants and needs.
3. Opportunity Cost: Opportunity cost is the value of the next best alternative that must be given up in order to pursue a certain action or decision.
4. Production Possibility Frontier (PPF): The PPF is a graphical representation of the different combinations of two goods or services that can be produced using all available resources efficiently.
5. Economic Systems: An economic system is the way in which a society organizes the production, distribution, and consumption of goods and services. The main types of economic systems are traditional, command, market, and mixed.
6. Microeconomics and Macroeconomics: Microeconomics is the study of individual economic units, such as households and firms, while macroeconomics is the study of the economy as a whole, including topics such as inflation, unemployment, and economic growth.




### Goals and applications of networks

Computer networks have several goals and applications. Some of the most important ones are:

1. **Resource sharing**: Networks allow multiple devices to share resources such as printers, storage devices, and internet connections.

2. **Communication**: Networks enable communication between devices, allowing users to exchange information and collaborate.

3. **Data exchange**: Networks facilitate the exchange of data between devices, making it easier to transfer files and share information.

4. **Remote access**: Networks allow users to access resources and data remotely, enabling telecommuting and remote work.

5. **Scalability**: Networks can be expanded to accommodate more devices and users as needed.

6. **Reliability**: Networks can be designed to be fault-tolerant, ensuring that data and resources remain available even in the event of failures.

7. **Security**: Networks can be secured to protect against unauthorized access and data breaches.

These are just some of the many goals and applications of computer networks. They play a crucial role in enabling communication and collaboration, and in facilitating the exchange of information and resources.



### Categories of Networks

Computer networks can be categorized based on their size, geographical scope, and purpose. Here are some common categories of networks:

1. **Local Area Network (LAN):** A LAN is a network that connects computers and devices within a limited geographical area, such as a home, school, or office building. LANs are typically owned, controlled, and managed by a single person or organization.

2. **Wide Area Network (WAN):** A WAN is a network that covers a large geographical area, such as a city, country, or even the entire world. WANs are typically owned and operated by large organizations or telecommunications companies.

3. **Metropolitan Area Network (MAN):** A MAN is a network that connects computers and devices within a metropolitan area, such as a city or large town. MANs are typically owned and operated by a single entity, such as a government or large corporation.

4. **Personal Area Network (PAN):** A PAN is a network that connects devices within the close proximity of an individual, such as a person's smartphone, smartwatch, and laptop. PANs can be wired or wireless and are typically used for personal or small-scale purposes.

5. **Storage Area Network (SAN):** A SAN is a network that provides access to consolidated, block-level data storage. SANs are typically used in data centers to provide high-speed, reliable access to large amounts of data.

6. **Virtual Private Network (VPN):** A VPN is a network that uses encryption and other security measures to create a secure, private connection between two or more devices over a public network, such as the internet. VPNs are commonly used by businesses and individuals to protect sensitive data and online activity.

These are some of the common categories of networks. Each type of network serves a specific purpose and has its own set of advantages and disadvantages. It is important to understand the differences between these types of networks when designing, implementing, and managing a computer network.



### Organization of the Internet

The Internet is a loosely-organized international collaboration of autonomous, interconnected networks that supports host-to-host communication through voluntary adherence to open protocols and procedures defined by Internet Standards. There are also many isolated internets, i.e., sets of interconnected networks, which are not connected to the Internet but use the Internet Standards.

The organization of the Internet can be viewed at different levels. At the highest level, the Internet is controlled and organized at four particular levels:
1. Computer and network hardware
2. Internet access and Internet service providers (ISPs)
3. Navigation within the Internet
4. Individual sites and communities which develop their own rules and terms of usage.

The Internet Society is a non-profit organization that empowers people to keep the Internet a force for good: open, globally connected, secure, and trustworthy. Through members, chapters, special interest groups, and partners, they are the hub of the largest global network of people and organizations working to build the Internet for everyone.



### ISP

- ISP stands for Internet Service Provider.
- An ISP is a company that provides internet access to customers.
- ISPs connect customers to the internet by providing them with a connection to their network.
- ISPs can provide internet access through various technologies such as DSL, cable, fiber optics, and satellite.
- ISPs may also offer additional services such as email, web hosting, and virtual private networks (VPNs).
- ISPs are regulated by government agencies to ensure fair competition and consumer protection.
- The speed and reliability of an internet connection can vary depending on the ISP and the technology used.
- Customers can choose from different ISPs and plans based on their needs and budget.



### Network Structure and Architecture

Network structure and architecture refer to the design and layout of a computer network. It includes the physical components, such as routers, switches, and cables, as well as the logical components, such as protocols, addressing schemes, and routing algorithms.

Some key points to consider when studying network structure and architecture are:

1. **Topology**: The physical and logical arrangement of the network's components. Common topologies include bus, star, ring, and mesh.
2. **Protocols**: The rules and standards that govern communication between devices on the network. Examples include TCP/IP, HTTP, and FTP.
3. **Addressing**: The method used to assign unique identifiers to devices on the network. This can include IP addresses, MAC addresses, and hostnames.
4. **Routing**: The process of determining the best path for data to travel from one device to another on the network. This can involve static or dynamic routing algorithms.
5. **Scalability**: The ability of the network to grow and accommodate more devices and traffic without a significant decrease in performance.
6. **Security**: The measures taken to protect the network and its data from unauthorized access and malicious attacks.

These are some of the key concepts to consider when studying network structure and architecture in the context of Unit 1 - Introductory Concepts in the subject of Computer Networks. It is important to have a solid understanding of these concepts in order to design and maintain effective and efficient computer networks.



### Layering Principles

1. **Layering** is a design principle that divides a complex system into smaller, more manageable parts, called layers.
2. Each layer provides a specific set of services to the layer above it, and relies on the services provided by the layer below it.
3. The **OSI model** is an example of a layered architecture, with seven layers: Physical, Data Link, Network, Transport, Session, Presentation, and Application.
4. The **TCP/IP model** is another example of a layered architecture, with four layers: Link, Internet, Transport, and Application.
5. Layering allows for **modularity**, where each layer can be developed and updated independently of the others.
6. Layering also allows for **interoperability**, where different implementations of the same layer can communicate with each other.
7. However, layering can also introduce **overhead**, as each layer adds its own processing and data to the overall system.
8. The **encapsulation** process involves adding a header to the data at each layer, with the header containing information specific to that layer's protocol.
9. The **decapsulation** process involves removing the header at each layer, and using the information in the header to determine how to process the data.
10. The **peer-to-peer communication** between the same layers on different devices is achieved through the use of protocols, which define the rules and formats for exchanging data.




### Services for the notes of the Unit 1 - Introductory Concepts in the subject of Computer Networks

- A service is a set of functions or capabilities that a layer provides to the layer above it.
- The services provided by a layer can be divided into two categories: connection-oriented and connectionless.
- Connection-oriented services establish a connection between two devices before any data is transmitted.
- Connectionless services do not require a connection to be established before data is transmitted.
- The services provided by a layer can also be divided into two categories: reliable and unreliable.
- Reliable services ensure that data is transmitted without errors and in the correct order.
- Unreliable services do not guarantee error-free or in-order transmission of data.
- The services provided by a layer can also be divided into two categories: acknowledged and unacknowledged.
- Acknowledged services provide a mechanism for the sender to receive confirmation that the data has been received by the receiver.
- Unacknowledged services do not provide any confirmation that the data has been received by the receiver.
- The services provided by a layer can also be divided into two categories: flow-controlled and non-flow-controlled.
- Flow-controlled services provide a mechanism for the sender to regulate the rate at which data is transmitted to the receiver.
- Non-flow-controlled services do not provide any mechanism for the sender to regulate the rate at which data is transmitted to the receiver.



### Protocols and Standards

1. **Protocols** are a set of rules and standards that govern the communication between devices on a network.
2. These rules define how data is transmitted, formatted, and processed between devices.
3. Protocols are essential for ensuring that devices can communicate with each other and exchange information accurately and efficiently.
4. Some common protocols used in computer networks include TCP/IP, HTTP, FTP, and SMTP.
5. **Standards** are agreed-upon guidelines that ensure consistency and interoperability between different devices and systems.
6. Standards are developed by organizations such as the International Organization for Standardization (ISO) and the Institute of Electrical and Electronics Engineers (IEEE).
7. Standards help to ensure that devices from different manufacturers can work together on the same network.
8. Some common standards used in computer networks include Ethernet, Wi-Fi, and Bluetooth.




### The OSI reference model

The OSI (Open Systems Interconnection) reference model is a conceptual framework used to describe the functions of a networking system. It was developed by the International Organization for Standardization (ISO) in 1984. The model divides the tasks involved in moving information between networked computers into seven layers. Each layer is responsible for a specific aspect of the communication process.

1. **Physical Layer:** This layer is responsible for the transmission of raw data bits over a physical medium. It defines the electrical, mechanical, and functional specifications for activating, maintaining, and deactivating the physical link between communicating network systems.
2. **Data Link Layer:** This layer provides reliable transmission of data frames between two nodes connected by a physical layer. It is responsible for error detection and correction, flow control, and framing.
3. **Network Layer:** This layer is responsible for routing data packets between networks. It provides logical addressing, routing, and congestion control.
4. **Transport Layer:** This layer provides end-to-end communication services for applications. It is responsible for establishing, maintaining, and terminating virtual circuits, as well as providing reliable data transfer, flow control, and error recovery.
5. **Session Layer:** This layer establishes, manages, and terminates sessions between applications. It provides synchronization, dialog control, and critical operation management.
6. **Presentation Layer:** This layer is responsible for data representation, including character encoding, data compression, and encryption/decryption. It ensures that the data is in a format that can be understood by the receiving application.
7. **Application Layer:** This layer provides services for user applications, such as email, file transfer, and remote login. It is the interface between the user and the network.

The OSI reference model is an important tool for understanding how data is transmitted over a network. It provides a common language for discussing network functionality and helps to ensure that different network technologies can work together.



### TCP/IP Protocol Suite

The TCP/IP protocol suite is a set of communication protocols used for the internet and other similar networks. It is named after the two most important protocols in the suite: the Transmission Control Protocol (TCP) and the Internet Protocol (IP).

1. **Transmission Control Protocol (TCP)**: This protocol is responsible for providing reliable, ordered, and error-checked delivery of data between applications running on different hosts. It does this by breaking the data into packets, transmitting them, and then reassembling them at the destination.

2. **Internet Protocol (IP)**: This protocol is responsible for routing data packets across the network. It does this by assigning a unique numerical label, called an IP address, to each device on the network. The IP address is used to identify the source and destination of the data packets.

3. **Other Protocols**: In addition to TCP and IP, the TCP/IP protocol suite also includes other protocols such as the User Datagram Protocol (UDP), the File Transfer Protocol (FTP), and the Simple Mail Transfer Protocol (SMTP). These protocols provide additional functionality, such as support for file transfers and email.

The TCP/IP protocol suite is the foundation of the internet and is widely used in both local and wide area networks. It is an open standard, meaning that it is freely available for anyone to use and implement. This has led to its widespread adoption and use.



### Network devices and components

Unit 1 - Introductory Concepts in the subject of Computer Networks

1. **Network Interface Card (NIC)**: A hardware component that connects a computer to a network.
2. **Hub**: A device that connects multiple devices on a network and sends data received from one device to all other devices.
3. **Switch**: A device that connects multiple devices on a network and sends data received from one device to the intended recipient device.
4. **Router**: A device that connects multiple networks and routes data between them.
5. **Modem**: A device that modulates and demodulates signals to enable data transmission over a communication line.
6. **Firewall**: A network security system that monitors and controls incoming and outgoing network traffic based on predetermined security rules.
7. **Wireless Access Point (WAP)**: A device that allows wireless devices to connect to a wired network.
8. **Repeater**: A device that amplifies and retransmits a signal to extend the range of a network.
9. **Bridge**: A device that connects two networks and filters data to reduce network traffic.
10. **Gateway**: A device that connects two networks using different protocols and translates data between them.




## Unit 2 - Physical Layer

The physical layer is the first layer of the OSI model. It is responsible for the transmission and reception of raw data between a device and a physical transmission medium. Some of the key functions of the physical layer include:

1. Defining the physical characteristics of the transmission medium, such as the type of cable, connectors, and signaling methods.
2. Encoding and decoding data into electrical, optical, or radio signals.
3. Transmitting and receiving data over the physical medium.
4. Establishing and terminating connections between devices.
5. Managing the flow of data to prevent congestion and ensure reliable transmission.

The physical layer is responsible for converting the digital data from the upper layers into a format that can be transmitted over the physical medium. This process is known as encoding. The physical layer also performs the reverse process, decoding the received signals back into digital data.

The physical layer is also responsible for managing the flow of data between devices. This includes controlling the rate at which data is transmitted, detecting and correcting errors, and ensuring that data is transmitted reliably.

Overall, the physical layer plays a crucial role in enabling communication between devices by providing the means for transmitting and receiving data over a physical medium. It is the foundation upon which all other layers of the OSI model are built.



### Network Topology Design

Network topology refers to the arrangement of the various elements (links, nodes, etc.) of a computer network. It is the physical and logical interconnection between the different devices that make up a network. The design of a network topology is important because it determines the performance, reliability, and scalability of the network.

There are several types of network topologies, including:

1. **Bus topology:** In a bus topology, all devices are connected to a central cable, called the bus or backbone. This topology is easy to install and requires less cable than other topologies, but it can be slow and is not very scalable.

2. **Star topology:** In a star topology, all devices are connected to a central hub or switch. This topology is easy to install and manage, and it is scalable, but it requires more cable than a bus topology.

3. **Ring topology:** In a ring topology, all devices are connected in a closed loop. This topology is easy to install and is scalable, but it can be slow and is not very fault-tolerant.

4. **Mesh topology:** In a mesh topology, every device is connected to every other device. This topology is very fault-tolerant and provides high performance, but it is complex to install and manage and requires a lot of cable.

5. **Tree topology:** In a tree topology, devices are connected in a hierarchical structure. This topology is scalable and provides good performance, but it can be complex to install and manage.

When designing a network topology, it is important to consider the size and complexity of the network, the performance and reliability requirements, and the cost of installation and maintenance. A well-designed network topology can provide a stable and efficient network that meets the needs of its users.



### Types of connections for the notes of the Unit 2 - Physical Layer in the subject of Computer Networks

There are two main types of connections in computer networks: point-to-point and multipoint.

1. **Point-to-point connection**: A point-to-point connection is a direct link between two devices. This type of connection is typically used for dedicated links between two devices, such as a computer and a printer, or between two routers. Point-to-point connections can be either full-duplex or half-duplex.

2. **Multipoint connection**: A multipoint connection, also known as a broadcast connection, is a link between multiple devices. This type of connection is typically used in local area networks (LANs) where multiple devices share a common communication channel. Multipoint connections can be either full-duplex or half-duplex.

In summary, point-to-point connections are used for dedicated links between two devices, while multipoint connections are used for shared communication channels between multiple devices. Both types of connections can be either full-duplex or half-duplex.



### Transmission Media

Transmission media refers to the physical pathways that carry data signals between devices in a computer network. There are two main types of transmission media: guided and unguided.

1. **Guided media:** Guided media, also known as wired or bounded media, refers to the transmission of data over a physical medium that is guided along a specific path. Examples of guided media include twisted pair cable, coaxial cable, and fiber optic cable.

    - **Twisted pair cable:** Twisted pair cable consists of two insulated copper wires twisted together to reduce interference. It is commonly used for telephone and Ethernet connections.
    - **Coaxial cable:** Coaxial cable consists of a central copper conductor surrounded by an insulating layer, a braided metal shield, and an outer insulating layer. It is commonly used for cable television and broadband internet connections.
    - **Fiber optic cable:** Fiber optic cable consists of thin strands of glass or plastic that transmit data using light. It is commonly used for high-speed, long-distance data transmission.

2. **Unguided media:** Unguided media, also known as wireless or unbounded media, refers to the transmission of data through the air or space without the use of a physical medium. Examples of unguided media include radio waves, microwaves, and infrared waves.

    - **Radio waves:** Radio waves are electromagnetic waves with frequencies ranging from 3 kHz to 300 GHz. They are commonly used for wireless communication, such as in mobile phones and wireless networks.
    - **Microwaves:** Microwaves are electromagnetic waves with frequencies ranging from 300 MHz to 300 GHz. They are commonly used for satellite communication and wireless networking.
    - **Infrared waves:** Infrared waves are electromagnetic waves with frequencies ranging from 300 GHz to 400 THz. They are commonly used for short-range communication, such as in remote controls and wireless keyboards.

These are the main types of transmission media used in computer networks. Each type has its own advantages and disadvantages, and the choice of transmission media depends on factors such as the distance, data rate, and cost.



### Signal transmission and encoding for the notes of the Unit 2 - Physical Layer in the subject of Computer Networks

1. The Physical Layer in the OSI model plays the role of interacting with actual hardware and signaling mechanism. It is the only layer of the OSI network model which actually deals with the physical connectivity of two different stations .
2. It consists of various network components such as power plugs, connectors, receivers, cable types, etc. The Physical Layer sends data bits from one device (s) (like a computer) to another device (s) .
3. The Physical Layer defines the types of encoding (that is how the 0’s and 1’s are encoded in a signal) .
4. The OSI Physical Layer provides the means to transport the bits that make up a data link layer frame across the network media. This layer accepts a complete frame from the data link layer and encodes it as a series of signals that are transmitted to the local media .
5. Network signals might be propagated through guided media such as metal wires (twisted pairs, coaxial cable, etc.) or optical fibers (flashing light, off for 0, on for 1), or in free space, such as with radio waves or microwaves .
6. The Physical Layer deals with transporting bits between two machines. It communicates 0’s and 1’s across a medium by varying some sort of physical property such as voltage or current. By representing the property as a function of time, it can be analyzed mathematically .




### Network performance and transmission impairments

- Transmission impairments refer to any damage or harm caused to the signal during transmission. Due to transmission impairments, the signal received at the receiver end may differ from the signal sent by the sender.
- Transmission media are not ideal and have some imperfections, which cause signal impairment.
- Impairments may be called noise or anything else that might cause an error in data transmission.
- There are several types of noise such as induced noise, crosstalk noise, thermal noise, and impulse noise which may corrupt the signal.
- Induced noise comes from sources such as motors and appliances. These devices act as sending antennas and the transmission medium acts as a receiving antenna.
- Transmission impairments can degrade the signal quality as it progresses towards its destination, and the received bit-error rate (BER) at the destination node might become unacceptably high.
- Previous work on lightpath routing and wavelength assignment (RWA) assumed an ideal physical layer and ignored these transmission impairments.



### Switching Techniques and Multiplexing

Switching techniques and multiplexing are important concepts in the physical layer of computer networks. These techniques are used to efficiently transmit data over a communication channel.

#### Switching Techniques

Switching techniques are used to establish a connection between two devices in a network. There are three main types of switching techniques:

1. **Circuit Switching:** In circuit switching, a dedicated communication path is established between two devices for the duration of their communication. This path is reserved exclusively for the use of these two devices.

2. **Packet Switching:** In packet switching, data is divided into small packets and transmitted over the network. Each packet is sent independently and may take a different path to reach its destination. This technique is more efficient than circuit switching as it allows multiple devices to share the same communication channel.

3. **Message Switching:** In message switching, the entire message is transmitted from one device to another. The message is stored at intermediate nodes and forwarded to the next node when the communication channel is available. This technique is less efficient than packet switching as it requires the entire message to be stored at each node.

#### Multiplexing

Multiplexing is the process of combining multiple signals into a single signal for transmission over a communication channel. This technique is used to increase the efficiency of the communication channel by allowing multiple devices to share the same channel. There are several types of multiplexing techniques, including:

1. **Frequency Division Multiplexing (FDM):** In FDM, the communication channel is divided into multiple frequency bands, and each band is assigned to a different device. Each device transmits its signal using its assigned frequency band.

2. **Time Division Multiplexing (TDM):** In TDM, the communication channel is divided into time slots, and each slot is assigned to a different device. Each device transmits its signal during its assigned time slot.

3. **Code Division Multiplexing (CDM):** In CDM, each device is assigned a unique code, and the signals from multiple devices are combined using these codes. The combined signal is transmitted over the communication channel, and the receiver uses the codes to separate the signals from different devices.

These are the basic concepts of switching techniques and multiplexing in the physical layer of computer networks. Understanding these concepts is essential for the study of computer networks.



## Unit 3 - Link layer

The link layer is the lowest layer in the OSI model of computer networking. It is responsible for the transmission of data between two directly connected nodes. The link layer is responsible for the following tasks:

1. Framing: The link layer takes the packets from the network layer and encapsulates them into frames. This process adds a header and a trailer to the packet, which contains information such as the source and destination addresses, error detection codes, and other control information.

2. Error detection and correction: The link layer is responsible for detecting and correcting errors that may occur during transmission. This is done using techniques such as parity checking and cyclic redundancy checks.

3. Flow control: The link layer is responsible for regulating the flow of data between the two nodes. This is done to prevent the receiver from being overwhelmed by the sender.

4. Media access control: The link layer is responsible for controlling access to the shared communication medium. This is done using techniques such as carrier sense multiple access with collision detection (CSMA/CD) and token passing.

5. Physical addressing: The link layer is responsible for assigning physical addresses to the nodes in the network. These addresses are used to identify the source and destination of the frames.

The link layer is an essential component of any computer network, as it provides the foundation for the transmission of data between nodes. It is responsible for ensuring that the data is transmitted reliably and efficiently, and for managing access to the shared communication medium.



### Framing for the notes of the Unit 3 - Link layer in the subject of Computer Networks

1. The link layer is responsible for transferring data between two directly connected nodes.
2. Framing is the process of encapsulating data into frames for transmission over the link layer.
3. Frames typically include a header, payload, and trailer.
4. The header contains information such as the source and destination addresses, and the type of data being transmitted.
5. The payload is the actual data being transmitted.
6. The trailer typically contains error-detection and/or error-correction information to ensure the integrity of the data.
7. Different link layer protocols use different framing methods, such as character stuffing, bit stuffing, or length-based framing.
8. The link layer is responsible for detecting and possibly correcting errors that may occur during transmission.
9. The link layer may also provide flow control to prevent the sender from overwhelming the receiver with data.
10. The link layer is an important part of the overall network architecture, providing a reliable and efficient means of transmitting data between directly connected nodes.




### Error Detection and Correction

Error detection and correction are techniques used in data communication and storage to ensure the integrity of transmitted data. These techniques are used to detect and correct errors that may occur during transmission or storage due to various reasons such as noise, interference, or hardware failures.

1. **Error Detection:** Error detection techniques are used to detect the presence of errors in the transmitted data. Some common error detection techniques include parity check, checksum, and cyclic redundancy check (CRC).
    - **Parity Check:** Parity check is a simple error detection technique where an additional bit, called the parity bit, is added to the data to make the total number of 1s in the data either even or odd. If the received data has an incorrect number of 1s, an error is detected.
    - **Checksum:** Checksum is another error detection technique where a value is calculated from the data and sent along with the data. The receiver recalculates the checksum from the received data and compares it with the received checksum to detect any errors.
    - **Cyclic Redundancy Check (CRC):** CRC is a more complex error detection technique that involves dividing the data by a predetermined polynomial and sending the remainder along with the data. The receiver performs the same division and compares the remainder with the received remainder to detect any errors.

2. **Error Correction:** Error correction techniques are used to correct errors in the received data. Some common error correction techniques include Hamming code, Reed-Solomon code, and convolutional code.
    - **Hamming Code:** Hamming code is an error correction technique that adds additional bits, called check bits, to the data to detect and correct single-bit errors.
    - **Reed-Solomon Code:** Reed-Solomon code is an error correction technique that can detect and correct multiple errors. It is commonly used in storage devices such as CDs and DVDs.
    - **Convolutional Code:** Convolutional code is an error correction technique that adds redundancy to the data by convolving the data with a predetermined sequence. It is commonly used in wireless communication.

These are some of the common error detection and correction techniques used in data communication and storage. They play a crucial role in ensuring the integrity of transmitted data.



### Flow Control in Link Layer of Computer Networks

Flow control is a mechanism used in the link layer of computer networks to regulate the flow of data between two nodes. It ensures that the sender does not overwhelm the receiver by sending too much data at once. Here are some key points to remember about flow control in the link layer:

1. Flow control is necessary because the sender and receiver may have different processing speeds or buffer sizes. If the sender sends data faster than the receiver can process it, the receiver's buffer may overflow, resulting in lost data.

2. There are two main types of flow control: stop-and-wait and sliding window. In stop-and-wait flow control, the sender sends one frame at a time and waits for an acknowledgment from the receiver before sending the next frame. In sliding window flow control, the sender can send multiple frames at once without waiting for an acknowledgment, but the number of frames that can be sent is limited by the size of the window.

3. Flow control can be implemented at the link layer or at higher layers of the network stack. When implemented at the link layer, flow control is typically done using hardware mechanisms, such as buffer memory and flow control signals.

4. Flow control is important for maintaining the reliability and efficiency of data transmission in a computer network. By regulating the flow of data, flow control helps to prevent data loss and ensure that the network is used efficiently.



### Elementary Data Link Protocols

1. **Simplex Stop-and-Wait Protocol**: This protocol is used for error-free channels. The sender sends a frame and waits for an acknowledgement from the receiver before sending the next frame.
2. **Simplex Protocol for a Noisy Channel**: This protocol is used for channels with errors. The sender sends a frame and waits for an acknowledgement from the receiver. If the acknowledgement is not received within a certain time, the sender retransmits the frame.
3. **Sliding Window Protocol**: This protocol is used to improve the efficiency of the Simplex Stop-and-Wait Protocol. The sender can send multiple frames before waiting for an acknowledgement from the receiver. The number of frames that can be sent is determined by the window size.
4. **Go-Back-N ARQ**: This protocol is a type of sliding window protocol. If an error is detected in a frame, the receiver discards all subsequent frames and requests the retransmission of the erroneous frame. The sender then retransmits the erroneous frame and all subsequent frames.
5. **Selective Repeat ARQ**: This protocol is also a type of sliding window protocol. If an error is detected in a frame, the receiver only requests the retransmission of the erroneous frame. The sender then retransmits only the erroneous frame.




### Sliding Window Protocols

Sliding Window Protocols are a method of flow control for data transmission in computer networks. They are used in the link layer of the OSI model. The basic idea behind sliding window protocols is to allow the sender to transmit multiple packets before receiving an acknowledgment from the receiver. This increases the efficiency of the transmission by reducing the time spent waiting for acknowledgments.

There are two main types of sliding window protocols: Go-Back-N and Selective Repeat.

1. **Go-Back-N**: In this protocol, the sender is allowed to transmit up to N packets before receiving an acknowledgment. If an acknowledgment is not received for a packet, the sender assumes that the packet was lost and retransmits all packets starting from the lost packet. This can result in the retransmission of packets that were successfully received, which reduces the efficiency of the transmission.

2. **Selective Repeat**: In this protocol, the sender is allowed to transmit up to N packets before receiving an acknowledgment. If an acknowledgment is not received for a packet, the sender only retransmits the lost packet. This increases the efficiency of the transmission by reducing the number of retransmissions.

Both Go-Back-N and Selective Repeat use a sliding window to keep track of the packets that have been transmitted and acknowledged. The size of the window determines the maximum number of packets that can be transmitted before an acknowledgment is received. The window is moved forward as acknowledgments are received, allowing the sender to transmit more packets.

Sliding window protocols are widely used in computer networks, including in the Transmission Control Protocol (TCP). They provide an effective method of flow control, allowing for efficient data transmission while ensuring that the receiver is not overwhelmed with data.



## Unit 4 - Medium Access Control and Local Area Networks

1. **Medium Access Control (MAC)** is a sublayer of the Data Link Layer in the OSI model. It is responsible for controlling how devices in a network gain access to a shared medium and transmit data.
2. MAC protocols are used to coordinate the access of multiple devices to a shared communication medium, such as a cable or wireless channel.
3. Some common MAC protocols include Carrier Sense Multiple Access with Collision Detection (CSMA/CD), Carrier Sense Multiple Access with Collision Avoidance (CSMA/CA), and Time Division Multiple Access (TDMA).
4. **Local Area Networks (LANs)** are computer networks that span a relatively small area, such as a single building or group of buildings.
5. LANs are typically used to connect computers and other devices within a limited area, allowing them to share resources such as printers, files, and internet access.
6. Ethernet is a widely used LAN technology that uses MAC protocols such as CSMA/CD to coordinate access to the shared medium.
7. Wireless LANs (WLANs) use radio waves to transmit data between devices, and typically use MAC protocols such as CSMA/CA to coordinate access to the shared medium.
8. WLANs are commonly used in homes, offices, and public places to provide wireless internet access.




### Channel Allocation
- Channel allocation refers to the assignment of communication channels to different users or devices in a network.
- The goal of channel allocation is to efficiently use the available bandwidth and minimize interference between users.
- There are two main approaches to channel allocation: fixed and dynamic.
- Fixed channel allocation assigns a specific channel to each user or device for the duration of their communication. This approach is simple but can result in inefficient use of the available bandwidth.
- Dynamic channel allocation, on the other hand, assigns channels to users or devices on demand, based on their current communication needs. This approach can result in more efficient use of the available bandwidth, but requires more complex algorithms and management.
- In the context of Medium Access Control and Local Area Networks, channel allocation is an important consideration for ensuring efficient and reliable communication between devices on the network.



### Multiple Access Protocols

Multiple access protocols are used in computer networks to coordinate the access of multiple devices to a shared communication medium. These protocols are used in the Medium Access Control (MAC) layer of the OSI model. Some common multiple access protocols include:

1. **Carrier Sense Multiple Access (CSMA)**: This protocol is used in Ethernet networks. It allows multiple devices to share the same communication channel by sensing if the channel is free before transmitting data. If the channel is busy, the device waits for a random period of time before attempting to transmit again.

2. **Carrier Sense Multiple Access with Collision Detection (CSMA/CD)**: This protocol is an extension of CSMA and is also used in Ethernet networks. It adds the ability to detect collisions on the communication channel. If a collision is detected, the devices involved in the collision wait for a random period of time before attempting to transmit again.

3. **Carrier Sense Multiple Access with Collision Avoidance (CSMA/CA)**: This protocol is used in wireless networks. It adds the ability to avoid collisions on the communication channel by using a handshake mechanism before transmitting data. The transmitting device sends a request-to-send (RTS) message to the receiving device, which responds with a clear-to-send (CTS) message if the channel is free.

4. **Time Division Multiple Access (TDMA)**: This protocol is used in cellular networks. It divides the communication channel into time slots and assigns each device a specific time slot for transmitting data. This ensures that only one device is transmitting at a time, avoiding collisions on the communication channel.

5. **Frequency Division Multiple Access (FDMA)**: This protocol is used in radio and television broadcasting. It divides the communication channel into frequency bands and assigns each device a specific frequency band for transmitting data. This ensures that multiple devices can transmit simultaneously without interfering with each other.

6. **Code Division Multiple Access (CDMA)**: This protocol is used in cellular networks. It assigns a unique code to each device for transmitting data. The codes are designed in such a way that multiple devices can transmit simultaneously on the same communication channel without interfering with each other.

These are some of the common multiple access protocols used in computer networks. Each protocol has its own advantages and disadvantages and is suitable for different types of networks and communication scenarios. It is important to choose the right protocol for the specific requirements of the network.



### LAN standards for the notes of the Unit 4 - Medium Access Control and Local Area Networks in the subject of Computer Networks

- LAN (Local Area Network) standards are a set of guidelines and specifications that define the operation of local area networks.
- These standards are developed and maintained by organizations such as the IEEE (Institute of Electrical and Electronics Engineers) and the ISO (International Organization for Standardization).
- Some of the most widely used LAN standards include Ethernet, Token Ring, and FDDI (Fiber Distributed Data Interface).
- Ethernet is the most widely used LAN technology and is defined by the IEEE 802.3 standard. It uses a bus or star topology and supports data transfer rates of up to 10 Gbps.
- Token Ring is another popular LAN technology that is defined by the IEEE 802.5 standard. It uses a ring topology and supports data transfer rates of up to 16 Mbps.
- FDDI is a high-speed LAN technology that is defined by the ANSI X3T9.5 standard. It uses a dual-ring topology and supports data transfer rates of up to 100 Mbps.
- These standards ensure that different devices and systems can communicate and interoperate with each other on a local area network. They also provide a framework for the development of new technologies and the improvement of existing ones.



### Link layer switches & bridges for the notes of the Unit 4 - Medium Access Control and Local Area Networks in the subject of Computer Networks

- Link layer switches and bridges are devices that operate at the link layer (layer 2) of the OSI model.
- They are used to connect multiple network segments and forward data packets between them.
- Switches and bridges use MAC addresses to determine the destination of a data packet and forward it to the appropriate port.
- Switches and bridges can improve network performance by reducing the number of collisions and increasing the available bandwidth.
- Switches are more advanced than bridges and can support more ports and faster data transfer rates.
- Switches can also support advanced features such as VLANs, Quality of Service (QoS), and link aggregation.
- Bridges are simpler devices that are typically used to connect two network segments.
- Bridges can also be used to divide a large network into smaller segments to reduce the number of collisions and improve performance.
- Both switches and bridges can be used to create larger and more complex network topologies.




### Learning Bridge and Spanning Tree Algorithms

Bridge and spanning tree algorithms are important concepts in the study of computer networks, particularly in the context of medium access control and local area networks. Here are some key points to consider when learning about these algorithms:

1. **Bridge algorithms** are used to connect multiple local area networks (LANs) together to form a larger network. These algorithms work by forwarding data frames between the connected LANs, allowing devices on different LANs to communicate with each other.

2. **Spanning tree algorithms** are used to create a loop-free topology for a network of connected bridges. This is important because loops in a network can cause problems such as broadcast storms, where a single data frame is endlessly forwarded between bridges, consuming network resources and potentially causing network congestion.

3. One common spanning tree algorithm is the **Spanning Tree Protocol (STP)**, which is used in Ethernet networks. STP works by selecting a root bridge and then determining the shortest path from the root bridge to all other bridges in the network. This creates a tree-like topology, with the root bridge at the top and all other bridges connected to it in a hierarchical fashion.

4. Another important concept in the study of bridge and spanning tree algorithms is the **bridge ID**. This is a unique identifier assigned to each bridge in a network, and is used by the spanning tree algorithm to determine the root bridge and the shortest path between bridges.

5. When studying bridge and spanning tree algorithms, it is important to understand the **role of timers** in the operation of these algorithms. Timers are used to control the flow of data frames between bridges, and to ensure that the network topology remains stable and loop-free.

These are some of the key points to consider when learning about bridge and spanning tree algorithms in the context of medium access control and local area networks. By understanding these concepts, you will be better equipped to design and implement effective and efficient computer networks.



## Unit 5 - Network Layer

The network layer is responsible for routing data packets from the source to the destination. It is the third layer of the OSI model, sitting above the data link layer and below the transport layer.

Some of the key functions of the network layer include:
1. Logical addressing: The network layer assigns logical addresses, such as IP addresses, to devices on the network. These addresses are used to identify devices and route data between them.
2. Routing: The network layer is responsible for determining the best path for data to travel from the source to the destination. This is done using routing algorithms and protocols.
3. Packet forwarding: Once the best path has been determined, the network layer is responsible for forwarding data packets along that path.
4. Fragmentation and reassembly: If a data packet is too large to be transmitted over a particular link, the network layer may fragment the packet into smaller pieces. These pieces are then reassembled at the destination.
5. Error handling and diagnostics: The network layer is responsible for detecting and handling errors that may occur during transmission. This includes lost or corrupted packets, as well as network congestion.

Some common protocols used at the network layer include IP (Internet Protocol), ICMP (Internet Control Message Protocol), and IGMP (Internet Group Management Protocol). These protocols provide the necessary functionality for routing and managing data on a network.



### Point-to-point networks

Point-to-point networks are a type of computer network in which each connection is a direct link between two endpoints. In this type of network, data is transmitted directly from one endpoint to another without the need for intermediate devices such as switches or routers.

Some key points to consider when studying point-to-point networks in the context of the Network Layer of Computer Networks are:

1. Point-to-point networks can be implemented using various physical layer technologies such as Ethernet, DSL, or fiber optic cables.
2. In a point-to-point network, each endpoint is identified by a unique network address, which is used to route data between endpoints.
3. Point-to-point networks can be used to connect devices over short or long distances, depending on the physical layer technology used.
4. Point-to-point networks are often used in situations where a dedicated, high-speed connection is required between two endpoints, such as in data center or telecommunications networks.
5. The Network Layer is responsible for routing data between endpoints in a point-to-point network, using routing protocols such as OSPF or BGP.
6. Point-to-point networks can be contrasted with other types of networks such as broadcast or multicast networks, in which data is transmitted to multiple endpoints simultaneously.




### Logical Addressing

Logical addressing is used in the network layer of the OSI model to identify devices on a network. It is an abstraction that provides a way to reference a particular device on a network without having to know its physical address.

1. Logical addresses are assigned by the network layer protocol, such as the Internet Protocol (IP) in the TCP/IP protocol suite.
2. The logical address is used to route data between networks and to identify the destination device on the network.
3. Logical addresses are hierarchical, meaning that they are structured in a way that allows for efficient routing of data between networks.
4. In the case of IP, the logical address is known as an IP address and is made up of two parts: the network ID and the host ID.
5. The network ID identifies the network on which the device is located, while the host ID identifies the specific device on that network.
6. The network layer uses routing tables to determine the best path for data to take between networks, based on the destination logical address.
7. Logical addressing is an essential component of the network layer, as it provides a way to route data between networks and to identify devices on a network.



### Basic Internetworking

Internetworking refers to the process of connecting multiple computer networks together to form a larger network. This is achieved through the use of various networking devices and protocols. Here are some key points to consider when studying basic internetworking for the Network Layer in the subject of Computer Networks:

1. **Routers**: Routers are networking devices that are used to connect multiple networks together. They operate at the Network Layer of the OSI model and use routing protocols to determine the best path for data to travel between networks.

2. **IP Addressing**: IP addressing is a fundamental concept in internetworking. Each device on a network is assigned a unique IP address, which is used to identify the device and route data to it.

3. **Subnetting**: Subnetting is the process of dividing a larger network into smaller subnetworks. This is done to improve network performance and security.

4. **Routing Protocols**: Routing protocols are used by routers to exchange information about the network topology and determine the best path for data to travel between networks. Some common routing protocols include OSPF, EIGRP, and BGP.

5. **Network Address Translation (NAT)**: NAT is a technique used to allow devices on a private network to access the internet using a single public IP address. This is done by translating the private IP addresses of the devices on the network to the public IP address of the router.

These are some of the basic concepts of internetworking that are important to understand when studying the Network Layer in the subject of Computer Networks. It is important to have a strong foundation in these concepts in order to effectively design and implement internetworks.



### IP for the notes of the Unit 5 - Network Layer in the subject of Computer Networks

- IP stands for Internet Protocol.
- It is a protocol used for communication over the internet.
- IP is responsible for routing data packets between devices on a network.
- It is a connectionless protocol, meaning that it does not establish a dedicated end-to-end connection before transmitting data.
- IP is the primary protocol in the Internet Layer of the Internet Protocol Suite.
- The current version of IP is IPv4, but IPv6 is gradually being adopted to replace it.
- IP addresses are used to identify devices on a network.
- An IP address is a numerical label assigned to each device connected to a computer network that uses the Internet Protocol for communication.
- There are two types of IP addresses: IPv4 and IPv6.
- IPv4 addresses are 32-bit numbers, while IPv6 addresses are 128-bit numbers.
- The main advantage of IPv6 over IPv4 is the larger address space, which allows for more devices to be connected to the internet.
- IP is responsible for the fragmentation and reassembly of data packets when they are too large to be transmitted over a network.
- IP also provides error checking to ensure that data is transmitted correctly.
- The IP header contains information such as the source and destination IP addresses, the protocol being used, and the length of the data packet.
- The IP header also contains a Time to Live (TTL) field, which specifies the maximum number of hops a data packet can take before being discarded.
- IP is an essential component of the internet and plays a crucial role in enabling communication between devices on a network.



### CIDR for the notes of the Unit 5 - Network Layer in the subject of Computer Networks

- CIDR stands for Classless Inter-Domain Routing.
- It is a method for allocating IP addresses and routing Internet Protocol packets.
- CIDR notation is a compact representation of an IP address and its associated routing prefix.
- CIDR notation is written as the IP address, followed by a forward slash, and then the prefix length.
- The prefix length specifies how many of the leftmost contiguous bits of the address comprise the network portion of the address.
- CIDR allows for more efficient use of IP address space and more flexible routing than the previous classful addressing system.
- CIDR was introduced in 1993 to replace the previous classful addressing system.
- CIDR is now the standard method for routing IP packets on the Internet.



### ARP (Address Resolution Protocol)

ARP is a protocol used to map an IP address to a physical address (MAC address) on a local network. It is a crucial component of the network layer in the OSI model. Here are some key points to remember about ARP:

1. ARP operates at the Data Link layer of the OSI model.
2. ARP is used to find the MAC address of a device on the local network, given its IP address.
3. ARP maintains a cache of recently resolved IP-MAC address mappings to reduce the number of ARP requests.
4. ARP requests are broadcast to all devices on the local network.
5. ARP replies are sent directly to the requesting device.
6. ARP is a stateless protocol, meaning it does not maintain any information about the state of the communication between devices.
7. ARP can be vulnerable to spoofing attacks, where an attacker sends false ARP replies to redirect traffic to their device.




### RARP

- RARP stands for Reverse Address Resolution Protocol.
- It is a network layer protocol used to resolve an IP address from a given hardware address, such as an Ethernet address.
- RARP is the opposite of the Address Resolution Protocol (ARP), which is used to resolve a hardware address from a given IP address.
- RARP is used in diskless workstations, which do not have a permanent storage to store their IP addresses.
- When a diskless workstation is powered on, it broadcasts a RARP request containing its hardware address to the network.
- A RARP server on the network receives the request and responds with the IP address that is associated with the hardware address.
- The diskless workstation can then use the IP address to communicate with other devices on the network.
- RARP is defined in RFC 903 and uses the same packet format as ARP.
- RARP has been largely replaced by other protocols, such as BOOTP and DHCP, which provide more functionality and flexibility in assigning IP addresses to devices on a network.



### DHCP (Dynamic Host Configuration Protocol)
- DHCP is a network management protocol used on Internet Protocol (IP) networks.
- It is used for automatically assigning IP addresses and other network configuration parameters to devices on a network.
- DHCP operates on a client-server model, where a DHCP server dynamically assigns an IP address and other network configuration parameters to each device on a network.
- The DHCP server maintains a pool of IP addresses and leases an address to any DHCP-enabled client when it starts up on the network.
- The lease time for an IP address is configurable, and after the lease time has expired, the client must request a new IP address.
- DHCP can also provide other network configuration parameters, such as the subnet mask, default gateway, and Domain Name System (DNS) server addresses.
- DHCP is an essential part of modern networks and is used in homes, businesses, and other organizations to simplify network management and reduce the need for manual configuration.



### ICMP

- ICMP stands for Internet Control Message Protocol.
- It is a network layer protocol used by network devices to send error messages and operational information.
- ICMP is used by routers, hosts, and network devices to generate error messages when there are issues with IP packet delivery.
- ICMP messages are encapsulated within IP datagrams, and are therefore delivered in the same way as any other IP packet.
- ICMP messages have a specific format, consisting of an 8-bit type field, an 8-bit code field, and a checksum.
- Some common ICMP message types include:
  - Echo Request and Echo Reply (used by the ping command)
  - Destination Unreachable
  - Time Exceeded
  - Redirect
- ICMP is an important tool for network troubleshooting and management.



### Routing for the notes of the Unit 5 - Network Layer in the subject of Computer Networks

1. Routing is the process of selecting a path for traffic in a network, or between or across multiple networks.
2. The primary function of the network layer is to provide routing and forwarding of packets from a source to a destination.
3. Routing algorithms determine the specific choice of route.
4. Routing is performed for many types of networks, including circuit-switched networks, such as the public switched telephone network (PSTN), and computer networks, such as the Internet.
5. In packet-switched networks, routing directs packet forwarding (the transit of logically addressed network packets from their source toward their ultimate destination) through intermediate nodes.
6. Intermediate nodes are typically network hardware devices such as routers, bridges, gateways, firewalls, or switches.
7. Routing is a key feature of the Internet because it enables messages to pass from one computer to another and eventually reach the target machine.
8. Each intermediary computer performs routing by passing along the message to the next computer.
9. Routing is often contrasted with bridging, which performs a similar function but works at a different layer of the protocol stack and uses different network devices.
10. Routing operates at the network layer (layer 3) of the OSI model, while bridging operates at the data link layer (layer 2).



### Forwarding and Delivery

Forwarding and delivery are two important concepts in the network layer of the OSI model in computer networks.

1. **Forwarding** refers to the process of moving a packet from an incoming link to the appropriate outgoing link at a router. This is done by using a forwarding table, which maps destination addresses to outgoing links.

2. **Delivery** refers to the process of delivering a packet from the source host to the destination host. This involves forwarding the packet through a series of routers until it reaches the destination host.

In summary, forwarding is the process of moving a packet within a router, while delivery is the process of moving a packet from the source host to the destination host. Both processes are essential for the proper functioning of the network layer.



### Static and dynamic routing for the notes of the Unit 5 - Network Layer in the subject of Computer Networks

1. **Static routing** is a type of routing in which the network administrator manually configures the routing table on a router. This means that the administrator must specify the routes for each destination network and the next hop router for each route.
2. Static routing is simple to implement and requires less processing power and memory than dynamic routing. However, it is not scalable and can be difficult to manage in large networks.
3. **Dynamic routing** is a type of routing in which the router automatically learns and updates its routing table based on information it receives from other routers in the network. This is done using routing protocols such as OSPF, RIP, and BGP.
4. Dynamic routing is more scalable and easier to manage than static routing, as the router automatically adapts to changes in the network topology. However, it requires more processing power and memory than static routing.
5. In summary, static routing is best suited for small, simple networks, while dynamic routing is better for larger, more complex networks.




### Routing algorithms and protocols for the notes of the Unit 5 - Network Layer in the subject of Computer Networks

Routing algorithms and protocols are used in computer networks to determine the best path for data packets to travel from their source to their destination. These algorithms and protocols take into account various factors such as network topology, link costs, and traffic congestion to make routing decisions.

Some common routing algorithms and protocols include:

1. **Shortest Path First (SPF)**: This algorithm calculates the shortest path between two nodes in a network based on the link costs. It is used in link-state routing protocols such as OSPF (Open Shortest Path First) and IS-IS (Intermediate System to Intermediate System).

2. **Distance Vector**: This algorithm calculates the best path between two nodes based on the distance (or hop count) between them. It is used in distance-vector routing protocols such as RIP (Routing Information Protocol) and IGRP (Interior Gateway Routing Protocol).

3. **Link-State**: This algorithm calculates the best path between two nodes based on the link-state information, which includes the link costs and network topology. It is used in link-state routing protocols such as OSPF and IS-IS.

4. **Path Vector**: This algorithm calculates the best path between two nodes based on the path information, which includes the AS (Autonomous System) path and other attributes. It is used in path-vector routing protocols such as BGP (Border Gateway Protocol).

These are just a few examples of the many routing algorithms and protocols used in computer networks. Each algorithm and protocol has its own strengths and weaknesses, and the choice of which to use depends on the specific requirements of the network.



### Congestion Control Algorithms

Congestion control is a mechanism that controls the entry of data packets into the network, enabling a better use of a shared network infrastructure and avoiding congestive collapse. Congestive-Avoidance Algorithms (CAA) are implemented at the TCP layer as the mechanism to avoid congestive collapse in a network.

There are two congestion control algorithms which are as follows:

1. **Leaky Bucket Algorithm**: The leaky bucket algorithm discovers its use in the context of network traffic shaping or rate-limiting. A leaky bucket implementation and a token bucket implementation are predominantly used for traffic shaping algorithms.

2. **Congestion-Avoidance Algorithms**: Congestion control in the network layer occurs when a node or link carries data beyond its limit. This often leads to the queuing of packets—and in the worst case, loss of packets—as well as a decrease in the network’s Quality of Service (QoS).



### IPv6

- IPv6 or Internet Protocol Version 6 is a network layer protocol that allows communication to take place over the network.
- IPv6 was designed by the Internet Engineering Task Force (IETF) in December 1998 with the purpose of superseding the IPv4 due to the global exponentially growing internet users.
- Each device on the internet has a unique IP address used to identify it and figure out where it is.
- IPv6 is the newest version of internet protocol formulated by the Internet Engineering Task Force (IETF), which helps identify and local endpoint systems on a computer network and route online traffic while addressing the problem of IPv4 address depletion due to prolonged internet use worldwide.
- IPv6 is an Internet Layer protocol for packet-switched internetworking and provides end-to-end datagram transmission across multiple IP networks, closely adhering to the design principles developed in the previous version of the protocol, Internet Protocol Version 4 (IPv4).



## Unit 6 - Transport Layer

The transport layer is the fourth layer in the OSI model and is responsible for end-to-end communication between devices. Some of the key functions of the transport layer include:

1. **Segmentation and reassembly**: The transport layer divides the data into smaller segments that can be transmitted over the network. At the receiving end, these segments are reassembled into the original data.

2. **Connection control**: The transport layer can establish, maintain, and terminate connections between devices.

3. **Flow control**: The transport layer can regulate the flow of data between devices to prevent the receiver from being overwhelmed by the sender.

4. **Error control**: The transport layer can detect and correct errors that may occur during transmission.

5. **Multiplexing**: The transport layer can multiplex multiple communication streams between devices.

Two of the most common transport layer protocols are the Transmission Control Protocol (TCP) and the User Datagram Protocol (UDP). TCP is a connection-oriented protocol that provides reliable data transmission, while UDP is a connectionless protocol that provides faster but less reliable data transmission.



### Process-to-process delivery

Process-to-process delivery is a key function of the transport layer in computer networks. It involves the delivery of data from one application process on one device to another application process on another device. Here are some key points to remember about process-to-process delivery:

1. The transport layer is responsible for providing logical communication between application processes running on different hosts.
2. To achieve this, the transport layer uses port numbers to identify different application processes and to distinguish between them.
3. The transport layer also provides mechanisms for error control, flow control, and congestion control to ensure reliable and efficient data transfer between application processes.
4. Two common transport layer protocols that provide process-to-process delivery are the Transmission Control Protocol (TCP) and the User Datagram Protocol (UDP).
5. TCP provides reliable, connection-oriented service, while UDP provides an unreliable, connectionless service.
6. The choice of transport layer protocol depends on the requirements of the application, with some applications requiring the reliability of TCP, while others can tolerate the unreliability of UDP in exchange for lower overhead and faster transmission.




### Transport Layer Protocols

The transport layer is responsible for providing end-to-end communication services for applications. It provides logical communication between application processes running on different hosts. Some of the key transport layer protocols are:

1. **Transmission Control Protocol (TCP):** TCP is a connection-oriented protocol that provides reliable, ordered, and error-checked delivery of data between applications. It uses a three-way handshake to establish a connection between two hosts and employs flow control, congestion control, and error recovery mechanisms to ensure reliable data transfer.

2. **User Datagram Protocol (UDP):** UDP is a connectionless protocol that provides a simple, unreliable, and unordered datagram service. It is useful for applications that do not require the reliability of TCP, such as real-time streaming of audio and video.

3. **Stream Control Transmission Protocol (SCTP):** SCTP is a connection-oriented protocol that provides reliable, ordered delivery of data. It is similar to TCP, but it also supports multihoming and multistreaming, which allows multiple network paths and multiple streams of data to be used simultaneously.

4. **Datagram Congestion Control Protocol (DCCP):** DCCP is a connection-oriented protocol that provides unreliable, unordered delivery of data. It is designed for applications that require congestion control, such as multimedia streaming, but do not require the reliability of TCP.

These are some of the key transport layer protocols used in computer networks. They provide a range of services to support different types of applications and their communication needs.



### UDP and TCP for the notes of the Unit 6 - Transport Layer in the subject of Computer Networks

- **UDP (User Datagram Protocol)** and **TCP (Transmission Control Protocol)** are two of the main protocols in the transport layer of the TCP/IP model.
- **UDP** is a connectionless protocol that provides a simple and unreliable message service for transaction-oriented services.
    - It is useful for applications that require fast, efficient transmission, such as games or voice and video communication.
    - It does not provide error checking, flow control, or retransmission of lost packets.
- **TCP** is a connection-oriented protocol that provides a reliable, stream-oriented service.
    - It is used by applications that require guaranteed delivery, such as web browsing, email, and file transfer.
    - It provides error checking, flow control, and retransmission of lost packets.
    - It establishes a virtual connection between the sender and receiver before transmitting data.
- Both protocols use port numbers to identify the specific process running on the host computer that the data is intended for.
- The main difference between the two protocols is the level of reliability and the way they handle data transmission.



### Multiplexing

Multiplexing is a technique used in communication systems to allow multiple signals to be transmitted over a single communication channel. This is achieved by combining the signals into a composite signal, which is then transmitted over the channel. At the receiving end, the composite signal is separated into its individual components, allowing the original signals to be recovered.

There are several types of multiplexing techniques, including:

1. **Frequency Division Multiplexing (FDM):** This technique divides the available bandwidth of the communication channel into multiple frequency bands, each of which is used to transmit a separate signal. The signals are modulated onto different carrier frequencies and combined into a composite signal for transmission.

2. **Time Division Multiplexing (TDM):** This technique divides the time available for transmission into multiple time slots, with each time slot used to transmit a separate signal. The signals are transmitted one after the other in a repeating cycle, with each signal occupying a specific time slot.

3. **Code Division Multiplexing (CDM):** This technique assigns a unique code to each signal, allowing multiple signals to be transmitted simultaneously over the same frequency band. The signals are spread over a wide frequency band using the assigned codes, and can be separated at the receiving end using the same codes.

4. **Wavelength Division Multiplexing (WDM):** This technique is used in optical communication systems and is similar to FDM. It divides the available bandwidth of the optical fiber into multiple wavelength bands, each of which is used to transmit a separate optical signal.

Multiplexing is an important concept in the transport layer of computer networks, as it allows multiple connections to be established and maintained over a single network link. This improves the efficiency of the network and allows multiple applications to share the available bandwidth. The transport layer uses port numbers to identify different connections and to multiplex and demultiplex data transmitted over the network.



### Connection Management

Connection management is a key function of the transport layer in computer networks. It involves establishing, maintaining, and terminating connections between two or more devices. Here are some key points to remember about connection management:

1. **Connection establishment**: Before data can be transmitted between two devices, a connection must be established. This involves a three-way handshake, where the two devices exchange messages to synchronize and agree on the parameters for the connection.

2. **Data transfer**: Once a connection is established, data can be transmitted between the two devices. The transport layer is responsible for ensuring that the data is transmitted reliably and in the correct order.

3. **Connection termination**: When the data transfer is complete, the connection must be terminated. This involves another exchange of messages between the two devices to ensure that all data has been transmitted and received, and to release any resources that were used for the connection.

4. **Connection state**: The transport layer maintains the state of the connection, including information about the sequence numbers of the data packets that have been sent and received, and any retransmission or flow control information.

5. **Error and flow control**: The transport layer is responsible for detecting and correcting errors that may occur during data transmission, and for controlling the flow of data to prevent congestion or overloading of the network.

Connection management is an important aspect of the transport layer, as it ensures that data is transmitted reliably and efficiently between devices in a computer network. It is essential for the proper functioning of many network applications and services.



### Flow control and retransmission for the notes of the Unit 6 - Transport Layer in the subject of Computer Networks

Flow control and retransmission are two important mechanisms used in the transport layer of computer networks to ensure reliable data transmission.

1. **Flow control** is the process of managing the rate of data transmission between two nodes to prevent a fast sender from overwhelming a slow receiver. This is achieved by using techniques such as buffering, windowing, and congestion avoidance.

2. **Retransmission** is the process of re-sending data that has been lost or corrupted during transmission. This is achieved by using techniques such as error detection, error correction, and retransmission protocols.

Together, flow control and retransmission help to ensure that data is transmitted reliably and efficiently over a computer network. These mechanisms are implemented in transport layer protocols such as TCP (Transmission Control Protocol) and UDP (User Datagram Protocol).



### Window Management

Window management is a flow control mechanism used in the transport layer of computer networks. It is used to control the amount of data that can be sent by a sender before receiving an acknowledgment from the receiver. This mechanism helps to prevent network congestion and ensures reliable data transmission.

Here are some key points to remember about window management:

1. The sender maintains a sending window, which specifies the range of sequence numbers that can be sent without receiving an acknowledgment.
2. The receiver maintains a receiving window, which specifies the range of sequence numbers that can be received and acknowledged.
3. The size of the sending and receiving windows can be dynamically adjusted based on network conditions.
4. The sender can only send data within the sending window and must wait for an acknowledgment from the receiver before sending more data.
5. The receiver can only accept data within the receiving window and must send an acknowledgment to the sender to update the sending window.
6. Window management helps to prevent network congestion by limiting the amount of data that can be sent at any given time.




### TCP Congestion Control

- Transmission Control Protocol (TCP) uses a network congestion-avoidance algorithm that includes various aspects of an additive increase/multiplicative decrease (AIMD) scheme, along with other schemes including slow start and congestion window (CWND), to achieve congestion avoidance.
- TCP Congestion Control is meant for handling Congestion in Network. TCP Congestion Control Policy consists of three phases- Slow start phase, Congestion Avoidance Phase, Congestion Detection Phase. Congestion refers to a network state where the message traffic becomes so heavy that it slows down network response time.
- TCP Congestion Control techniques prevent congestion or help mitigate the congestion after it occurs. Unlike the sliding window (rwnd) used in the flow control mechanism and maintained by the receiver, TCP uses the congestion window (cwnd) maintained by the sender.
- There are three phases that TCP uses for congestion control: slow start, congestion avoidance, and congestion detection. In the first phase of congestion control, the sender sends the packet and gradually increases the number of packets until it reaches a threshold.
- Some algorithms require custom fields to be added to the TCP packet structure, such as Explicit Control Protocol (XCP) and MaxNet.



### Quality of Service (QoS) in Transport Layer of Computer Networks

Quality of Service (QoS) refers to the ability of a network to provide improved service to certain network traffic. The primary goal of QoS is to provide priority including dedicated bandwidth, controlled jitter and latency, and improved loss characteristics to specific flows of data in a network.

In the context of the Transport Layer of Computer Networks, QoS can be achieved through several mechanisms, including:

1. **Resource Reservation:** Reserving resources such as bandwidth and buffer space for specific flows of data to ensure that they receive the required level of service.

2. **Traffic Shaping:** Controlling the rate at which data is sent into the network to prevent congestion and ensure that traffic conforms to a specified traffic profile.

3. **Packet Scheduling:** Determining the order in which packets are transmitted to ensure that high-priority traffic is transmitted before lower-priority traffic.

4. **Admission Control:** Deciding whether to admit a new flow of data into the network based on the current network conditions and the QoS requirements of the new flow.

By implementing these mechanisms, the Transport Layer can provide improved QoS to specific flows of data, ensuring that they receive the required level of service. This can be particularly important for applications such as voice and video, which require low latency and jitter, as well as for mission-critical applications that require high levels of reliability.



## Unit 7 - Application Layer

The Application Layer is the topmost layer of the OSI model and is responsible for providing services to the user. It is the layer closest to the user and provides a means for the user to interact with the network. Some of the key functions of the Application Layer include:

1. **Resource Sharing:** The Application Layer enables users to share resources such as files, printers, and databases across the network.

2. **Remote Access:** The Application Layer provides remote access to network resources, allowing users to access resources from remote locations.

3. **Email Services:** The Application Layer provides email services, allowing users to send and receive emails across the network.

4. **Network Management:** The Application Layer provides network management services, allowing network administrators to monitor and manage the network.

5. **Directory Services:** The Application Layer provides directory services, allowing users to search for and access network resources.

Some common protocols used in the Application Layer include HTTP, FTP, SMTP, and DNS. These protocols provide the rules and standards for communication between applications on different devices.



### Domain Name System (DNS)

- The Domain Name System (DNS) is a hierarchical and decentralized naming system for computers, services, or other resources connected to the Internet or a private network.
- DNS associates various information with domain names assigned to each of the participating entities.
- The most prominent function of DNS is to translate human-readable domain names (such as www.example.com) into the numerical IP addresses needed for the purpose of locating and identifying computer services and devices with the underlying network protocols.
- DNS is an essential component of the functionality of the Internet, as it provides a worldwide, distributed directory service.
- The Domain Name System is maintained by a distributed database system, which uses the client-server model.
- The nodes of this database are the name servers.
- Each domain has at least one authoritative DNS server that publishes information about that domain and the name servers of any domains subordinate to it.
- The top of the hierarchy is served by the root name servers, the servers to query when looking up (resolving) a top-level domain name.
- DNS also defines the technical underpinnings of the functionality of this distributed database service.
- It defines the DNS protocol, a detailed specification of the data structures and data communication exchanges used in DNS, as part of the Internet Protocol Suite.



### World Wide Web and Hyper Text Transfer Protocol

The World Wide Web (WWW) is a system of interlinked hypertext documents accessed through the Internet. It was invented by Tim Berners-Lee in 1989 while working at CERN, the European Organization for Nuclear Research.

The HyperText Transfer Protocol (HTTP) is the protocol used for transmitting web pages over the Internet. It is an application layer protocol that runs on top of the Transmission Control Protocol (TCP) and the Internet Protocol (IP).

Here are some key points about the World Wide Web and HTTP:

1. The World Wide Web is a system of interlinked hypertext documents accessed through the Internet.
2. HTTP is the protocol used for transmitting web pages over the Internet.
3. HTTP is an application layer protocol that runs on top of TCP and IP.
4. The first web page was put online on August 6, 1991.
5. The World Wide Web has revolutionized the way we access and share information.




### Electronic Mail

Electronic mail, commonly known as email, is a method of exchanging digital messages between people using electronic devices. It is one of the most widely used services on the internet and is an essential component of the Application Layer in the subject of Computer Networks.

1. **Components of Email System:** An email system consists of several components, including user agents, mail servers, and the Simple Mail Transfer Protocol (SMTP).
2. **User Agents:** A user agent, also known as an email client, is a software application that allows users to compose, send, receive, and manage email messages. Examples of user agents include Microsoft Outlook, Apple Mail, and Gmail.
3. **Mail Servers:** A mail server is a computer system that stores and manages email messages. It receives incoming messages from other mail servers or user agents and stores them in the appropriate mailbox. It also forwards outgoing messages to their destination.
4. **Simple Mail Transfer Protocol (SMTP):** SMTP is the standard protocol used for sending email messages between mail servers. It is used to transfer messages from the sender's mail server to the recipient's mail server.
5. **Email Message Format:** An email message consists of two main parts: the header and the body. The header contains information about the sender, recipient, subject, and other metadata. The body contains the actual content of the message.
6. **Email Security:** Email security is an important concern as email messages can contain sensitive information. Common security measures include encryption, digital signatures, and spam filtering.




### File Transfer Protocol

File Transfer Protocol (FTP) is a standard network protocol used to transfer files from one host to another over a TCP-based network, such as the Internet. FTP is built on a client-server architecture and uses separate control and data connections between the client and the server.

Here are some key points to remember about FTP:

1. FTP is a standard network protocol used for transferring files over the Internet.
2. FTP uses a client-server architecture.
3. FTP uses separate control and data connections between the client and the server.
4. FTP is commonly used to transfer files between a local computer and a remote server.
5. FTP can be used to upload files to a server or to download files from a server.
6. FTP can be used with a command-line interface or with a graphical user interface (GUI).
7. FTP uses port 21 for control connections and port 20 for data connections.
8. FTP supports both active and passive modes for data transfer.
9. FTP supports anonymous access, where users can log in with the username "anonymous" and their email address as the password.
10. FTP is not a secure protocol and does not encrypt data in transit. For secure file transfers, other protocols such as SFTP or FTPS can be used.




### Remote Login

Remote login is a process that allows a user to log into a computer system from a remote location. This is typically done using a remote login protocol, such as Telnet or Secure Shell (SSH), which provides a secure, encrypted connection between the remote user and the host computer.

Here are some key points to remember about remote login:

1. Remote login allows a user to access a computer system from a remote location, as if they were sitting in front of the computer.
2. This is typically done using a remote login protocol, such as Telnet or SSH.
3. Telnet is an older protocol that is not considered secure, as it sends data in plaintext.
4. SSH is a more secure alternative to Telnet, as it provides an encrypted connection between the remote user and the host computer.
5. Remote login is commonly used by system administrators to manage servers and other network devices remotely.
6. It can also be used by users to access their work computer from home or while traveling.
7. To use remote login, the user must have an account on the host computer and must know the hostname or IP address of the host computer.
8. The user must also have the appropriate software installed on their computer, such as an SSH client.




### Network Management

Network management refers to the activities, methods, procedures, and tools that pertain to the operation, administration, maintenance, and provisioning of networked systems. It is a broad term that covers a range of tasks, including:

1. **Fault management:** This involves detecting, isolating, and correcting faults in the network. This can include monitoring the network for problems, analyzing the cause of the problem, and taking corrective action to fix the problem.

2. **Configuration management:** This involves managing the configuration of the network, including the hardware and software components. This can include tracking changes to the network, maintaining an inventory of network components, and managing the configuration of network devices.

3. **Accounting management:** This involves tracking the usage of network resources and allocating costs to users or departments. This can include monitoring the usage of network bandwidth, storage, and other resources, and generating reports on usage and costs.

4. **Performance management:** This involves monitoring and optimizing the performance of the network. This can include monitoring network traffic, analyzing network performance, and taking action to improve network performance.

5. **Security management:** This involves protecting the network from unauthorized access and ensuring the confidentiality, integrity, and availability of data. This can include implementing security measures such as firewalls, intrusion detection systems, and encryption.

Network management is an essential part of maintaining a reliable and efficient network. It involves a range of tasks and requires the use of specialized tools and techniques to ensure that the network is operating at its best.



### Data Compression

Data compression is the process of encoding information using fewer bits than the original representation. It is used to reduce the amount of data that needs to be transmitted or stored. Data compression can be lossless or lossy.

- **Lossless compression** algorithms reduce the size of the data without losing any information. The original data can be perfectly reconstructed from the compressed data. Examples of lossless compression algorithms include Huffman coding, Lempel-Ziv-Welch (LZW) and Run-length encoding (RLE).

- **Lossy compression** algorithms reduce the size of the data by discarding some information. The original data cannot be perfectly reconstructed from the compressed data. Lossy compression is typically used for multimedia data such as images, audio and video. Examples of lossy compression algorithms include JPEG, MPEG and MP3.

Data compression is used in many applications, including file storage, data transmission and streaming media. In the context of computer networks, data compression can be used to reduce the amount of bandwidth required to transmit data over a network.



### Cryptography – basic concepts for the notes of the Unit 7 - Application Layer in the subject of Computer Networks

Cryptography is the practice of secure communication in the presence of third parties. It involves techniques for secure communication, data storage, and data transmission. Here are some basic concepts in cryptography:

1. **Encryption**: The process of converting plaintext (readable data) into ciphertext (encoded data) to prevent unauthorized access.
2. **Decryption**: The reverse process of encryption, where ciphertext is converted back into plaintext.
3. **Cipher**: An algorithm used for encryption and decryption.
4. **Key**: A piece of information used by a cipher to encrypt and decrypt data.
5. **Symmetric-key cryptography**: A type of cryptography where the same key is used for both encryption and decryption.
6. **Asymmetric-key cryptography**: A type of cryptography where different keys are used for encryption and decryption. It is also known as public-key cryptography.
7. **Hash function**: A function that takes an input and produces a fixed-size output, commonly used for verifying the integrity of data.
8. **Digital signature**: A cryptographic technique used to verify the authenticity and integrity of a message or document.
9. **Certificate authority (CA)**: An entity that issues digital certificates, which are used to verify the identity of the owner of a public key.

These are some of the basic concepts in cryptography. Cryptography plays an important role in securing communication and data in computer networks. It is used in various applications, such as secure email, online banking, and virtual private networks (VPNs).

