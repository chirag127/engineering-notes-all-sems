 Here is the formal content in Markdown format without any emojis or external links:

#### Introduction to Big SQL

1. Big SQL is a query engine that runs on top of Hadoop to provide SQL-like capabilities to query data in Hadoop.
2. It allows you to use a familiar SQL-like syntax to query data residing in HDFS, regardless of the format or schema.
3. You can query data in various storage formats like JSON, Avro, SequenceFiles, etc. without requiring the data to be in a fixed schema or database table format.
4. It provides a JDBC/ODBC driver interface so you can use your favorite BI tool or programming language to query data in Hadoop.
5. It translates the SQL queries into MapReduce jobs which are executed on Hadoop to return the query results.
6. Some key benefits of using Big SQL are:
- Familiar SQL-like syntax
- Schema-on-read
- Cost-effective as it utilizes Hadoop
- Scales to query huge volumes of data
- Integrates with Hadoop ecosystem

The above content summarizes some key points about Big SQL in a formal tone with points and without any emojis or external links as per the given criteria. Please let me know if you would like me to modify or expand the answer.