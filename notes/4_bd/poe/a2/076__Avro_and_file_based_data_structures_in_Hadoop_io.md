 Here is the content in markdown format with formal tone and without any emojis or external links:

##### Avro and file based data structures in Hadoop io

1. Avro is a data serialization system. It provides:
- Rich data structures.
- A compact, fast, binary data format.
- A container file, to store persistent data.
- Remote procedure call (RPC).
- Simple integration with dynamic languages. Code generation is not required to read or write data files nor to use or implement RPC protocols. Code generation can be used to enhance performance.

2. Avro provides:
- Rich data structures - Records, Enums, Arrays, Maps, Unions, and Primitives
- A compact, fast, binary data format
- A container file to store persistent data
- Remote procedure call (RPC)
- Simple integration with dynamic languages. Code generation is not required.

3. Avro data files are:
- Self-describing - contains schema
- Language independent - many language bindings exist
- Compact - efficient binary encoding

Using Avro, we can define a schema, compile it to generate data serialization/de-serialization code, and then easily read and write data in a variety of languages. Avro provides data structure for complex data. It uses JSON for defining data schemas. The schema is stored with the data, so that files may be processed without extra code. Avro is suited for efficient I/O, to transmit data over network quickly and for storage in a compact format.

That's all for the given topic. I have provided the points and written the content in a formal manner without any emojis or external links as instructed. Please let me know if you would like me to modify or expand the content.