A possible ASCII diagram for 11. WAP that takes two operands and one operator from the user, perform the operation, and prints the result by using Switch statement is:

```
+----------------+        +----------------+        +----------------+
|                |        |                |        |                |
|  User input:   |        |  Switch case:  |        |  Output:       |
|  a, b, op      |------->|  Perform op on |------->|  Print a op b  |
|                |        |  a and b       |        |                |
+----------------+        +----------------+        +----------------+
```

The diagram shows the three main steps of the program:

- User input: The program takes two operands (a and b) and one operator (op) from the user.
- Switch case: The program uses a switch statement to perform the operation (op) on the operands (a and b).
- Output: The program prints the result of the operation (a op b) to the screen.