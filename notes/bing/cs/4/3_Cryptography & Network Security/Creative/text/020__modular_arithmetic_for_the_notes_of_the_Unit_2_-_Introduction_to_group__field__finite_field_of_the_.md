### Modular arithmetic

- Modular arithmetic is a system of arithmetic for integers, where values reset to zero and begin to increase again, after reaching a certain predefined value, called the modulus (modulo) .
- Modular arithmetic is widely used in computer science and cryptography, as it directly underpins public key systems such as RSA and Diffie–Hellman, and provides finite fields which underlie elliptic curves, and is used in a variety of symmetric key algorithms including Advanced Encryption Standard (AES), International Data Encryption Algorithm (IDEA), and RC4 .
- In modular arithmetic, we use the notation A ≡ B (mod N) to mean that A and B have the same remainder when divided by N, or equivalently, that A - B is divisible by N. For example, 17 ≡ 2 (mod 5) because both 17 and 2 have the same remainder of 2 when divided by 5, or because 17 - 2 = 15 is divisible by 5 .
- Modular arithmetic has some basic properties that are useful for cryptography, such as:
  - (A + B) mod N = (A mod N + B mod N) mod N . This means that we can add two numbers before or after taking the modulus, and the result will be the same.
  - (A - B) mod N = (A mod N - B mod N) mod N . This means that we can subtract two numbers before or after taking the modulus, and the result will be the same.
  - (A * B) mod N = (A mod N * B mod N) mod N . This means that we can multiply two numbers before or after taking the modulus, and the result will be the same.
  - (A / B) mod N = (A mod N * B^-1 mod N) mod N, where B^-1 is the multiplicative inverse of B modulo N, if it exists . This means that we can divide two numbers before or after taking the modulus, and the result will be the same, as long as the divisor has an inverse modulo N.
- Modular arithmetic is also related to some important concepts and algorithms in cryptography, such as:
  - Prime and relative prime numbers: A prime number is a positive integer that has exactly two positive divisors, 1 and itself. A relative prime number is a positive integer that has no common divisors with another positive integer, except 1. For example, 17 is a prime number, and 17 and 12 are relative prime numbers, as they have no common divisors other than 1. Prime and relative prime numbers are important for public key cryptography, as they are used to generate keys and encrypt and decrypt messages .
  - Extended Euclidean Algorithm: The Extended Euclidean Algorithm is an algorithm that can find the greatest common divisor (GCD) of two positive integers, as well as the coefficients of Bézout's identity, which states that for any two positive integers A and B, there exist integers X and Y such that AX + BY = GCD(A, B). The Extended Euclidean Algorithm can also be used to find the multiplicative inverse of a number modulo another number, if it exists. For example, the multiplicative inverse of 3 modulo 11 is 4, because 3 * 4 ≡ 1 (mod 11). The Extended Euclidean Algorithm can be used to solve linear congruences, which are equations of the form AX ≡ B (mod N), where A, B, and N are known and X is unknown .
  - Fermat's and Euler's theorem: Fermat's theorem states that if P is a prime number and A is any integer that is not divisible by P, then A^(P-1) ≡ 1 (mod P). Euler's theorem is a generalization of Fermat's theorem, which states that if A and N are positive integers that are relative prime, then A^φ(N) ≡ 1 (mod N), where φ(N) is the Euler's totient function, which counts the number of positive integers that are smaller than and relative prime to N. For example, φ(10) = 4, because the positive integers that are smaller than and relative prime to 10 are 1, 3, 7, and 9. Fermat's and Euler's theorem are useful for public key cryptography, as they are used