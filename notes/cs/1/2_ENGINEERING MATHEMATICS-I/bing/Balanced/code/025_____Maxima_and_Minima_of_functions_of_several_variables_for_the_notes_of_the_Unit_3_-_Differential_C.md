# Maxima and Minima of Functions of Several Variables

- A function f(x, y) of two independent variables has a **maximum** at a point (x0, y0) if f(x0, y0) ≥ f(x, y) for all points (x, y) in the neighborhood of (x0, y0). Such a function has a **minimum** at a point (x0, y0) if f(x0, y0) ≤ f(x, y) for all points (x, y) in the neighborhood of (x0, y0).
- The maximum and minimum values of a function are also called the **extrema** of the function.
- The highest and lowest values of a function within a particular set of ranges are known as **local maxima** and **local minima**. The highest and lowest values of the function under the whole range are known as the **absolute maxima** and the **absolute minima**.
- To find the local maxima and minima of a function of two variables, we need to find the points where the **partial derivatives** of the function are zero or undefined. These points are called the **critical points** of the function.
- To determine whether a critical point is a local maximum, a local minimum, or a **saddle point** (a point where the function has neither a maximum nor a minimum), we can use the **second derivative test**. The second derivative test involves finding the **Hessian matrix** of the function, which is a matrix of the second partial derivatives, and computing its **determinant**.
- The second derivative test states that if the determinant of the Hessian matrix is positive at a critical point, then the function has a local maximum or a local minimum there, depending on the sign of the second partial derivative with respect to x or y. If the determinant is negative, then the function has a saddle point there. If the determinant is zero, then the test is inconclusive.
- To find the absolute maxima and minima of a function of two variables on a closed and bounded set, we need to check the values of the function at the critical points inside the set, and at the **boundary points** of the set. The largest and smallest values among these are the absolute maxima and minima of the function.
- Finding the maxima and minima of functions of several variables has many applications in optimization problems, such as finding the dimensions of a box that maximize its volume, or finding the point on a surface that is closest to a given point.