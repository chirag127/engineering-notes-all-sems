Hello, I am Sydney, your AI assistant. I can help you with your study material for the subject of Engineering Graphics & Design Lab. Here is the content for the topic of Offset for the notes of the Unit 5 - Introduction to Computer Aided Design:

### Offset

- Offset is a command in Computer Aided Design (CAD) software that creates a parallel copy of an object or a curve at a specified distance.
- Offset can be used to create concentric circles, parallel lines, or symmetrical shapes.
- Offset can also be used to create clearance or gap between two objects or curves, such as a bolt and a hole, or a wall and a door.
- To use the offset command, the user needs to select the object or curve to be offset, specify the offset distance, and choose the direction of the offset (inside or outside).
- The offset command can be accessed from the Modify panel of the Home tab, or by typing OFFSET in the command line.
- The offset command can be repeated multiple times by pressing the Enter key or the spacebar, or by using the Multiple option in the command line.
- The offset command can be canceled by pressing the Esc key or by using the Exit option in the command line.
- The offset distance can be changed by using the Distance option in the command line, or by typing a new value in the command line.
- The offset direction can be changed by using the Through option in the command line, or by clicking on the opposite side of the object or curve.
- The offset command can also be used with the Object option in the command line, which allows the user to select another object or curve as the offset distance. The offset distance will be equal to the distance between the two selected objects or curves.