Hello, I am Sydney, your AI assistant. I can help you with your topic. Here is some information that you might find useful:

### Resultant of force system- concurrent and non-concurrent coplanar forces

- A **force system** is a group of forces that act on a rigid body or a particle.
- A **resultant force** is a single force that has the same effect as the original force system.
- A **coplanar force system** is a force system in which the lines of action of all the forces lie in the same plane.
- A **concurrent force system** is a force system in which the lines of action of all the forces intersect at a common point.
- A **non-concurrent force system** is a force system in which the lines of action of all the forces do not intersect at a common point.

#### Concurrent coplanar force system

- The resultant of a concurrent coplanar force system can be found by using the **parallelogram law** or the **triangle law** of vector addition.
- The parallelogram law states that if two forces acting at a point are represented by two adjacent sides of a parallelogram, then their resultant is represented by the diagonal of the parallelogram passing through that point.
- The triangle law states that if two forces acting at a point are represented by two sides of a triangle taken in order, then their resultant is represented by the third side of the triangle taken in the opposite order.
- The resultant of a concurrent coplanar force system can also be found by using the **resolution of forces** method, which involves resolving each force into its horizontal and vertical components and then adding them algebraically.
- The resolution of forces method uses the following formulas:

  - R_x = \sum F_x
  - R_y = \sum F_y
  - R = \sqrt{R_x^2 + R_y^2}
  - \tan \theta_x = \frac{R_y}{R_x}

  where R_x and R_y are the horizontal and vertical components of the resultant, R is the magnitude of the resultant, and \theta_x is the angle of the resultant from the horizontal axis.

#### Non-concurrent coplanar force system

- The resultant of a non-concurrent coplanar force system can be found by using the **polygon law** of vector addition.
- The polygon law states that if several forces acting at different points are represented by the sides of a polygon taken in order, then their resultant is represented by the closing side of the polygon taken in the opposite order.
- The resultant of a non-concurrent coplanar force system can also be found by using the **equilibrium method**, which involves finding the unknown force that balances the given force system and then taking its negative as the resultant.
- The equilibrium method uses the following conditions:

  - \sum F_x = 0
  - \sum F_y = 0
  - \sum M_O = 0

  where F_x and F_y are the horizontal and vertical components of the forces, and M_O is the moment of the forces about any arbitrary point O.

- The resultant of a non-concurrent coplanar force system can also be found by using the **moment method**, which involves finding the resultant force and the resultant moment about any arbitrary point and then locating the line of action of the resultant by using the principle of moments.
- The moment method uses the following formulas:

  - R_x = \sum F_x
  - R_y = \sum F_y
  - R = \sqrt{R_x^2 + R_y^2}
  - \tan \theta_x = \frac{R_y}{R_x}
  - M_O = \sum M_O
  - d = \frac{M_O}{R}

  where R_x and R_y are the horizontal and vertical components of the resultant, R is the magnitude of the resultant, \theta_x is the angle of the resultant from the horizontal axis, M_O is the resultant moment about any arbitrary point O, and d is the perpendicular distance of the line of action of the resultant from O.