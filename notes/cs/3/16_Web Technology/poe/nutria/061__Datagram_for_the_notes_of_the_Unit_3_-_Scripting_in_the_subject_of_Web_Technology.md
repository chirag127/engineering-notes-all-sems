
### Datagram for the Notes of the Unit 3 - Scripting in the Subject of Web Technology

1. A datagram is a type of packet-switched network protocol that does not require a reliable connection between two nodes in order to transmit data. 
2. In a datagram network, each packet is sent independently, without being connected to any other packet. 
3. The main advantage of using datagrams is that they allow for faster and more reliable data transmission, since each packet can be routed independently. 
4. Datagrams are typically used in applications that require low latency, such as streaming video or audio, or for applications that require reliable delivery of data, such as online gaming. 
5. Datagrams are also used in web technologies, such as HTTP and FTP, as well as in network protocols, such as IP and UDP. 
6. Datagrams are also used in scripting languages, such as JavaScript and Python, to send and receive data across the internet. 
7. When sending data using a datagram, the sender must specify the destination address for the data, as well as the size of the data packet. 
8. The sender must also specify the type of data being sent, such as text, audio, or video. 
9. When the data is received, the receiver must then process the data according to the specified type. 
10. Datagrams are also used in distributed computing applications, such as distributed databases and distributed file systems.