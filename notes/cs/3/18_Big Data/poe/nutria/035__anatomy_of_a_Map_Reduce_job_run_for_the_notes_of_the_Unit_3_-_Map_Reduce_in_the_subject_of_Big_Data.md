
### Anatomy of a Map Reduce Job Run

1. Map Reduce is a programming model used to process large datasets in a distributed environment.
2. The Map Reduce model consists of two phases: the map phase and the reduce phase.
3. In the map phase, the input data is split into smaller chunks and each chunk is processed by a separate map task.
4. The output of the map tasks is then sent to the reduce phase, where the data is aggregated and the results are generated.
5. In the reduce phase, the output from the map phase is sorted and combined to produce the final output.
6. The Map Reduce model is used to process large datasets in a distributed environment, and it is particularly useful for data processing tasks such as sorting, aggregating, and joining.
7. The Map Reduce model is based on the concept of divide and conquer, which means that the data is split into smaller chunks and each chunk is processed independently.
8. The Map Reduce model is able to process large datasets in parallel, which makes it an efficient way to process large datasets.
9. The Map Reduce model is also fault tolerant, which means that if one of the tasks fails, the other tasks can still complete the job.
10. The Map Reduce model is a powerful tool for processing large datasets in a distributed environment and is often used in applications such as web search, data mining, and machine learning.