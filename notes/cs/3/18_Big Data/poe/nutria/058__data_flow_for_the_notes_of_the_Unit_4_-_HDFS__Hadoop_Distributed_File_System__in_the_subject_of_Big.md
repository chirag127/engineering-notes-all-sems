
### Data Flow for the Notes of the Unit 4 - HDFS (Hadoop Distributed File System) in the Subject of Big Data

1. HDFS is a distributed file system designed to store large amounts of data reliably and efficiently.
2. HDFS is designed to be fault-tolerant and able to handle large amounts of data.
3. HDFS provides high throughput access to application data, making it suitable for applications that require large data sets.
4. HDFS is designed to be deployed on commodity hardware, meaning that it is not necessary to purchase expensive hardware to store and process data.
5. HDFS is a distributed file system that runs on a cluster of computers.
6. HDFS is designed to be used with large-scale applications and is optimized for throughput rather than latency.
7. HDFS stores data in blocks that are distributed across multiple nodes in the cluster.
8. HDFS replicates data blocks across multiple nodes to ensure that data is not lost in the event of a node failure.
9. HDFS is designed to be highly scalable, meaning that the cluster can be expanded to accommodate more data.
10. HDFS is used by a wide range of applications, including Apache Hadoop, Apache Spark, and Apache Flink.