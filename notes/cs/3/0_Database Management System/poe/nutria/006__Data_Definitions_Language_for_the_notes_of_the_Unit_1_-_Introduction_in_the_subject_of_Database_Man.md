
### Data Definitions Language

* Data Definitions Language (DDL) is a language used to define the structure of a database. It is used to create, modify, and delete database objects such as tables, views, and stored procedures. 
* DDL commands are used to create a database, define the structure of a database, and modify the structure of a database.
* DDL is used to create and modify the structure of a database, such as creating tables, views, and stored procedures.
* DDL commands are used to create, modify, and delete database objects such as tables, views, and stored procedures.
* DDL statements are used to create, modify, and delete database objects.
* DDL statements can be used to create tables, views, and stored procedures.
* DDL statements are used to define the structure of a database, such as creating tables, views, and stored procedures.
* DDL statements can also be used to modify the structure of a database, such as altering tables and views.