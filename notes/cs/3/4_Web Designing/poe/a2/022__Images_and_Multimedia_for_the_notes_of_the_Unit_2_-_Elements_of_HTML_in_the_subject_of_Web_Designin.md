 Here is the content in Markdown format without any emojis or external links:

### Images and Multimedia for the notes of the Unit 2 - Elements of HTML

1. Images
- Images are inserted in HTML using `<img>` tag
- `<img>` tag has attributes like `src` to specify image source URL, `alt` to specify alternate text and `height`, `width` to specify dimensions
- Images should be in JPEG, PNG or GIF format
- Use `alt` attribute to specify alternate text for images as it provides accessibility and is displayed if image is not loaded

2. Multimedia
- Multimedia like videos, audio, flash animations etc. can be added to HTML using `<embed>`, `<object>` or `<iframe>` tags
- `<embed>` tag indicates embedded content, `<object>` can include alternate content if multimedia is not supported and `<iframe>` is used to embed another web page within a web page
- Appropriate attributes are used with these tags to specify the source and other details of multimedia

The content covers key points about adding images and multimedia to HTML. The explanation is formal and no feelings or friendliness is expressed. Markdown format is used and no emojis or external links are included. Please let me know if you would like me to modify or add anything.