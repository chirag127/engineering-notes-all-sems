### Three Address Code

Three address code is an intermediate code used in the syntax-directed translation of programming languages. It is a type of code that is commonly used in the implementation of compilers.

Here are some key points to remember about three address code:

1. Three address code is a type of intermediate code that is used in the syntax-directed translation of programming languages.
2. It is called "three address code" because each instruction typically has three operands: two source operands and one destination operand.
3. The operands can be constants, variables, or temporary variables.
4. Three address code is commonly used in the implementation of compilers because it is easy to generate and can be easily translated into machine code.
5. Three address code can be represented in various forms, including quadruples, triples, and indirect triples.
6. The use of three address code can simplify the process of code optimization and can make it easier to implement certain compiler optimizations.
