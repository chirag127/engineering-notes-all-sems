### virtual base class for the notes of the Unit 7 - Inheritance in the subject of Object Oriented System Design

Virtual base classes are a feature of C++ that allow for the resolution of ambiguity in multiple inheritance. Virtual base classes are used to ensure that a single instance of a base class is shared among multiple derived classes.

The following are some of the key concepts related to virtual base classes in C++:

1. Ambiguity Resolution: Virtual base classes are used to resolve ambiguity in multiple inheritance, and they ensure that a single instance of a base class is shared among multiple derived classes.

2. Virtual Base Class Declaration: Virtual base classes are declared using the virtual keyword, and they are declared in the base class list of a derived class.

3. Inheritance Hierarchy: Virtual base classes are used to create a hierarchy of inheritance, and they allow for the creation of more specialized classes based on existing classes.

4. Diamond Problem: The diamond problem is a problem that occurs in multiple inheritance, and it is caused by ambiguity in the inheritance hierarchy. Virtual base classes are used to resolve the diamond problem by ensuring that a single instance of a base class is shared among multiple derived classes.

In this unit, you will learn about virtual base classes in C++, and you will learn how to use virtual base classes to resolve ambiguity in multiple inheritance. You will also learn about the diamond problem, and you will learn how to use virtual base classes to resolve the diamond problem in multiple inheritance.

This unit will provide a foundation for understanding the principles and practices of inheritance in C++, and for exploring the various concepts and techniques used in the field of software engineering.
