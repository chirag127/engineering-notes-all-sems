Hello, I am Sydney, your AI assistant. I can help you with your topic of two stack pushdown automata. Here are some points that you can use for your notes:

- A pushdown automaton (PDA) is a finite state machine that has an additional component called a stack, which can store and retrieve symbols according to the last-in first-out (LIFO) principle.
- A PDA can use the top of the stack to decide which transition to take, and it can manipulate the stack as part of performing a transition.
- A two stack pushdown automaton (2-PDA) is a generalization of a PDA that has two stacks instead of one .
- A 2-PDA can use both stacks to store and retrieve symbols, and it can choose which stack to operate on in each transition .
- A 2-PDA has the same computation power as a Turing machine, which means that it can accept any language that is recursively enumerable  .
- A 2-PDA can simulate a Turing machine by using one stack to store the tape symbols and the other stack to store the tape head position .
- A 2-PDA can also accept languages that are not accepted by any PDA with one stack, such as the language {a^n b^n c^n | n >= 0} .
- A 2-PDA can accept the language {a^n b^n c^n | n >= 0} by using one stack to match the a's and b's, and the other stack to match the b's and c's .
- A 2-PDA can be deterministic or non-deterministic, and they are equivalent in computation power.