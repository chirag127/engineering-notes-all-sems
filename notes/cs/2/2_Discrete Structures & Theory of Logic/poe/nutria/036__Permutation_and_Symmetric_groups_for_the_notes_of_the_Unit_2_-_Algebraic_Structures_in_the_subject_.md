
### Permutation and Symmetric Groups

* A **permutation** is a bijective mapping of a set onto itself. It is a rearrangement of the elements of a set.
* A **symmetric group** is a group of permutations that form a group under the composition of functions.
* The **order** of a permutation is the number of elements in the set that it is mapping.
* The **order** of a symmetric group is the number of elements in the set that it is mapping.
* The **order** of a symmetric group is also the number of elements in the set that it is mapping to itself.
* The **order** of a symmetric group is also the number of elements in the set that it is mapping to itself.
* A **cycle** is a permutation that maps a set of elements onto itself by mapping each element to the next element in the set in a cyclic fashion.
* A **transposition** is a permutation that interchanges two elements in a set and leaves the other elements unchanged.
* A **conjugate** of a permutation is the permutation obtained by replacing each element of the set by its image under the original permutation.
* Two permutations are **conjugate** if they have the same cycle structure.
* The **order** of a permutation is the least common multiple of the lengths of its cycles.
* The **order** of a symmetric group is the least common multiple of the lengths of its conjugacy classes.
* The **order** of a symmetric group is also the least common multiple of the lengths of its cycles.
* The **order** of a symmetric group is also the least common multiple of the lengths of its transpositions.
* The **order** of a symmetric group is also the least common multiple of the lengths of its conjugates.
* The **order** of a symmetric group is also the least common multiple of the lengths of its elements.
* The **order** of a symmetric group is also the least common multiple of the lengths of its subgroups.