## Unit 3 - Function: Parts of A Function , Execution of A Function , Keyword and Default Arguments ,Scope Rules.

Functions are an essential part of programming, and they allow us to write reusable code. In this unit, we will cover the following topics related to functions:

### Parts of A Function
- A function is composed of a header and a body.
- The header contains the function name, parameters in parentheses, and a colon.
- The function body contains the code that the function executes.
- The function can also have a return statement that returns a value.

### Execution of A Function
- To execute a function, we call the function by its name and pass arguments (if any) in parentheses.
- The code inside the function body is executed, and the result is returned (if a return statement is present).
- The function can be called multiple times with different arguments.

### Keyword and Default Arguments
- Keyword arguments allow us to pass arguments to a function using their names.
- Default arguments are used when a function is called without passing a value for that argument.
- Default arguments are specified in the function header, and if a value is not passed, the default value is used.

### Scope Rules
- The scope of a variable refers to the region of the program where the variable is accessible.
- In Python, there are two types of scopes: global and local.
- A global variable is accessible throughout the program, while a local variable is accessible only within the function in which it is defined.
- A local variable can have the same name as a global variable, but the local variable takes precedence within the function.

In conclusion, understanding the parts of a function, execution of a function, keyword and default arguments, and scope rules is crucial for writing efficient and reusable code. Practice using these concepts in your code to become a better programmer.