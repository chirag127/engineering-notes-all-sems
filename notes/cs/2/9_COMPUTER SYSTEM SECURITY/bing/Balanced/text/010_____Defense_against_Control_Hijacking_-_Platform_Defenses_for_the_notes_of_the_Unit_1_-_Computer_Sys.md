### Defense against Control Hijacking - Platform Defenses

Control hijacking is a type of attack that exploits a vulnerability in a program to alter its execution flow and execute malicious code. Control hijacking can be performed by overwriting code pointers, such as return addresses, function pointers, or exception handlers, with the address of the attacker's payload. The payload can be injected into the program's memory or stored in an existing code segment, such as the stack, the heap, or a library.

To defend against control hijacking, several platform defenses have been proposed, such as:

- **Fixing bugs**: The most effective way to prevent control hijacking is to eliminate the root cause of the vulnerability, such as buffer overflows, format string errors, or integer overflows. This can be done by auditing the software, using automated tools, or rewriting the software in a type-safe language.
- **Stack protection**: A common technique to protect the stack from being overwritten by a buffer overflow is to use a canary value, such as a random number or a checksum, that is placed between the buffer and the return address. The canary is checked before returning from a function, and if it is modified, the program aborts. Examples of stack protection mechanisms are StackGuard, ProPolice, and StackShield.
- **Heap protection**: A similar technique to protect the heap from being overwritten by a buffer overflow is to use metadata, such as size and flags, that is placed before and after each heap chunk. The metadata is checked before allocating or freeing a chunk, and if it is corrupted, the program aborts. Examples of heap protection mechanisms are DieHard, GuardMalloc, and Electric Fence.
- **Non-executable memory**: Another technique to prevent the execution of injected code is to mark the memory regions that contain data, such as the stack and the heap, as non-executable. This can be done by using hardware support, such as the NX bit or the XN bit, or by using software emulation, such as PaX or W^X. If an attempt is made to execute code from a non-executable region, a segmentation fault is triggered.
- **Address space layout randomization (ASLR)**: A technique to make the address of the attacker's payload unpredictable is to randomize the layout of the address space, such as the base address of the stack, the heap, the libraries, and the executable. This can be done by using operating system support, such as Linux ASLR or Windows ASLR, or by using compiler support, such as Position-Independent Code (PIC) or Position-Independent Executable (PIE). ASLR makes it harder for the attacker to guess the correct address of the payload or a code segment.
- **Control flow integrity (CFI)**: A technique to enforce the intended control flow of the program is to use a control flow graph (CFG) that represents all the valid transitions between code blocks. The CFG can be constructed statically, dynamically, or hybridly, and can be enforced at compile time, run time, or both. CFI inserts checks before each indirect control transfer, such as a function call or a return, and verifies that the target address is consistent with the CFG. If the target address is invalid, the program aborts.