# Uninformed Search Methods

Uninformed search methods are a type of search algorithms that do not use any additional information to guide the search process. They only use the problem definition, which consists of the initial state, the goal state, and the actions that can be performed to move from one state to another. Uninformed search methods explore the search space in a systematic, but blind, manner without considering the cost of reaching the goal or the likelihood of finding a solution .

Some of the common uninformed search methods are:

- **Depth-first search (DFS)**: This method starts from the initial state and explores as deep as possible in a single path before backtracking to the previous state and trying another path. DFS uses a stack data structure to store the nodes that are not yet expanded. DFS is complete (guarantees to find a solution if one exists) and optimal (guarantees to find the least-cost solution) only for tree search and when the branching factor is finite .
- **Breadth-first search (BFS)**: This method starts from the initial state and explores all the neighboring states at the same level before moving to the next level. BFS uses a queue data structure to store the nodes that are not yet expanded. BFS is complete and optimal for both tree search and graph search, but it requires a lot of memory to store the nodes .
- **Uniform-cost search (UCS)**: This method is similar to BFS, but it uses a priority queue data structure to store the nodes that are not yet expanded. The priority queue is ordered by the path cost from the initial state to the current state, so the node with the lowest cost is expanded first. UCS is complete and optimal for both tree search and graph search, but it may expand more nodes than BFS if the path cost is not proportional to the depth .
- **Depth-limited search (DLS)**: This method is a variation of DFS that imposes a limit on the depth of the search. If the limit is reached, the node is treated as a failure and the search backtracks. DLS is useful to avoid infinite loops in cyclic graphs, but it may miss a solution if the limit is too low. DLS is neither complete nor optimal, unless the limit is equal to the depth of the shallowest goal state .
- **Iterative deepening depth-first search (IDDFS)**: This method is a combination of DFS and DLS that gradually increases the depth limit until a solution is found or the entire search space is explored. IDDFS combines the advantages of DFS (low memory requirement) and BFS (completeness and optimality) for both tree search and graph search .
- **Bidirectional search (BDS)**: This method is a variation of BFS that runs two simultaneous searches: one from the initial state and one from the goal state. The search stops when the two searches meet in the middle. BDS is useful to reduce the search space and the memory requirement, but it requires that the goal state is known and that the actions are reversible. BDS is complete and optimal for both tree search and graph search, if the branching factor is the same in both directions .