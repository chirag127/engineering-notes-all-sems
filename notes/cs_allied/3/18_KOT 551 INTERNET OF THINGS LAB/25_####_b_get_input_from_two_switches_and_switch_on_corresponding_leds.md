#### b) Get input from two switches and switch on corresponding LEDs

Sure, I'd be happy to help!

Getting input from two switches and switching on corresponding LEDs is a common task in embedded systems and involves using digital inputs and outputs to control the state of the system. The process typically involves the following steps:

1. Connect the switches to the microcontroller: The switches are connected to the microcontroller using digital inputs. The microcontroller reads the state of the switches and determines whether they are in the "on" or "off" state.

2. Read the state of the switches: The microcontroller reads the state of the switches by reading the values of the digital inputs. The values of the digital inputs are either "high" or "low", depending on whether the switch is in the "on" or "off" state.

3. Switch on the corresponding LEDs: Based on the state of the switches, the microcontroller switches on the corresponding LEDs by setting the values of the digital outputs. If the first switch is "on", the first LED is switched on, and if the second switch is "on", the second LED is switched on.

4. Repeat the process: The process of reading the state of the switches and switching on the corresponding LEDs is repeated continuously, allowing the state of the system to be updated in real-time based on the state of the switches.

In conclusion, getting input from two switches and switching on corresponding LEDs is a common task in embedded systems that involves using digital inputs and outputs to control the state of the system. The process involves connecting the switches to the microcontroller, reading the state of the switches, switching on the corresponding LEDs, and repeating the process continuously. By following these steps, it is possible to create a simple and effective system for controlling the state of the LEDs based on the state of the switches.
