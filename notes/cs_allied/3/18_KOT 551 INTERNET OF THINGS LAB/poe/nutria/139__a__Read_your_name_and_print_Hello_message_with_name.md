
#### a) Read your name and print Hello message with name

1. To read a name, you must first create a program that will prompt the user to enter their name. This can be done by using the `input()` function in Python, for example.

2. Once the user has entered their name, the program must store the name in a variable. This can be done by assigning the user's input to a variable, such as `name = input("What is your name?")`.

3. Finally, the program must print a greeting to the user using their name. This can be done by using the `print()` function in Python, for example. The program could print a greeting such as `"Hello, NAME"`, where `NAME` is the variable that holds the user's name.