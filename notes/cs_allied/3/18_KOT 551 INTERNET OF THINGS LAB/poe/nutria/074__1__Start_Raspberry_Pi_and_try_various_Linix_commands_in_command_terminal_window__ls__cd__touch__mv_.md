
#### 1. Start Raspberry Pi and try various Linux commands in command terminal window:

* `ls`: List the contents of the current directory
* `cd`: Change the current directory
* `touch`: Create a new file in the current directory
* `mv`: Move a file from one directory to another
* `rm`: Remove a file or directory
* `man`: Display information about a Linux command