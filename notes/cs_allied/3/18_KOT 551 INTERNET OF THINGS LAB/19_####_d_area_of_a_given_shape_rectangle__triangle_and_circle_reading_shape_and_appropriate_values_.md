#### d) Area of a given shape (rectangle, triangle and circle) reading shape and appropriate values from standard

Sure! The area of a given shape is a fundamental concept in geometry and can be calculated by using different formulas, depending on the shape. In this case, you are asking about the area of a rectangle, triangle, and circle.

For a rectangle, the area is calculated by multiplying the length by the width. The formula for the area of a rectangle is:

```
A = l * w
```

where `A` is the area, `l` is the length, and `w` is the width. To calculate the area of a rectangle, you need to read the length and width values from the standard input and then apply the formula.

For a triangle, the area is calculated by multiplying the base by the height and dividing by 2. The formula for the area of a triangle is:

```
A = (b * h) / 2
```

where `A` is the area, `b` is the base, and `h` is the height. To calculate the area of a triangle, you need to read the base and height values from the standard input and then apply the formula.

For a circle, the area is calculated by multiplying pi by the square of the radius. The formula for the area of a circle is:

```
A = pi * r^2
```

where `A` is the area, `pi` is the mathematical constant pi, and `r` is the radius. To calculate the area of a circle, you need to read the radius value from the standard input and then apply the formula.

In conclusion, the area of a given shape can be calculated by using different formulas, depending on the shape. For a rectangle, the area is calculated by multiplying the length by the width. For a triangle, the area is calculated by multiplying the base by the height and dividing by 2. For a circle, the area is calculated by multiplying pi by the square of the radius. To calculate the area of a shape, you need to read the appropriate values from the standard input and then apply the appropriate formula.
