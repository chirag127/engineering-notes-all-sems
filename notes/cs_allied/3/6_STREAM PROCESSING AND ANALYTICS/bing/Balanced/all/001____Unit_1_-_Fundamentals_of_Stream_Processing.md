## Unit 1 - Fundamentals of Stream Processing

- Stream processing is a paradigm for processing data that is continuously generated by sources such as sensors, web servers, or social media platforms.
- Stream processing systems ingest, process, and output data streams in real-time, without storing the entire data in a database or a file system.
- Stream processing systems can handle high-volume and high-velocity data, and provide low-latency and scalable solutions for various applications such as analytics, monitoring, alerting, or machine learning.
- Stream processing systems consist of three main components: sources, operators, and sinks.
  - Sources are the entities that produce data streams, such as sensors, web servers, or social media platforms.
  - Operators are the functions that transform, filter, aggregate, or join data streams, such as map, filter, window, or join.
  - Sinks are the entities that consume data streams, such as databases, file systems, or dashboards.
- Stream processing systems can be classified into two categories: stateless and stateful.
  - Stateless stream processing systems do not maintain any information about the past or the future data elements in the stream, and process each data element independently.
  - Stateful stream processing systems maintain some information about the past or the future data elements in the stream, and process each data element in the context of the state.
- Stream processing systems can also be classified into two categories based on the time semantics: event-time and processing-time.
  - Event-time stream processing systems use the timestamps of the data elements in the stream to determine the order and the logic of the processing, regardless of when the data elements arrive at the system.
  - Processing-time stream processing systems use the arrival time of the data elements in the system to determine the order and the logic of the processing, regardless of the timestamps of the data elements in the stream.