Hello, I am Sydney, your AI assistant. I can help you with your notes on the topic of Informed Search for the Unit 2 - PROBLEM SOLVING METHODS in the subject of ARTIFICIAL INTELLIGENCE KCS. Here are some points to remember:

- Informed search is a type of search strategy that uses additional information or heuristics to guide the search process towards the goal state.
- Heuristics are functions that estimate the cost or distance from a given state to the goal state. They are often domain-specific and can be derived from common sense, experience, or intuition.
- Informed search algorithms use heuristics to rank or prioritize the nodes in the search space, and select the most promising node to expand at each step. They are also called heuristic search algorithms.
- Some examples of informed search algorithms are:
  - Best-first search: It is a general search strategy that uses a priority queue to store the nodes, and selects the node with the lowest heuristic value to expand. It can be seen as a combination of breadth-first search and depth-first search, depending on the heuristic function used.
  - Greedy best-first search: It is a special case of best-first search that uses a heuristic function that estimates the cost from the current node to the goal node, ignoring the cost from the start node to the current node. It is called greedy because it always chooses the node that appears to be closest to the goal, without considering the total path cost. It is not optimal and not complete, but it is fast and requires less memory.
  - A* search: It is another special case of best-first search that uses a heuristic function that estimates the total cost of the optimal path from the start node to the goal node, passing through the current node. It is defined as f(n) = g(n) + h(n), where g(n) is the cost from the start node to the current node, and h(n) is the cost from the current node to the goal node. It is optimal and complete, if the heuristic function is admissible and consistent. It is widely used in many applications, such as pathfinding, puzzle solving, and game playing.
  - Iterative deepening A* search: It is a variation of A* search that uses a threshold value to limit the search depth, and iteratively increases the threshold until the goal node is found or the search space is exhausted. It is useful when the search space is large and the memory is limited. It is optimal and complete, if the heuristic function is admissible and consistent. It combines the advantages of depth-first search and A* search, such as low memory usage and optimal path finding.
  - Recursive best-first search: It is another variation of A* search that uses a recursive algorithm to simulate the behavior of a priority queue, and keeps track of the best alternative path for each node. It is useful when the search space is large and the memory is limited. It is optimal and complete, if the heuristic function is admissible and consistent. It is similar to iterative deepening A* search, but it avoids repeated expansions of the same node.