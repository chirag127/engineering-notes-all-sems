
#### Functions and Closures in Scala

* Functions in Scala are objects that can be passed as parameters to other functions. They can also be assigned to variables and returned from other functions. 
* Closures are functions that can access variables outside their scope.
* Scala supports higher-order functions, which means functions can be passed as parameters to other functions.
* Scala has a powerful type system that allows the programmer to create abstract types and generic functions.
* Scala has pattern matching which allows the programmer to match on the structure of data.
* Scala has support for anonymous functions, also known as lambda functions, which can be used to create concise, expressive code.
* Scala has a powerful functional programming model that allows the programmer to create functions that are referentially transparent.
* Scala has a powerful type inference system that allows the programmer to write code without having to explicitly declare types.
* Scala has a powerful type system that allows the programmer to create abstract types and generic functions.

Mnemonic: 
* F = Functions (objects)
* C = Closures (access variables outside scope)
* H = Higher-order functions (passed as parameters)
* T = Type system (abstract types and generic functions)
* P = Pattern matching (match on structure of data)
* A = Anonymous functions (lambda functions)
* F = Functional programming (referentially transparent)
* I = Type inference (no explicit type declaration)
* T = Type system (abstract types and generic functions)