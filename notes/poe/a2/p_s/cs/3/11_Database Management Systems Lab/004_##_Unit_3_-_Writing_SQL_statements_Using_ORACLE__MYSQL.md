 Here is the content in markdown format for the given topic:

## Unit 3 - Writing SQL statements Using ORACLE /MYSQL

### 1. SELECT statement
- The SELECT statement is used to fetch data from a database table which returns this data in the form of a result table.
- The basic syntax is:
SELECT column1, column2, ...
FROM table_name;
- You can select all columns using * or select specific columns.
- Column names can be renamed using AS keyword.
- DISTINCT keyword can be used to return only distinct values.

### 2. WHERE clause
- The WHERE clause is used to filter records.
- It is used to extract only those records that fulfill a specified condition.
- The basic syntax is:
SELECT column1, column2, ...
FROM table_name
WHERE condition;
- Conditions can use operators like >, <, =, !=, >=, <=, AND, OR, NOT, etc.
- Conditions can be added for multiple columns to get more specific results.

[Detailed explanations, examples and diagrams can be added here for SELECT and WHERE clause.]

...

[Other topics like data manipulation using INSERT, UPDATE, DELETE statements, aggregate functions, sorting, joining tables, subqueries, etc. can be explained in detail with examples for Oracle and MySQL databases.]

The content is written in a formal tone with points and can include additional details and examples as required to serve as a study material for exams related to writing SQL statements using Oracle and MySQL databases.