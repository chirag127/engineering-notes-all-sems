 Here is the content in markdown format:

### Access the above developed static web pages for books web site, using these servers by putting the web pages developed for the notes of the Unit 5 - Design server site applications using JDDC,ODBC and section tracking API in the subject of Web Technology Lab

- The static web pages developed for the book store web site in the previous units are to be accessed using servers.
- The web pages are to be placed in the servers by uploading them.
- The servers used can be either local servers or remote servers.
- Some of the commonly used servers are Apache Tomcat server, IBM WebSphere, etc.
- The web pages can be accessed using the URL of the server by anyone across the network.
- The notes web pages developed for the fifth unit of Web Technology Lab dealing with designing server site applications are to be placed in the servers.
- The fifth unit covers connecting to databases from servers using JDBC, connecting to databases from servers using ODBC and tracking sections in databases from servers using section tracking API.
- The notes cover the concepts, examples and codes of using JDBC, ODBC and section tracking API to connect servers to databases and track sections.
- Placing the web pages in servers and accessing them from anywhere across the network is useful to learn and revise the concepts, examples, codes, etc. from the notes of the fifth unit.
- The notes in web pages can be enriched with diagrams, tables, etc. to make effective study materials.
- Storing the web pages in servers and accessing them using URLs is a convenient way to use the notes as self-study materials.