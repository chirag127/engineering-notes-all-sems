
### Decision Surface for Unit 2 - Regression in Machine Learning Techniques

Regression is a type of supervised machine learning technique used to predict continuous values. It is used to estimate the relationship between a dependent variable and one or more independent variables. In regression, a decision surface is the line or surface that separates the data points into two or more classes. 

The decision surface is determined by a set of parameters, which are optimized to minimize the error between the predicted and actual values. The parameters can be used to determine the shape of the decision surface, which can be linear, polynomial, or non-linear.

Linear regression is the simplest type of regression, and is used when the relationship between the dependent and independent variables is linear. In linear regression, the decision surface is a straight line. 

Polynomial regression is used when the relationship between the dependent and independent variables is non-linear. In polynomial regression, the decision surface is a polynomial equation. 

Non-linear regression is used when the relationship between the dependent and independent variables is non-linear. In non-linear regression, the decision surface is a non-linear equation. 

Advantages of using regression include its ability to predict continuous values, as well as its ability to identify relationships between variables. Disadvantages of using regression include its inability to accurately predict values outside of the training data, as well as its inability to identify relationships between non-linear variables.

Regression is used in a variety of applications, such as predicting stock prices, predicting housing prices, and predicting the weather. 

The decision surface for regression is an important concept for understanding how regression works. It is the line or surface that separates the data points into two or more classes. The decision surface is determined by a set of parameters, which are optimized to minimize the error between the predicted and actual values.