
### Write HTML/Java scripts to display your CV in navigator, your Institute website, Department Website and Tutorial website for specific subject for the notes of the Unit 1 - Develop static web pages using HTML in the subject of Web Technology Lab

Writing HTML and Java scripts to display your CV in a web browser is a fundamental skill for any web developer. HTML (Hypertext Markup Language) is the language used to create webpages and Java scripts are used to create dynamic webpages.

To display your CV in a web browser, you will need to use HTML to create the structure of the page and Java scripts to add interactivity.

First, you will need to create an HTML document to contain your CV. This document should include the basic HTML tags such as `<html>`, `<head>`, `<body>` and `<title>`. You can also add additional tags such as `<h1>`, `<h2>`, `<p>` and `<ul>` to create a more structured document.

Once you have created the HTML document, you will need to add the content of your CV. This can be done by using HTML tags such as `<h1>`, `<h2>`, `<p>` and `<ul>` to structure the content. You can also use `<a>` tags to link to external websites.

Finally, you will need to add Java scripts to the HTML document to add interactivity. This can be done by using the `<script>` tag to include the Java scripts. This will allow you to add features such as dropdown menus, accordions and forms.

Once you have created the HTML document and added the content and Java scripts, you can then upload it to your Institute website, Department website or Tutorial website for the notes of the Unit 1 - Develop static web pages using HTML in the subject of Web Technology Lab. This will allow you to display your CV in a web browser.