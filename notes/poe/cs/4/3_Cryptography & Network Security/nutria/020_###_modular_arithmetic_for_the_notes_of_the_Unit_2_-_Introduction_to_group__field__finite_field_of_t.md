
### Modular Arithmetic for the Notes of the Unit 2 - Introduction to Group, Field, Finite Field of the Form GF(p), Modular Arithmetic, Prime and Relative Prime Numbers, Extended Euclidean Algorithm, Advanced Encryption Standard (AES) Encryption and DecryptionFermat’s and Euler’s Theorem, Primarily Testing, Chinese Remainder Theorem, Discrete Logarithmic Problem, Principals of Public Key Crypto Systems, RSA Algorithm, Security of RSA in the Subject of Cryptography & Network Security

Modular arithmetic is a branch of mathematics that deals with the arithmetic of numbers modulo a given number. It is used in cryptography, computer science, and other areas of mathematics.

Modular arithmetic is based on the concept of congruence. Two numbers are said to be congruent modulo a given number if they have the same remainder when divided by that number. For example, 6 and 19 are congruent modulo 7 because both have a remainder of 5 when divided by 7.

The most commonly used operations in modular arithmetic are addition, subtraction, multiplication, and division. All of these operations can be performed using the same basic rules as for normal arithmetic, but with a few modifications.

For addition and subtraction, the usual rules apply, but the result is taken modulo the given number. For example, if we are working modulo 7, then 8 + 5 = 13, but 13 modulo 7 = 6.

For multiplication, the usual rules apply, but the result is taken modulo the given number. For example, if we are working modulo 7, then 8 x 5 = 40, but 40 modulo 7 = 6.

For division, the usual rules apply, but the result is taken modulo the given number. For example, if we are working modulo 7, then 8 divided by 5 = 1.6, but 1.6 modulo 7 = 6.

Modular arithmetic is used in cryptography to create secure encryption algorithms. The most common example is the Advanced Encryption Standard (AES), which uses modular arithmetic to encrypt and decrypt data.

Modular arithmetic is also used in the study of group theory, which is the study of mathematical structures known as groups. In group theory, the concept of a finite field is used to describe the set of possible values of a group. Finite fields are used to study modular arithmetic and the properties of prime and relative prime numbers.

Euler's theorem and Fermat's theorem are two important theorems in modular arithmetic. Euler's theorem states that if two numbers are relatively prime (i.e. have no common factors), then their product is congruent to 1 modulo the given number. Fermat's theorem states that if a number is a prime number, then it is congruent to 1 or -1 modulo the given number.

The Chinese Remainder Theorem is another important theorem in modular arithmetic. It states that if two numbers are relatively prime, then their product is congruent to the product of their remainders modulo the given number.

The Discrete Logarithmic Problem is a problem in cryptography that is related to modular arithmetic. It involves finding a number that is the logarithm of another number modulo a given number.

Finally, the principals of public key crypto systems, the RSA algorithm, and the security of RSA are all related to modular arithmetic. Public key crypto systems use modular arithmetic to encrypt and decrypt data, while the RSA algorithm uses modular arithmetic to generate and verify digital signatures. The security of RSA is based on the difficulty of solving the Discrete Logarithmic Problem.