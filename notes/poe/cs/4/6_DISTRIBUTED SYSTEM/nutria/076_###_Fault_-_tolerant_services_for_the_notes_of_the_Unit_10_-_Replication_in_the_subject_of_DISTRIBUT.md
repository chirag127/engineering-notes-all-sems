
### Fault-tolerant Services for the Notes of the Unit 10 - Replication in the Subject of Distributed Systems

* Fault-tolerant services are systems that are designed to continue to operate normally even when one or more components fail. 
* Fault-tolerant services are essential for distributed systems, as they enable the system to continue to operate in the face of component failures.
* Fault tolerance can be achieved through replication, which is the duplication of data or services across multiple nodes.
* Replication is used to ensure that data is available even if one or more nodes fail.
* Replication can also be used to improve performance, as requests can be directed to the node that is closest to the user.
* There are several types of replication, including primary-backup replication, active-active replication, and quorum replication.
* Primary-backup replication is the most common type of replication, and it involves one node acting as the primary node and the other nodes acting as backups.
* Active-active replication involves multiple nodes acting as both primary and backup nodes.
* Quorum replication is a type of replication that requires a majority of nodes to agree before a request can be processed.
* Fault-tolerant services are also used to ensure that data is consistent across the system.
* Consistency is achieved by using techniques such as two-phase commit and three-phase commit protocols.
* These protocols ensure that all nodes in the system have the same version of the data.
* Fault-tolerant services are essential for distributed systems, as they enable the system to continue to operate even in the face of component failures.