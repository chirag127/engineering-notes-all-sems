

### Oracles for the Notes of the Unit 1 - Review of Software Engineering in the Subject of Software Testing

* An **oracle** is an entity that provides a definitive answer to a question. In software engineering, oracles are used to determine the correctness of a program's output.
* **Black-box testing** is a type of software testing that does not require knowledge of the internal workings of the program. Instead, the program is tested by inputting data and examining the output. In black-box testing, oracles are used to compare the program's output to the expected output.
* **White-box testing** is a type of software testing that requires knowledge of the internal workings of the program. It is used to test the program's logic and control flow. In white-box testing, oracles are used to compare the program's output to the expected output.
* **Fault injection** is a type of software testing that involves deliberately introducing errors into a program in order to test its resilience. In fault injection, oracles are used to compare the program's output to the expected output.
* **Mutation testing** is a type of software testing that involves deliberately introducing errors into a program in order to test its resilience. In mutation testing, oracles are used to compare the program's output to the expected output.
* **Regression testing** is a type of software testing that involves testing a program for errors after changes have been made. In regression testing, oracles are used to compare the program's output to the expected output.
* **Model-based testing** is a type of software testing that involves creating a model of the program's behavior and then testing the program against the model. In model-based testing, oracles are used to compare the program's output to the expected output.