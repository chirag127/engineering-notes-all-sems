
### Audio Wave Net for the Notes of the Unit 5 - CASE STUDY AND APPLICATIONS in the Subject of Deep Learning

- Audio Wave Net is a deep learning model that was developed by Google to generate realistic audio from text. It uses a convolutional neural network to generate the audio.

- The model was first introduced in 2016 and has since been used in various applications such as text-to-speech, music generation, and speech recognition.

- Audio Wave Net works by taking in text as input and then generating a corresponding audio file as output. It uses a sequence of layers to process the text and generate the audio.

- The model is composed of several layers, including an embedding layer, a convolutional layer, a recurrent layer, and an output layer. The embedding layer takes in the text and converts it into a numerical representation. The convolutional layer then processes the numerical representation and generates an audio signal. The recurrent layer is used to model the temporal dynamics of the audio signal. Finally, the output layer generates the audio file.

- Audio Wave Net has been used in various applications such as text-to-speech, music generation, and speech recognition. It has been found to be more accurate and efficient than traditional methods.

- One of the main advantages of Audio Wave Net is its ability to generate realistic audio from text. It is also able to generate audio from text in multiple languages.

- Some of the disadvantages of Audio Wave Net include its high computational cost and the difficulty of training the model.

- Mnemonics and learning tricks for Audio Wave Net are not easily available, however, the model is relatively simple and can be understood with some basic knowledge of deep learning.