#### Protocols and standards in networks architecture in Computer Networks

- Network protocols are the rules and procedures that define how devices communicate and exchange data over a network. They specify the format, timing, sequencing, and error control of the data packets that are transmitted and received by the devices.
- Network standards are the agreed-upon conventions and guidelines that ensure interoperability and compatibility among different devices, vendors, and technologies in a network. They are developed and maintained by various organizations and committees, such as IEEE, IETF, ISO, etc.
- Network architecture is the design and structure of a network, including its hardware, software, transmission media, topology, and protocols. It determines how the network components interact and cooperate to achieve the network's goals and functions.
- Some of the common network protocols and standards are:

  - TCP/IP: Transmission Control Protocol/Internet Protocol is the most widely used protocol suite in the Internet and other networks. It consists of four layers: application, transport, internet, and network access. It provides reliable, connection-oriented, and end-to-end data delivery, as well as addressing, routing, and fragmentation functions.
  - UDP: User Datagram Protocol is a connectionless and unreliable transport protocol that offers low-overhead and fast data transmission. It is suitable for real-time applications, such as voice and video streaming, that can tolerate some data loss and delay.
  - HTTP: Hypertext Transfer Protocol is an application layer protocol that defines how web browsers and web servers communicate and exchange web resources, such as HTML documents, images, and scripts. It uses a request-response model, where the browser sends an HTTP request to the server, and the server responds with an HTTP response containing the requested resource or an error message.
  - FTP: File Transfer Protocol is an application layer protocol that enables the transfer of files between a client and a server over a TCP connection. It supports both binary and ASCII modes, as well as authentication, authorization, and directory listing functions.
  - DNS: Domain Name System is a distributed and hierarchical system that maps domain names, such as www.google.com, to their corresponding IP addresses, such as 172.217.14.206. It uses a network of name servers that store and resolve the domain name queries from the clients.
  - DHCP: Dynamic Host Configuration Protocol is an application layer protocol that assigns IP addresses and other network configuration parameters to the devices in a network dynamically. It uses a four-step process: discover, offer, request, and acknowledge, to allocate and renew the IP leases for the devices.
  - ARP: Address Resolution Protocol is a network access layer protocol that resolves the IP addresses to their corresponding MAC addresses, which are the physical addresses of the network interface cards. It uses a two-step process: request and reply, to broadcast and cache the ARP entries for the devices in a local network.
  - IEEE 802: This is a family of standards that define the physical and data link layer specifications for various types of networks, such as Ethernet, Wi-Fi, Bluetooth, etc. They specify the characteristics of the transmission media, the encoding and modulation schemes, the frame formats, the error detection and correction methods, the access control and flow control mechanisms, etc.